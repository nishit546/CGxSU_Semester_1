# Python Mixed Problem-Solving — 50 Unique Questions

## Topics

These questions intentionally mix:

- `for` loops
- Nested `for` loops
- Strings
- `if-elif-else`
- Conditions inside loops
- String + loop problems
- Number + string problems
- Pattern logic
- Real-world problem-solving

> **Rule:** Do not solve these by memorizing patterns. First identify the input, processing, conditions, loops, and output.

---

## 1. Digit and Character Analyzer

Take a string containing letters, digits, spaces, and special characters.

Using a `for` loop:

- Count uppercase letters.
- Count lowercase letters.
- Count digits.
- Count spaces.
- Count special characters.
- Print which category has the highest count.
- If two or more categories have the same highest count, print `"Tie"`.

---

## 2. Student Performance Analyzer

Take marks of 10 students using a `for` loop.

For each student:

- Print `"Fail"` if marks are below 35.
- Print `"Pass"` for 35–49.
- Print `"Good"` for 50–74.
- Print `"Excellent"` for 75–100.

At the end, print the number of students in each category.

---

## 3. Word Score Calculator

Take a sentence.

For every word:

- Vowel = 2 points.
- Consonant = 1 point.
- Digit = 3 points.
- Special character = 4 points.

Calculate the score of every word and print the word with the highest score.

Do not use `max()`.

---

## 4. Password Batch Validator

Take passwords for 5 users using a `for` loop.

For every password, check:

- Minimum length of 8.
- At least one uppercase letter.
- At least one lowercase letter.
- At least one digit.
- At least one special character.

Print `"Strong"`, `"Medium"`, or `"Weak"` based on the number of conditions satisfied.

---

## 5. Sentence Word Analyzer

Take a sentence and examine every word.

For each word:

- Print its length.
- Print `"Short"` if length ≤ 3.
- Print `"Medium"` if length is 4–6.
- Print `"Long"` if length > 6.

At the end, print the number of short, medium, and long words.

---

## 6. Number-String Conversion Challenge

Take 5 numbers from the user.

For each number:

- Convert it to a string.
- Examine every digit using a loop.
- Count even and odd digits.
- Print which type occurs more.
- If equal, print `"Equal"`.

---

## 7. Repeated Character Report

Take a string.

For every character, determine how many times it appears in the string without using `count()`.

Print only characters that appear more than once.

Also classify them:

- 2 occurrences → `"Duplicate"`
- 3–4 occurrences → `"Repeated"`
- More than 4 → `"Highly Repeated"`

---

## 8. Shopping Cart Analyzer

Take prices of 8 products.

For every price:

- Below 500 → `"Budget"`
- 500–1999 → `"Regular"`
- 2000–4999 → `"Premium"`
- 5000 or more → `"Luxury"`

Calculate:

- Total amount.
- Number of products in each category.
- Average product price.

---

## 9. Character Position Challenge

Take a string.

For every character, print:

- Character
- Position
- Whether position is even or odd
- Whether character is vowel, consonant, digit, or special character

At the end, count how many characters fall into each category.

---

## 10. Number Pattern With Conditions

Take `n`.

Print a pattern where each row contains numbers from `1` to the row number.

Replace:

- Multiples of 3 with `X`
- Multiples of 5 with `Y`
- Multiples of both 3 and 5 with `Z`

Example for `n = 5`:

```text
1
1 2 X
1 2 X 4 Y
1 2 X 4 Y X
1 2 X 4 Y X 7 8 X
```

Use nested `for` loops.

---

## 11. Username Analyzer

Take 5 usernames.

For every username:

- Check length.
- Check first character.
- Count digits.
- Count underscores.
- Detect invalid special characters.

Classify each username as:

- `"Valid"`
- `"Needs Improvement"`
- `"Invalid"`

---

## 12. Vowel-Consonant Battle

Take a sentence.

Count vowels and consonants.

Then:

- Print `"Vowels Win"` if vowels are greater.
- Print `"Consonants Win"` if consonants are greater.
- Print `"Draw"` if equal.

Also print the frequency of each vowel.

---

## 13. Electricity Bill Calculator

Take electricity usage for 6 customers.

Calculate the bill using:

- First 100 units → ₹5/unit
- Next 100 units → ₹7/unit
- Next 200 units → ₹10/unit
- Above 400 units → ₹15/unit

Also classify:

- Below ₹1000 → `"Low"`
- ₹1000–₹3000 → `"Medium"`
- Above ₹3000 → `"High"`

Print total revenue.

---

## 14. Word Character Balance

Take a sentence.

For every word:

- Count vowels.
- Count consonants.
- If vowels > consonants → `"Vowel Heavy"`
- If consonants > vowels → `"Consonant Heavy"`
- Otherwise → `"Balanced"`

Print the result for every word.

---

## 15. Matrix Value Analyzer

Take a 3 × 3 matrix using nested `for` loops.

For every number:

- Identify even/odd.
- Identify positive/negative/zero.

At the end, print:

- Even count
- Odd count
- Positive count
- Negative count
- Zero count
- Largest number

Do not use `max()`.

---

## 16. Password Character Distribution

Take a password.

Count:

- Uppercase
- Lowercase
- Digits
- Special characters

Calculate the percentage of each category.

Then classify the password based on which category dominates.

---

## 17. Student Name and Marks

Take the name and marks of 5 students.

For every student:

- Calculate grade.
- Count vowels in the student's name.
- Count characters in the name.
- Print whether the name has more vowels or consonants.

Finally, print the student with the highest marks.

Do not use `max()`.

---

## 18. ATM Transaction Analyzer

Take transactions for 7 days.

Each transaction can be:

- Deposit
- Withdrawal

For every transaction:

- Update balance.
- Reject withdrawal if balance is insufficient.
- Print `"Low Balance"` if balance falls below ₹1000.

At the end, print final balance and transaction counts.

---

## 19. Sentence Security Scanner

Take a sentence.

Detect whether it contains:

- Digits
- URLs-like text containing `.`
- `@`
- Password-like patterns
- Repeated special characters

Print a security classification:

- `"Safe"`
- `"Review"`
- `"Suspicious"`

---

## 20. Multiplication Grid Analyzer

Take `n`.

Generate an `n × n` multiplication grid using nested loops.

For every result:

- Print `"E"` if even.
- Print `"O"` if odd.
- Print `"F"` if divisible by 5.

---

## 21. Shopping Discount System

Take prices of 10 products.

Apply:

- ≥ ₹5000 → 20% discount
- ≥ ₹3000 → 15%
- ≥ ₹1000 → 10%
- Otherwise → no discount

Then:

- Calculate final price.
- Count how many products received each discount.
- Calculate total discount.

---

## 22. String Compression Counter

Take a string such as:

```text
aaabbccccdd
```

Print:

```text
a3b2c4d2
```

Use loops and conditions.

Do not use libraries for compression.

---

## 23. Employee Salary Analyzer

Take salaries of 8 employees.

Classify:

- Below ₹25,000 → `"Junior"`
- ₹25,000–₹50,000 → `"Mid"`
- ₹50,001–₹1,00,000 → `"Senior"`
- Above ₹1,00,000 → `"Executive"`

Print counts and average salary.

---

## 24. Secret Word Detector

Take a sentence and a secret word.

Check whether the secret word exists without using the `in` operator.

If found:

- Print its starting position.
- Print how many times it occurs.

If not found, print `"Secret word not found"`.

---

## 25. Number Pyramid With Classification

Take `n`.

For every number printed in the pyramid:

- Even → print `E`
- Odd → print `O`
- Divisible by 3 → print `T`
- Divisible by both 3 and 5 → print `F`

Use nested loops.

---

## 26. Movie Rating Analyzer

Take ratings of 10 movies.

Classify:

- 0–3 → `"Poor"`
- 3.1–5 → `"Average"`
- 5.1–7 → `"Good"`
- 7.1–9 → `"Excellent"`
- 9.1–10 → `"Outstanding"`

Print category counts and average rating.

---

## 27. Word Frequency Without Dictionary

Take a sentence.

Find how many times every word appears.

Do not use:

- Dictionary
- `Counter`
- `count()`

Print only words appearing more than once.

---

## 28. Number With Maximum Even Digits

Take 10 integers.

For each number:

- Convert it into a string.
- Count even digits.
- Count odd digits.

Print the number having the highest number of even digits.

If tied, print all tied numbers.

---

## 29. Email Validator

Take 5 email-like strings.

Check:

- Exactly one `@`.
- At least one `.` after `@`.
- No spaces.
- Text before `@` must not be empty.
- Domain must not be empty.

Print `"Valid"` or `"Invalid"` for each.

Do not use regular expressions.

---

## 30. Character Pair Analyzer

Take a string.

Compare every character with every other character using nested loops.

Count how many pairs are:

- Same characters.
- Different characters.
- Both vowels.
- Both digits.

Avoid counting the same pair twice.

---

## 31. Exam Result Processor

Take marks of 5 subjects for 5 students using nested loops.

For every student:

- Calculate total.
- Calculate percentage.
- Check whether every subject has at least 35.
- Assign grade.

Finally print:

- Number of passed students.
- Number of failed students.
- Highest percentage.
- Lowest percentage.

---

## 32. Password Guess Counter

Take a secret password and 5 attempted passwords.

For every attempt:

- Compare characters one by one.
- Count matching positions.
- Print the number of matching characters.
- Print `"Correct"` only if the complete password matches.

Do not use direct string equality for the main comparison.

---

## 33. Product Code Validator

A product code follows this format:

```text
ABC12345
```

Check whether:

- First 3 characters are uppercase letters.
- Remaining 5 characters are digits.
- Total length is exactly 8.

Print `"Valid Product Code"` or `"Invalid Product Code"`.

---

## 34. Number Triangle With Prime Check

Take `n`.

Print numbers from 1 to the row number.

For each number:

- Print `P` if prime.
- Print `E` if even but not prime.
- Print `O` if odd and not prime.

Use nested loops.

---

## 35. Sentence Statistics

Take a sentence.

Calculate:

- Number of words.
- Number of characters.
- Longest word.
- Shortest word.
- Number of words beginning with a vowel.
- Number of words ending with a vowel.
- Number of words containing digits.

Use loops for all major processing.

---

## 36. Restaurant Billing System

Take orders for 5 customers.

For each customer:

- Take prices of 3 items.
- Calculate subtotal.
- Apply discount:
  - ≥ ₹2000 → 15%
  - ≥ ₹1000 → 10%
  - Otherwise → 0%

If the customer is a member, apply an additional 5%.

Print each final bill and total restaurant revenue.

---

## 37. Character Pyramid

Take a word.

For every character position, print a growing pattern.

For:

```text
PYTHON
```

Output:

```text
P
PY
PYT
PYTH
PYTHO
PYTHON
```

Then print a second pattern in reverse.

Use nested loops.

---

## 38. Duplicate Word Detector

Take a sentence.

Find duplicate words.

For each duplicate:

- Print the word.
- Print its frequency.
- Classify it as:
  - `"Repeated"` → 2 times
  - `"Frequently Repeated"` → 3–4 times
  - `"Highly Repeated"` → more than 4 times

Do not use `count()`.

---

## 39. Bus Ticket Calculator

Take passenger details for 8 passengers.

For each passenger:

- Take age.
- Take distance.

Fare rules:

- Age below 5 → Free
- Age 5–12 → 50% discount
- Age 60+ → 30% discount
- Otherwise → full fare

Base fare:

- ₹10 per kilometer.

Print each passenger's fare and total collection.

---

## 40. String Mirror Check

Take two strings.

Compare the first character of the first string with the last character of the second string, then the second with the second-last, and so on.

Determine whether the strings are mirror-compatible.

Do not use slicing.

---

## 41. Classroom Attendance Analyzer

Take attendance for 7 students over 5 days.

Use:

```text
P = Present
A = Absent
```

Using nested loops:

- Count attendance for each student.
- Calculate attendance percentage.
- Print:
  - `"Excellent"` → 90%+
  - `"Good"` → 75–89%
  - `"Warning"` → below 75%

---

## 42. Matrix Diagonal Analyzer

Take a 4 × 4 matrix.

Calculate:

- Main diagonal sum.
- Secondary diagonal sum.
- Number of even values on the main diagonal.
- Number of odd values on the secondary diagonal.

Compare both diagonal sums using `if-elif-else`.

---

## 43. Text Password Generator Logic

Take a word.

Create a transformed password using these rules:

- Vowels → `@`
- Consonants → lowercase
- Digits → `#`
- Spaces → `_`
- Special characters → `!`

Print the transformed result.

Example:

```text
Python 123
```

Possible output:

```text
@yth@n_###
```

---

## 44. Bank Account Risk Analyzer

Take transactions for an account.

Each transaction contains:

- `D` for deposit
- `W` for withdrawal

After every transaction:

- Update balance.
- If balance < ₹500 → `"Low Balance"`
- If balance < 0 → `"Overdraft"`
- Otherwise → `"Normal"`

At the end, print total deposits, total withdrawals, and final balance.

---

## 45. Word Pattern Matching

Take two words.

Compare them character by character.

For each position:

- Same character → `S`
- Different character → `D`

If lengths differ, process up to the shorter length and report the extra characters.

Example:

```text
hello
hallo
```

Output:

```text
SSDS S
```

---

## 46. Number Box Pattern

Take `n`.

Print an `n × n` box.

Rules:

- Border → `*`
- Inside even positions → `E`
- Inside odd positions → `O`

Example for `n = 5`:

```text
*****
*EOE*
*OEO*
*EOE*
*****
```

Use nested loops and conditions.

---

## 47. Inventory Analyzer

Take names and quantities of 8 products.

For every product:

- Quantity 0 → `"Out of Stock"`
- Quantity 1–5 → `"Critical"`
- Quantity 6–20 → `"Low"`
- Quantity >20 → `"Available"`

Also count the number of products in each category.

Find the product with the highest quantity.

---

## 48. Social Media Username Scanner

Take 10 usernames.

For every username:

- Count letters.
- Count digits.
- Count `_`.
- Detect spaces.
- Detect other special characters.
- Check length.

Classify:

- `"Clean"`
- `"Acceptable"`
- `"Invalid"`

based on the conditions.

---

## 49. Mini Search Engine

Take a list of 10 sentences.

Take a search word.

For every sentence:

- Search for the word manually using loops.
- Ignore case.
- Print the sentence number if the word exists.
- Count how many times the word appears in each sentence.

At the end, print the total number of occurrences across all sentences.

Do not use `count()`.

---

## 50. Complete Data Analyzer Challenge

Take 10 inputs from the user. Each input may contain letters, digits, spaces, and special characters.

Build one program that analyzes the complete dataset.

For every input:

1. Count uppercase letters.
2. Count lowercase letters.
3. Count vowels.
4. Count consonants.
5. Count digits.
6. Count spaces.
7. Count special characters.
8. Find the longest word.
9. Find the number of repeated characters.
10. Calculate a score:
   - Vowel = 2
   - Consonant = 1
   - Digit = 3
   - Special character = 4
   - Space = 0

Then print a final report containing:

- Input with the highest score.
- Input with the most vowels.
- Input with the most digits.
- Total vowels across all inputs.
- Total consonants across all inputs.
- Total digits across all inputs.
- Total special characters.
- Overall classification:
  - `"Text Heavy"`
  - `"Number Heavy"`
  - `"Special Character Heavy"`
  - `"Balanced"`

Do not use `max()`, `min()`, `count()`, `Counter()`, or `sorted()`.

---

# Recommended Solving Method

For every problem, students should follow this order:

```text
1. Understand the problem
        ↓
2. Identify the input
        ↓
3. Identify required output
        ↓
4. Break problem into smaller steps
        ↓
5. Decide where loops are needed
        ↓
6. Decide where conditions are needed
        ↓
7. Write pseudocode
        ↓
8. Dry run
        ↓
9. Write Python code
        ↓
10. Test with different inputs
```

# Extra Challenge

After solving all 50 questions, try solving selected problems again with these restrictions:

- Do not use `max()`
- Do not use `min()`
- Do not use `count()`
- Do not use `set()`
- Do not use `sorted()`
- Do not use `Counter`
- Avoid shortcuts that directly solve the main logic
- Use loops and conditions to build the logic yourself
