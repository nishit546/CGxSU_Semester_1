### 1. **Score Turns Gold When > 50**
- **What**: When score goes above 50, make the score text **gold**.
- **Where**: In score update → add `style.color` check.

---

### 2. **"Whack!" Message on Hit**
- **What**: Show **"Whack!"** briefly when mole is hit.
- **Where**: In `bonk()` → update a message element.

---

### 3. **Start Button Says "Play Again"**
- **What**: After game ends, change Start button to **"Play Again"**.
- **Where**: In `endGame()` → `startBtn.innerText`.

---

### 4. **Mole Speed Increases (Time Left < 10)**
- **What**: When time < 10s, moles pop **faster** (use smaller `randomTime`).
- **Where**: In `popUp()` → adjust `min` and `max` based on `timeLeft`.

---

### 5. **Hit Counter (Separate from Score)**
- **What**: Show **"Hits: 12"** next to score.
- **Where**: Add `hits` variable → update in `bonk()` → display in new `<div>`.

---

### 6. **"New Record!" Glow on Best Score**
- **What**: When new max score, make **Best** score **glow yellow** for 1 second.
- **Where**: In `saveMaxScore()` → `maxScoreDisplay.style.textShadow`.

---

### 7. **"Last Game Score" Using `sessionStorage`**
- **What**: Show **"Last game: 42"** under the scoreboard — only for current tab/session.
- **Where**:  
  - Save in `endGame()`: `sessionStorage.setItem('lastScore', score)`  
  - Show on load: `sessionStorage.getItem('lastScore')`  
- **Bonus**: Clear when clicking **Start Game**.

---

### 8. **"Fastest Hit" Timer Using `sessionStorage`**
- **What**: Track **fastest time** to hit a mole (in ms) — saved per session.
- **Where**:  
  - In `popUp()` → record `moleStartTime` when mole appears  
  - In `bonk()` → calculate `timeTaken = Date.now() - moleStartTime`  
  - Save if faster: `sessionStorage.setItem('fastestHit', time)`  
  - Show: `"Fastest: 320ms"` under scoreboard

---
