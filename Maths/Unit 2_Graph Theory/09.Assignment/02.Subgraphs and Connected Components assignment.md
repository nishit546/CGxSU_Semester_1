
## 📘 Assignment: Subgraphs & Connected Components


---

### **Q1️⃣**

Consider the graph ( G ) shown below:

```
   A — B — C
   |     /
   D — E
```

a) Draw a **subgraph** ( H ) by removing vertices {C, E}.
b) Write the set of vertices and edges of subgraph ( H ). ✏️

---

Q2. 🔹 Connected Components Count
P --- Q    R --- S


(i) How many connected components?
(ii) List the vertices in each component.
---

### **Q3️⃣**

📌 Identify whether the following graph is **connected** or **disconnected**:

```
1 — 2 — 3     4 — 5
```

If disconnected, mention the number of components.

---

### **Q4️⃣**

Find **two different subgraphs** of the graph below:

```
P — Q — R — S
```

👉 Write the vertices and edges included in each subgraph.

---

### **Q5️⃣**

In the following graph, highlight all **connected components**:

```
A     B — C — D
```

Write your answer in set form.

---

### **Q6️⃣**

Given a graph (G) with edges:
( E = {(A,B),(B,C),(C,A),(D,E)} )
Draw the graph and count **connected components**. 🧩

---

### **Q7️⃣**

Draw a **proper subgraph** of the graph:

```
X — Y — Z — W
```

(such that at least one vertex and one edge are removed)

---

### **Q8️⃣**

Determine whether the below is a subgraph of the original graph:
Original: ( {1,2,3,4} ), edges ( {(1,2),(2,3),(3,4)} )
Subgraph: ( {1,2,3} ), edges ( {(1,2),(1,3)} )
👉 Justify your answer. ❓

---

### **Q9️⃣**

Find the number of connected components in the following figure:

```
  A — B     C — D — E     F
```

Provide proper reasoning. 🧠

---

### **Q🔟**

Draw all possible **subgraphs** containing exactly 2 vertices from the graph:

```
M — N — O
```

---

### **Q1️⃣1️⃣**

Given a graph having **three connected components**, is it possible to add exactly **two edges** to make the entire graph connected?
Explain with a figure. 🔄

---

### **Q1️⃣2️⃣**

Check whether the subgraph formed by vertices
( {P, Q, S} )
from the graph

```
P — Q — R — S — T
```

is **connected or disconnected**. Draw and answer.

---

### **Q1️⃣3️⃣**

Find all connected components in the graph below:

```
1 — 2     3     4 — 5 — 6
```

Write each connected component as a set.

---

### **Q1️⃣4️⃣**

Can a **tree** be disconnected?
If yes/no — justify using an example subgraph. 🌲

---

### **Q1️⃣5️⃣**

In the below:

```
U — V — W     X — Y
```

a) Count connected components
b) Draw a **subgraph** that contains only the first component
c) Mention vertices + edges included

---

