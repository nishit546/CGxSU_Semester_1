

# 🧮✨ **Graph Basics – Mathematical Assignment**

---

### **Q1️⃣ Definition-Based Problem** 🔹

Let a graph **G = (V, E)** where
( V = {A, B, C, D} ) and
( E = {AB, AC, AD, BC, CD} ).
👉 Find:

* ( |V| ) = ?
* ( |E| ) = ?
  📊 *Draw the structure of the graph to visualize connections.*

---

### **Q2️⃣ Edge Count Problem** 🔗

In a simple graph with **5 vertices**, each vertex has a **degree of 2**.
👉 Find the **total number of edges (|E|)**.
💡 *(Use formula → Sum of degrees = 2 × |E|)*

---

### **Q3️⃣ Directed Degree Problem** 🎯

For a directed graph:

* In-degree(A) = 2, Out-degree(A) = 3
* In-degree(B) = 1, Out-degree(B) = 2
  👉 Calculate **Total degree(A)** and **Total degree(B)**.

---

### **Q4️⃣ Degree Sum Calculation** 📈

A graph has **6 vertices** and **9 edges**.
👉 Find the **sum of degrees of all vertices.**
💡 *(Hint: Sum of degrees = 2 × |E|)*

---

### **Q5️⃣ Cycle Check Problem** 🔄

Given ( V = {1, 2, 3, 4} ) and
( E = {(1,2), (2,3), (3,4), (4,1)} ),
👉 Find the **degree of each vertex** and verify if the graph forms a **cycle**.

📘 *Visual Hint:*

```
(1) — (2)
 |      |
(4) — (3)
```

---

### **Q6️⃣ Single Vertex Degree** 🧱

If a vertex **A** is connected to **4 other vertices**,
👉 What is the **degree(A)**?
💡 *(Remember: Degree = number of connected edges)*

---

### **Q7️⃣ Edge Calculation from Degrees** 🧮

In a graph, total degree of all vertices = **16**.
👉 Find **number of edges** in the graph.
✳️ *(Use formula: |E| = (Sum of degrees)/2)*

---

### **Q8️⃣ Degree Distribution** 📊

A graph has 5 vertices: {P, Q, R, S, T}.
Edges = {PQ, PR, PS, QT, RT, ST}
👉 Find **degree of each vertex**.
📘 *Hint:* List how many connections each vertex has.

---

### **Q9️⃣ Directed Graph Analysis** 🧭

Given directed edges:
A → B, B → C, C → D, D → A.
👉 Find **In-degree** and **Out-degree** of all vertices.
💡 *Draw arrows to understand direction.*

---

### **Q🔟 Total Edge Calculation** ⚙️

In an undirected graph, there are **8 vertices**, and each vertex has a **degree of 3**.
👉 Find **total number of edges** in the graph.
*(Formula: Sum of degrees = 2 × |E|)*

---

### **Q11️⃣ Path Enumeration** 🚶‍♂️

If edges are ( E = {AB, BC, CD, DA, AC} ),
👉 List **all possible paths from A → D**.
💡 *Use combinations of connecting vertices.*

---

### **Q12️⃣ Weighted Path Calculation** 💰

Given a path: A → B → C → D
Edge weights: AB = 4, BC = 5, CD = 3
👉 Find the **total path cost**.
📘 *Formula:*
[
\text{Path cost} = AB + BC + CD
]

---

### **Q13️⃣ Cycle Verification** 🔁

Edges: A → B, B → C, C → A
👉 Check if the given directed graph contains a **cycle**.
💡 *(If start and end vertices are same ⇒ cycle exists)*

---

### **Q14️⃣ Self-Loop Degree Problem** ♻️

A vertex **X** has **one self-loop** and **two other edges** connected.
👉 Find **Degree(X)**.
📘 *Hint:* Self-loop contributes **2** to the degree.

---

### **Q15️⃣ Complete Graph Problem** 🕸️

If a graph has **7 vertices**, and each vertex is connected to **every other vertex**,
👉 Find the **total number of edges**.
💡 *Formula:*
[
|E| = \frac{n(n-1)}{2}
]


