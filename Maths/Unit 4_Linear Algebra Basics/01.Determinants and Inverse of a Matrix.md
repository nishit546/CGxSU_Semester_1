
# 🧮 **Determinants and Inverse of a Matrix (Basic Problems)**

---

## 🔹 **1️⃣ Determinant of a Matrix — Concept & Explanation**

A **determinant** is a scalar value that can be computed from a square matrix. It helps to determine whether a matrix has an inverse or not.
👉 For a square matrix **A**, the determinant is denoted as **|A|** or **det(A)**.

### 💡 **Key Points:**

1. Determinants exist only for **square matrices** (n × n).
2. If **|A| = 0**, then the matrix is **singular** (no inverse exists).
3. If **|A| ≠ 0**, then the matrix is **non-singular** (inverse exists).
4. Determinants have geometric importance — they represent the **area/volume** scaling factor of linear transformations.

---

### 📊 **Visual Representation:**

```
For a 2×2 Matrix:
|A| = | a   b |
       | c   d |

Determinant = ad - bc
```

---

### 🧠 **6 Examples with Step-by-Step Explanation:**

#### 🟩 **Example 1:**

A = (\begin{bmatrix} 2 & 3 \ 1 & 4 \end{bmatrix})
|A| = (2×4) - (3×1) = **8 - 3 = 5** ✅

#### 🟩 **Example 2:**

B = (\begin{bmatrix} 5 & 7 \ 2 & 6 \end{bmatrix})
|B| = (5×6) - (7×2) = **30 - 14 = 16** ✅

#### 🟩 **Example 3:**

C = (\begin{bmatrix} 1 & 2 \ 3 & 4 \end{bmatrix})
|C| = (1×4) - (2×3) = **4 - 6 = -2** ✅

#### 🟩 **Example 4:**

D = (\begin{bmatrix} 3 & 5 \ 6 & 10 \end{bmatrix})
|D| = (3×10) - (5×6) = **30 - 30 = 0** ⇒ *Singular Matrix* ❌

#### 🟩 **Example 5:**

E = (\begin{bmatrix} 7 & 2 \ 4 & 1 \end{bmatrix})
|E| = (7×1) - (2×4) = **7 - 8 = -1** ✅

#### 🟩 **Example 6:**

F = (\begin{bmatrix} 8 & 3 \ 2 & 9 \end{bmatrix})
|F| = (8×9) - (3×2) = **72 - 6 = 66** ✅

---

## 🔹 **2️⃣ Inverse of a Matrix — Concept & Explanation**

The **inverse of a matrix** is the matrix that, when multiplied with the original matrix, gives the **identity matrix (I)**.
If **A** is a square matrix, its inverse is denoted as **A⁻¹** and satisfies:
👉 **A × A⁻¹ = I**

---

### 💡 **Key Formula (for 2×2 matrix):**

If
A = (\begin{bmatrix} a & b \ c & d \end{bmatrix}),
then

[
A^{-1} = \frac{1}{|A|} \begin{bmatrix} d & -b \ -c & a \end{bmatrix}
]
provided that **|A| ≠ 0**.

---

### 📊 **Visual Representation:**

```
A = | a  b |
    | c  d |

A⁻¹ = (1 / (ad - bc)) × | d  -b |
                         | -c  a |
```

---

### 🧠 **6 Examples with Step-by-Step Explanation:**

#### 🟦 **Example 1:**

A = (\begin{bmatrix} 2 & 3 \ 1 & 4 \end{bmatrix})
|A| = (2×4) - (3×1) = **5**
[
A^{-1} = \frac{1}{5} \begin{bmatrix} 4 & -3 \ -1 & 2 \end{bmatrix}
]
✅ **A⁻¹ =** (\begin{bmatrix} 0.8 & -0.6 \ -0.2 & 0.4 \end{bmatrix})

---

#### 🟦 **Example 2:**

B = (\begin{bmatrix} 1 & 2 \ 3 & 4 \end{bmatrix})
|B| = -2
[
B^{-1} = \frac{1}{-2} \begin{bmatrix} 4 & -2 \ -3 & 1 \end{bmatrix}
]
✅ **B⁻¹ =** (\begin{bmatrix} -2 & 1 \ 1.5 & -0.5 \end{bmatrix})

---

#### 🟦 **Example 3:**

C = (\begin{bmatrix} 5 & 2 \ 7 & 3 \end{bmatrix})
|C| = (5×3)-(2×7)= **15-14=1**
✅ (C^{-1} = \begin{bmatrix} 3 & -2 \ -7 & 5 \end{bmatrix})

---

#### 🟦 **Example 4:**

D = (\begin{bmatrix} 4 & 7 \ 2 & 6 \end{bmatrix})
|D| = (4×6)-(7×2)= **24-14=10**
✅ (D^{-1} = \frac{1}{10}\begin{bmatrix} 6 & -7 \ -2 & 4 \end{bmatrix})

---

#### 🟦 **Example 5:**

E = (\begin{bmatrix} 3 & 0 \ 2 & 5 \end{bmatrix})
|E| = (3×5)-(0×2)= **15**
✅ (E^{-1} = \frac{1}{15}\begin{bmatrix} 5 & 0 \ -2 & 3 \end{bmatrix})

---

#### 🟦 **Example 6:**

F = (\begin{bmatrix} 2 & 1 \ 7 & 4 \end{bmatrix})
|F| = (2×4)-(1×7)= **8-7=1**
✅ (F^{-1} = \begin{bmatrix} 4 & -1 \ -7 & 2 \end{bmatrix})

---

### ✨ **Key Notes:**

* A matrix has an inverse **only if its determinant ≠ 0**.
* The **identity matrix** acts like “1” in matrix multiplication.
* Inverse is used to solve linear equations of the form **AX = B**, by finding **X = A⁻¹B**.

---
