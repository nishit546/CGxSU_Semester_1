
# Local Authentication (`expo-local-authentication`)

---

# What is `expo-local-authentication`?

`expo-local-authentication` is an Expo SDK module that allows a React Native application to use the authentication mechanisms provided by the user's device.

Depending on the device and platform, this can include:

* Fingerprint
* Face authentication
* Touch ID
* Other biometric authentication methods supported by the device

For example, imagine that your application has a screen containing private information.

You may want to prevent someone from opening that screen simply by tapping a button.

Instead, your application can ask the device:

```text
"Please verify that the person using this app
is the device's authorized user."
```

The device can then display its native authentication UI.

For example:

```text
        ┌─────────────────────────┐
        │                         │
        │     Authenticate        │
        │                         │
        │      [ Face ]           │
        │                         │
        │   Look at your phone    │
        │                         │
        └─────────────────────────┘
```

After the device finishes authentication, your application receives a result.

The result can indicate that authentication:

```text
Succeeded
```

or

```text
Failed
```

or

```text
Was cancelled
```

or another supported result.

---

# Why Do We Need Local Authentication?

Normal application authentication and local device authentication solve different problems.

For example, suppose a user logs into your application using:

```text
Email
+
Password
```

The server verifies the credentials and creates an authenticated session.

However, after the user closes the app and opens it again, you may want an additional protection layer.

Instead of asking the user to type their password every time:

```text
Open App
   ↓
Enter Password
   ↓
Open Private Data
```

you can use local authentication:

```text
Open App
   ↓
Biometric Authentication
   ↓
Open Private Data
```

This can make accessing protected parts of an application both faster and more convenient.

---

# Real-World Examples

Local authentication can be useful for applications such as:

### Banking Applications

Before showing account information:

```text
Open Banking App
        ↓
Authenticate
        ↓
Show Account
```

### Payment Applications

Before confirming a sensitive action:

```text
Make Payment
     ↓
Authenticate
     ↓
Confirm Payment
```

### Password Managers

```text
Open Password Manager
        ↓
Authenticate
        ↓
Show Saved Passwords
```

### Private Notes

```text
Open Private Notes
        ↓
Authenticate
        ↓
Show Notes
```

### Admin Applications

```text
Open Admin Panel
        ↓
Authenticate
        ↓
Allow Access
```

---

# What Can We Build Using Local Authentication?

Using `expo-local-authentication`, we can build features such as:

* Biometric login
* Protected screens
* Fingerprint authentication
* Face authentication
* Touch ID authentication
* Authentication before payments
* Authentication before viewing sensitive information
* Authentication before performing sensitive actions
* Biometric capability checking
* Authentication error handling
* Authentication cancellation handling
* Authentication retry flows

For example:

```text
User opens Profile
        ↓
Profile contains sensitive information
        ↓
Ask for authentication
        ↓
Authentication successful?
       / \
     Yes  No
      ↓    ↓
   Show   Keep
   Data   Locked
```

---

# How Does Local Authentication Work?

The important thing to understand is that your React Native application does **not directly read the user's fingerprint or face**.

Instead, the application asks the operating system to perform authentication.

The simplified flow is:

```text
React Native Application

        ↓

expo-local-authentication

        ↓

Device Operating System

        ↓

Biometric System

        ↓

Fingerprint / Face / Other Method

        ↓

Authentication Result

        ↓

React Native Application
```

For example:

```js
const result = await LocalAuthentication.authenticateAsync();
```

Your application asks the device to authenticate the user.

The device displays the appropriate authentication interface.

After the authentication process completes, the application receives the result.

---

# Important Security Concept

Your application generally does **not** receive:

```text
 User's fingerprint image
 User's face image
 Fingerprint template
 Face biometric data
```

Instead, the application receives an authentication result.

Conceptually:

```text
Biometric Data
      ↓
Device OS handles it
      ↓
Authentication Result
      ↓
Your Application
```

This is an important distinction.

Your application is requesting:

> "Authenticate the user."

It is not asking:

> "Give me the user's fingerprint."

---

# Hardware vs Enrollment

One of the most important concepts in local authentication is understanding the difference between:

1. Biometric hardware
2. Biometric enrollment

These are **not the same thing**.

---

# 1. Biometric Hardware

First ask:

> Does this device have biometric hardware?

Use:

```js
const hasHardware =
  await LocalAuthentication.hasHardwareAsync();
```

The result is a boolean:

```js
true
```

or

```js
false
```

### Example

```js
const hasHardware =
  await LocalAuthentication.hasHardwareAsync();

console.log(hasHardware);
```

Possible output:

```text
true
```

This means the device reports that biometric hardware is available.

---

# What Does Hardware Mean?

Examples of biometric hardware include:

```text
Fingerprint sensor
Face authentication hardware
Other supported biometric hardware
```

For example, imagine a phone that has a fingerprint sensor.

Then:

```js
await LocalAuthentication.hasHardwareAsync();
```

may return:

```js
true
```

But this does **not** automatically mean that the user has configured a fingerprint.

That brings us to enrollment.

---

# 2. Biometric Enrollment

The next question is:

> Has the user actually enrolled a biometric credential on the device?

Use:

```js
const isEnrolled =
  await LocalAuthentication.isEnrolledAsync();
```

Again, the result is:

```js
true
```

or

```js
false
```

---

# Hardware Available Does Not Mean Enrollment Exists

Consider this situation:

```text
Biometric Hardware
       ↓
      YES

Biometric Credential Enrolled
       ↓
       NO
```

This means:

```js
hasHardwareAsync()
```

returns:

```js
true
```

but:

```js
isEnrolledAsync()
```

returns:

```js
false
```

The device supports biometric authentication, but the user has not configured an eligible biometric credential.

---

# Example

```js
const hasHardware =
  await LocalAuthentication.hasHardwareAsync();

const isEnrolled =
  await LocalAuthentication.isEnrolledAsync();

console.log("Hardware:", hasHardware);
console.log("Enrolled:", isEnrolled);
```

Possible output:

```text
Hardware: true
Enrolled: false
```

This is a perfectly possible state.

---

# Think of It Like a Lock

A useful analogy is a door lock.

```text
Hardware
=
Does the door have a lock?

Enrollment
=
Has someone configured a key for that lock?
```

You can have:

```text
Lock exists
+
No key configured
```

Similarly:

```text
Biometric hardware exists
+
No biometric credential enrolled
```

Therefore, checking only hardware is not enough.

---

# Authentication Types vs Security Level

Another important distinction is between:

```text
Authentication Type
```

and:

```text
Security Level
```

These answer different questions.

---

# Authentication Type

Authentication type asks:

> What biometric authentication types are supported by the device?

Use:

```js
const types =
  await LocalAuthentication.supportedAuthenticationTypesAsync();
```

The method returns an array of authentication type values.

For example, conceptually:

```text
[
  Fingerprint
]
```

or:

```text
[
  Face
]
```

or potentially multiple supported types.

---

# Why Is This Useful?

Suppose your application wants to display information such as:

```text
Biometric authentication available:
Fingerprint
```

or:

```text
Biometric authentication available:
Face
```

You can inspect the supported authentication types.

Example:

```js
const types =
  await LocalAuthentication.supportedAuthenticationTypesAsync();

console.log(types);
```

You can then determine which authentication mechanisms the device supports according to the values returned by the API.

---

# Authentication Type Does Not Mean Authentication Success

This is very important.

Suppose:

```js
supportedAuthenticationTypesAsync()
```

returns a fingerprint-related type.

That means:

```text
Fingerprint authentication is supported.
```

It does **not** mean:

```text
The user has successfully authenticated.
```

These are completely different concepts.

---

# Compare the Two

### Capability

```js
supportedAuthenticationTypesAsync()
```

asks:

> What authentication methods are supported?

### Authentication

```js
authenticateAsync()
```

asks:

> Can the user successfully authenticate right now?

So:

```text
supportedAuthenticationTypesAsync()
        ↓
"What can this device support?"

authenticateAsync()
        ↓
"Can this user authenticate?"
```

---

# Security Level

The next concept is the enrolled authentication security level.

Use:

```js
const level =
  await LocalAuthentication.getEnrolledLevelAsync();
```

This tells you about the security level of the authentication mechanism enrolled on the device.

The API returns a value from the module's `SecurityLevel` enum.

For example:

```js
LocalAuthentication.SecurityLevel.NONE
```

Other possible enum values include stronger authentication categories supported by the platform.

You should use the enum values rather than assuming numeric values.

---

# Why Do We Need Security Level?

Suppose your application has a highly sensitive feature.

You may want to know not only:

```text
Is some biometric capability available?
```

but also:

```text
What level of authentication is enrolled?
```

This can be useful when your application has security requirements.

For example:

```text
Normal Feature
      ↓
Basic local authentication may be enough

Highly Sensitive Feature
      ↓
Require an appropriate device authentication level
```

The exact security semantics depend on the platform.

Therefore, don't assume that every authentication method provides the same security characteristics.

---

# Complete Mental Model

You can remember the APIs using four questions.

### Question 1

> Does the device have biometric hardware?

```js
hasHardwareAsync()
```

### Question 2

> Has the user enrolled biometric credentials?

```js
isEnrolledAsync()
```

### Question 3

> What authentication types are supported?

```js
supportedAuthenticationTypesAsync()
```

### Question 4

> What authentication security level is enrolled?

```js
getEnrolledLevelAsync()
```

Finally:

### Question 5

> Can the user authenticate successfully?

```js
authenticateAsync()
```

---

# Complete Flow

```text
                 APP
                  │
                  ▼
       Does hardware exist?
                  │
          hasHardwareAsync()
                  │
             ┌────┴────┐
             │         │
            No        Yes
             │         │
             ▼         ▼
        Stop/      Is biometric
        fallback   enrolled?
                       │
                isEnrolledAsync()
                       │
                  ┌────┴────┐
                  │         │
                 No        Yes
                  │         │
                  ▼         ▼
             Ask user   Check supported
             to setup   authentication
                         types
                            │
                            ▼
                    Authenticate user
                            │
                            ▼
                   authenticateAsync()
                            │
                     ┌──────┴──────┐
                     │             │
                  Success        Failure
                     │             │
                     ▼             ▼
                Allow access   Keep locked
```

---

# Main APIs

| API                                   | Question It Answers                                        | Return                               |
| ------------------------------------- | ---------------------------------------------------------- | ------------------------------------ |
| `hasHardwareAsync()`                  | Does the device have biometric hardware?                   | `Promise<boolean>`                   |
| `isEnrolledAsync()`                   | Is a biometric credential enrolled?                        | `Promise<boolean>`                   |
| `supportedAuthenticationTypesAsync()` | What biometric types are supported?                        | `Promise<AuthenticationType[]>`      |
| `getEnrolledLevelAsync()`             | What security level is enrolled?                           | `Promise<SecurityLevel>`             |
| `authenticateAsync()`                 | Can the user authenticate?                                 | `Promise<LocalAuthenticationResult>` |
| `cancelAuthenticate()`                | Can an active Android authentication request be cancelled? | `void`                               |

Notice that most methods are asynchronous.

That is why we normally use:

```js
await
```

with them.

---

# Part 1 — Installation and Setup

Before using any API, we need to install the package.

---

# Step 1 — Install Expo Local Authentication

For an Expo project:

```bash
npx expo install expo-local-authentication
```

Using:

```bash
npx expo install
```

is recommended for Expo projects because Expo can install the package version appropriate for the project's Expo SDK.

---

# Why Not Just Use `npm install`?

You could install packages using npm directly, but for Expo SDK packages, the recommended approach is:

```bash
npx expo install expo-local-authentication
```

This helps keep Expo package versions aligned with the Expo SDK version being used by your project.

For example:

```bash
npx expo install expo-local-authentication
```

instead of manually choosing a version such as:

```bash
npm install expo-local-authentication@some-version
```

---

# Step 2 — Import the Package

After installation:

```js
import * as LocalAuthentication from "expo-local-authentication";
```

Now the module is available through:

```js
LocalAuthentication
```

---

# Why Do We Use `* as LocalAuthentication`?

The package contains multiple exported APIs.

For example:

```js
LocalAuthentication.hasHardwareAsync();

LocalAuthentication.isEnrolledAsync();

LocalAuthentication.supportedAuthenticationTypesAsync();

LocalAuthentication.getEnrolledLevelAsync();

LocalAuthentication.authenticateAsync();
```

Instead of importing each function individually:

```js
import {
  hasHardwareAsync,
  isEnrolledAsync,
  authenticateAsync
} from "expo-local-authentication";
```

we can import the entire module as a namespace:

```js
import * as LocalAuthentication
  from "expo-local-authentication";
```

Then all APIs are grouped under:

```text
LocalAuthentication
```

This makes the code easier to understand when learning the module.

---

# Understanding the Namespace

Think of:

```js
LocalAuthentication
```

as a container.

Inside it are methods such as:

```text
LocalAuthentication
│
├── hasHardwareAsync()
├── isEnrolledAsync()
├── supportedAuthenticationTypesAsync()
├── getEnrolledLevelAsync()
├── authenticateAsync()
└── cancelAuthenticate()
```

So when you write:

```js
LocalAuthentication.hasHardwareAsync();
```

you are calling:

```text
hasHardwareAsync
```

from the:

```text
LocalAuthentication
```

module.

---

# Installation Complete

Installing the package does **not** automatically authenticate the user.

This:

```bash
npx expo install expo-local-authentication
```

only installs the library.

You still need to write application logic.

The overall process is:

```text
Install Package
       ↓
Import Package
       ↓
Check Device Capability
       ↓
Check Enrollment
       ↓
Authenticate
       ↓
Handle Result
```

---

# Example 1 — Smallest Hardware Check

Let's create a simple function:

```js
import * as LocalAuthentication from "expo-local-authentication";

async function checkHardware() {
  const hasHardware =
    await LocalAuthentication.hasHardwareAsync();

  console.log("Has biometric hardware:", hasHardware);
}
```

Calling:

```js
checkHardware();
```

might produce:

```text
Has biometric hardware: true
```

or:

```text
Has biometric hardware: false
```

---

# Example 2 — Check Enrollment

```js
import * as LocalAuthentication from "expo-local-authentication";

async function checkEnrollment() {
  const isEnrolled =
    await LocalAuthentication.isEnrolledAsync();

  console.log("Biometric enrolled:", isEnrolled);
}
```

Possible result:

```text
Biometric enrolled: true
```

This means an eligible local authentication credential is enrolled.

---

# Example 3 — Check Both

Usually, we want to check both.

```js
import * as LocalAuthentication from "expo-local-authentication";

async function checkBiometricSupport() {
  const hasHardware =
    await LocalAuthentication.hasHardwareAsync();

  const isEnrolled =
    await LocalAuthentication.isEnrolledAsync();

  console.log("Hardware:", hasHardware);
  console.log("Enrolled:", isEnrolled);
}
```

Possible output:

```text
Hardware: true
Enrolled: true
```

This is a much more useful check than checking hardware alone.

---

# Example 4 — Simple Capability Decision

We can use the results to decide what the application should do.

```js
async function checkBiometricSupport() {
  const hasHardware =
    await LocalAuthentication.hasHardwareAsync();

  if (!hasHardware) {
    console.log("Biometric hardware is not available.");
    return;
  }

  const isEnrolled =
    await LocalAuthentication.isEnrolledAsync();

  if (!isEnrolled) {
    console.log("No biometric credentials are enrolled.");
    return;
  }

  console.log("Biometric authentication can be offered.");
}
```

The logic is:

```text
Hardware?
   │
   ├── No → Stop
   │
   └── Yes
        │
        ▼
     Enrolled?
        │
        ├── No → Stop / Ask user to configure
        │
        └── Yes → Authentication can be offered
```

---

# Example 5 — Check Supported Authentication Types

```js
async function checkAuthenticationTypes() {
  const types =
    await LocalAuthentication
      .supportedAuthenticationTypesAsync();

  console.log("Supported authentication types:", types);
}
```

The returned array contains authentication type enum values.

You can inspect the values returned by the API and compare them against the module's `AuthenticationType` enum.

For example, the module exposes enum members such as:

```js
LocalAuthentication.AuthenticationType.FINGERPRINT
```

and:

```js
LocalAuthentication.AuthenticationType.FACIAL_RECOGNITION
```

When writing application logic, prefer these named enum values instead of hard-coding numeric values.

---

# Example 6 — Determine Whether Fingerprint Is Supported

```js
const types =
  await LocalAuthentication
    .supportedAuthenticationTypesAsync();

const supportsFingerprint =
  types.includes(
    LocalAuthentication.AuthenticationType.FINGERPRINT
  );

console.log(
  "Supports fingerprint:",
  supportsFingerprint
);
```

This asks:

```text
Does the array of supported authentication
types contain FINGERPRINT?
```

---

# Example 7 — Determine Whether Face Authentication Is Supported

```js
const types =
  await LocalAuthentication
    .supportedAuthenticationTypesAsync();

const supportsFace =
  types.includes(
    LocalAuthentication.AuthenticationType.FACIAL_RECOGNITION
  );

console.log(
  "Supports face authentication:",
  supportsFace
);
```

---

# Example 8 — Check Enrolled Security Level

```js
const level =
  await LocalAuthentication.getEnrolledLevelAsync();

console.log("Enrolled security level:", level);
```

You can compare the result with:

```js
LocalAuthentication.SecurityLevel.NONE
```

and other enum members exposed by the module.

---

# Part 3 — Checking Biometric Hardware

## `hasHardwareAsync()`

Before asking a user to authenticate with biometrics, an application should first understand whether the device provides the required biometric hardware.

Expo provides:

```js
LocalAuthentication.hasHardwareAsync()
```

This method checks whether the device has biometric hardware that can be used for local authentication.

---

# What Does `hasHardwareAsync()` Do?

`hasHardwareAsync()` answers a simple question:

> **Does this device have biometric hardware available?**

Example:

```js
const hasHardware =
  await LocalAuthentication.hasHardwareAsync();

console.log(hasHardware);
```

The method returns a boolean:

```js
true
```

or:

```js
false
```

So the result can be thought of as:

```text
true
 ↓
Biometric hardware is available

false
 ↓
Biometric hardware is not available
```

---

# Why Do We Check Hardware First?

Imagine that your application has a button:

```text
┌──────────────────────────┐
│                          │
│   🔐 Unlock Secure Data  │
│                          │
└──────────────────────────┘
```

When the user taps the button, your application wants to use biometric authentication.

But what if the device does not have biometric hardware?

For example:

```text
Device
  ↓
No biometric hardware
  ↓
Cannot provide biometric authentication
```

In this situation, your application should not blindly assume that biometric authentication is available.

Instead, it can check first:

```js
const hasHardware =
  await LocalAuthentication.hasHardwareAsync();
```

Then decide what to do.

---

# Basic Flow

The basic idea is:

```text
Application
     ↓
Check biometric hardware
     ↓
hasHardwareAsync()
     ↓
   ┌───────────────┐
   │               │
  true            false
   │               │
   ↓               ↓
Continue        Use another
checking        authentication
```

Notice that `true` does **not** immediately mean:

```text
"Authenticate the user"
```

It only means:

```text
"Biometric hardware is available."
```

There are additional checks that may be required.

---

# Method Syntax

The method is called like this:

```js
LocalAuthentication.hasHardwareAsync()
```

Because the method is asynchronous, we normally use:

```js
await
```

Example:

```js
const hasHardware =
  await LocalAuthentication.hasHardwareAsync();
```

---

# Why Is `await` Required?

The method does not immediately return the final boolean value.

Conceptually:

```js
LocalAuthentication.hasHardwareAsync()
```

returns a Promise.

So this:

```js
const result =
  LocalAuthentication.hasHardwareAsync();

console.log(result);
```

does not give you the final:

```js
true
```

or:

```js
false
```

Instead, you are dealing with a Promise.

Using:

```js
await
```

waits for the asynchronous operation to complete:

```js
const result =
  await LocalAuthentication.hasHardwareAsync();
```

Now:

```js
result
```

contains the boolean result.

---

# Example — Basic Check

```js
import * as LocalAuthentication from "expo-local-authentication";

const checkHardware = async () => {
  const hasHardware =
    await LocalAuthentication.hasHardwareAsync();

  console.log(hasHardware);
};
```

If biometric hardware is available:

```text
true
```

If biometric hardware is unavailable:

```text
false
```

---

# Example — Using `if`

Usually, we don't just print the result.

We use it to make a decision.

```js
const checkHardware = async () => {
  const hasHardware =
    await LocalAuthentication.hasHardwareAsync();

  if (hasHardware) {
    console.log("Biometric hardware available");
  } else {
    console.log("Biometric hardware not available");
  }
};
```

The logic is:

```text
hasHardware
     ↓
   true?
   /   \
 Yes    No
  ↓      ↓
Continue Stop / fallback
```

---

# Understanding the Boolean Result

The returned value is a JavaScript boolean.

That means there are only two possibilities:

```js
true
```

or:

```js
false
```

You can therefore use normal JavaScript conditional logic.

For example:

```js
if (hasHardware) {
  // Hardware exists
}
```

or:

```js
if (!hasHardware) {
  // Hardware does not exist
}
```

---

# What Does `true` Mean?

If:

```js
const hasHardware =
  await LocalAuthentication.hasHardwareAsync();
```

returns:

```js
true
```

it means the device reports that biometric hardware is available.

For example:

```text
Device
  ↓
Biometric hardware available
  ↓
hasHardwareAsync()
  ↓
true
```

However, **do not interpret this as authentication being ready**.

There are more things to check.

---

# What Does `false` Mean?

If:

```js
const hasHardware =
  await LocalAuthentication.hasHardwareAsync();
```

returns:

```js
false
```

the device does not have biometric hardware available for this purpose.

Your application can then decide to provide another option.

For example:

```text
Biometric unavailable
       ↓
Show password login
       OR
Show PIN
       OR
Use another supported authentication flow
```

The exact fallback depends on your application's design.

---

# Hardware Does Not Mean Enrollment

This is one of the most important concepts.

Suppose a phone has a fingerprint sensor.

That means:

```text
Biometric hardware
       ↓
Available
```

So:

```js
hasHardwareAsync()
```

may return:

```js
true
```

But imagine that the user has never registered a fingerprint on that device.

Then:

```text
Hardware
   ↓
Available

Biometric credential
   ↓
Not enrolled
```

Therefore:

```js
hasHardwareAsync()
```

can return:

```js
true
```

while:

```js
isEnrolledAsync()
```

returns:

```js
false
```

This is completely valid.

---

# Hardware vs Enrollment

These two APIs answer different questions.

| API                  | Question                            |
| -------------------- | ----------------------------------- |
| `hasHardwareAsync()` | Does biometric hardware exist?      |
| `isEnrolledAsync()`  | Is a biometric credential enrolled? |

Think about it this way:

```text
hasHardwareAsync()
        ↓
"Does the device have the equipment?"
```

while:

```text
isEnrolledAsync()
        ↓
"Has the user configured a credential?"
```

---

# Real-World Example

Imagine a new phone.

The phone has a fingerprint sensor:

```text
Fingerprint Sensor
       ↓
       YES
```

But the user has not configured any fingerprint:

```text
Fingerprint Enrolled
       ↓
       NO
```

The state is:

```text
hasHardwareAsync()
        ↓
      true

isEnrolledAsync()
        ↓
      false
```

Therefore, the application should not conclude:

```text
"User can authenticate with fingerprint."
```

from `hasHardwareAsync()` alone.

---

# Important Difference

`hasHardwareAsync()` does **not** answer:

> Can the user authenticate right now?

It only answers:

> Is biometric hardware available?

For example:

```text
Hardware available
       ↓
true
```

does not guarantee:

```text
Credential enrolled
       ↓
true
```

and it does not mean:

```text
Authentication successful
       ↓
true
```

These are three different things.

---

# Three Different Questions

### Question 1 — Hardware

```js
await LocalAuthentication.hasHardwareAsync();
```

Asks:

> Does the device have biometric hardware?

---

### Question 2 — Enrollment

```js
await LocalAuthentication.isEnrolledAsync();
```

Asks:

> Is an authentication credential enrolled?

---

### Question 3 — Authentication

```js
await LocalAuthentication.authenticateAsync();
```

Asks the device to actually authenticate the user.

So:

```text
Hardware
   ↓
hasHardwareAsync()

Enrollment
   ↓
isEnrolledAsync()

Actual Authentication
   ↓
authenticateAsync()
```

Do not confuse these three operations.

---

# Example — Only Checking Hardware

If your requirement is only:

> "Tell me whether biometric hardware exists."

then this is enough:

```js
const hasHardware =
  await LocalAuthentication.hasHardwareAsync();

console.log(hasHardware);
```

You don't need to call `authenticateAsync()` just to check hardware.

---

# Example — Returning the Result

You can create a reusable function:

```js
const checkHardware = async () => {
  const hasHardware =
    await LocalAuthentication.hasHardwareAsync();

  return hasHardware;
};
```

Then:

```js
const result = await checkHardware();

console.log(result);
```

Possible output:

```text
true
```

---

# Example — Using the Result in React Native

Suppose you have a React Native component.

```js
import { useEffect } from "react";
import * as LocalAuthentication from "expo-local-authentication";

export default function App() {

  useEffect(() => {
    checkHardware();
  }, []);

  const checkHardware = async () => {
    const hasHardware =
      await LocalAuthentication.hasHardwareAsync();

    console.log(
      "Biometric hardware:",
      hasHardware
    );
  };

  return null;
}
```

When the component mounts:

```text
App mounted
    ↓
useEffect()
    ↓
checkHardware()
    ↓
hasHardwareAsync()
    ↓
true / false
```

---

# Example — Store the Result in State

If you want to display the result in the UI, you can use React state.

```js
import { useEffect, useState } from "react";
import { Text, View } from "react-native";
import * as LocalAuthentication from "expo-local-authentication";

export default function App() {

  const [hasHardware, setHasHardware] = useState(null);

  useEffect(() => {
    checkHardware();
  }, []);

  const checkHardware = async () => {
    const result =
      await LocalAuthentication.hasHardwareAsync();

    setHasHardware(result);
  };

  return (
    <View>
      <Text>
        Biometric Hardware:
        {String(hasHardware)}
      </Text>
    </View>
  );
}
```

Initially:

```text
hasHardware = null
```

because the check has not completed yet.

After the asynchronous operation finishes:

```text
hasHardware = true
```

or:

```text
hasHardware = false
```

This gives us three UI states:

```text
null
 ↓
Checking...

true
 ↓
Available

false
 ↓
Unavailable
```

---

# Why Start With `null`?

This is a useful React concept.

If you write:

```js
const [hasHardware, setHasHardware] = useState(false);
```

then before the check finishes, your UI may incorrectly think:

```text
Hardware unavailable
```

But you haven't actually checked yet.

Using:

```js
const [hasHardware, setHasHardware] = useState(null);
```

allows you to represent:

```text
null
=
We don't know yet
```

Then:

```text
true
=
Hardware available
```

and:

```text
false
=
Hardware unavailable
```

---

# Example — Display Different UI

```js
import { useEffect, useState } from "react";
import { Text, View } from "react-native";
import * as LocalAuthentication from "expo-local-authentication";

export default function App() {

  const [hasHardware, setHasHardware] = useState(null);

  useEffect(() => {
    checkHardware();
  }, []);

  const checkHardware = async () => {
    const result =
      await LocalAuthentication.hasHardwareAsync();

    setHasHardware(result);
  };

  if (hasHardware === null) {
    return (
      <View>
        <Text>Checking biometric support...</Text>
      </View>
    );
  }

  if (!hasHardware) {
    return (
      <View>
        <Text>
          Biometric hardware is unavailable.
        </Text>
      </View>
    );
  }

  return (
    <View>
      <Text>
        Biometric hardware is available.
      </Text>
    </View>
  );
}
```

The UI now follows:

```text
             Start
               ↓
             null
               ↓
     Checking biometric support
               ↓
          ┌────┴────┐
          ↓         ↓
        true       false
          ↓         ↓
      Available   Unavailable
```

---

# Example — Hardware Check Before Showing a Button

Imagine that your application has a biometric unlock button.

Instead of always displaying:

```text
[ Unlock with Biometrics ]
```

you can first check hardware.

Conceptually:

```text
Check hardware
      ↓
     true
      ↓
Show biometric option
```

But:

```text
Check hardware
      ↓
     false
      ↓
Don't offer biometric option
```

Example:

```js
const hasHardware =
  await LocalAuthentication.hasHardwareAsync();

if (hasHardware) {
  console.log("Show biometric unlock option");
} else {
  console.log("Use another authentication method");
}
```

---

# Recommended Flow

A more complete flow is:

```text
                  Start
                    ↓
          Check biometric hardware
                    ↓
             hasHardwareAsync()
                    ↓
               ┌────┴────┐
               │         │
             false      true
               │         │
               ↓         ↓
          Biometrics   Check
          unavailable  enrollment
                         │
                         ▼
                isEnrolledAsync()
```

So `hasHardwareAsync()` is usually an **early capability check**, not the final authentication check.

---

# Example — Hardware + Enrollment

Here is a practical helper function:

```js
const prepareAuthentication = async () => {

  const hasHardware =
    await LocalAuthentication.hasHardwareAsync();

  if (!hasHardware) {
    console.log(
      "Biometric hardware is unavailable."
    );

    return;
  }

  const enrolled =
    await LocalAuthentication.isEnrolledAsync();

  if (!enrolled) {
    console.log(
      "No biometric credentials are enrolled."
    );

    return;
  }

  console.log(
    "Device is ready for biometric authentication."
  );
};
```

The flow is:

```text
hasHardwareAsync()
       ↓
   Hardware?
    /     \
  No       Yes
  ↓         ↓
Stop    isEnrolledAsync()
            ↓
        Enrolled?
         /     \
       No       Yes
       ↓         ↓
     Stop     Authenticate
```

---

# Example — Adding a Fallback

A real application should usually have a fallback when biometric authentication isn't available.

For example:

```js
const prepareAuthentication = async () => {

  const hasHardware =
    await LocalAuthentication.hasHardwareAsync();

  if (!hasHardware) {
    console.log(
      "Biometric authentication is unavailable."
    );

    console.log(
      "Use password authentication instead."
    );

    return;
  }

  const enrolled =
    await LocalAuthentication.isEnrolledAsync();

  if (!enrolled) {
    console.log(
      "No biometric credentials are enrolled."
    );

    console.log(
      "Use password authentication instead."
    );

    return;
  }

  console.log(
    "Biometric authentication is available."
  );
};
```

The user experience becomes:

```text
                 User
                  ↓
           Open protected area
                  ↓
        Biometric hardware?
             /           \
           No             Yes
           ↓               ↓
       Password        Enrolled?
                         /     \
                       No       Yes
                       ↓         ↓
                   Password   Biometric
                              Authentication
```

---

# What `hasHardwareAsync()` Does NOT Check

This method only checks hardware availability.

It does **not** tell you all of the following:

```text
 Whether a biometric is enrolled
 Which biometric type is supported
 Whether authentication will succeed
 Whether the user entered the correct biometric
 Whether the user cancelled authentication
 Whether the authentication prompt succeeded
```

Those are separate concerns.

---

# `hasHardwareAsync()` vs `isEnrolledAsync()`

This distinction is worth memorizing.

### `hasHardwareAsync()`

```js
const hasHardware =
  await LocalAuthentication.hasHardwareAsync();
```

Question:

> Does the device have biometric hardware?

---

### `isEnrolledAsync()`

```js
const enrolled =
  await LocalAuthentication.isEnrolledAsync();
```

Question:

> Has the user enrolled a biometric credential?

---

# `hasHardwareAsync()` vs `authenticateAsync()`

These are even more different.

### Hardware check

```js
const hasHardware =
  await LocalAuthentication.hasHardwareAsync();
```

Result:

```js
true
```

or:

```js
false
```

It does not show an authentication prompt.

---

### Authentication

```js
const result =
  await LocalAuthentication.authenticateAsync();
```

This actually starts an authentication attempt.

Conceptually:

```text
hasHardwareAsync()
       ↓
"Is biometric hardware available?"

authenticateAsync()
       ↓
"Authenticate this user now."
```

---

# Does `hasHardwareAsync()` Show a Prompt?

No.

Calling:

```js
await LocalAuthentication.hasHardwareAsync();
```

does not mean:

```text
Show fingerprint prompt
```

or:

```text
Show face authentication UI
```

It is a capability check.

The actual authentication prompt belongs to:

```js
LocalAuthentication.authenticateAsync()
```

---

# Does `hasHardwareAsync()` Ask for a Fingerprint?

No.

It does not ask the user to touch a fingerprint sensor or look at the camera.

The method simply checks whether the device has the required biometric hardware.

So:

```js
await LocalAuthentication.hasHardwareAsync();
```

is more like asking:

```text
"Device, do you have biometric hardware?"
```

rather than:

```text
"Authenticate this user."
```

---

# Complete Example

Here is a simple example combining the concepts learned so far:

```js
import * as LocalAuthentication from "expo-local-authentication";

const prepareAuthentication = async () => {

  // Step 1: Check hardware
  const hasHardware =
    await LocalAuthentication.hasHardwareAsync();

  if (!hasHardware) {
    console.log(
      "Biometric hardware is unavailable."
    );

    return;
  }

  console.log(
    "Biometric hardware is available."
  );

  // Step 2: Check enrollment
  const enrolled =
    await LocalAuthentication.isEnrolledAsync();

  if (!enrolled) {
    console.log(
      "No biometric credentials are enrolled."
    );

    return;
  }

  console.log(
    "Biometric credentials are enrolled."
  );

  // Authentication can be started here.
};
```

Notice that we have **not yet called**:

```js
authenticateAsync()
```

That is intentional.

This example is only preparing the device for authentication.

---

# Part 4 — Checking Biometric Enrollment

## `isEnrolledAsync()`

Having biometric hardware on a device is **not enough** to use biometric authentication.

The device may have:

```text
Fingerprint Sensor
```

or:

```text
Face Authentication Hardware
```

but the user may not have configured any biometric credential.

Therefore, after checking whether biometric hardware is available, we should also check whether the user has actually enrolled a biometric credential.

Expo provides:

```js
LocalAuthentication.isEnrolledAsync()
```

---

# What Does `isEnrolledAsync()` Do?

`isEnrolledAsync()` answers the question:

> **Has the user enrolled a biometric/local authentication credential on this device?**

Example:

```js
const enrolled =
  await LocalAuthentication.isEnrolledAsync();

console.log(enrolled);
```

The method returns a boolean result after the asynchronous operation completes.

Possible results are:

```js
true
```

or:

```js
false
```

---

# What Does `true` Mean?

If:

```js
const enrolled =
  await LocalAuthentication.isEnrolledAsync();
```

returns:

```js
true
```

it means the device reports that an eligible authentication credential is enrolled.

Conceptually:

```text
Device
  ↓
Authentication credential enrolled
  ↓
isEnrolledAsync()
  ↓
true
```

This means your application can proceed with its authentication flow, subject to the other checks and platform behavior.

---

# What Does `false` Mean?

If the result is:

```js
false
```

it means that no eligible biometric credential is currently enrolled according to the device/platform.

For example:

```text
Device has fingerprint hardware
        ↓
But user has not registered fingerprint
        ↓
isEnrolledAsync()
        ↓
false
```

Your application should not assume that biometric authentication is ready.

Depending on the application's design, you might:

* Use another authentication method
* Tell the user that biometric authentication is not configured
* Direct the user to the appropriate device settings
* Hide or disable a biometric-only option

---

# Basic Example

```js
const checkEnrollment = async () => {
  const enrolled =
    await LocalAuthentication.isEnrolledAsync();

  if (enrolled) {
    console.log(
      "Biometric credentials are enrolled"
    );
  } else {
    console.log(
      "No biometric credentials are enrolled"
    );
  }
};
```

The flow is:

```text
isEnrolledAsync()
       ↓
     Result
    /      \
  true    false
   ↓        ↓
Enrolled  Not enrolled
```

---

# Why Is `await` Used?

Just like `hasHardwareAsync()`, this method is asynchronous.

When you call:

```js
LocalAuthentication.isEnrolledAsync()
```

you are working with a Promise.

Therefore, in an `async` function, we normally use:

```js
const enrolled =
  await LocalAuthentication.isEnrolledAsync();
```

Now `enrolled` contains the resolved boolean value.

---

# Example Without `await`

Avoid treating the Promise itself as the final result:

```js
const enrolled =
  LocalAuthentication.isEnrolledAsync();

console.log(enrolled);
```

The value of `enrolled` here is a Promise, not the resolved boolean.

Instead:

```js
const enrolled =
  await LocalAuthentication.isEnrolledAsync();

console.log(enrolled);
```

Now:

```text
true
```

or:

```text
false
```

is available.

---

# Why Do We Need Enrollment?

Consider a phone with a fingerprint sensor.

The hardware exists:

```text
Fingerprint Sensor
       ↓
      YES
```

But suppose the user has never registered a fingerprint.

Then:

```text
Biometric Hardware
       ↓
     Available

Biometric Credential
       ↓
     Not Enrolled
```

If your application only checks:

```js
hasHardwareAsync()
```

it may receive:

```js
true
```

But that doesn't mean the user has configured a biometric credential.

This is why we also check:

```js
isEnrolledAsync()
```

---

# Hardware vs Enrollment

These two methods answer completely different questions.

### `hasHardwareAsync()`

```js
await LocalAuthentication.hasHardwareAsync();
```

asks:

> **Does the device have biometric hardware?**

---

### `isEnrolledAsync()`

```js
await LocalAuthentication.isEnrolledAsync();
```

asks:

> **Is a biometric/local authentication credential enrolled?**

---

# Simple Analogy

Think about a locked door.

### Hardware

```text
Does the door have a lock?
```

This is similar to:

```js
hasHardwareAsync()
```

### Enrollment

```text
Has a key been configured for the lock?
```

This is similar to:

```js
isEnrolledAsync()
```

### Authentication

```text
Can the person unlock the door right now?
```

This is similar to:

```js
authenticateAsync()
```

So:

```text
Hardware
   ↓
Does the lock exist?

Enrollment
   ↓
Is a key configured?

Authentication
   ↓
Can the user unlock it?
```

---

# Three Different States

It is useful to understand these three concepts separately:

```text
1. Hardware
2. Enrollment
3. Authentication
```

They represent different stages.

---

## Stage 1 — Hardware

```js
const hasHardware =
  await LocalAuthentication.hasHardwareAsync();
```

Question:

```text
Does the device have the required hardware?
```

---

## Stage 2 — Enrollment

```js
const enrolled =
  await LocalAuthentication.isEnrolledAsync();
```

Question:

```text
Has the user enrolled a credential?
```

---

## Stage 3 — Authentication

```js
const result =
  await LocalAuthentication.authenticateAsync();
```

Question:

```text
Can the user successfully authenticate now?
```

---

# Complete Conceptual Flow

```text
                  DEVICE
                    │
                    ▼
          Does hardware exist?
                    │
                    ▼
          hasHardwareAsync()
                    │
              ┌─────┴─────┐
              │           │
            false        true
              │           │
              ▼           ▼
           Stop       Is credential
                      enrolled?
                          │
                          ▼
                   isEnrolledAsync()
                          │
                    ┌─────┴─────┐
                    │           │
                  false        true
                    │           │
                    ▼           ▼
                 Fallback   Authenticate
                              user
                                │
                                ▼
                       authenticateAsync()
```

---

# Hardware `true` + Enrollment `false`

This is the most important case to understand.

Suppose:

```js
const hasHardware =
  await LocalAuthentication.hasHardwareAsync();

console.log(hasHardware);
```

returns:

```text
true
```

Then:

```js
const enrolled =
  await LocalAuthentication.isEnrolledAsync();

console.log(enrolled);
```

returns:

```text
false
```

The state is:

```text
Hardware = true
Enrollment = false
```

What does that mean?

```text
Biometric hardware exists
        +
No biometric credential is enrolled
```

Therefore:

```text
 Do not assume biometric authentication is ready
```

---

# Example

```js
const checkBiometricSupport = async () => {

  const hasHardware =
    await LocalAuthentication.hasHardwareAsync();

  console.log(
    "Hardware:",
    hasHardware
  );

  const enrolled =
    await LocalAuthentication.isEnrolledAsync();

  console.log(
    "Enrolled:",
    enrolled
  );
};
```

Possible output:

```text
Hardware: true
Enrolled: false
```

This tells us:

```text
The device supports biometric hardware,
but the user has not enrolled a credential.
```

---

# Hardware `false` + Enrollment `false`

Another possible state is:

```text
Hardware = false
Enrollment = false
```

For example:

```text
No biometric hardware
        ↓
No biometric credential
```

If hardware isn't available, the application generally has no reason to treat biometric authentication as available.

---

# Hardware `true` + Enrollment `true`

The ideal state for offering biometric authentication is:

```text
Hardware = true
Enrollment = true
```

Conceptually:

```text
Biometric hardware available
        +
Authentication credential enrolled
        ↓
Authentication can be attempted
```

At this point, the application can move toward:

```js
LocalAuthentication.authenticateAsync();
```

---

# What About `false` + `true`?

For normal application logic, you should not build your authentication flow around assuming every possible boolean combination represents a meaningful device state.

The important states to reason about are:

```text
Hardware unavailable
Hardware available but not enrolled
Hardware available and enrolled
```

The platform and API determine the actual values returned.

Your application should rely on the API rather than trying to infer device state from theoretical combinations.

---

# Part 5 — Checking Supported Authentication Types

## `supportedAuthenticationTypesAsync()`

A device can support different kinds of biometric authentication.

For example, one device may support:

```text
Fingerprint
```

while another may support:

```text
Face Authentication
```

Some devices/platforms may expose more than one supported authentication type.

Expo provides:

```js
LocalAuthentication.supportedAuthenticationTypesAsync()
```

This method allows your application to find out **which authentication types the device supports**.

---

# What Does `supportedAuthenticationTypesAsync()` Do?

It answers the question:

> **What biometric authentication types are supported by this device?**

Example:

```js
const types =
  await LocalAuthentication
    .supportedAuthenticationTypesAsync();

console.log(types);
```

The method returns a Promise.

After using `await`, the result is an array:

```js
const types =
  await LocalAuthentication
    .supportedAuthenticationTypesAsync();
```

For example, conceptually:

```js
[1]
```

or:

```js
[2]
```

or potentially:

```js
[1, 2]
```

The numbers correspond to values in Expo's `AuthenticationType` enum.

---

# What Is `AuthenticationType`?

Expo provides:

```js
LocalAuthentication.AuthenticationType
```

as an enum containing the supported authentication type constants.

The documented values are:

```js
LocalAuthentication.AuthenticationType.FINGERPRINT
```

```js
LocalAuthentication.AuthenticationType.FACIAL_RECOGNITION
```

```js
LocalAuthentication.AuthenticationType.IRIS
```

The corresponding documented numeric values are:

| Authentication Type  | Value |
| -------------------- | ----: |
| `FINGERPRINT`        |   `1` |
| `FACIAL_RECOGNITION` |   `2` |
| `IRIS`               |   `3` |

For application code, prefer the named enum constants instead of directly writing the numbers.

Use:

```js
LocalAuthentication.AuthenticationType.FINGERPRINT
```

instead of:

```js
1
```

Why?

Because this:

```js
LocalAuthentication.AuthenticationType.FINGERPRINT
```

clearly tells another developer what the value represents.

---

# Why Does the API Return an Array?

The API returns an array because a device can expose multiple supported authentication types.

For example:

```js
[
  LocalAuthentication.AuthenticationType.FINGERPRINT
]
```

means the device supports fingerprint authentication.

Another device might return:

```js
[
  LocalAuthentication.AuthenticationType.FACIAL_RECOGNITION
]
```

meaning facial recognition is supported.

Conceptually, multiple types could be represented as:

```js
[
  LocalAuthentication.AuthenticationType.FINGERPRINT,
  LocalAuthentication.AuthenticationType.FACIAL_RECOGNITION
]
```

The important point is:

> **The result is an array, so you should check whether a particular type exists inside that array.**

---

# Why Do We Use `.includes()`?

Because the result is an array.

For example:

```js
const types = [
  LocalAuthentication.AuthenticationType.FINGERPRINT
];
```

If we want to know whether fingerprint is supported, we can use:

```js
types.includes(
  LocalAuthentication.AuthenticationType.FINGERPRINT
);
```

`.includes()` asks:

> Does this array contain this value?

For example:

```js
const numbers = [1, 2, 3];

console.log(numbers.includes(2));
```

Output:

```text
true
```

But:

```js
console.log(numbers.includes(5));
```

Output:

```text
false
```

The same idea is used with `AuthenticationType`.

---

# Checking for Fingerprint

```js
const types =
  await LocalAuthentication
    .supportedAuthenticationTypesAsync();

if (
  types.includes(
    LocalAuthentication.AuthenticationType.FINGERPRINT
  )
) {
  console.log("Fingerprint is supported");
}
```

The logic is:

```text
Get supported types
        ↓
       types
        ↓
Does types contain FINGERPRINT?
        ↓
      Yes?
        ↓
Fingerprint is supported
```

---

# Checking for Facial Recognition

```js
const types =
  await LocalAuthentication
    .supportedAuthenticationTypesAsync();

if (
  types.includes(
    LocalAuthentication.AuthenticationType.FACIAL_RECOGNITION
  )
) {
  console.log(
    "Facial recognition is supported"
  );
}
```

---

# Checking for Iris

The module also exposes:

```js
LocalAuthentication.AuthenticationType.IRIS
```

You can check it in the same way:

```js
const types =
  await LocalAuthentication
    .supportedAuthenticationTypesAsync();

if (
  types.includes(
    LocalAuthentication.AuthenticationType.IRIS
  )
) {
  console.log("Iris authentication is supported");
}
```

---

# Checking Multiple Authentication Types

You can check several types independently.

```js
const types =
  await LocalAuthentication
    .supportedAuthenticationTypesAsync();

if (
  types.includes(
    LocalAuthentication.AuthenticationType.FINGERPRINT
  )
) {
  console.log("Fingerprint supported");
}

if (
  types.includes(
    LocalAuthentication.AuthenticationType.FACIAL_RECOGNITION
  )
) {
  console.log("Face authentication supported");
}

if (
  types.includes(
    LocalAuthentication.AuthenticationType.IRIS
  )
) {
  console.log("Iris authentication supported");
}
```

This is useful when your application wants to understand the device's available authentication capabilities.

---

# Example Output

Suppose:

```js
const types =
  await LocalAuthentication
    .supportedAuthenticationTypesAsync();
```

returns:

```js
[1]
```

Then:

```js
types.includes(
  LocalAuthentication.AuthenticationType.FINGERPRINT
);
```

will evaluate to:

```js
true
```

while:

```js
types.includes(
  LocalAuthentication.AuthenticationType.FACIAL_RECOGNITION
);
```

will evaluate to:

```js
false
```

---

# Don't Hard-Code the Numbers

You may see:

```js
if (types.includes(1)) {
  console.log("Fingerprint supported");
}
```

This may work because:

```text
FINGERPRINT → 1
```

But it is not the preferred approach.

Instead, write:

```js
if (
  types.includes(
    LocalAuthentication.AuthenticationType.FINGERPRINT
  )
) {
  console.log("Fingerprint supported");
}
```

This is much easier to understand.

Compare:

```js
types.includes(1)
```

with:

```js
types.includes(
  LocalAuthentication.AuthenticationType.FINGERPRINT
)
```

The second version immediately tells us what we are checking.

---

# Why Are Enum Constants Useful?

Suppose another developer sees:

```js
if (types.includes(1)) {
```

They have to remember:

```text
1 = FINGERPRINT
```

But if they see:

```js
if (
  types.includes(
    LocalAuthentication.AuthenticationType.FINGERPRINT
  )
) {
```

the meaning is obvious.

Therefore:

> **Use the named enum constants rather than manually writing numeric values.**

---

# Empty Array

The method can return an empty array:

```js
[]
```

An empty array means that there are no supported biometric authentication types being reported by the device/platform.

Example:

```js
const types =
  await LocalAuthentication
    .supportedAuthenticationTypesAsync();

console.log(types);
```

Possible output:

```js
[]
```

In that case:

```js
types.includes(
  LocalAuthentication.AuthenticationType.FINGERPRINT
);
```

returns:

```js
false
```

and:

```js
types.includes(
  LocalAuthentication.AuthenticationType.FACIAL_RECOGNITION
);
```

also returns:

```js
false
```

---

# Checking Whether Any Type Exists

You can also check whether the array contains anything:

```js
const types =
  await LocalAuthentication
    .supportedAuthenticationTypesAsync();

if (types.length > 0) {
  console.log(
    "At least one authentication type is supported."
  );
} else {
  console.log(
    "No supported authentication types found."
  );
}
```

The important JavaScript concept here is:

```js
types.length
```

For example:

```js
[].length
```

is:

```text
0
```

while:

```js
[1].length
```

is:

```text
1
```

---

# Example — Simple Helper Function

We can create a reusable function:

```js
const getSupportedAuthenticationTypes = async () => {
  const types =
    await LocalAuthentication
      .supportedAuthenticationTypesAsync();

  return types;
};
```

Then:

```js
const types =
  await getSupportedAuthenticationTypes();

console.log(types);
```

---

# Example — Check Fingerprint

A reusable helper can make the code cleaner:

```js
const supportsFingerprint = async () => {
  const types =
    await LocalAuthentication
      .supportedAuthenticationTypesAsync();

  return types.includes(
    LocalAuthentication.AuthenticationType.FINGERPRINT
  );
};
```

Now:

```js
const result = await supportsFingerprint();

console.log(result);
```

Possible output:

```text
true
```

or:

```text
false
```

---

# Example — Check Face Authentication

Similarly:

```js
const supportsFace = async () => {
  const types =
    await LocalAuthentication
      .supportedAuthenticationTypesAsync();

  return types.includes(
    LocalAuthentication.AuthenticationType.FACIAL_RECOGNITION
  );
};
```

Then:

```js
const result = await supportsFace();

console.log(result);
```

---

# Example — React Native UI

Suppose you want to display what type of biometric authentication the device supports.

You could first retrieve the types:

```js
const types =
  await LocalAuthentication
    .supportedAuthenticationTypesAsync();
```

Then determine the type:

```js
if (
  types.includes(
    LocalAuthentication.AuthenticationType.FINGERPRINT
  )
) {
  console.log("Fingerprint authentication available");
}

if (
  types.includes(
    LocalAuthentication.AuthenticationType.FACIAL_RECOGNITION
  )
) {
  console.log("Face authentication available");
}
```

Your UI could then display something such as:

```text
Biometric Authentication

Fingerprint authentication available
```

or:

```text
Biometric Authentication

Face authentication available
```

---

# Example — Choosing a Label

Suppose your button should have a meaningful label.

You could create:

```js
const getBiometricLabel = async () => {
  const types =
    await LocalAuthentication
      .supportedAuthenticationTypesAsync();

  if (
    types.includes(
      LocalAuthentication.AuthenticationType.FACIAL_RECOGNITION
    )
  ) {
    return "Unlock with Face";
  }

  if (
    types.includes(
      LocalAuthentication.AuthenticationType.FINGERPRINT
    )
  ) {
    return "Unlock with Fingerprint";
  }

  return "Unlock with Biometrics";
};
```

Then:

```js
const label = await getBiometricLabel();

console.log(label);
```

The result might be:

```text
Unlock with Face
```

or:

```text
Unlock with Fingerprint
```

or:

```text
Unlock with Biometrics
```

This can make the UI more descriptive.

---

# Very Important: Supported Does Not Mean Enrolled

This is one of the most important concepts in this part.

Suppose:

```js
const types =
  await LocalAuthentication
    .supportedAuthenticationTypesAsync();
```

returns a fingerprint type.

That means:

```text
Fingerprint authentication is supported.
```

It does **not automatically mean**:

```text
The user has enrolled a fingerprint.
```

These are different questions.

---

# Supported vs Enrolled

### Supported Type

```js
supportedAuthenticationTypesAsync()
```

asks:

> What authentication types does the device/platform support?

### Enrollment

```js
isEnrolledAsync()
```

asks:

> Is an authentication credential enrolled?

So:

```text
SUPPORTED
    ↓
"What can this device support?"

ENROLLED
    ↓
"What credential is configured?"
```

---

# Example

Imagine a phone with fingerprint hardware.

The device supports fingerprint authentication:

```text
Supported:
Fingerprint
```

But the user hasn't registered a fingerprint:

```text
Enrolled:
No
```

The results can therefore conceptually be:

```text
supportedAuthenticationTypesAsync()
        ↓
Fingerprint

isEnrolledAsync()
        ↓
false
```

So you should not interpret:

```text
Fingerprint is supported
```

as:

```text
Fingerprint is ready to authenticate
```

---

# Supported vs Enrolled vs Authenticated

Now we have three separate concepts:

```text
1. Supported
2. Enrolled
3. Authenticated
```

### Supported

```js
supportedAuthenticationTypesAsync()
```

means:

```text
"What types are supported?"
```

### Enrolled

```js
isEnrolledAsync()
```

means:

```text
"Is a credential enrolled?"
```

### Authenticated

```js
authenticateAsync()
```

means:

```text
"Can the user authenticate successfully?"
```

Therefore:

```text
Supported
    ↓
Enrolled
    ↓
Authenticate
```

These are not interchangeable.

---

# Example of the Difference

Consider this situation:

```text
Device
 │
 ├── Fingerprint hardware
 │
 ├── Fingerprint supported
 │
 └── No fingerprint enrolled
```

Then:

```js
hasHardwareAsync()
```

may return:

```js
true
```

and:

```js
supportedAuthenticationTypesAsync()
```

may include:

```js
AuthenticationType.FINGERPRINT
```

but:

```js
isEnrolledAsync()
```

may return:

```js
false
```

This tells us:

```text
Hardware exists
        +
Fingerprint is supported
        +
No credential is enrolled
```

So authentication is not yet ready.

---

# Authentication Type Does Not Authenticate the User

Calling:

```js
LocalAuthentication.AuthenticationType.FINGERPRINT
```

does not start authentication.

It is simply an enum value used to identify an authentication type.

For example:

```js
const types =
  await LocalAuthentication
    .supportedAuthenticationTypesAsync();

if (
  types.includes(
    LocalAuthentication.AuthenticationType.FINGERPRINT
  )
) {
  console.log("Fingerprint is supported");
}
```

Nothing here asks the user to provide a fingerprint.

The actual authentication operation is:

```js
const result =
  await LocalAuthentication.authenticateAsync();
```

---

# Important Comparison

| Code                                  | What it does                                            |
| ------------------------------------- | ------------------------------------------------------- |
| `hasHardwareAsync()`                  | Checks whether biometric hardware exists                |
| `isEnrolledAsync()`                   | Checks whether an authentication credential is enrolled |
| `supportedAuthenticationTypesAsync()` | Gets supported authentication types                     |
| `getEnrolledLevelAsync()`             | Gets the enrolled authentication security level         |
| `authenticateAsync()`                 | Starts actual authentication                            |

Think:

```text
Hardware
   ↓
Enrollment
   ↓
Supported Types
   ↓
Security Level
   ↓
Authentication
```

The first four are information/capability checks.

The last one actually performs authentication.

---

# Example — Complete Capability Check

We can now combine the first three concepts:

```js
const checkBiometricCapability = async () => {

  // Step 1: Hardware
  const hasHardware =
    await LocalAuthentication.hasHardwareAsync();

  if (!hasHardware) {
    return false;
  }

  // Step 2: Enrollment
  const enrolled =
    await LocalAuthentication.isEnrolledAsync();

  if (!enrolled) {
    return false;
  }

  // Step 3: Supported types
  const types =
    await LocalAuthentication
      .supportedAuthenticationTypesAsync();

  if (types.length === 0) {
    return false;
  }

  return true;
};
```

The flow is:

```text
Hardware?
   ↓
Enrollment?
   ↓
Supported authentication type?
   ↓
Ready to attempt authentication
```

---

# Example — More Detailed Result

Instead of returning only `true` or `false`, we can return useful information.

```js
const getBiometricInfo = async () => {

  const hasHardware =
    await LocalAuthentication.hasHardwareAsync();

  const enrolled =
    await LocalAuthentication.isEnrolledAsync();

  const types =
    await LocalAuthentication
      .supportedAuthenticationTypesAsync();

  return {
    hasHardware,
    enrolled,
    types,
  };
};
```

Then:

```js
const info = await getBiometricInfo();

console.log(info);
```

Conceptually, you might get:

```js
{
  hasHardware: true,
  enrolled: true,
  types: [1]
}
```

Now your application has more information about the device.

---

# Example — Detect Fingerprint or Face

```js
const getBiometricType = async () => {

  const types =
    await LocalAuthentication
      .supportedAuthenticationTypesAsync();

  if (
    types.includes(
      LocalAuthentication.AuthenticationType.FACIAL_RECOGNITION
    )
  ) {
    return "Face";
  }

  if (
    types.includes(
      LocalAuthentication.AuthenticationType.FINGERPRINT
    )
  ) {
    return "Fingerprint";
  }

  if (
    types.includes(
      LocalAuthentication.AuthenticationType.IRIS
    )
  ) {
    return "Iris";
  }

  return "Unknown";
};
```

Then:

```js
const type = await getBiometricType();

console.log(type);
```

Possible output:

```text
Face
```

or:

```text
Fingerprint
```

or:

```text
Iris
```

---

# Important Note About Platform Differences

Biometric authentication is ultimately provided by the underlying device operating system.

Therefore, the exact authentication mechanisms available can vary depending on:

```text
Platform
Device
Operating-system capabilities
Device configuration
```

Do not assume that every Android or iOS device supports the same biometric technology.

This is exactly why capability APIs such as:

```js
supportedAuthenticationTypesAsync()
```

are useful.

Instead of hard-coding:

```text
"Every phone has fingerprint"
```

your application asks the device what it supports.

---

# Complete Flow Learned So Far

We now have three important APIs:

```text
1. hasHardwareAsync()
2. isEnrolledAsync()
3. supportedAuthenticationTypesAsync()
```

Their responsibilities are:

```text
hasHardwareAsync()
        ↓
"Does hardware exist?"

isEnrolledAsync()
        ↓
"Is a credential enrolled?"

supportedAuthenticationTypesAsync()
        ↓
"What authentication types are supported?"
```

Then:

```text
authenticateAsync()
        ↓
"Authenticate the user."
```

---

# Visual Mental Model

```text
                 DEVICE
                    │
                    ▼
        ┌─────────────────────┐
        │ Biometric hardware? │
        └──────────┬──────────┘
                   │
                   ▼
          hasHardwareAsync()
                   │
              ┌────┴────┐
              │         │
             No        Yes
              │         │
              ▼         ▼
          Fallback   Credential
                     enrolled?
                         │
                         ▼
                  isEnrolledAsync()
                         │
                    ┌────┴────┐
                    │         │
                   No        Yes
                    │         │
                    ▼         ▼
                Fallback   Supported
                           types?
                              │
                              ▼
             supportedAuthenticationTypesAsync()
                              │
                              ▼
                     Fingerprint / Face /
                         Iris / etc.
                              │
                              ▼
                       Ready to offer
                       authentication
                              │
                              ▼
                    authenticateAsync()
                              │
                         ┌────┴────┐
                         │         │
                      Success    Failure
                         │         │
                         ▼         ▼
                    Allow access  Keep
                                  protected
```

---

# Part 6 — Understanding Authentication Security Levels

## `getEnrolledLevelAsync()`

So far, we have learned that local authentication can answer questions such as:

```text
Does the device have biometric hardware?
        ↓
hasHardwareAsync()

Is an authentication credential enrolled?
        ↓
isEnrolledAsync()

What authentication types are supported?
        ↓
supportedAuthenticationTypesAsync()
```

There is another important question:

> **What security level does the enrolled authentication have?**

Expo provides:

```js
LocalAuthentication.getEnrolledLevelAsync()
```

This API gives your application information about the **security level of the authentication credential enrolled on the device**.

---

# Why Do We Need a Security Level?

Consider two devices.

### Device A

```text
Fingerprint
```

### Device B

```text
Face Authentication
```

Knowing the authentication type tells us:

```text
"What authentication mechanism is available?"
```

But it doesn't describe the security classification of that authentication.

The security-level API gives us another piece of information:

```text
"How is the enrolled authentication classified in terms of security?"
```

So:

```text
Authentication Type
        ↓
Fingerprint / Face / Iris

Security Level
        ↓
None / Secret / Weak / Strong
```

These are **different concepts**.

---

# `SecurityLevel`

Expo provides the `SecurityLevel` enum:

```js
LocalAuthentication.SecurityLevel
```

It contains:

```js
LocalAuthentication.SecurityLevel.NONE
```

```js
LocalAuthentication.SecurityLevel.SECRET
```

```js
LocalAuthentication.SecurityLevel.BIOMETRIC_WEAK
```

```js
LocalAuthentication.SecurityLevel.BIOMETRIC_STRONG
```

The documented numeric values are:

| Security Level     | Value | General Meaning                               |
| ------------------ | ----: | --------------------------------------------- |
| `NONE`             |   `0` | No enrolled authentication                    |
| `SECRET`           |   `1` | Non-biometric secret such as a PIN or pattern |
| `BIOMETRIC_WEAK`   |   `2` | Weaker biometric security class               |
| `BIOMETRIC_STRONG` |   `3` | Strong biometric security class               |

For application code, prefer the named constants instead of directly comparing numbers.

Use:

```js
LocalAuthentication.SecurityLevel.BIOMETRIC_STRONG
```

instead of:

```js
3
```

---

# The Four Security Levels

Let's understand each level individually.

---

# 1. `NONE`

```js
LocalAuthentication.SecurityLevel.NONE
```

Value:

```text
0
```

Conceptually:

```text
No enrolled authentication
```

This represents the lowest level because there is no enrolled authentication credential at the relevant security level.

Example:

```js
const level =
  await LocalAuthentication.getEnrolledLevelAsync();

if (
  level ===
  LocalAuthentication.SecurityLevel.NONE
) {
  console.log(
    "No enrolled authentication available."
  );
}
```

---

# 2. `SECRET`

```js
LocalAuthentication.SecurityLevel.SECRET
```

Value:

```text
1
```

This represents a **non-biometric secret-based authentication mechanism**.

Examples can include:

```text
PIN
Pattern
Passcode
```

The important point is:

```text
SECRET ≠ Biometric
```

It represents a secret-based authentication mechanism rather than a biometric classification.

---

# Example

```js
const level =
  await LocalAuthentication.getEnrolledLevelAsync();

if (
  level ===
  LocalAuthentication.SecurityLevel.SECRET
) {
  console.log(
    "A secret-based authentication method is enrolled."
  );
}
```

---

# 3. `BIOMETRIC_WEAK`

```js
LocalAuthentication.SecurityLevel.BIOMETRIC_WEAK
```

Value:

```text
2
```

This represents a biometric authentication mechanism that belongs to the platform's **weaker biometric security class**.

The important thing to understand is that:

```text
BIOMETRIC_WEAK
```

does **not** mean:

```text
"Biometric authentication is broken."
```

It is a security classification defined by the underlying platform.

Think of it as:

```text
Biometric
    ↓
Platform classifies it
    ↓
WEAK
```

---

# 4. `BIOMETRIC_STRONG`

```js
LocalAuthentication.SecurityLevel.BIOMETRIC_STRONG
```

Value:

```text
3
```

This represents a biometric authentication mechanism classified as **strong biometric authentication** by the platform.

For example, an application with a higher security requirement may want to know whether the enrolled authentication meets a strong biometric classification.

Example:

```js
const level =
  await LocalAuthentication.getEnrolledLevelAsync();

if (
  level ===
  LocalAuthentication.SecurityLevel.BIOMETRIC_STRONG
) {
  console.log(
    "Strong biometric authentication is enrolled."
  );
}
```

---

# Important: Security Level Is Not the Same as Authentication Type

This is probably the most important concept in this part.

Authentication type answers:

> **What kind of authentication is this?**

For example:

```text
Fingerprint
Face
Iris
```

Security level answers:

> **What security classification does the enrolled authentication have?**

For example:

```text
None
Secret
Biometric Weak
Biometric Strong
```

So:

```text
              AUTHENTICATION
                    │
          ┌─────────┴─────────┐
          │                   │
       TYPE                SECURITY
          │                   │
    Fingerprint             Weak
    Face                    Strong
    Iris                    Secret
```

---

# Example

Suppose a device supports fingerprint.

You might use:

```js
const types =
  await LocalAuthentication
    .supportedAuthenticationTypesAsync();
```

This tells you about the **authentication type**.

You might get:

```text
Fingerprint
```

Separately:

```js
const level =
  await LocalAuthentication
    .getEnrolledLevelAsync();
```

might return a security level such as:

```text
BIOMETRIC_STRONG
```

These two pieces of information describe different properties.

Conceptually:

```text
Type:
Fingerprint

Security Level:
BIOMETRIC_STRONG
```

---

# Another Example

Imagine a device with a facial authentication system.

You could have:

```text
Authentication Type
        ↓
FACIAL_RECOGNITION
```

and separately:

```text
Security Level
        ↓
BIOMETRIC_WEAK
```

The first tells you:

```text
"What type?"
```

The second tells you:

```text
"What security classification?"
```

---

# `getEnrolledLevelAsync()`

Now let's use the actual API.

```js
const level =
  await LocalAuthentication.getEnrolledLevelAsync();

console.log(level);
```

The returned value corresponds to one of the `SecurityLevel` values.

You should compare the result against the enum:

```js
if (
  level ===
  LocalAuthentication.SecurityLevel.BIOMETRIC_STRONG
) {
  console.log("Strong biometric authentication enrolled");
}
```

---

# Why Use `await`?

Just like the other Expo Local Authentication APIs, this method is asynchronous.

Therefore:

```js
const level =
  await LocalAuthentication.getEnrolledLevelAsync();
```

gives you the resolved value.

Conceptually:

```text
getEnrolledLevelAsync()
        ↓
     Promise
        ↓
      await
        ↓
 SecurityLevel value
```

---

# Example — Checking All Security Levels

You can handle every documented value:

```js
const checkSecurityLevel = async () => {

  const level =
    await LocalAuthentication.getEnrolledLevelAsync();

  if (
    level ===
    LocalAuthentication.SecurityLevel.NONE
  ) {
    console.log(
      "No enrolled authentication."
    );

  } else if (
    level ===
    LocalAuthentication.SecurityLevel.SECRET
  ) {
    console.log(
      "Secret-based authentication enrolled."
    );

  } else if (
    level ===
    LocalAuthentication.SecurityLevel.BIOMETRIC_WEAK
  ) {
    console.log(
      "Weak biometric authentication enrolled."
    );

  } else if (
    level ===
    LocalAuthentication.SecurityLevel.BIOMETRIC_STRONG
  ) {
    console.log(
      "Strong biometric authentication enrolled."
    );
  }
};
```

This is a good learning example because you can see all possible enum values.

---

# Better Approach — Store the Enum

You can also make the code easier to read:

```js
const SecurityLevel =
  LocalAuthentication.SecurityLevel;

const level =
  await LocalAuthentication.getEnrolledLevelAsync();

if (level === SecurityLevel.NONE) {
  console.log("No authentication");

} else if (level === SecurityLevel.SECRET) {
  console.log("Secret authentication");

} else if (level === SecurityLevel.BIOMETRIC_WEAK) {
  console.log("Weak biometric");

} else if (level === SecurityLevel.BIOMETRIC_STRONG) {
  console.log("Strong biometric");
}
```

This avoids repeatedly typing:

```js
LocalAuthentication.SecurityLevel
```

---

# Don't Compare With Numbers

You might see:

```js
if (level === 3) {
  console.log("Strong biometric");
}
```

Because:

```text
BIOMETRIC_STRONG → 3
```

However, this is less readable.

Prefer:

```js
if (
  level ===
  LocalAuthentication.SecurityLevel.BIOMETRIC_STRONG
) {
  console.log("Strong biometric");
}
```

The second version makes the intent obvious.

---

# Why Might an Application Care About Strong Biometrics?

Imagine an application has a sensitive operation:

```text
Change Bank Account
        ↓
Very Sensitive
```

The application might have a security policy requiring a strong authentication mechanism.

Conceptually:

```text
Sensitive Operation
        ↓
Check Security Level
        ↓
Is it BIOMETRIC_STRONG?
        ↓
Yes → Continue
No  → Use another authentication method
```

However, an important point is:

> **The application should define its security requirements carefully.**

You should not assume that simply checking `BIOMETRIC_STRONG` automatically makes an operation secure.

The overall authentication design matters.

---

# Example — High-Security Feature

```js
const canAccessHighSecurityFeature = async () => {

  const level =
    await LocalAuthentication.getEnrolledLevelAsync();

  return (
    level ===
    LocalAuthentication.SecurityLevel.BIOMETRIC_STRONG
  );
};
```

Then:

```js
const allowed =
  await canAccessHighSecurityFeature();

if (allowed) {
  console.log(
    "Strong biometric requirement satisfied."
  );
} else {
  console.log(
    "Strong biometric requirement not satisfied."
  );
}
```

---

# Example — Protecting a Sensitive Action

Suppose your app has:

```text
Delete All Documents
```

You might conceptually perform:

```text
User taps "Delete All"
        ↓
Check authentication requirements
        ↓
Check security level
        ↓
Authenticate user
        ↓
If authentication succeeds
        ↓
Perform deletion
```

Notice something important:

```text
Security Level Check
```

and:

```text
Authentication
```

are still separate operations.

Knowing that strong biometric authentication is enrolled does **not** itself authenticate the user.

---

# Security Level Does Not Authenticate the User

This is another major point.

Calling:

```js
await LocalAuthentication.getEnrolledLevelAsync();
```

does not show a biometric prompt.

It does not ask the user to:

```text
Touch the fingerprint sensor
```

or:

```text
Look at the device
```

It only retrieves the enrolled security level.

So:

```text
getEnrolledLevelAsync()
        ↓
"What security level is enrolled?"
```

while:

```text
authenticateAsync()
        ↓
"Authenticate the user now."
```

---

# Security Level vs Enrollment

These two APIs are also related but different.

### `isEnrolledAsync()`

```js
await LocalAuthentication.isEnrolledAsync();
```

answers:

```text
Is an authentication credential enrolled?
```

Result:

```text
true / false
```

---

### `getEnrolledLevelAsync()`

```js
await LocalAuthentication.getEnrolledLevelAsync();
```

answers:

```text
What security level does the enrolled authentication have?
```

Result:

```text
SecurityLevel
```

---

# Example

Suppose:

```js
const enrolled =
  await LocalAuthentication.isEnrolledAsync();
```

returns:

```text
true
```

That tells us:

```text
Something is enrolled.
```

Then:

```js
const level =
  await LocalAuthentication.getEnrolledLevelAsync();
```

may provide additional information about its security classification.

Conceptually:

```text
isEnrolledAsync()
        ↓
      true
        ↓
Credential exists

getEnrolledLevelAsync()
        ↓
BIOMETRIC_STRONG
        ↓
Security classification
```

---

# Security Level vs Supported Type

There is another important difference.

`supportedAuthenticationTypesAsync()` asks:

```text
What authentication types does the device support?
```

For example:

```text
Fingerprint
Face
Iris
```

Whereas:

```js
getEnrolledLevelAsync()
```

asks about the security level of the enrolled authentication.

So:

```text
SUPPORTED TYPE
       ↓
Fingerprint / Face / Iris

ENROLLED SECURITY LEVEL
       ↓
Secret / Weak / Strong
```

---

# Three APIs Compared

| API                                   | Question                         | Result           |
| ------------------------------------- | -------------------------------- | ---------------- |
| `isEnrolledAsync()`                   | Is a credential enrolled?        | `true` / `false` |
| `supportedAuthenticationTypesAsync()` | What types are supported?        | Array            |
| `getEnrolledLevelAsync()`             | What security level is enrolled? | `SecurityLevel`  |

And finally:

| API                   | Question                                |
| --------------------- | --------------------------------------- |
| `authenticateAsync()` | Can the user authenticate successfully? |

---

# Complete Mental Model

At this point, we can think about local authentication in layers:

```text
                 DEVICE
                    │
                    ▼
        ┌─────────────────────┐
        │ Hardware available? │
        └──────────┬──────────┘
                   │
                   ▼
          hasHardwareAsync()
                   │
                   ▼
        ┌─────────────────────┐
        │ Credential enrolled?│
        └──────────┬──────────┘
                   │
                   ▼
           isEnrolledAsync()
                   │
                   ▼
        ┌─────────────────────┐
        │ Supported types?    │
        └──────────┬──────────┘
                   │
                   ▼
 supportedAuthenticationTypesAsync()
                   │
                   ▼
        Fingerprint / Face / Iris
                   │
                   ▼
        ┌─────────────────────┐
        │ Security level?     │
        └──────────┬──────────┘
                   │
                   ▼
       getEnrolledLevelAsync()
                   │
                   ▼
          None / Secret /
        Weak / Strong
                   │
                   ▼
            Authentication
                   │
                   ▼
          authenticateAsync()
```

---

# Example — Complete Information Check

We can collect the information together:

```js
const getAuthenticationInfo = async () => {

  const hasHardware =
    await LocalAuthentication.hasHardwareAsync();

  const enrolled =
    await LocalAuthentication.isEnrolledAsync();

  const types =
    await LocalAuthentication
      .supportedAuthenticationTypesAsync();

  const level =
    await LocalAuthentication
      .getEnrolledLevelAsync();

  return {
    hasHardware,
    enrolled,
    types,
    level,
  };
};
```

Then:

```js
const info =
  await getAuthenticationInfo();

console.log(info);
```

Conceptually, the result could look like:

```js
{
  hasHardware: true,
  enrolled: true,
  types: [1],
  level: 3
}
```

Instead of remembering what `1` and `3` mean, compare them with the Expo constants.

---

# Example — Converting the Level Into Text

For debugging or displaying information, you can create a helper:

```js
const getSecurityLevelName = (level) => {

  if (
    level ===
    LocalAuthentication.SecurityLevel.NONE
  ) {
    return "None";
  }

  if (
    level ===
    LocalAuthentication.SecurityLevel.SECRET
  ) {
    return "Secret";
  }

  if (
    level ===
    LocalAuthentication.SecurityLevel.BIOMETRIC_WEAK
  ) {
    return "Biometric Weak";
  }

  if (
    level ===
    LocalAuthentication.SecurityLevel.BIOMETRIC_STRONG
  ) {
    return "Biometric Strong";
  }

  return "Unknown";
};
```

Usage:

```js
const level =
  await LocalAuthentication.getEnrolledLevelAsync();

console.log(
  getSecurityLevelName(level)
);
```

Possible output:

```text
Biometric Strong
```

---

# Example — Showing Information in React Native

For learning/debugging purposes, you could display the information:

```js
<Text>
  Security Level: {securityLevel}
</Text>
```

Or convert the enum into a human-readable string first:

```js
<Text>
  Security Level: {securityLevelName}
</Text>
```

For example:

```text
Security Level: Biometric Strong
```

Remember that security-level values are primarily useful for **application logic**, not necessarily something you need to expose to normal users.

---

# When Should You Use `getEnrolledLevelAsync()`?

You may use it when your application needs to distinguish between different authentication security classifications.

For example:

```text
High-security feature
        ↓
Requires strong authentication
        ↓
Check enrolled security level
```

It can also be useful when:

* Building a security settings screen
* Diagnosing authentication capabilities
* Applying security policies
* Deciding whether an authentication method meets a requirement
* Understanding the device's enrolled authentication configuration

For many ordinary applications, you may not need to check the security level at all.

---

# When You May NOT Need It

If your application simply needs:

```text
"Unlock this screen using device authentication."
```

you may not need to manually inspect every security level.

A simpler flow can be:

```text
Check capability
      ↓
Authenticate
      ↓
Check result
```

The security-level API becomes useful when your application has a specific security policy that depends on authentication strength.

---

# Android-Specific Note

Expo's documentation includes an Android-specific detail concerning older Android versions.

On Android versions **before Android M**, `SECRET` may be returned in a situation involving SIM-lock enrollment.

This does **not** mean:

```text
authenticateAsync()
```

will use the SIM lock as the authentication method for your application.

In other words:

```text
SecurityLevel.SECRET
```

in this particular Android scenario should not be interpreted as:

```text
"Expo will show the SIM-lock authentication prompt."
```

The security-level result and the actual authentication prompt are separate concepts.

---

# Important Security Distinction

Always remember:

```text
ENROLLED
    ↓
The credential exists.

SECURITY LEVEL
    ↓
The credential has a security classification.

AUTHENTICATION
    ↓
The user successfully proved their identity
through the authentication flow.
```

These are three different things.

---

# Practical Example

Imagine a banking app.

The user wants to open:

```text
Transfer Money
```

The application might conceptually do:

```text
User taps Transfer Money
          ↓
Check authentication capability
          ↓
Check security requirements
          ↓
Start authentication
          ↓
User authenticates
          ↓
Authentication succeeds?
       /          \
     Yes           No
      ↓             ↓
Continue        Keep screen
                protected
```

The security-level API can help with the **security requirement**, while `authenticateAsync()` performs the actual verification.

---

# API Comparison — Parts 

| API                                   | Main Question                    | Result                |
| ------------------------------------- | -------------------------------- | --------------------- |
| `hasHardwareAsync()`                  | Does hardware exist?             | Boolean               |
| `isEnrolledAsync()`                   | Is a credential enrolled?        | Boolean               |
| `supportedAuthenticationTypesAsync()` | What types are supported?        | Array                 |
| `getEnrolledLevelAsync()`             | What security level is enrolled? | `SecurityLevel`       |
| `authenticateAsync()`                 | Can the user authenticate?       | Authentication result |

---

# Part 7 — Authenticating the User

We have now learned how to inspect the device:

```text
Hardware available?
        ↓
hasHardwareAsync()

Credential enrolled?
        ↓
isEnrolledAsync()

What authentication types are supported?
        ↓
supportedAuthenticationTypesAsync()

What security level is enrolled?
        ↓
getEnrolledLevelAsync()
```

All of these APIs are primarily **checking or retrieving information**.

Now we reach the API that actually asks the user to authenticate:

```js
LocalAuthentication.authenticateAsync()
```

This is the most important API in `expo-local-authentication`.

---

# What Does `authenticateAsync()` Do?

`authenticateAsync()` starts the device's local authentication process.

In simple words:

> **Ask the device to verify the user.**

Depending on the device and its configuration, the user may be asked to authenticate using:

```text
Fingerprint

Face

Other supported biometric authentication

Device authentication fallback
```

The exact authentication UI is controlled by the operating system.

Your React Native application does **not** implement the fingerprint scanner or face-recognition algorithm itself.

Instead:

```text
Your React Native App
        ↓
authenticateAsync()
        ↓
Operating System
        ↓
Authentication Prompt
        ↓
User Authenticates
        ↓
Result returned to App
```

---

# Basic Syntax

The simplest usage is:

```js
const result =
  await LocalAuthentication.authenticateAsync();
```

Because authentication requires user interaction, this function is asynchronous.

Therefore:

```js
await
```

is normally used inside an `async` function.

---

# Basic Example

```js
import * as LocalAuthentication
  from "expo-local-authentication";

const authenticateUser = async () => {
  const result =
    await LocalAuthentication.authenticateAsync();

  console.log(result);
};
```

Then you can call:

```js
authenticateUser();
```

This starts the authentication process.

---

# What Happens When This Code Runs?

Suppose the device has biometric authentication configured.

When:

```js
await LocalAuthentication.authenticateAsync();
```

runs, the operating system may show an authentication prompt.

Conceptually:

```text
User taps "Unlock"
        ↓
authenticateAsync()
        ↓
Device authentication prompt
        ↓
┌─────────────────────────┐
│ Authenticate to continue│
│                         │
│                       │
│                         │
│      [Cancel]           │
└─────────────────────────┘
        ↓
User authenticates
        ↓
Result returned
```

Your application then uses that result.

---

# The Result Object

`authenticateAsync()` returns a result object.

The most important property is:

```js
result.success
```

This tells your application whether authentication succeeded.

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync();

if (result.success) {
  console.log("Authentication successful");
} else {
  console.log("Authentication failed");
}
```

The basic mental model is:

```text
authenticateAsync()
        ↓
     result
      /   \
 success  failure
   ↓        ↓
 true     false
```

---

# When `success` Is `true`

If:

```js
result.success === true
```

the authentication attempt succeeded.

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync();

if (result.success) {
  console.log("User authenticated");
}
```

At this point, your application can perform whatever protected action is appropriate.

For example:

```text
Authentication successful
        ↓
Open protected screen
```

or:

```text
Authentication successful
        ↓
Show sensitive information
```

or:

```text
Authentication successful
        ↓
Continue sensitive operation
```

---

# When `success` Is `false`

If:

```js
result.success === false
```

the authentication attempt did not succeed.

For example:

```js
const result =
  await LocalAuthentication.authenticateAsync();

if (result.success) {
  console.log("Authentication successful");
} else {
  console.log("Authentication unsuccessful");
  console.log(result.error);
}
```

The result can also contain an error describing what happened.

---

# Why Should We Check `success`?

Do not simply write:

```js
await LocalAuthentication.authenticateAsync();

navigate("/home");
```

This would be a bad authentication flow because you are not checking whether authentication actually succeeded.

Instead:

```js
const result =
  await LocalAuthentication.authenticateAsync();

if (result.success) {
  navigate("/home");
}
```

Now the protected screen is opened only after successful authentication.

The important rule is:

> **Never treat calling `authenticateAsync()` as proof that authentication succeeded. Check the returned result.**

---

# Authentication Is Not the Same as Calling the API

This distinction is extremely important.

Calling:

```js
LocalAuthentication.authenticateAsync();
```

means:

```text
"Start authentication."
```

It does **not** mean:

```text
"The user has authenticated successfully."
```

The correct sequence is:

```text
Call authenticateAsync()
        ↓
Wait for result
        ↓
Check result.success
        ↓
If true → authenticated
If false → not authenticated
```

---

# Complete Basic Example

```js
import * as LocalAuthentication
  from "expo-local-authentication";

const authenticateUser = async () => {

  const result =
    await LocalAuthentication.authenticateAsync();

  if (result.success) {
    console.log("Authentication successful");
  } else {
    console.log("Authentication failed");
    console.log("Error:", result.error);
  }
};
```

---

# Adding Hardware Check

Although `authenticateAsync()` can be called directly, applications often check device capability first.

```js
const authenticateUser = async () => {

  const hasHardware =
    await LocalAuthentication.hasHardwareAsync();

  if (!hasHardware) {
    console.log(
      "Biometric hardware is unavailable."
    );
    return;
  }

  const result =
    await LocalAuthentication.authenticateAsync();

  if (result.success) {
    console.log("Authentication successful");
  } else {
    console.log("Authentication failed");
  }
};
```

The flow becomes:

```text
Hardware available?
        ↓
       Yes
        ↓
Authenticate
        ↓
Check result
```

---

# Adding Enrollment Check

We can make the check more complete:

```js
const authenticateUser = async () => {

  const hasHardware =
    await LocalAuthentication.hasHardwareAsync();

  if (!hasHardware) {
    console.log(
      "Biometric hardware is unavailable."
    );
    return;
  }

  const enrolled =
    await LocalAuthentication.isEnrolledAsync();

  if (!enrolled) {
    console.log(
      "No biometric credentials are enrolled."
    );
    return;
  }

  const result =
    await LocalAuthentication.authenticateAsync();

  if (result.success) {
    console.log("Authentication successful");
  } else {
    console.log("Authentication failed");
    console.log(result.error);
  }
};
```

Now the flow is:

```text
             Start
               ↓
      Hardware available?
          /          \
        No            Yes
        ↓              ↓
     Stop          Enrolled?
                    /      \
                  No        Yes
                  ↓          ↓
                Stop     Authenticate
                              ↓
                        Check result
                         /        \
                      Success    Failure
                        ↓           ↓
                    Continue      Stop
```

---

# Do We Always Need These Pre-Checks?

Not necessarily.

`authenticateAsync()` itself interacts with the platform authentication system.

The pre-checks are useful when you want to:

* Provide better UI
* Show an appropriate message
* Decide whether to offer biometric login
* Explain why authentication cannot be used
* Build a more controlled authentication flow

For a simple application, you may choose to call `authenticateAsync()` and handle its result/error.

For a more polished application, capability checks can improve the user experience.

---

# Authentication Options

`authenticateAsync()` accepts an options object.

The basic pattern is:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    // options
  });
```

For example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage: "Authenticate to continue",
  });
```

The options allow you to customize parts of the authentication request.

---

# `promptMessage`

One of the most useful options is:

```js
promptMessage
```

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage: "Authenticate to open Secure Notes",
  });
```

This provides a message associated with the authentication request.

A good prompt should explain **why authentication is being requested**.

For example:

```js
promptMessage: "Authenticate to view your private notes"
```

is better than something vague such as:

```js
promptMessage: "Authentication"
```

---

# Good Prompt Messages

For a banking feature:

```js
promptMessage: "Authenticate to view your account"
```

For private notes:

```js
promptMessage: "Authenticate to open your private notes"
```

For application settings:

```js
promptMessage: "Authenticate to access security settings"
```

For a protected document:

```js
promptMessage: "Authenticate to open this document"
```

The message should describe the purpose of authentication.

---

# Example — Protected Screen

Imagine a private notes application.

The user taps:

```text
Open Private Notes
```

Your code could be:

```js
const openPrivateNotes = async () => {

  const result =
    await LocalAuthentication.authenticateAsync({
      promptMessage:
        "Authenticate to open your private notes",
    });

  if (result.success) {
    console.log("Opening private notes...");
  } else {
    console.log("Authentication failed");
  }
};
```

The logic is:

```text
Tap Private Notes
        ↓
Authentication Prompt
        ↓
User authenticates
        ↓
Success?
   /          \
 Yes           No
 ↓             ↓
Open Notes    Stay protected
```

---

# `cancelLabel`

The authentication request can also provide a custom label for the cancel action.

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage: "Authenticate to continue",
    cancelLabel: "Cancel",
  });
```

This can be useful when you want the cancellation action to be clearly represented in the authentication UI.

---

# `disableDeviceFallback`

Another option is:

```js
disableDeviceFallback
```

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage: "Authenticate to continue",
    disableDeviceFallback: true,
  });
```

This option controls whether device-level fallback authentication is allowed.

Conceptually:

```text
Biometric authentication
        ↓
Fails / unavailable
        ↓
Can device authentication fallback be used?
```

With:

```js
disableDeviceFallback: true
```

you are requesting that device fallback not be allowed.

---

# Why Is Device Fallback Important?

Suppose the device supports biometric authentication but the user cannot successfully use it.

Depending on platform behavior and configuration, the authentication system may offer another device authentication method.

For example:

```text
Biometric
   ↓
Unable to authenticate
   ↓
Device passcode / PIN / other fallback
```

Whether and how this happens is controlled by the underlying operating system.

If your application has a specific requirement around biometric-only authentication, this option becomes important.

---

# Important Security Consideration

Do not automatically assume:

```js
disableDeviceFallback: true
```

is always better.

It depends on the application's security requirements.

For example:

```text
Normal app
→ Device fallback may be acceptable

Very specific biometric-only requirement
→ Fallback may need to be disabled
```

Your security policy should determine the appropriate behavior.

---

# `fallbackLabel`

Another option is:

```js
fallbackLabel
```

It controls the label associated with a fallback action on supported platforms.

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage: "Authenticate to continue",
    fallbackLabel: "Use Passcode",
  });
```

This is primarily useful when you want to customize the wording of the fallback action where the platform supports it.

---

# `biometricsSecurityLevel`

On Android, Expo provides an option for controlling the biometric security level accepted by the authentication request.

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage: "Authenticate to continue",
    biometricsSecurityLevel: "strong",
  });
```

The option is relevant when your application has a requirement concerning the strength of biometric authentication.

The important concept is:

```text
Authentication Type
        ↓
Fingerprint / Face / etc.

Biometric Security Level
        ↓
Weak / Strong
```

This is related to the security-level concepts we discussed in Part 6.

---

# Why Would We Use `biometricsSecurityLevel`?

Imagine an application has a sensitive operation:

```text
Change important security settings
```

The application may have a requirement that only stronger biometric authentication be accepted.

Conceptually:

```text
Sensitive operation
        ↓
Require stronger biometric authentication
        ↓
Authenticate
```

This is an advanced option and should be used only when the application actually has such a requirement.

---

# Example — Strong Biometric Requirement

Conceptually:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage:
      "Authenticate to change security settings",
    biometricsSecurityLevel: "strong",
  });

if (result.success) {
  console.log("Authentication successful");
}
```

The exact accepted values and platform behavior should follow the Expo SDK version being used.

---

# Authentication Options Together

A request can combine options:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage:
      "Authenticate to open your private notes",

    cancelLabel: "Cancel",

    disableDeviceFallback: false,

    fallbackLabel: "Use Passcode",
  });
```

Do not add every option just because it exists.

Only configure options that match your application's requirements.

---

# A Good Authentication Function

A clean application can keep authentication logic in one function:

```js
const authenticateUser = async () => {

  const hasHardware =
    await LocalAuthentication.hasHardwareAsync();

  if (!hasHardware) {
    return {
      success: false,
      reason: "NO_HARDWARE",
    };
  }

  const enrolled =
    await LocalAuthentication.isEnrolledAsync();

  if (!enrolled) {
    return {
      success: false,
      reason: "NOT_ENROLLED",
    };
  }

  const result =
    await LocalAuthentication.authenticateAsync({
      promptMessage:
        "Authenticate to continue",
    });

  if (result.success) {
    return {
      success: true,
    };
  }

  return {
    success: false,
    reason: result.error,
  };
};
```

Now the rest of the application doesn't need to know all the implementation details.

---

# Using the Authentication Function

For example:

```js
const handleProtectedAction = async () => {

  const result =
    await authenticateUser();

  if (result.success) {
    console.log(
      "Perform protected action"
    );
    return;
  }

  console.log(
    "Authentication was not successful:",
    result.reason
  );
};
```

This gives you a clean architecture:

```text
UI
 ↓
handleProtectedAction()
 ↓
authenticateUser()
 ↓
LocalAuthentication
 ↓
Result
 ↓
UI decides what to do
```

---

# Example — React Native Button

```js
import { Button } from "react-native";
import * as LocalAuthentication
  from "expo-local-authentication";

const authenticateUser = async () => {

  const result =
    await LocalAuthentication.authenticateAsync({
      promptMessage:
        "Authenticate to continue",
    });

  if (result.success) {
    console.log("Access granted");
  } else {
    console.log(
      "Access denied:",
      result.error
    );
  }
};

export default function App() {

  return (
    <Button
      title="Unlock"
      onPress={authenticateUser}
    />
  );
}
```

The flow is:

```text
User taps Unlock
       ↓
authenticateUser()
       ↓
authenticateAsync()
       ↓
Device prompt
       ↓
Result
       ↓
Success / Failure
```

---

# Example — Protecting Navigation

Suppose you have a protected screen.

You should not navigate immediately when the button is pressed.

Incorrect:

```js
const openSecureScreen = () => {
  router.push("/secure");
};
```

Instead:

```js
const openSecureScreen = async () => {

  const result =
    await LocalAuthentication.authenticateAsync({
      promptMessage:
        "Authenticate to open this screen",
    });

  if (result.success) {
    router.push("/secure");
  }
};
```

Now:

```text
Button
  ↓
Authenticate
  ↓
Success?
  ↓
Yes
  ↓
Navigate
```

The important security principle is:

> **The protected action should happen only after successful authentication.**

---

# Example — Protecting Sensitive Data

Imagine:

```js
const privateData = {
  accountNumber: "123456",
  secretNotes: "..."
};
```

You should not reveal the sensitive data simply because the authentication API was called.

Instead:

```js
const showPrivateData = async () => {

  const result =
    await LocalAuthentication.authenticateAsync({
      promptMessage:
        "Authenticate to view private data",
    });

  if (!result.success) {
    console.log(
      "Authentication failed"
    );
    return;
  }

  console.log(
    "Show private data"
  );
};
```

The important sequence is:

```text
Request authentication
        ↓
Wait
        ↓
Check success
        ↓
Only then access/show protected data
```

---

# Handling Failure

Authentication can fail for different reasons.

For example:

```js
const result =
  await LocalAuthentication.authenticateAsync();

if (!result.success) {

  console.log(
    "Authentication failed"
  );

  console.log(
    "Error:",
    result.error
  );

  console.log(
    "Message:",
    result.warning
  );
}
```

The exact result fields and error values should be handled according to the Expo SDK version you're using.

The key concept is that failure should be treated as:

```text
Authentication not completed successfully
```

not as:

```text
Authentication succeeded
```

---

# Cancellation

A user may cancel the authentication prompt.

For example:

```text
┌─────────────────────────┐
│ Authenticate to continue│
│                         │
│        [Cancel]         │
└─────────────────────────┘
```

The user presses:

```text
Cancel
```

Your application should then normally keep the protected resource protected.

For example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage:
      "Authenticate to continue",
  });

if (!result.success) {
  console.log(
    "Authentication was not completed."
  );

  return;
}

console.log(
  "Authentication successful."
);
```

The important security rule is:

```text
Cancel
  ↓
No authentication success
  ↓
Do not unlock protected content
```

---

# Authentication Failure vs Application Error

These should not automatically be treated as the same thing.

For example:

```text
Authentication failed
```

could mean the user did not successfully authenticate.

An application error could mean something unexpected happened while trying to perform the operation.

Therefore, production applications should inspect the returned result and handle documented error values appropriately.

---

# Android: `cancelAuthenticate()`

Expo also provides:

```js
LocalAuthentication.cancelAuthenticate()
```

This is used to cancel an active authentication attempt on Android.

Example:

```js
LocalAuthentication.cancelAuthenticate();
```

Conceptually:

```text
Authentication currently running
        ↓
cancelAuthenticate()
        ↓
Cancel active authentication
```

This is different from:

```js
authenticateAsync()
```

because:

```text
authenticateAsync()
→ starts authentication

cancelAuthenticate()
→ cancels an active authentication attempt
```

---

# Important Difference

These two APIs have opposite purposes:

```js
LocalAuthentication.authenticateAsync();
```

starts authentication.

While:

```js
LocalAuthentication.cancelAuthenticate();
```

cancels an active authentication attempt on Android.

---

# Complete Authentication Flow

A production-style flow can look like:

```text
User wants protected resource
            ↓
Check device capability
            ↓
Check enrollment
            ↓
Start authentication
            ↓
Operating system shows prompt
            ↓
User interacts
            ↓
Result returned
            ↓
┌───────────────┐
│ success=true? │
└───────┬───────┘
        │
   ┌────┴────┐
   │         │
  Yes        No
   │         │
   ↓         ↓
Unlock    Keep protected
resource
```

---

# Important: Your App Does Not Receive Biometric Data

When authentication succeeds, your application receives the authentication result.

It does not receive:

```text
Fingerprint image
Face image
Raw biometric data
```

The operating system handles the biometric verification.

Conceptually:

```text
Your App
   ↓
"Please authenticate this user."
   ↓
Operating System
   ↓
Biometric hardware / secure authentication system
   ↓
Authentication decision
   ↓
Your App
   ↓
Success / failure
```

This separation is extremely important.

---

# Why Is This Better?

Your application doesn't need to implement:

```text
Fingerprint matching
Face recognition
Biometric template storage
Biometric sensor communication
```

The platform handles those responsibilities.

Your application mainly needs to decide:

```text
What should happen if authentication succeeds?
What should happen if authentication fails?
```

---

# Example — Complete Practical Function

Here is a more realistic reusable function:

```js
import * as LocalAuthentication
  from "expo-local-authentication";

const authenticateUser = async () => {

  try {

    // Check hardware
    const hasHardware =
      await LocalAuthentication.hasHardwareAsync();

    if (!hasHardware) {
      return {
        success: false,
        reason: "NO_HARDWARE",
      };
    }

    // Check enrollment
    const enrolled =
      await LocalAuthentication.isEnrolledAsync();

    if (!enrolled) {
      return {
        success: false,
        reason: "NOT_ENROLLED",
      };
    }

    // Start authentication
    const result =
      await LocalAuthentication.authenticateAsync({
        promptMessage:
          "Authenticate to continue",
        cancelLabel: "Cancel",
      });

    // Check authentication result
    if (result.success) {
      return {
        success: true,
      };
    }

    return {
      success: false,
      reason: result.error,
    };

  } catch (error) {

    console.error(
      "Authentication error:",
      error
    );

    return {
      success: false,
      reason: "UNKNOWN_ERROR",
    };
  }
};
```

This gives the rest of your application a simple result:

```js
const result =
  await authenticateUser();

if (result.success) {
  console.log("Access granted");
} else {
  console.log(
    "Access denied:",
    result.reason
  );
}
```

---

# Why Use `try...catch`?

Authentication APIs are asynchronous.

Unexpected errors can occur while interacting with the native authentication system.

Therefore, application code can use:

```js
try {
  // authentication
} catch (error) {
  // unexpected error
}
```

For example:

```js
try {

  const result =
    await LocalAuthentication.authenticateAsync();

  console.log(result);

} catch (error) {

  console.error(
    "Unexpected authentication error:",
    error
  );
}
```

Remember:

```text
Normal authentication failure
        ≠
Unexpected JavaScript/native error
```

Your application can choose to handle these separately.

---

# `authenticateAsync()` vs Previous APIs

This is a very important comparison.

| API                                   | Purpose                                  |
| ------------------------------------- | ---------------------------------------- |
| `hasHardwareAsync()`                  | Check hardware availability              |
| `isEnrolledAsync()`                   | Check whether authentication is enrolled |
| `supportedAuthenticationTypesAsync()` | Find supported authentication types      |
| `getEnrolledLevelAsync()`             | Find enrolled security level             |
| `authenticateAsync()`                 | Actually authenticate the user           |
| `cancelAuthenticate()`                | Cancel active authentication on Android  |

The first four mostly answer:

```text
"What is available/configured?"
```

`authenticateAsync()` answers:

```text
"Did the user successfully authenticate?"
```

---

# Simple Real-World Example

Imagine a private notes app.

The user opens the app:

```text
┌─────────────────────────────┐
│                             │
│       Private Notes         │
│                             │
│          Locked             │
│                             │
│       [ Unlock ]            │
│                             │
└─────────────────────────────┘
```

The user taps:

```text
Unlock
```

Your application calls:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage:
      "Authenticate to open your private notes",
  });
```

The device displays its authentication UI.

If successful:

```js
if (result.success) {
  setIsUnlocked(true);
}
```

The UI changes:

```text
┌─────────────────────────────┐
│                             │
│       Private Notes         │
│                             │
│          Unlocked           │
│                             │
│       Your private data     │
│                             │
└─────────────────────────────┘
```

If the user cancels:

```text
┌─────────────────────────────┐
│                             │
│       Private Notes         │
│                             │
│        Still Locked         │
│                             │
└─────────────────────────────┘
```

This is the basic purpose of local authentication.

---

# Final Mental Model

Remember the entire process:

```text
                  APP
                   │
                   ▼
        User requests protected
              resource
                   │
                   ▼
       Check authentication setup
                   │
                   ▼
        authenticateAsync()
                   │
                   ▼
        ┌───────────────────┐
        │ Operating System  │
        │ Authentication UI │
        └─────────┬─────────┘
                  │
                  ▼
             User verifies
                  │
                  ▼
              Result
             /      \
          success   failure
            │          │
            ▼          ▼
       Unlock /     Keep resource
       continue      protected
```

---

# Part 8 — Authentication Options

In Part 7, we learned how to start authentication:

```js
const result =
  await LocalAuthentication.authenticateAsync();
```

This works, but sometimes an application needs more control over the authentication request.

For example, we may want to:

* Show a meaningful authentication message
* Customize the cancel button
* Control device fallback behavior
* Customize Android authentication text
* Customize the iOS fallback button
* Require stronger biometric authentication on Android
* Control confirmation behavior on Android

For this purpose, `authenticateAsync()` accepts an **options object**.

---

# Basic Syntax

The general syntax is:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    option1: value,
    option2: value,
  });
```

For example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage: "Authenticate to continue",
  });
```

The options object is optional.

Therefore, both are valid:

```js
await LocalAuthentication.authenticateAsync();
```

and:

```js
await LocalAuthentication.authenticateAsync({
  promptMessage: "Authenticate to continue",
});
```

---

# Why Do We Need Options?

Without options:

```js
await LocalAuthentication.authenticateAsync();
```

the operating system uses its normal/default authentication behavior.

With options:

```js
await LocalAuthentication.authenticateAsync({
  promptMessage: "Authenticate to open private notes",
});
```

we can provide additional instructions to the authentication system.

Think of it as:

```text
authenticateAsync()
        ↓
"Start authentication"

authenticateAsync(options)
        ↓
"Start authentication
 with these additional settings"
```

---

# Available Authentication Options

The commonly used options are:

| Option                    | Platform      | Purpose                              |
| ------------------------- | ------------- | ------------------------------------ |
| `promptMessage`           | Android + iOS | Main authentication message          |
| `cancelLabel`             | Android + iOS | Cancel button label                  |
| `disableDeviceFallback`   | Android + iOS | Controls device fallback             |
| `fallbackLabel`           | iOS           | Customizes iOS fallback button       |
| `promptDescription`       | Android       | Android prompt description           |
| `promptSubtitle`          | Android       | Android prompt subtitle              |
| `requireConfirmation`     | Android       | Controls confirmation behavior       |
| `biometricsSecurityLevel` | Android       | Controls accepted biometric strength |

Not every option applies to every platform.

---

# 1. `promptMessage`

`promptMessage` allows you to provide the main message associated with the authentication request.

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage: "Authenticate to continue",
  });
```

---

# Why Is `promptMessage` Useful?

A user should ideally understand **why** the application is asking for authentication.

Compare:

```js
promptMessage: "Authentication"
```

with:

```js
promptMessage:
  "Authenticate to open your private notes"
```

The second message provides much more context.

A good authentication message tells the user:

> What am I being asked to authenticate for?

---

# Example — Banking App

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage:
      "Authenticate to view your account",
  });
```

Conceptually:

```text
User wants to view account
        ↓
Authentication prompt
        ↓
"Authenticate to view your account"
        ↓
User authenticates
```

---

# Example — Private Notes

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage:
      "Authenticate to open your private notes",
  });
```

---

# Example — Payment

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage:
      "Authenticate to confirm this payment",
  });
```

The message should accurately describe the action that authentication protects.

---

# Important Security Principle

Do not use a misleading prompt.

For example, if authentication is required before opening private data:

```js
promptMessage: "Welcome!"
```

doesn't clearly communicate what the authentication is for.

Prefer:

```js
promptMessage:
  "Authenticate to view your private data"
```

The user should understand why authentication is being requested.

---

# 2. `cancelLabel`

`cancelLabel` allows you to customize the label used for the cancellation action where supported.

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    cancelLabel: "Cancel",
  });
```

Another example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    cancelLabel: "Not Now",
  });
```

The purpose is to make the cancellation action clearer for your particular flow.

---

# Example

Suppose the application is asking the user to unlock a secure screen:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage:
      "Authenticate to unlock",
    cancelLabel: "Stay Locked",
  });
```

Conceptually:

```text
┌─────────────────────────────┐
│ Authenticate to unlock      │
│                             │
│        🔐                   │
│                             │
│ [ Stay Locked ]             │
└─────────────────────────────┘
```

The exact appearance depends on the platform.

---

# Important Platform Note

The operating system ultimately controls the authentication UI.

Therefore, your application should not assume that the authentication dialog will look exactly the same on Android and iOS.

Your code provides configuration.

The platform decides how that configuration is presented.

---

# 3. `disableDeviceFallback`

This option controls whether device fallback authentication is disabled.

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    disableDeviceFallback: true,
  });
```

The documented default is:

```js
false
```

Therefore:

```js
disableDeviceFallback: false
```

means fallback is not explicitly disabled.

---

# What Is Device Fallback?

Imagine the authentication flow:

```text
Biometric authentication
        ↓
User cannot authenticate biometrically
        ↓
Device may offer another authentication method
```

Depending on the platform and configuration, this may involve a device passcode, PIN, pattern, or another supported device-level authentication mechanism.

This is called **device fallback**.

---

# Example With Fallback Enabled

Conceptually:

```text
Authenticate
      ↓
Biometric
      ↓
Unable to authenticate
      ↓
Device fallback
      ↓
PIN / Passcode / other supported method
```

This can provide a smoother user experience.

---

# Example With Fallback Disabled

If:

```js
disableDeviceFallback: true
```

the application requests that device fallback not be allowed.

Conceptually:

```text
Authenticate
      ↓
Biometric
      ↓
Biometric authentication fails
      ↓
No device fallback
      ↓
Authentication unsuccessful
```

The exact platform behavior can vary.

---

# When Should You Use It?

For many applications, allowing the device's normal fallback behavior may be perfectly appropriate.

For example:

```text
Private notes
Password manager
Personal application
```

may allow device fallback depending on the application's security design.

A particularly sensitive workflow may have a requirement for biometric-only authentication.

In that case:

```js
disableDeviceFallback: true
```

may be appropriate.

The important point is:

> Do not disable fallback simply because it sounds more secure. Choose the behavior according to the application's actual security requirements.

---

# 4. `fallbackLabel` — iOS

`fallbackLabel` is an iOS-specific option.

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    fallbackLabel: "Use Passcode",
  });
```

This customizes the text associated with the fallback action on iOS.

---

# Example

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage:
      "Authenticate to open private notes",
    fallbackLabel: "Use Passcode",
  });
```

Conceptually, the iOS authentication UI may provide an action such as:

```text
Authenticate to open private notes

[ Use Passcode ]
```

The exact UI is controlled by iOS.

---

# Hiding the iOS Fallback Button

Expo's documentation also allows an empty string for `fallbackLabel`.

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    fallbackLabel: "",
  });
```

This can be used to hide the fallback button on iOS.

However, hiding a UI option and disabling fallback are conceptually different things.

Remember:

```text
fallbackLabel
        ↓
Controls the label/visibility of the iOS fallback button

disableDeviceFallback
        ↓
Controls whether device fallback is disabled
```

Do not confuse these two options.

---

# 5. `promptDescription` — Android

`promptDescription` is an Android-specific option.

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptDescription:
      "Confirm your identity",
  });
```

It allows the application to provide additional descriptive text for the Android authentication prompt.

---

# Example

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage:
      "Open Private Notes",

    promptDescription:
      "Confirm your identity to continue",
  });
```

Conceptually:

```text
┌──────────────────────────────┐
│ Open Private Notes            │
│                              │
│ Confirm your identity        │
│ to continue                  │
│                              │
│                              │
│                              │
│ [ Cancel ]                   │
└──────────────────────────────┘
```

The exact UI depends on the Android version and device.

---

# `promptMessage` vs `promptDescription`

These can be confusing.

Think of them as different pieces of prompt text.

```text
promptMessage
        ↓
Main authentication message

promptDescription
        ↓
Additional Android description
```

For example:

```js
{
  promptMessage: "Open Private Notes",
  promptDescription:
    "Confirm your identity to continue",
}
```

---

# 6. `promptSubtitle` — Android

Android also supports:

```js
promptSubtitle
```

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage: "Open Private Notes",

    promptSubtitle:
      "Use your biometric to continue",
  });
```

This provides subtitle text for the Android authentication prompt.

---

# Example

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage:
      "Authenticate to continue",

    promptSubtitle:
      "Use your fingerprint or face",
  });
```

Conceptually:

```text
┌──────────────────────────────┐
│ Authenticate to continue     │
│                              │
│ Use your fingerprint or face │
│                              │
│                              │
│                              │
│          [Cancel]            │
└──────────────────────────────┘
```

Again, the actual UI is platform-controlled.

---

# 7. `requireConfirmation` — Android

Android provides:

```js
requireConfirmation
```

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    requireConfirmation: true,
  });
```

The documented default is:

```js
true
```

---

# What Does Confirmation Mean?

This option relates to whether the user must provide an explicit confirmation after a biometric match, where the underlying biometric modality/device supports this behavior.

Conceptually:

```text
Biometric detected
        ↓
Authentication recognized
        ↓
Confirmation required?
        ↓
Yes
        ↓
User confirms
        ↓
Authentication completes
```

However, the operating system and biometric modality can affect how this behaves.

---

# Important Point

Do not assume:

```js
requireConfirmation: true
```

will always produce exactly the same UI on every Android device.

The Expo documentation notes that the system may ignore this setting depending on the biometric modality or device behavior.

So the correct mental model is:

```text
Application requests confirmation behavior
        ↓
Android authentication system
        ↓
Device decides how it can be applied
```

---

# Example

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage:
      "Confirm authentication",

    requireConfirmation: true,
  });
```

---

# 8. `biometricsSecurityLevel` — Android

Android also provides:

```js
biometricsSecurityLevel
```

This controls which biometric security classes can be used for the authentication request.

The documented values are:

```text
"weak"
"strong"
```

---

# `"weak"`

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    biometricsSecurityLevel: "weak",
  });
```

`"weak"` allows both weaker and stronger biometric classes.

Conceptually:

```text
"weak"
   ↓
Weak biometric   
Strong biometric 
```

---

# `"strong"`

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    biometricsSecurityLevel: "strong",
  });
```

This restricts the authentication request to stronger biometric security.

Conceptually:

```text
"strong"
    ↓
Weak biometric    
Strong biometric  
```

---

# Default Security Level

The documented default is:

```js
"weak"
```

So if you write:

```js
await LocalAuthentication.authenticateAsync();
```

you are not explicitly requesting:

```js
biometricsSecurityLevel: "strong"
```

---

# Why Would We Use `"strong"`?

Imagine an application has a highly sensitive operation:

```text
Change security settings
```

The application may require a stronger biometric class.

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage:
      "Authenticate to change security settings",

    biometricsSecurityLevel:
      "strong",
  });
```

This is an advanced security requirement.

Most applications should not use `"strong"` without understanding why they require it.

---

# Authentication Type vs Security Level

These are different concepts.

We learned earlier:

```js
supportedAuthenticationTypesAsync()
```

which tells us about authentication types.

For example:

```text
Fingerprint
Face
Iris
```

Security level is different.

```text
Authentication Type
        ↓
"What kind of biometric?"

Security Level
        ↓
"How strong is the biometric security class?"
```

For example, do not assume:

```text
Face = always strong
Fingerprint = always strong
```

The security classification depends on the platform's biometric implementation.

---

# Combining Options

Multiple options can be used together.

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage:
      "Authenticate to open Private Notes",

    cancelLabel:
      "Cancel",

    disableDeviceFallback:
      false,
  });
```

On Android, you can additionally use Android-specific options:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage:
      "Authenticate to open Private Notes",

    promptDescription:
      "Confirm your identity to continue",

    promptSubtitle:
      "Use your biometric",

    requireConfirmation:
      true,

    biometricsSecurityLevel:
      "strong",
  });
```

---

# Platform-Specific Options

This is very important.

Some options are Android-specific while others are iOS-specific.

| Option                    | Android | iOS |
| ------------------------- | :-----: | :-: |
| `promptMessage`           |    ✓    |  ✓  |
| `cancelLabel`             |    ✓    |  ✓  |
| `disableDeviceFallback`   |    ✓    |  ✓  |
| `fallbackLabel`           |    —    |  ✓  |
| `promptDescription`       |    ✓    |  —  |
| `promptSubtitle`          |    ✓    |  —  |
| `requireConfirmation`     |    ✓    |  —  |
| `biometricsSecurityLevel` |    ✓    |  —  |

This means you should not design your authentication UI assuming that every option works identically across platforms.

---

# Example — Cross-Platform Configuration

Suppose you want a common configuration:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage:
      "Authenticate to continue",

    cancelLabel:
      "Cancel",

    disableDeviceFallback:
      false,
  });
```

These options can be used as part of a cross-platform authentication request.

---

# Example — Android-Specific Configuration

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage:
      "Authenticate to continue",

    promptDescription:
      "Confirm your identity",

    promptSubtitle:
      "Use your biometric",

    requireConfirmation:
      true,

    biometricsSecurityLevel:
      "strong",
  });
```

This configuration is specifically concerned with Android behavior.

---

# Example — iOS-Specific Configuration

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage:
      "Authenticate to continue",

    fallbackLabel:
      "Use Passcode",
  });
```

Here:

```js
fallbackLabel
```

is specifically an iOS option.

---

# A More Complete Example

Let's build a realistic authentication function.

```js
import * as LocalAuthentication
  from "expo-local-authentication";

const authenticateUser = async () => {

  const result =
    await LocalAuthentication.authenticateAsync({
      promptMessage:
        "Authenticate to access your private data",

      cancelLabel:
        "Cancel",

      disableDeviceFallback:
        false,
    });

  if (result.success) {
    console.log(
      "Authentication successful"
    );

    return true;
  }

  console.log(
    "Authentication failed"
  );

  console.log(
    "Error:",
    result.error
  );

  return false;
};
```

---

# Using It With a Button

```js
import { Button } from "react-native";

<Button
  title="Unlock"
  onPress={authenticateUser}
/>
```

The flow becomes:

```text
User taps Unlock
        ↓
authenticateUser()
        ↓
authenticateAsync(options)
        ↓
System authentication prompt
        ↓
User authenticates
        ↓
Result returned
        ↓
success?
   /          \
 Yes           No
 ↓             ↓
Unlock       Remain locked
```

---

# Example — Protecting a Screen

```js
const openPrivateScreen = async () => {

  const result =
    await LocalAuthentication.authenticateAsync({
      promptMessage:
        "Authenticate to open Private Notes",
    });

  if (!result.success) {
    return;
  }

  router.push("/private-notes");
};
```

Notice the important order:

```text
Authenticate
      ↓
Check success
      ↓
Navigate
```

Not:

```text
Navigate
      ↓
Authenticate
```

---

# Example — Strong Android Authentication

Suppose a sensitive Android operation requires strong biometric authentication.

```js
const authenticateForSensitiveAction = async () => {

  const result =
    await LocalAuthentication.authenticateAsync({
      promptMessage:
        "Authenticate to change security settings",

      biometricsSecurityLevel:
        "strong",
    });

  if (!result.success) {
    console.log(
      "Strong biometric authentication was not completed."
    );

    return;
  }

  console.log(
    "Authentication successful."
  );

  // Perform protected action
};
```

---

# Do Not Add Every Option

A common beginner mistake is to write something like:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage: "Authenticate",
    cancelLabel: "Cancel",
    disableDeviceFallback: false,
    fallbackLabel: "Use Passcode",
    promptDescription: "Description",
    promptSubtitle: "Subtitle",
    requireConfirmation: true,
    biometricsSecurityLevel: "strong",
  });
```

This is not automatically better.

Some of these options are platform-specific, and some are only useful for particular security requirements.

A better approach is:

```text
What does my application need?
        ↓
Choose only the relevant options
        ↓
Test on the target platform/device
```

---

# Default Behavior vs Explicit Configuration

It is useful to distinguish between:

```js
authenticateAsync()
```

and:

```js
authenticateAsync({
  promptMessage: "...",
  ...
})
```

The first asks the platform to authenticate using its normal/default configuration.

The second provides explicit instructions.

For example:

```js
await LocalAuthentication.authenticateAsync();
```

versus:

```js
await LocalAuthentication.authenticateAsync({
  promptMessage:
    "Authenticate to open private notes",
});
```

The authentication mechanism is still provided by the operating system.

The options mainly customize/configure the request.

---

# Important: Options Do Not Replace Authentication Checks

Adding:

```js
promptMessage: "Authenticate"
```

does not mean the device has biometric hardware.

Adding:

```js
biometricsSecurityLevel: "strong"
```

does not mean strong biometrics are enrolled.

Options configure the authentication request.

The earlier APIs answer different questions:

```text
hasHardwareAsync()
→ Is biometric hardware available?

isEnrolledAsync()
→ Is an authentication credential enrolled?

supportedAuthenticationTypesAsync()
→ What biometric types are supported?

getEnrolledLevelAsync()
→ What security level is enrolled?

authenticateAsync()
→ Ask the user to authenticate.
```

---

# Recommended Mental Model

Think of `authenticateAsync()` as having two parts:

```text
authenticateAsync()
        │
        ├── Options
        │      ↓
        │   How should the request behave?
        │
        └── Result
               ↓
          Did authentication succeed?
```

For example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage:
      "Authenticate to continue",
  });
```

The option:

```js
promptMessage
```

controls part of the request.

The returned:

```js
result.success
```

tells us what happened.

---

# Complete Example

Here is a practical example combining the concepts:

```js
import * as LocalAuthentication
  from "expo-local-authentication";

const unlockPrivateData = async () => {

  try {

    // 1. Check hardware
    const hasHardware =
      await LocalAuthentication.hasHardwareAsync();

    if (!hasHardware) {
      console.log(
        "Biometric hardware is unavailable."
      );
      return;
    }

    // 2. Check enrollment
    const enrolled =
      await LocalAuthentication.isEnrolledAsync();

    if (!enrolled) {
      console.log(
        "No biometric credentials are enrolled."
      );
      return;
    }

    // 3. Authenticate
    const result =
      await LocalAuthentication.authenticateAsync({
        promptMessage:
          "Authenticate to view private data",

        cancelLabel:
          "Cancel",

        disableDeviceFallback:
          false,
      });

    // 4. Handle result
    if (result.success) {

      console.log(
        "Authentication successful."
      );

      // Show protected content here

    } else {

      console.log(
        "Authentication was not successful."
      );

      console.log(
        "Error:",
        result.error
      );
    }

  } catch (error) {

    console.error(
      "Unexpected authentication error:",
      error
    );
  }
};
```

---

# Full Flow

We can now visualize the complete process:

```text
User wants protected resource
            ↓
hasHardwareAsync()
            ↓
      Hardware exists?
       /           \
     No             Yes
     ↓               ↓
   Stop        isEnrolledAsync()
                       ↓
                Credential enrolled?
                   /          \
                 No            Yes
                 ↓              ↓
               Stop      authenticateAsync()
                                ↓
                              Options
                                ↓
                    Operating system prompt
                                ↓
                        User authenticates
                                ↓
                             Result
                          /           \
                       success       failure
                         ↓              ↓
                  Allow access      Deny access
```

---

# Part 9 — Authentication Results and Errors

In the previous parts, we learned how to start authentication:

```js
const result =
  await LocalAuthentication.authenticateAsync();
```

Calling this function starts the authentication process, but it does not tell us whether the user successfully authenticated.

For that, we need to inspect the returned **result object**.

---

# Understanding the Result

When `authenticateAsync()` finishes, it returns an object describing what happened.

The most important property is:

```js
result.success
```

The basic possibilities are:

```text
success = true
        ↓
Authentication succeeded
```

or:

```text
success = false
        ↓
Authentication did not succeed
```

---

# Successful Authentication

When authentication succeeds, the result contains:

```js
{
  success: true
}
```

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync();

if (result.success) {
  console.log("Authentication successful");
}
```

At this point, the application can perform the protected operation.

For example:

```js
if (result.success) {
  router.push("/private");
}
```

The important rule is:

> **Only grant access after `result.success` is `true`.**

---

# Failed Authentication

When authentication does not succeed, the result contains:

```js
{
  success: false,
  error: "authentication_failed"
}
```

The result may also contain a warning.

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync();

if (!result.success) {
  console.log("Authentication failed");
  console.log("Error:", result.error);
}
```

---

# `success` Is the First Thing to Check

A good pattern is:

```js
const result =
  await LocalAuthentication.authenticateAsync();

if (result.success) {
  // Authentication succeeded
  return;
}

// Authentication did not succeed
console.log(result.error);
```

This is better than immediately assuming that authentication succeeded.

---

# Result Object Mental Model

Think of the result as:

```text
authenticateAsync()
        ↓
      Result
        ↓
 ┌───────────────┐
 │ success       │
 └───────┬───────┘
         │
     ┌───┴───┐
     ↓       ↓
   true     false
     ↓       ↓
 Success   Check error
```

So the application should first ask:

```js
result.success
```

and only when it is `false` should it inspect:

```js
result.error
```

---

# Authentication Errors

Expo provides documented authentication error values.

The documented values include:

```text
not_enrolled
user_cancel
app_cancel
not_available
lockout
no_space
timeout
unable_to_process
unknown
system_cancel
user_fallback
invalid_context
passcode_not_set
authentication_failed
```

These values help your application understand **why authentication did not complete successfully**.

---

# Why Do We Need Error Values?

Consider this:

```js
if (!result.success) {
  console.log("Authentication failed");
}
```

This tells us that authentication did not succeed.

But it does not tell us **why**.

For example:

```text
User cancelled
        ↓
Authentication unsuccessful

Fingerprint did not match
        ↓
Authentication unsuccessful

Too many failed attempts
        ↓
Authentication unsuccessful
```

All of these can result in:

```js
success: false
```

But the application may want to respond differently.

That's why:

```js
result.error
```

is useful.

---

# 1. `not_enrolled`

This indicates that no biometric credentials are enrolled.

Conceptually:

```text
Device supports biometric authentication
        ↓
No biometric credential enrolled
        ↓
not_enrolled
```

Example:

```js
if (result.error === "not_enrolled") {
  console.log(
    "No biometric credentials are enrolled."
  );
}
```

---

# What Should the UI Do?

If the user hasn't enrolled biometrics, you could show:

```text
Biometric authentication isn't set up.

Please configure biometrics
in your device settings.
```

However, the exact message and available settings flow depend on the platform.

---

# Important Difference

Remember:

```js
hasHardwareAsync()
```

and:

```js
isEnrolledAsync()
```

already allow you to check this situation before authentication.

Therefore, a production application can often detect this earlier.

Still, you should understand:

```text
not_enrolled
```

because authentication can return documented errors describing the authentication state.

---

# 2. `user_cancel`

This means the user cancelled the authentication prompt.

Conceptually:

```text
Authentication prompt
        ↓
User presses Cancel
        ↓
user_cancel
```

Example:

```js
if (result.error === "user_cancel") {
  console.log(
    "User cancelled authentication."
  );
}
```

---

# What Should the App Do?

Usually, simply keep the protected content protected.

For example:

```js
if (result.error === "user_cancel") {
  console.log("Authentication cancelled");
  return;
}
```

There is normally no reason to show a serious error message for an intentional cancellation.

A user may simply have changed their mind.

---

# 3. `app_cancel`

`app_cancel` indicates that the application cancelled the authentication operation.

Conceptually:

```text
Authentication running
        ↓
Application cancels request
        ↓
app_cancel
```

This is different from:

```text
user_cancel
```

because the cancellation originated from the application rather than directly from the user.

---

# Example

```js
if (result.error === "app_cancel") {
  console.log(
    "Authentication was cancelled by the app."
  );
}
```

This distinction can be useful when debugging authentication flows.

---

# 4. `not_available`

`not_available` indicates that authentication is not available in the current situation.

Example:

```js
if (result.error === "not_available") {
  console.log(
    "Local authentication is not available."
  );
}
```

This can represent a device/platform state where the requested authentication mechanism cannot currently be used.

---

# 5. `lockout`

`lockout` indicates that the authentication system has been locked.

This can happen because of repeated failed authentication attempts or platform security rules.

Conceptually:

```text
Authentication attempt
        ↓
Failed
        ↓
Failed
        ↓
Failed
        ↓
Security threshold reached
        ↓
lockout
```

Example:

```js
if (result.error === "lockout") {
  console.log(
    "Authentication is temporarily locked."
  );
}
```

---

# Why Is `lockout` Different?

This is not simply:

```text
"The user's fingerprint didn't match."
```

It indicates a security state imposed by the authentication system.

Therefore, your UI may want to tell the user:

```text
Authentication is temporarily unavailable.

Please use your device's supported authentication
method or try again later.
```

The exact recovery behavior depends on the platform.

---

# 6. `timeout`

`timeout` indicates that the authentication operation timed out.

Example:

```js
if (result.error === "timeout") {
  console.log(
    "Authentication timed out."
  );
}
```

The application can allow the user to try again if appropriate.

---

# Example Retry

```js
const authenticateAgain = async () => {

  const result =
    await LocalAuthentication.authenticateAsync({
      promptMessage:
        "Authenticate to continue",
    });

  if (result.success) {
    console.log("Success");
    return;
  }

  if (result.error === "timeout") {
    console.log(
      "Authentication timed out."
    );

    return;
  }

  console.log(
    "Authentication failed:",
    result.error
  );
};
```

---

# 7. `user_fallback`

`user_fallback` indicates that the user selected the fallback authentication option.

Conceptually:

```text
Biometric authentication prompt
        ↓
User chooses fallback
        ↓
user_fallback
```

Example:

```js
if (result.error === "user_fallback") {
  console.log(
    "User selected fallback authentication."
  );
}
```

---

# Why Is This Important?

This is different from:

```text
authentication_failed
```

because the user may have explicitly chosen another authentication path.

For example:

```text
Biometric
   ↓
User chooses "Use Passcode"
   ↓
Fallback flow
```

The exact behavior depends on the platform and authentication configuration.

---

# 8. `passcode_not_set`

This indicates that the required device passcode is not configured.

Example:

```js
if (result.error === "passcode_not_set") {
  console.log(
    "A device passcode is not configured."
  );
}
```

This is particularly relevant to authentication flows that depend on device-level security.

---

# Example UI

You could explain the situation to the user:

```text
A device passcode is required
to use this authentication feature.

Please configure one in your
device security settings.
```

The exact requirement depends on the platform and authentication configuration.

---

# 9. `authentication_failed`

This is one of the most common errors.

It means the authentication attempt was unsuccessful.

For example:

```js
if (result.error === "authentication_failed") {
  console.log(
    "The authentication attempt failed."
  );
}
```

Conceptually:

```text
User attempts authentication
        ↓
Authentication does not match
        ↓
authentication_failed
```

---

# Important Difference: Failure vs Cancellation

These are not the same:

```text
authentication_failed
```

means the authentication attempt failed.

While:

```text
user_cancel
```

means the user cancelled the authentication prompt.

For example:

```text
User scans fingerprint
        ↓
Fingerprint doesn't match
        ↓
authentication_failed
```

versus:

```text
Authentication prompt appears
        ↓
User taps Cancel
        ↓
user_cancel
```

Your UI can respond differently.

---

# 10. `system_cancel`

`system_cancel` indicates that the authentication operation was cancelled by the system.

Example:

```js
if (result.error === "system_cancel") {
  console.log(
    "Authentication was cancelled by the system."
  );
}
```

This is different from:

```text
user_cancel
```

and:

```text
app_cancel
```

because the cancellation source is different.

Think of the three as:

```text
user_cancel
    ↓
User cancelled

app_cancel
    ↓
Application cancelled

system_cancel
    ↓
System cancelled
```

---

# 11. `no_space`

`no_space` is a documented authentication error value indicating that the authentication operation cannot proceed because the required system resources/storage space are unavailable.

Example:

```js
if (result.error === "no_space") {
  console.log(
    "Authentication cannot proceed because of a space/resource issue."
  );
}
```

This is a less common case, but it is part of the documented error set.

---

# 12. `unable_to_process`

This indicates that the authentication system was unable to process the authentication request.

Example:

```js
if (result.error === "unable_to_process") {
  console.log(
    "The authentication request could not be processed."
  );
}
```

This is different from a normal authentication mismatch.

Conceptually:

```text
Authentication request
        ↓
System attempts to process it
        ↓
Unable to process
        ↓
unable_to_process
```

---

# 13. `invalid_context`

`invalid_context` indicates that the authentication request was made with an invalid authentication context.

Example:

```js
if (result.error === "invalid_context") {
  console.log(
    "The authentication context is invalid."
  );
}
```

This is generally a lower-level/platform-related condition rather than a normal user mistake.

---

# 14. `unknown`

`unknown` represents an unspecified or otherwise unclassified authentication error.

Example:

```js
if (result.error === "unknown") {
  console.log(
    "An unknown authentication error occurred."
  );
}
```

When handling this case, a generic message is usually appropriate.

---

# Complete Error List

For revision:

| Error                   | Meaning                                         |
| ----------------------- | ----------------------------------------------- |
| `not_enrolled`          | No biometric credentials are enrolled           |
| `user_cancel`           | User cancelled authentication                   |
| `app_cancel`            | Application cancelled authentication            |
| `not_available`         | Authentication is unavailable                   |
| `lockout`               | Authentication is locked                        |
| `no_space`              | Required system space/resources are unavailable |
| `timeout`               | Authentication timed out                        |
| `unable_to_process`     | Authentication request could not be processed   |
| `unknown`               | Unknown/unspecified error                       |
| `system_cancel`         | System cancelled authentication                 |
| `user_fallback`         | User selected fallback authentication           |
| `invalid_context`       | Authentication context is invalid               |
| `passcode_not_set`      | Required device passcode is not configured      |
| `authentication_failed` | Authentication attempt failed                   |

---

# Handling Errors With `switch`

A `switch` statement is a clean way to handle multiple documented errors.

```js
const result =
  await LocalAuthentication.authenticateAsync();

if (result.success) {

  console.log(
    "Authentication successful"
  );

  return;
}

switch (result.error) {

  case "user_cancel":
    console.log(
      "You cancelled authentication."
    );
    break;

  case "authentication_failed":
    console.log(
      "Authentication failed."
    );
    break;

  case "not_enrolled":
    console.log(
      "No biometric credentials are enrolled."
    );
    break;

  case "lockout":
    console.log(
      "Authentication is locked."
    );
    break;

  case "timeout":
    console.log(
      "Authentication timed out."
    );
    break;

  case "user_fallback":
    console.log(
      "User selected the fallback option."
    );
    break;

  case "passcode_not_set":
    console.log(
      "Device passcode is not configured."
    );
    break;

  default:
    console.log(
      "Authentication error:",
      result.error
    );
}
```

---

# A Better Production Pattern

Instead of only printing messages, your application can return a meaningful result.

```js
const authenticateUser = async () => {

  const result =
    await LocalAuthentication.authenticateAsync({
      promptMessage:
        "Authenticate to continue",
    });

  if (result.success) {
    return {
      success: true,
    };
  }

  return {
    success: false,
    error: result.error,
  };
};
```

Then:

```js
const result =
  await authenticateUser();

if (result.success) {

  console.log(
    "Access granted"
  );

} else {

  console.log(
    "Access denied:",
    result.error
  );
}
```

This keeps authentication logic separate from UI logic.

---

# Creating User-Friendly Messages

It is usually better not to display raw error codes directly to users.

Avoid:

```text
Error: authentication_failed
```

Prefer:

```text
Authentication failed. Please try again.
```

For:

```text
user_cancel
```

you could simply:

```text
Authentication cancelled.
```

For:

```text
lockout
```

you could display:

```text
Authentication is temporarily locked.
```

For:

```text
not_enrolled
```

you could display:

```text
No biometric authentication is configured
on this device.
```

---

# Example Error Message Function

You can create a helper:

```js
const getAuthenticationMessage = (error) => {

  switch (error) {

    case "user_cancel":
      return "Authentication cancelled.";

    case "authentication_failed":
      return "Authentication failed. Please try again.";

    case "not_enrolled":
      return "No biometric authentication is configured.";

    case "lockout":
      return "Authentication is temporarily locked.";

    case "timeout":
      return "Authentication timed out.";

    case "passcode_not_set":
      return "A device passcode is not configured.";

    case "user_fallback":
      return "Fallback authentication was selected.";

    default:
      return "Authentication could not be completed.";
  }
};
```

Then:

```js
const result =
  await LocalAuthentication.authenticateAsync();

if (!result.success) {

  const message =
    getAuthenticationMessage(
      result.error
    );

  console.log(message);
}
```

---

# Example With React Native Alert

```js
import { Alert } from "react-native";

const authenticateUser = async () => {

  const result =
    await LocalAuthentication.authenticateAsync({
      promptMessage:
        "Authenticate to continue",
    });

  if (result.success) {
    console.log(
      "Authentication successful"
    );
    return;
  }

  switch (result.error) {

    case "user_cancel":
      return;

    case "authentication_failed":
      Alert.alert(
        "Authentication Failed",
        "Please try again."
      );
      break;

    case "lockout":
      Alert.alert(
        "Authentication Locked",
        "Authentication is temporarily unavailable."
      );
      break;

    case "not_enrolled":
      Alert.alert(
        "Biometrics Not Set Up",
        "Please configure biometric authentication on your device."
      );
      break;

    default:
      Alert.alert(
        "Authentication Error",
        "Authentication could not be completed."
      );
  }
};
```

Notice that `user_cancel` does not necessarily need an `Alert`.

Why?

Because the user intentionally cancelled the operation.

Showing another error dialog immediately after they pressed Cancel can create a poor user experience.

---

# Error Handling Flow

A useful mental model is:

```text
authenticateAsync()
        ↓
      result
        ↓
success?
   /        \
 true       false
  ↓           ↓
Allow      result.error
access        ↓
        ┌─────┼──────────────┐
        ↓     ↓              ↓
      cancel failure      lockout
        ↓     ↓              ↓
      Stop  Retry       Handle security state
```

---

# Don't Treat Every Error as Retryable

This is an important production concept.

For example:

```text
authentication_failed
```

may reasonably allow:

```text
Try Again
```

But:

```text
lockout
```

may require different handling.

Similarly:

```text
not_enrolled
```

doesn't make much sense to solve by repeatedly calling:

```js
authenticateAsync();
```

The user needs to configure authentication first.

Therefore:

```text
Error
  ↓
Understand what happened
  ↓
Choose appropriate action
```

---

# Example of Smart Error Handling

```js
const handleAuthentication = async () => {

  const result =
    await LocalAuthentication.authenticateAsync({
      promptMessage:
        "Authenticate to continue",
    });

  if (result.success) {
    return {
      status: "success",
    };
  }

  switch (result.error) {

    case "user_cancel":
      return {
        status: "cancelled",
      };

    case "authentication_failed":
      return {
        status: "retry",
      };

    case "lockout":
      return {
        status: "locked",
      };

    case "not_enrolled":
      return {
        status: "not_enrolled",
      };

    default:
      return {
        status: "error",
        error: result.error,
      };
  }
};
```

Now the UI can decide what to do:

```js
const result =
  await handleAuthentication();

switch (result.status) {

  case "success":
    // Open protected screen
    break;

  case "cancelled":
    // Stay on current screen
    break;

  case "retry":
    // Offer retry
    break;

  case "locked":
    // Explain lockout
    break;

  case "not_enrolled":
    // Explain setup requirement
    break;

  case "error":
    // Generic error handling
    break;
}
```

This is a much cleaner architecture for a larger application.

---

# `result.error` Is Not a User Message

This is an important distinction.

The application receives:

```js
result.error
```

such as:

```js
"authentication_failed"
```

This is an **error code**, not necessarily a message you should display directly.

Instead:

```text
Error code
    ↓
Application interprets it
    ↓
User-friendly message
    ↓
Display to user
```

For example:

```js
"authentication_failed"
```

becomes:

```text
"Authentication failed. Please try again."
```

---

# What About `result.warning`?

The authentication result can also contain an optional warning.

Example:

```js
console.log(result.warning);
```

You should not automatically assume that a warning means authentication failed.

The primary success decision should still be based on:

```js
result.success
```

Think of it as:

```text
success
  ↓
Primary authentication result

warning
  ↓
Additional information, when provided
```

---

# Recommended Basic Pattern

For most applications, start with this:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage:
      "Authenticate to continue",
  });

if (result.success) {

  // Protected action

} else {

  console.log(
    "Authentication error:",
    result.error
  );
}
```

Then add more detailed handling only when your application needs it.

---

# Complete Example

Here is a reusable example combining the concepts:

```js
import * as LocalAuthentication
  from "expo-local-authentication";

const authenticateUser = async () => {

  try {

    const result =
      await LocalAuthentication.authenticateAsync({
        promptMessage:
          "Authenticate to access private data",

        cancelLabel:
          "Cancel",
      });

    if (result.success) {

      console.log(
        "Authentication successful"
      );

      return {
        success: true,
      };
    }

    switch (result.error) {

      case "user_cancel":
        console.log(
          "User cancelled authentication."
        );
        break;

      case "authentication_failed":
        console.log(
          "Authentication failed."
        );
        break;

      case "not_enrolled":
        console.log(
          "No biometric credentials are enrolled."
        );
        break;

      case "lockout":
        console.log(
          "Authentication is locked."
        );
        break;

      case "timeout":
        console.log(
          "Authentication timed out."
        );
        break;

      case "user_fallback":
        console.log(
          "User selected fallback authentication."
        );
        break;

      case "passcode_not_set":
        console.log(
          "Device passcode is not configured."
        );
        break;

      default:
        console.log(
          "Authentication error:",
          result.error
        );
    }

    return {
      success: false,
      error: result.error,
    };

  } catch (error) {

    console.error(
      "Unexpected authentication error:",
      error
    );

    return {
      success: false,
      error: "unknown",
    };
  }
};
```

---

# The Most Important Distinction

Students often see:

```js
success: false
```

and think:

> "The fingerprint was wrong."

That's not necessarily true.

`success: false` only means:

> **The authentication request did not complete successfully.**

The reason is described by:

```js
result.error
```

For example:

```text
success: false
error: "user_cancel"
```

means:

```text
Authentication unsuccessful
because the user cancelled.
```

While:

```text
success: false
error: "lockout"
```

means:

```text
Authentication unsuccessful
because the authentication system is locked.
```

And:

```text
success: false
error: "authentication_failed"
```

means:

```text
Authentication unsuccessful
because the authentication attempt failed.
```

---

# Final Mental Model

Remember:

```text
authenticateAsync()
        ↓
      result
        ↓
 ┌───────────────┐
 │ success       │
 └───────┬───────┘
         │
    ┌────┴────┐
    ↓         ↓
  true       false
    ↓          ↓
 Success    result.error
    ↓          ↓
Allow       Understand
access      why it failed
               ↓
          Appropriate UI
```

The most important code to remember is:

```js
const result =
  await LocalAuthentication.authenticateAsync();

if (result.success) {

  // Authentication succeeded
  // Allow protected action

} else {

  // Authentication did not succeed
  // Check why

  console.log(
    "Authentication error:",
    result.error
  );
}
```

---

# Part 10 — Platform Differences and Configuration

So far, we have learned the common APIs provided by:

```js
expo-local-authentication
```

For example:

```js
LocalAuthentication.hasHardwareAsync();

LocalAuthentication.isEnrolledAsync();

LocalAuthentication.supportedAuthenticationTypesAsync();

LocalAuthentication.getEnrolledLevelAsync();

LocalAuthentication.authenticateAsync();
```

These APIs provide a common JavaScript interface.

However, the actual authentication system is provided by the operating system.

That means:

```text
React Native / Expo App
        ↓
expo-local-authentication
        ↓
 ┌───────────────┐
 ↓               ↓
Android         iOS
 ↓               ↓
Android         Apple
Biometric       Local
System          Authentication
```

Because Android and iOS have different authentication systems, some behavior and options are platform-specific.

---

# Why Does Platform Difference Matter?

Suppose you write:

```js
await LocalAuthentication.authenticateAsync({
  promptMessage: "Authenticate to continue",
});
```

The JavaScript code is almost the same on both platforms.

But internally:

```text
Android
   ↓
Android biometric authentication system

iOS
   ↓
Apple Local Authentication framework
```

So the actual UI, available biometric methods, fallback behavior, security classifications, and configuration requirements can differ.

---

# Android vs iOS

A simple comparison:

| Feature                   | Android                         | iOS                                  |
| ------------------------- | ------------------------------- | ------------------------------------ |
| Fingerprint               | Supported on compatible devices | Touch ID on compatible devices       |
| Face authentication       | Supported on compatible devices | Face ID on compatible devices        |
| Iris                      | Some devices/platforms          | Not generally applicable             |
| Security levels           | Android biometric classes       | Different Apple authentication model |
| `biometricsSecurityLevel` | Yes                             | No                                   |
| `requireConfirmation`     | Yes                             | No                                   |
| `promptDescription`       | Yes                             | No                                   |
| `promptSubtitle`          | Yes                             | No                                   |
| `fallbackLabel`           | No                              | Yes                                  |
| `cancelAuthenticate()`    | Yes                             | No                                   |
| Face ID usage description | No                              | Required when using Face ID          |

The important lesson is:

> **The Expo API is shared, but not every option is available on every platform.**

---

# Android Authentication

Android provides a system biometric authentication mechanism.

Depending on the device, Android may provide:

```text
Fingerprint
Face
Other supported biometric modalities
```

The exact authentication method depends on the hardware and Android device.

Your application normally does not implement fingerprint scanning or face recognition itself.

Instead:

```text
Your App
   ↓
authenticateAsync()
   ↓
Android biometric system
   ↓
User authenticates
   ↓
Result returned to App
```

---

# Android-Specific Options

Expo provides several options specifically for Android.

The most important ones we have already seen are:

```js
biometricsSecurityLevel
```

and:

```js
requireConfirmation
```

There are also:

```js
promptDescription
```

and:

```js
promptSubtitle
```

---

# `biometricsSecurityLevel`

This option controls the minimum biometric security class accepted by the Android authentication flow.

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage: "Authenticate to continue",
    biometricsSecurityLevel: "strong",
  });
```

The documented values are:

```text
"weak"
"strong"
```

---

# `"weak"`

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    biometricsSecurityLevel: "weak",
  });
```

`"weak"` allows authentication using biometric classes that meet the weaker threshold as well as stronger ones.

Conceptually:

```text
weak
 ↓
Weak biometric
+
Strong biometric
```

---

# `"strong"`

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    biometricsSecurityLevel: "strong",
  });
```

This requests stronger biometric authentication.

Conceptually:

```text
strong
   ↓
Strong biometric only
```

This can be useful when your application has a higher security requirement.

---

# Example: High-Security Operation

Imagine a banking application.

For a sensitive operation:

```text
Transfer Money
      ↓
Authenticate
      ↓
Strong biometric required
      ↓
Continue
```

The code could be:

```js
const authenticateForPayment = async () => {

  const result =
    await LocalAuthentication.authenticateAsync({
      promptMessage:
        "Authenticate to confirm payment",

      biometricsSecurityLevel:
        "strong",
    });

  if (result.success) {
    console.log(
      "Payment authentication successful"
    );
  } else {
    console.log(
      "Authentication failed:",
      result.error
    );
  }
};
```

The exact security policy should always match the requirements of your application.

---

# `requireConfirmation`

Android also provides:

```js
requireConfirmation
```

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage:
      "Authenticate to continue",

    requireConfirmation: true,
  });
```

The documented default is:

```js
true
```

This option requests confirmation behavior from the Android biometric prompt.

However, the system may ignore this setting depending on the biometric modality or device behavior.

Therefore, your application should not assume that setting:

```js
requireConfirmation: true
```

guarantees identical UI behavior on every Android device.

---

# Android Prompt Customization

Android also supports:

```js
promptDescription
```

and:

```js
promptSubtitle
```

Example:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage:
      "Authenticate to access your account",

    promptDescription:
      "Confirm your identity",

    promptSubtitle:
      "Use your biometric",
  });
```

Think of these as Android-specific customization options for the system authentication prompt.

---

# Android Authentication Cancellation

Expo also provides:

```js
LocalAuthentication.cancelAuthenticate()
```

This API is used to cancel an active authentication attempt on Android.

Example:

```js
LocalAuthentication.cancelAuthenticate();
```

The important point is that this is not the same as:

```js
authenticateAsync()
```

`authenticateAsync()`:

```text
Starts authentication
```

while:

```js
cancelAuthenticate()
```

```text
Attempts to cancel an active authentication
```

---

# Example: Cancel Button

Suppose your screen has a custom Cancel button while authentication is active.

Conceptually:

```text
Authentication Running
        ↓
User presses your Cancel button
        ↓
cancelAuthenticate()
        ↓
Authentication attempt cancelled
```

Example:

```js
const cancelAuthentication = () => {
  LocalAuthentication.cancelAuthenticate();
};
```

This API is Android-specific.

You should therefore consider the platform before using it:

```js
if (Platform.OS === "android") {
  LocalAuthentication.cancelAuthenticate();
}
```

Example:

```js
import { Platform } from "react-native";

const cancelAuthentication = () => {

  if (Platform.OS === "android") {
    LocalAuthentication.cancelAuthenticate();
  }

};
```

---

# iOS Authentication

On iOS, local authentication is provided by Apple's authentication system.

Compatible devices can provide authentication such as:

```text
Face ID
Touch ID
```

The actual method depends on the device.

For example:

```text
iPhone with Face ID
        ↓
Face ID

iPhone with Touch ID
        ↓
Touch ID
```

Your application does not need to manually implement the biometric sensor logic.

Instead:

```text
React Native App
       ↓
expo-local-authentication
       ↓
iOS Local Authentication
       ↓
Face ID / Touch ID
       ↓
Result
```

---

# Face ID Configuration

There is an important iOS-specific configuration requirement when using Face ID.

The application needs an appropriate Face ID usage description.

In an Expo project, this can be configured through the `expo-local-authentication` config plugin.

Example:

```json
{
  "expo": {
    "plugins": [
      [
        "expo-local-authentication",
        {
          "faceIDPermission": "Allow $(PRODUCT_NAME) to use Face ID."
        }
      ]
    ]
  }
}
```

---

# Understanding `faceIDPermission`

This:

```json
"faceIDPermission": "Allow $(PRODUCT_NAME) to use Face ID."
```

provides the native iOS usage description for Face ID.

The important part is:

```text
faceIDPermission
```

This tells the native iOS configuration what message should explain the application's use of Face ID.

---

# Why Does iOS Need This?

iOS protects access to sensitive device capabilities.

For Face ID, the system requires an explanation describing why the application wants to use it.

Conceptually:

```text
Application
     ↓
Requests Face ID
     ↓
iOS checks usage description
     ↓
Appropriate native configuration
     ↓
Face ID authentication
```

Therefore, Face ID is not simply a JavaScript-only feature.

There is also a native iOS configuration requirement.

---

# Example Expo Configuration

A typical `app.json` configuration can look like:

```json
{
  "expo": {
    "name": "MyApp",
    "slug": "my-app",

    "plugins": [
      [
        "expo-local-authentication",
        {
          "faceIDPermission": "Allow $(PRODUCT_NAME) to use Face ID."
        }
      ]
    ]
  }
}
```

The important part for this module is:

```json
[
  "expo-local-authentication",
  {
    "faceIDPermission": "Allow $(PRODUCT_NAME) to use Face ID."
  }
]
```

---

# Why Is This Called a Config Plugin?

Expo config plugins allow packages to modify native project configuration during the native build process.

Think of it like:

```text
app.json / app.config.js
        ↓
Expo Config Plugin
        ↓
Native project configuration
        ↓
iOS / Android application
```

This is different from normal JavaScript code.

For example:

```js
LocalAuthentication.authenticateAsync();
```

runs at application runtime.

Whereas:

```json
"faceIDPermission": "..."
```

is build-time/native configuration.

---

# Runtime vs Build-Time

This distinction is very important.

### Runtime

This happens while your application is running:

```js
const result =
  await LocalAuthentication.authenticateAsync();
```

### Build-time configuration

This is configured through Expo configuration:

```json
{
  "plugins": [
    [
      "expo-local-authentication",
      {
        "faceIDPermission": "Allow $(PRODUCT_NAME) to use Face ID."
      }
    ]
  ]
}
```

Mental model:

```text
Configuration
      ↓
Native Build
      ↓
Installed App
      ↓
JavaScript Runtime
      ↓
authenticateAsync()
```

---

# Expo Go and Face ID

There is an important SDK 54 consideration.

The Expo SDK 54 documentation notes:

> **Face ID is not supported in Expo Go on iOS.**

This can confuse beginners.

You may have code such as:

```js
await LocalAuthentication.authenticateAsync();
```

and still not be able to properly test Face ID through Expo Go on iOS.

That does not necessarily mean your JavaScript code is incorrect.

---

# Why Does Expo Go Have This Limitation?

Expo Go is a prebuilt application containing a predefined set of native capabilities.

Your project's native configuration is not the same as building your own native application.

Face ID usage configuration requires native application configuration.

Therefore:

```text
Expo Go
   ↓
Prebuilt native app
   ↓
Face ID testing limitation
```

Whereas a development build gives you:

```text
Your Expo project
       ↓
Native configuration
       ↓
Development build
       ↓
Face ID testing
```

---

# Development Build

To test Face ID on iOS, use a development build rather than relying on Expo Go.

Conceptually:

```text
Expo Go
   ↓
Face ID on iOS
   ↓
Not supported

Development Build
   ↓
Face ID on iOS
   ↓
Can be tested
```

This distinction becomes especially important when teaching Expo SDK 54.

---

# Android Permissions

`expo-local-authentication` also has native Android permission requirements.

The documented permissions include:

```xml
android.permission.USE_BIOMETRIC
```

and:

```xml
android.permission.USE_FINGERPRINT
```

---

# `USE_BIOMETRIC`

Modern Android uses:

```xml
android.permission.USE_BIOMETRIC
```

for biometric authentication.

This is the modern Android biometric permission.

---

# `USE_FINGERPRINT`

Android also has:

```xml
android.permission.USE_FINGERPRINT
```

This is an older fingerprint-specific permission and is deprecated on newer Android API levels.

The modern Android biometric permission is:

```xml
android.permission.USE_BIOMETRIC
```

---

# Do We Manually Request These Permissions?

Normally, with Expo Local Authentication, you do not write code such as:

```js
PermissionsAndroid.request(...)
```

just to use the module's biometric authentication.

The Expo module handles the relevant native configuration.

This is different from runtime permissions such as:

```text
Camera
Location
Microphone
Notifications
```

where applications may need explicit runtime permission requests.

---

# Important: Biometric Authentication Is Not the Same as Camera Permission

A common beginner misconception is:

> "Face authentication uses the camera, so I need camera permission."

Normally, you should not think of Face ID this way.

Face ID is handled by the operating system's secure authentication system.

Your application does not receive the user's Face ID camera/sensor data.

The flow is:

```text
Your App
   ↓
Request authentication
   ↓
iOS / Android system
   ↓
Biometric verification
   ↓
Success / failure
   ↓
Your App
```

Your application receives the authentication result, not the underlying biometric data.

---

# Android and iOS Do the Sensitive Work

This is one of the most important security concepts.

Your React Native code does not do this:

```text
Read fingerprint
      ↓
Compare fingerprint
      ↓
Read face
      ↓
Compare face
```

Instead:

```text
Your Application
       ↓
"Please authenticate the user"
       ↓
Operating System
       ↓
Secure biometric processing
       ↓
Authentication result
       ↓
Your Application
```

This is why local authentication can be integrated without your application storing raw biometric information.

---

# Platform-Specific Options Summary

Here is a useful reference:

| Option/API                | Android | iOS | Purpose                              |
| ------------------------- | ------: | --: | ------------------------------------ |
| `promptMessage`           |     Yes | Yes | Main authentication message          |
| `cancelLabel`             |     Yes | Yes | Cancel label                         |
| `disableDeviceFallback`   |     Yes | Yes | Controls device fallback             |
| `fallbackLabel`           |      No | Yes | iOS fallback button label            |
| `promptDescription`       |     Yes |  No | Android prompt description           |
| `promptSubtitle`          |     Yes |  No | Android prompt subtitle              |
| `requireConfirmation`     |     Yes |  No | Android confirmation behavior        |
| `biometricsSecurityLevel` |     Yes |  No | Android biometric security level     |
| `cancelAuthenticate()`    |     Yes |  No | Cancel active Android authentication |

---

# Example: Cross-Platform Authentication

Most applications can keep their main authentication code platform-independent.

```js
import * as LocalAuthentication
  from "expo-local-authentication";

const authenticateUser = async () => {

  const result =
    await LocalAuthentication.authenticateAsync({
      promptMessage:
        "Authenticate to continue",
    });

  if (result.success) {

    console.log(
      "Authentication successful"
    );

    return true;
  }

  console.log(
    "Authentication failed:",
    result.error
  );

  return false;
};
```

This can run on both Android and iOS.

---

# Adding Android-Specific Options

If you want Android-specific behavior, you can use:

```js
import { Platform } from "react-native";

import * as LocalAuthentication
  from "expo-local-authentication";

const authenticateUser = async () => {

  const options = {
    promptMessage:
      "Authenticate to continue",
  };

  if (Platform.OS === "android") {

    options.biometricsSecurityLevel =
      "strong";

    options.requireConfirmation =
      true;
  }

  const result =
    await LocalAuthentication.authenticateAsync(
      options
    );

  return result;
};
```

This demonstrates an important React Native pattern:

```text
Shared options
      +
Platform-specific options
      ↓
Cross-platform authentication
```

---

# Adding iOS-Specific Configuration

The runtime authentication code can remain simple:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage:
      "Authenticate to continue",
  });
```

While the iOS Face ID usage description is configured separately:

```json
{
  "expo": {
    "plugins": [
      [
        "expo-local-authentication",
        {
          "faceIDPermission": "Allow $(PRODUCT_NAME) to use Face ID."
        }
      ]
    ]
  }
}
```

This is a good example of separating:

```text
Runtime JavaScript
```

from:

```text
Native build configuration
```

---

# Example Project Structure

A simple Expo project might contain:

```text
my-app/
│
├── app/
│   ├── index.jsx
│   └── private.jsx
│
├── assets/
│
├── app.json
│
├── package.json
│
└── node_modules/
```

The Face ID configuration belongs in:

```text
app.json
```

while authentication logic belongs in your JavaScript/TypeScript files.

For example:

```text
app.json
   ↓
Face ID native configuration

app/index.jsx
   ↓
Authentication logic
```

---

# Complete Cross-Platform Example

Here is a more realistic example:

```js
import { Platform } from "react-native";

import * as LocalAuthentication
  from "expo-local-authentication";

const authenticateUser = async () => {

  try {

    // 1. Check hardware
    const hasHardware =
      await LocalAuthentication.hasHardwareAsync();

    if (!hasHardware) {

      return {
        success: false,
        error: "hardware_unavailable",
      };
    }

    // 2. Check enrollment
    const enrolled =
      await LocalAuthentication.isEnrolledAsync();

    if (!enrolled) {

      return {
        success: false,
        error: "not_enrolled",
      };
    }

    // 3. Create common options
    const options = {
      promptMessage:
        "Authenticate to access your account",

      cancelLabel:
        "Cancel",
    };

    // 4. Add Android-specific options
    if (Platform.OS === "android") {

      options.biometricsSecurityLevel =
        "strong";

      options.requireConfirmation =
        true;
    }

    // 5. Start authentication
    const result =
      await LocalAuthentication.authenticateAsync(
        options
      );

    // 6. Handle result
    if (result.success) {

      return {
        success: true,
      };
    }

    return {
      success: false,
      error: result.error,
    };

  } catch (error) {

    console.error(
      "Authentication error:",
      error
    );

    return {
      success: false,
      error: "unknown",
    };
  }
};
```

---

# Using the Result

Now you can use:

```js
const result =
  await authenticateUser();

if (result.success) {

  console.log(
    "Open protected screen"
  );

} else {

  console.log(
    "Authentication failed:",
    result.error
  );
}
```

This creates a clean separation:

```text
authenticateUser()
        ↓
Authentication logic
        ↓
Returns simple result
        ↓
UI decides what to do
```

---

# Platform Mental Model

Keep this diagram in mind:

```text
                    Your App
                       ↓
             expo-local-authentication
                       ↓
              ┌────────┴────────┐
              ↓                 ↓
           Android              iOS
              ↓                 ↓
       Android Biometric     Apple Local
            System           Authentication
              ↓                 ↓
       Fingerprint / Face    Touch ID / Face ID
              ↓                 ↓
              └────────┬────────┘
                       ↓
                  Result Object
                       ↓
                Your Application
```

---

# Configuration Mental Model

There are two separate things to remember.

### Runtime API

```js
LocalAuthentication.authenticateAsync();
```

This actually starts authentication.

### Native configuration

```json
{
  "plugins": [
    [
      "expo-local-authentication",
      {
        "faceIDPermission": "Allow $(PRODUCT_NAME) to use Face ID."
      }
    ]
  ]
}
```

This configures native behavior required by the package.

Therefore:

```text
Configuration
      ↓
Native Build
      ↓
Application
      ↓
Runtime API
      ↓
Authentication
```

---

# Part 11 — Building a Complete Local Authentication App

So far, we have learned the individual APIs:

```js
LocalAuthentication.hasHardwareAsync();

LocalAuthentication.isEnrolledAsync();

LocalAuthentication.supportedAuthenticationTypesAsync();

LocalAuthentication.getEnrolledLevelAsync();

LocalAuthentication.authenticateAsync();
```

Now we will combine these concepts into one complete React Native application.

The goal is to understand how these APIs work **together**, rather than treating each API as an isolated function.

---

# What Will We Build?

We will create a simple screen containing:

```text
┌──────────────────────────────┐
│                              │
│       Authentication         │
│                              │
│   Status: Not authenticated  │
│                              │
│      [ Authenticate ]         │
│                              │
└──────────────────────────────┘
```

When the user presses:

```text
Authenticate
```

the application will:

```text
Check Hardware
      ↓
Check Enrollment
      ↓
Start Authentication
      ↓
User authenticates
      ↓
Check Result
      ↓
Update UI
```

---

# Complete Example

```jsx
import { useState } from "react";

import {
  View,
  Text,
  Button,
  Alert,
  ActivityIndicator,
} from "react-native";

import * as LocalAuthentication
  from "expo-local-authentication";

export default function App() {

  const [message, setMessage] =
    useState("Not authenticated");

  const [loading, setLoading] =
    useState(false);

  const authenticateUser = async () => {

    try {

      setLoading(true);

      // --------------------------------
      // Step 1 - Check biometric hardware
      // --------------------------------

      const hasHardware =
        await LocalAuthentication.hasHardwareAsync();

      if (!hasHardware) {

        setMessage(
          "Biometric hardware is not available"
        );

        Alert.alert(
          "Not Available",
          "This device does not have biometric hardware."
        );

        return;
      }


      // --------------------------------
      // Step 2 - Check biometric enrollment
      // --------------------------------

      const enrolled =
        await LocalAuthentication.isEnrolledAsync();

      if (!enrolled) {

        setMessage(
          "No biometric credentials enrolled"
        );

        Alert.alert(
          "Not Enrolled",
          "Please enroll a fingerprint or face on your device."
        );

        return;
      }


      // --------------------------------
      // Step 3 - Start authentication
      // --------------------------------

      const result =
        await LocalAuthentication.authenticateAsync({
          promptMessage:
            "Authenticate to continue",

          cancelLabel:
            "Cancel",
        });


      // --------------------------------
      // Step 4 - Handle authentication result
      // --------------------------------

      if (result.success) {

        setMessage(
          "Authentication successful"
        );

        Alert.alert(
          "Success",
          "Authentication successful."
        );

      } else {

        setMessage(
          `Authentication failed: ${result.error}`
        );

      }

    } catch (error) {

      console.log(
        "Authentication error:",
        error
      );

      setMessage(
        "Something went wrong"
      );

      Alert.alert(
        "Error",
        "Unable to start authentication."
      );

    } finally {

      setLoading(false);

    }
  };


  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
        padding: 20,
      }}
    >

      <Text
        style={{
          fontSize: 24,
          marginBottom: 20,
        }}
      >
        Local Authentication
      </Text>


      <Text
        style={{
          fontSize: 18,
          marginBottom: 30,
          textAlign: "center",
        }}
      >
        {message}
      </Text>


      {loading ? (

        <ActivityIndicator size="large" />

      ) : (

        <Button
          title="Authenticate"
          onPress={authenticateUser}
        />

      )}

    </View>
  );
}
```

---

# Understanding the Complete Code

Let's break the application into individual steps.

---

# Step 1 — Import `useState`

We use:

```js
import { useState } from "react";
```

because the UI needs to remember two things:

```text
Authentication message
        +
Loading state
```

We create:

```js
const [message, setMessage] =
  useState("Not authenticated");
```

and:

```js
const [loading, setLoading] =
  useState(false);
```

---

# Why Do We Need `message`?

Initially:

```text
Not authenticated
```

After successful authentication:

```text
Authentication successful
```

After failure:

```text
Authentication failed: authentication_failed
```

So the message state allows the UI to reflect the current authentication state.

---

# Why Do We Need `loading`?

Authentication is asynchronous.

When we execute:

```js
await LocalAuthentication.authenticateAsync();
```

the application waits for the user.

The user may:

```text
Scan fingerprint
```

or:

```text
Look at Face ID
```

or:

```text
Cancel authentication
```

Therefore, authentication may take several seconds.

We can use:

```js
loading
```

to tell the UI that authentication is currently being processed.

---

# Step 2 — Start Loading

At the beginning:

```js
setLoading(true);
```

This changes:

```text
loading = false
```

to:

```text
loading = true
```

The UI then displays:

```jsx
<ActivityIndicator size="large" />
```

instead of the button.

---

# Step 3 — Check Hardware

Next:

```js
const hasHardware =
  await LocalAuthentication.hasHardwareAsync();
```

This asks:

> Does the device have biometric hardware?

The result is:

```js
true
```

or:

```js
false
```

If it is `false`, we stop:

```js
if (!hasHardware) {
  return;
}
```

There is no point continuing to the enrollment check if the device does not have the required biometric hardware.

---

# Step 4 — Check Enrollment

If hardware exists, we check:

```js
const enrolled =
  await LocalAuthentication.isEnrolledAsync();
```

This asks:

> Has the user enrolled biometric credentials on this device?

For example:

```text
Fingerprint registered
        ↓
true
```

or:

```text
No fingerprint / biometric enrolled
        ↓
false
```

If the result is `false`, we stop.

---

# Why Do We Need Both Checks?

Because these are two different conditions.

Consider:

```text
Hardware
   ↓
true

Enrollment
   ↓
false
```

This means:

```text
The device supports biometrics

BUT

The user has not configured one.
```

Therefore:

```js
hasHardwareAsync()
```

and:

```js
isEnrolledAsync()
```

should not be confused.

---

# Step 5 — Start Authentication

Only after the previous checks succeed do we call:

```js
const result =
  await LocalAuthentication.authenticateAsync({
    promptMessage:
      "Authenticate to continue",

    cancelLabel:
      "Cancel",
  });
```

Now the operating system displays its authentication interface.

Conceptually:

```text
Your Application
       ↓
authenticateAsync()
       ↓
Operating System
       ↓
Biometric Prompt
       ↓
User Authentication
```

---

# What Happens During `await`?

This is important for beginners.

When this code runs:

```js
const result =
  await LocalAuthentication.authenticateAsync();
```

JavaScript does not immediately continue to the next line.

It waits for the Promise to resolve.

The user may take time:

```text
authenticateAsync()
       ↓
Waiting...
       ↓
Fingerprint
       ↓
Waiting...
       ↓
Authentication complete
       ↓
result available
```

Then:

```js
result
```

contains the authentication result.

---

# Step 6 — Check `result.success`

After authentication:

```js
if (result.success) {
```

If it is:

```js
true
```

the authentication succeeded.

We can then allow the user to access the protected feature.

Example:

```js
if (result.success) {

  console.log(
    "Authentication successful"
  );

  // Allow access

}
```

---

# Authentication Failure

If:

```js
result.success === false
```

we can inspect:

```js
result.error
```

Example:

```js
console.log(
  result.error
);
```

Possible values include:

```text
user_cancel
authentication_failed
lockout
not_enrolled
not_available
timeout
app_cancel
```

So instead of simply saying:

```text
Authentication failed
```

we can understand **why** it failed.

---

# Example

Suppose the user presses Cancel.

The result could indicate:

```js
{
  success: false,
  error: "user_cancel"
}
```

Our application can display:

```text
Authentication cancelled
```

instead of:

```text
Wrong fingerprint
```

These are very different situations.

---

# Step 7 — `try...catch`

Authentication involves asynchronous native functionality.

Therefore, it is a good practice to protect the operation with:

```js
try {
  // authentication code
} catch (error) {
  // error handling
}
```

Example:

```js
try {

  const result =
    await LocalAuthentication.authenticateAsync();

} catch (error) {

  console.log(error);

}
```

This helps handle unexpected exceptions.

---

# `finally`

We also use:

```js
finally {
  setLoading(false);
}
```

Why?

Because regardless of the result:

```text
Success
Failure
Exception
```

we want:

```text
loading = false
```

Otherwise, the application could remain stuck showing the loading indicator.

---

# Why `finally` Is Useful

Without `finally`, you might write:

```js
if (result.success) {
  setLoading(false);
}
```

But what if authentication fails?

Or an exception occurs?

You would need to remember to reset loading in multiple places.

Instead:

```js
try {

  // authentication

} catch (error) {

  // error

} finally {

  setLoading(false);

}
```

guarantees that the cleanup happens after the operation finishes.

---

# Complete Authentication Flow

Our function now looks conceptually like:

```text
authenticateUser()
       ↓
setLoading(true)
       ↓
Check hardware
       ↓
Hardware available?
       │
       ├── No → Show message → Stop
       │
       └── Yes
             ↓
       Check enrollment
             ↓
       Biometric enrolled?
             │
             ├── No → Show message → Stop
             │
             └── Yes
                   ↓
             authenticateAsync()
                   ↓
             User authenticates
                   ↓
              Check result
              ↙         ↘
          Success      Failure
             ↓            ↓
       Allow access   Handle error
              \          /
               ↓        ↓
                setLoading(false)
```

---

# Important: `return` Does Not Break `finally`

This is an important JavaScript concept.

Consider:

```js
try {

  if (!hasHardware) {
    return;
  }

} finally {

  setLoading(false);

}
```

Even though:

```js
return;
```

is executed, the `finally` block still runs.

That is why our code can safely do:

```js
if (!hasHardware) {
  return;
}
```

and still reset:

```js
setLoading(false);
```

inside `finally`.

---

# Creating a Reusable Authentication Function

In a real project, you may not want all authentication logic directly inside a screen component.

You can create a reusable function.

For example:

```js
const authenticateUser = async () => {

  const hasHardware =
    await LocalAuthentication.hasHardwareAsync();

  if (!hasHardware) {
    return {
      success: false,
      error: "hardware_unavailable",
    };
  }

  const enrolled =
    await LocalAuthentication.isEnrolledAsync();

  if (!enrolled) {
    return {
      success: false,
      error: "not_enrolled",
    };
  }

  const result =
    await LocalAuthentication.authenticateAsync({
      promptMessage:
        "Authenticate to continue",
    });

  return result;
};
```

Then the screen can do:

```js
const handleAuthentication = async () => {

  const result =
    await authenticateUser();

  if (result.success) {

    console.log(
      "User authenticated"
    );

  } else {

    console.log(
      "Authentication failed:",
      result.error
    );

  }
};
```

This gives you a cleaner architecture.

---

# Why Reusable Functions Are Better?

Imagine your application has:

```text
Profile
Payment
Private Notes
Settings
Documents
Admin Panel
```

Multiple screens may need authentication.

If you write the entire authentication logic separately on every screen:

```text
Profile → authentication code
Payment → authentication code
Notes → authentication code
Settings → authentication code
```

you create duplication.

Instead:

```text
                 authenticateUser()
                    /    |    \
                   /     |     \
             Profile  Payment  Notes
```

One authentication function can be reused.

---

# Protecting a Screen

A very common real-world use case is a protected screen.

For example:

```text
Home Screen
    ↓
Private Data
    ↓
Authentication required
```

The application should authenticate the user **before** allowing access.

Example:

```js
const openPrivateScreen = async () => {

  const result =
    await LocalAuthentication.authenticateAsync({
      promptMessage:
        "Authenticate to open private data",
    });

  if (result.success) {

    // navigation.navigate("Private");

  }

};
```

The critical part is:

```js
if (result.success) {
```

Navigation should happen only after successful authentication.

---

# Incorrect Approach

Do not do this:

```js
const openPrivateScreen = () => {

  navigation.navigate("Private");

  LocalAuthentication.authenticateAsync();

};
```

Why is this problematic?

Because the user may already reach:

```text
Private Screen
```

before authentication finishes.

The authentication should happen first.

---

# Correct Approach

Do this:

```js
const openPrivateScreen = async () => {

  const result =
    await LocalAuthentication.authenticateAsync({
      promptMessage:
        "Authenticate to open private data",
    });

  if (result.success) {

    navigation.navigate("Private");

  }

};
```

The sequence is:

```text
Authenticate
     ↓
Wait
     ↓
Success?
     ↓
Yes
     ↓
Navigate
```

---

# Example: Protecting Payment

Imagine a payment button:

```jsx
<Button
  title="Pay ₹5,000"
  onPress={confirmPayment}
/>
```

The function could be:

```js
const confirmPayment = async () => {

  const result =
    await LocalAuthentication.authenticateAsync({
      promptMessage:
        "Authenticate to confirm payment",
    });

  if (!result.success) {

    Alert.alert(
      "Authentication Failed",
      "Payment was not confirmed."
    );

    return;
  }

  console.log(
    "Authentication successful"
  );

  // Continue with payment confirmation
};
```

The important security flow is:

```text
User presses Pay
       ↓
Authenticate
       ↓
Success?
       │
       ├── No → Stop
       │
       └── Yes
             ↓
       Continue payment
```

---

# Example: Protecting Private Notes

```js
const openPrivateNotes = async () => {

  const result =
    await LocalAuthentication.authenticateAsync({
      promptMessage:
        "Authenticate to open your private notes",
    });

  if (result.success) {

    console.log(
      "Opening private notes..."
    );

    // navigation.navigate("PrivateNotes");

  } else {

    console.log(
      "Access denied:",
      result.error
    );

  }
};
```

The application should never assume:

```text
Button pressed
     ↓
User is authenticated
```

Instead:

```text
Button pressed
     ↓
Authentication requested
     ↓
Authentication succeeds
     ↓
Protected content allowed
```

---

# Handling Different Errors

A better application can provide different messages.

Example:

```js
if (!result.success) {

  switch (result.error) {

    case "user_cancel":

      Alert.alert(
        "Cancelled",
        "Authentication was cancelled."
      );

      break;


    case "authentication_failed":

      Alert.alert(
        "Failed",
        "The biometric authentication failed."
      );

      break;


    case "lockout":

      Alert.alert(
        "Locked",
        "Biometric authentication is temporarily locked."
      );

      break;


    case "not_enrolled":

      Alert.alert(
        "Not Enrolled",
        "Please enroll biometrics on your device."
      );

      break;


    default:

      Alert.alert(
        "Authentication Error",
        `Authentication failed: ${result.error}`
      );

  }

}
```

This produces a much better user experience than showing:

```text
Authentication failed
```

for every situation.

---

# Example Authentication States

Your application can conceptually have these states:

```text
NOT_AUTHENTICATED
       ↓
CHECKING
       ↓
AUTHENTICATING
       ↓
       ├── AUTHENTICATED
       │
       └── FAILED
```

For a larger application, thinking in terms of states makes the authentication flow easier to manage.

---

# Simple State Example

```js
const [status, setStatus] =
  useState("idle");
```

Possible values:

```text
idle
checking
authenticating
success
failed
```

Then:

```js
setStatus("checking");
```

before checking hardware.

Then:

```js
setStatus("authenticating");
```

before:

```js
authenticateAsync();
```

Then:

```js
setStatus("success");
```

or:

```js
setStatus("failed");
```

This approach becomes useful in more complex applications.

---

# Complete Example With Status

```jsx
import { useState } from "react";

import {
  View,
  Text,
  Button,
  Alert,
} from "react-native";

import * as LocalAuthentication
  from "expo-local-authentication";

export default function App() {

  const [status, setStatus] =
    useState("idle");

  const authenticateUser = async () => {

    try {

      setStatus("checking");

      const hasHardware =
        await LocalAuthentication.hasHardwareAsync();

      if (!hasHardware) {

        setStatus("failed");

        Alert.alert(
          "Unavailable",
          "Biometric hardware is not available."
        );

        return;
      }


      const enrolled =
        await LocalAuthentication.isEnrolledAsync();

      if (!enrolled) {

        setStatus("failed");

        Alert.alert(
          "Not Enrolled",
          "No biometric credentials are enrolled."
        );

        return;
      }


      setStatus("authenticating");


      const result =
        await LocalAuthentication.authenticateAsync({
          promptMessage:
            "Authenticate to continue",
        });


      if (result.success) {

        setStatus("success");

      } else {

        setStatus("failed");

        console.log(
          "Authentication failed:",
          result.error
        );

      }

    } catch (error) {

      setStatus("failed");

      console.error(
        "Authentication error:",
        error
      );

    }

  };


  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
        padding: 20,
      }}
    >

      <Text
        style={{
          fontSize: 24,
          marginBottom: 20,
        }}
      >
        Local Authentication
      </Text>


      <Text
        style={{
          fontSize: 18,
          marginBottom: 20,
        }}
      >
        Status: {status}
      </Text>


      <Button
        title="Authenticate"
        onPress={authenticateUser}
      />

    </View>
  );
}
```

---

# What Happens When the Button Is Pressed?

Initially:

```text
Status: idle
```

After pressing the button:

```text
Status: checking
```

The app checks:

```js
hasHardwareAsync()
```

and:

```js
isEnrolledAsync()
```

Then:

```text
Status: authenticating
```

The system authentication prompt appears.

If successful:

```text
Status: success
```

If unsuccessful:

```text
Status: failed
```

---

# Important Security Rule

Local authentication should be treated as an **authorization gate**, not as a complete authentication system by itself.

For example, this:

```text
Fingerprint successful
```

does not automatically mean:

```text
User has a valid server session
```

In a real application, you might have:

```text
Login
  ↓
Server Authentication
  ↓
Authenticated Session
  ↓
Local Authentication
  ↓
Unlock Sensitive Feature
```

Local authentication is often used to unlock access to something that is already associated with the authenticated user.

---

# Example Real-World Flow

A banking application might work like:

```text
User logs in
     ↓
Email + Password
     ↓
Backend verifies user
     ↓
Session established
     ↓
User closes application
     ↓
User returns
     ↓
Local Authentication
     ↓
Fingerprint / Face
     ↓
Success
     ↓
Access application
```

This is different from using biometrics as a replacement for your entire backend authentication system.

---

# Important: Never Store Biometric Data Yourself

Your application should not attempt to store:

```text
Fingerprint image
Face image
Biometric template
```

The operating system handles the biometric verification.

Your application generally receives a result such as:

```js
{
  success: true
}
```

or:

```js
{
  success: false,
  error: "authentication_failed"
}
```

Your application should work with that result.

---

# Final Complete Flow

At this point, you should understand the complete process:

```text
                 User Action
                     ↓
             Authenticate Button
                     ↓
          hasHardwareAsync()
                     ↓
              Hardware?
              ↙       ↘
            No         Yes
            ↓           ↓
          Stop    isEnrolledAsync()
                        ↓
                    Enrolled?
                    ↙      ↘
                  No        Yes
                  ↓          ↓
                Stop   authenticateAsync()
                              ↓
                     System Authentication
                              ↓
                       User authenticates
                              ↓
                         Check result
                         ↙          ↘
                    success       failure
                       ↓             ↓
                 Allow access   Handle error
```

