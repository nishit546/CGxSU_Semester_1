# React Native – Notifications

This chapter explains notifications in React Native and Expo using an **Expo SDK 54** learning approach.

Notifications allow an application to communicate information to the user even when the application is not currently visible.

Common examples include:

- Reminders
- Alarms
- Task completion
- Messages
- Order updates
- Calendar events
- Habit reminders
- Download completion
- Payment updates
- Time-sensitive alerts

For Expo applications, notification functionality is primarily handled through `expo-notifications`.

> **SDK 54 Note:** This documentation focuses on concepts and APIs relevant to an **Expo SDK 54** project. We will first learn local notifications thoroughly before moving into remote push-notification infrastructure.

---

# Part 1 - Notification Fundamentals

Before writing notification code, understand what happens when a notification is created, scheduled, displayed, and interacted with.

The basic mental model is:

```text
Your React Native App
        ↓
expo-notifications
        ↓
Operating System
        ↓
Notification System
        ↓
User sees notification
```

Your React Native JavaScript code does not directly draw the notification banner. The operating system handles the notification UI.

For example:

```text
React Native App
      ↓
"Study reminder"
      ↓
expo-notifications
      ↓
Android / iOS notification system
      ↓
┌──────────────────────────────┐
│  Study Reminder              │
│ Time to study React Native!  │
└──────────────────────────────┘
```

---

# 1. What Is a Notification?

A notification is a message presented by the operating system to inform the user about something.

Example:

```text
┌──────────────────────────────────┐
│ WhatsApp                         │
│ Rajesh sent you a message        │
└──────────────────────────────────┘
```

Another example:

```text
┌──────────────────────────────────┐
│ Water Reminder                   │
│ Drink some water                 │
└──────────────────────────────────┘
```

A notification can contain information such as:

```text
Title
Body
Sound
Badge
Data
Actions
```

A simplified notification content object can look like:

```js
{
  title: 'Water Reminder',
  body: 'Drink some water',
}
```

---

# 2. Notification vs In-App UI

This is one of the first concepts to understand.

Normal React Native UI:

```jsx
<View>
  <Text>Hello</Text>
</View>
```

is rendered **inside your application**.

A notification is presented by the **operating system notification system**.

Therefore:

```text
React Native UI
      ↓
Inside App
```

while:

```text
Notification
      ↓
OS Notification System
      ↓
Outside / Above the App UI
```

For example, your application might currently display:

```text
┌─────────────────────────────┐
│       Your React Native App │
│                             │
│       Welcome!              │
│                             │
└─────────────────────────────┘
```

A notification can be presented separately by the operating system.

This is why notifications are different from simply showing another `<View>` or `<Alert>` inside the app.

---

# 3. Why Do Applications Use Notifications?

Users do not continuously keep every application open.

Imagine a reminder application.

Without notifications:

```text
User closes app
      ↓
App is no longer visible
      ↓
User may forget the task
```

With a scheduled notification:

```text
User closes app
      ↓
Notification scheduled
      ↓
Operating system waits
      ↓
Trigger time arrives
      ↓
Notification appears
```

Common notification use cases include:

- Reminders
- Alarms
- Chat messages
- Order updates
- Delivery updates
- Social interactions
- Calendar events
- Habit tracking
- Promotions
- Security alerts

---

# 4. Main Types of Notifications

For this course, the most important distinction is:

```text
Notifications
│
├── Local Notifications
│
└── Push Notifications
```

There are also important concepts such as:

```text
Foreground
Background
Terminated
Notification Response
Notification Data
```

These describe the application state and how the application reacts to notifications.

---

# 5. Local Notifications

A **local notification** is created or scheduled locally by the application/device.

Example:

```text
User
  ↓
Sets reminder for 8:00 AM
  ↓
App schedules notification
  ↓
Device manages the scheduled notification
  ↓
8:00 AM
  ↓
Notification appears
```

A backend server is not required for the basic reminder scenario.

Typical use cases:

- Water reminders
- Study reminders
- Habit trackers
- To-do reminders
- Alarm applications
- Daily tips
- Local calendar-style reminders

Example content:

```js
{
  title: 'Study Reminder',
  body: 'Time to continue your React Native course.',
}
```

The important idea is:

```text
App
 ↓
Create/schedule
 ↓
Device
 ↓
Notification
```

---

# 6. Push Notifications

A **push notification** normally originates from a remote system/server.

The basic architecture is:

```text
Your Backend
     ↓
Push Service
     ↓
Device
     ↓
Operating System
     ↓
Notification
```

For example, consider a shopping application:

```text
Order placed
     ↓
Backend
     ↓
Order shipped
     ↓
Push notification
     ↓
User's device
```

The user might receive:

```text
Order Update

Your order has been shipped.
```

Push notifications are useful for:

- Chat messages
- Order status
- Delivery status
- Social notifications
- Account alerts
- Server-generated events
- Time-sensitive remote information

---

# 7. Local vs Push Notifications

This distinction is extremely important.

| Feature | Local Notification | Push Notification |
|---|---|---|
| Main source | Device/app | Remote server |
| Backend required | Not for basic scheduling | Usually yes |
| Internet required for basic scheduling | No | Yes for remote delivery |
| Example | Water reminder | New chat message |


Think about this question:

> Does the information already exist on the device and simply need to be shown later?

If yes, a local notification is often appropriate.

Example:

```text
"Remind me tomorrow at 8 AM."
```

The device can schedule that reminder.

But:

> Tell the user when their order status changes on the server.

requires remote communication, so push notifications are appropriate.

---

# 8. Scheduled Notifications

A scheduled notification is configured to appear at a future time or after a specified trigger.

Basic flow:

```text
Now
 ↓
Schedule notification
 ↓
Wait
 ↓
Trigger condition occurs
 ↓
Notification appears
```

Possible examples:

```text
After 10 minutes
Every day at 8 AM
At a particular date/time
At a recurring time
```

Expo provides:

```js
Notifications.scheduleNotificationAsync()
```

The important idea is that scheduling creates a notification request with:

```text
Content
+
Trigger
```

Conceptually:

```js
{
  content: {
    title: 'Reminder',
    body: 'Time to study!',
  },

  trigger: {
    // when the notification should fire
  },
}
```

---

# 9. Immediate vs Scheduled Notification

These are different concepts.

## Immediate

You want the notification now:

```text
Button pressed
     ↓
Create notification
     ↓
Display notification
```

## Scheduled

You want it later:

```text
Button pressed
     ↓
Create notification request
     ↓
Register trigger
     ↓
Wait
     ↓
Trigger occurs
     ↓
Notification appears
```

Both concepts are important when learning local notifications.

---

# 10. Foreground State

Foreground means:

```text
Application is currently open and active
```

For example:

```text
User
 ↓
Opens your app
 ↓
App is visible
 ↓
Notification arrives
```

When a notification arrives while the application is in the foreground, the notification presentation behavior can be controlled using the notification handler.

This is why:

```js
Notifications.setNotificationHandler()
```

is important.

---

# 11. Background State

Background means the application is not currently the active screen.

For example:

```text
User opens app
      ↓
Presses Home
      ↓
Another screen/app becomes visible
      ↓
Your app is in background
```

A scheduled notification is useful in this situation because the operating system can present the notification without your React Native screen being visible.

---

# 12. Terminated / Quit State

The user may completely close the application.

For example:

```text
User opens app
      ↓
Schedules reminder
      ↓
Closes app
      ↓
Time passes
      ↓
Trigger time arrives
      ↓
Operating system can present notification
```

This is one of the major benefits of local scheduled notifications.

However, actual behavior can depend on:

- Operating system
- Permission state
- Device settings
- Battery restrictions
- Notification configuration
- Type of notification

---

# 13. Notification Permission

Notifications are permission-controlled functionality.

The application should not assume that notifications are automatically allowed.

General flow:

```text
Application
    ↓
Check permission
    ↓
Already granted?
   /       \
 Yes       No
  |         |
Continue    ↓
        Request permission
             ↓
        User decision
          /      \
     Granted    Denied
        |          |
    Continue   Explain /
               Settings
```

With `expo-notifications`, the important APIs are:

```js
Notifications.getPermissionsAsync()
```

and:

```js
Notifications.requestPermissionsAsync()
```

---

# 14. Checking Existing Permission

Before requesting permission, check the current status:

```js
const permission =
  await Notifications.getPermissionsAsync();

console.log(permission);
```

The returned object contains notification permission information.

Conceptually:

```text
Permission status
      ↓
granted / denied / undetermined
```

The complete object can contain additional platform-specific information, so inspect the result during development when debugging permissions.

---

# 15. Requesting Permission

A basic permission request:

```js
const { status } =
  await Notifications.requestPermissionsAsync();

if (status === 'granted') {

  console.log(
    'Notification permission granted'
  );

} else {

  console.log(
    'Notification permission denied'
  );

}
```

The flow is:

```text
requestPermissionsAsync()
        ↓
Operating system permission prompt
        ↓
User decision
        ↓
Your application receives status
```

---

# 16. Better Permission Pattern

A better implementation first checks the current permission.

```js
const requestNotificationPermission =
  async () => {

    const {
      status: existingStatus,
    } =
      await Notifications.getPermissionsAsync();

    let finalStatus =
      existingStatus;

    if (
      existingStatus !==
      'granted'
    ) {

      const {
        status,
      } =
        await Notifications.requestPermissionsAsync();

      finalStatus = status;
    }

    return (
      finalStatus ===
      'granted'
    );
  };
```

Usage:

```js
const allowed =
  await requestNotificationPermission();

if (allowed) {

  console.log(
    'Notifications enabled'
  );

} else {

  console.log(
    'Notifications disabled'
  );

}
```

This is better than requesting permission blindly every time the application starts.

---

# 17. Why Permission Should Be Requested Thoughtfully

A notification permission prompt is an important user decision.

Poor experience:

```text
App opens
 ↓
Immediately request permission
 ↓
User does not understand why
 ↓
User presses Don't Allow
```

Better experience:

```text
User opens notification feature
 ↓
Explain why notifications are useful
 ↓
User understands the feature
 ↓
Request permission
 ↓
User makes an informed decision
```

Example:

```text
Enable Study Reminders?

We can remind you about your scheduled
study sessions even when the app is closed.

[ Enable Notifications ]
```

Then request the system permission.

---

# 18. Installing `expo-notifications`

For an Expo project:

```bash
npx expo install expo-notifications
```

Import:

```js
import * as Notifications
  from 'expo-notifications';
```

For SDK 54, prefer:

```bash
npx expo install expo-notifications
```

rather than manually selecting an arbitrary package version.

---

# 19. Basic Notification Handler

For foreground notification presentation, configure a handler.

```js
Notifications.setNotificationHandler({

  handleNotification:
    async () => ({

      shouldShowBanner: true,
      shouldShowList: true,
      shouldPlaySound: true,
      shouldSetBadge: true,

    }),

});
```

The handler tells the notification system how a notification should be presented while the application is in the foreground.

---

# 20. Understanding `setNotificationHandler()`

The important concept is:

```js
Notifications.setNotificationHandler()
```

configures foreground presentation behavior.

Conceptually:

```text
Notification arrives
       ↓
Is app in foreground?
       ↓
Notification handler
       ↓
Presentation decision
```

Example:

```js
Notifications.setNotificationHandler({

  handleNotification:
    async () => ({

      shouldShowBanner: true,
      shouldShowList: true,
      shouldPlaySound: true,
      shouldSetBadge: true,

    }),

});
```

---

# 21. Handler Options

## `shouldShowBanner`

Controls whether the notification should be presented as a banner where supported.

```js
shouldShowBanner: true
```

---

## `shouldShowList`

Controls whether the notification should appear in the notification list where supported.

```js
shouldShowList: true
```

---

## `shouldPlaySound`

Controls whether a notification sound should play.

```js
shouldPlaySound: true
```

---

## `shouldSetBadge`

Controls badge behavior where supported.

```js
shouldSetBadge: true
```

A simple configuration is:

```js
Notifications.setNotificationHandler({

  handleNotification:
    async () => ({

      shouldShowBanner: true,
      shouldShowList: true,
      shouldPlaySound: true,
      shouldSetBadge: true,

    }),

});
```

---

# 22. Notification Content

A notification can contain information such as:

```text
Title
Body
Data
Sound
```

Example:

```js
{
  title: 'Task Completed',

  body: 'Great job! Keep going.',

  data: {
    taskId: '123',
  },
}
```

Think of it as:

```text
Notification
│
├── title
├── body
├── data
└── other options
```

---

# 23. `title`

The title is the main heading of the notification.

```js
title:
  'Task Completed'
```

Conceptually:

```text
┌──────────────────────────────┐
│ Task Completed               │
└──────────────────────────────┘
```

---

# 24. `body`

The body contains the main notification message.

```js
body:
  'Great job! Keep going.'
```

Together:

```js
{
  title:
    'Task Completed',

  body:
    'Great job! Keep going.',
}
```

Conceptually:

```text
┌──────────────────────────────┐
│ Task Completed               │
│ Great job! Keep going.       │
└──────────────────────────────┘
```

---

# 25. `data`

`data` is custom application data attached to the notification.

Example:

```js
data: {
  screen: 'TaskDetails',
  taskId: '123',
}
```

This becomes useful when the user interacts with the notification.

For example:

```text
User taps notification
        ↓
Read notification data
        ↓
screen = TaskDetails
        ↓
Navigate to TaskDetails
        ↓
Use taskId = 123
```

---

# 26. Why `data` Is Important

Suppose a user receives:

```text
New Message

Rajesh sent you a message.
```

The visible notification tells the user what happened.

But your application may also need:

```js
{
  screen: 'Chat',
  chatId: '12345',
}
```

Then:

```text
Notification tapped
       ↓
Read data
       ↓
chatId = 12345
       ↓
Open Chat screen
       ↓
Load conversation 12345
```

This is the foundation for notification-driven navigation.

---

# 27. Example - Study Reminder

Suppose the user wants:

> Remind me to study at 8 PM.

The flow is:

```text
User selects 8:00 PM
        ↓
App creates notification content
        ↓
App creates trigger
        ↓
Notification scheduled
        ↓
User closes app
        ↓
8:00 PM
        ↓
OS presents notification
```

Notification content:

```js
{
  title:
    'Study Reminder',

  body:
    'Time to continue your React Native course.',
}
```

This is a typical local-notification use case.

---

# 28. Example - Water Reminder

Suppose the application wants to remind the user periodically:

```text
Every 2 hours
      ↓
Notification
      ↓
"Drink Water"
```

Content:

```js
{
  title:
    'Drink Water',

  body:
    'Stay hydrated!',
}
```

The application does not need to contact a backend merely to remind the user about a locally configured reminder.

---

# 29. Example - To-Do Reminder

Suppose the user creates:

```text
Task:
Submit assignment

Reminder:
Tomorrow at 10 AM
```

The application could associate:

```js
{
  taskId: 'task-101',
  title: 'Submit assignment',
}
```

with notification data:

```js
data: {
  screen: 'TaskDetails',
  taskId: 'task-101',
}
```

Then:

```text
Reminder fires
      ↓
User taps notification
      ↓
Read taskId
      ↓
Open TaskDetails
      ↓
Show "Submit assignment"
```

---

# 30. Notification Lifecycle

A useful mental model is:

```text
Create notification content
          ↓
Choose trigger
          ↓
Schedule notification
          ↓
Operating system manages it
          ↓
Trigger condition occurs
          ↓
Notification is presented
          ↓
User sees notification
          ↓
User may tap notification
          ↓
Application receives response
          ↓
Application can navigate / perform action
```

For a scheduled local notification:

```text
scheduleNotificationAsync()
          ↓
        ID
          ↓
Operating System
          ↓
       Trigger
          ↓
 Notification shown
```

The notification identifier becomes useful when managing scheduled notifications later.

---

# Part 2 - Permissions & Complete `expo-notifications` Setup

In this part, we focus on the foundation required before actually scheduling notifications:

```text
Install package
      ↓
Configure notification behavior
      ↓
Check permission
      ↓
Request permission
      ↓
Handle denied permission
      ↓
Initialize notification setup
      ↓
Test configuration
```

The examples are written for an **Expo SDK 54** learning project.

---

# 1. Installing `expo-notifications`

If the package is not already installed:

```bash
npx expo install expo-notifications
```

For an Expo project, prefer:

```bash
npx expo install
```

instead of manually selecting a package version.

The installation flow is:

```text
Expo SDK 54 Project
        ↓
npx expo install expo-notifications
        ↓
expo-notifications
        ↓
Notification APIs available
```

> Installing the package does **not** mean the user has granted notification permission.

---

# 2. Importing the Package

After installation:

```js
import * as Notifications
  from 'expo-notifications';
```

The `Notifications` object gives access to APIs such as:

```js
Notifications.getPermissionsAsync()
```

```js
Notifications.requestPermissionsAsync()
```

```js
Notifications.scheduleNotificationAsync()
```

```js
Notifications.addNotificationReceivedListener()
```

and other notification APIs.

---

# 3. What Needs to Be Configured?

Notification functionality normally has several stages:

```text
1. Package
      ↓
2. Notification behavior
      ↓
3. Permission
      ↓
4. Notification creation/scheduling
      ↓
5. Notification listeners
      ↓
6. Notification interaction handling
```

Do not think:

```text
Install package
      ↓
Notifications automatically enabled
```

Instead:

```text
Install package
      ↓
Configure
      ↓
Request permission
      ↓
Create/schedule notifications
```

---

# 4. Notification Handler

One of the important setup APIs is:

```js
Notifications.setNotificationHandler()
```

It defines how notifications should be handled when the application is in the foreground.

Example:

```js
Notifications.setNotificationHandler({

  handleNotification:
    async () => ({

      shouldShowBanner: true,
      shouldShowList: true,
      shouldPlaySound: true,
      shouldSetBadge: true,

    }),

});
```

---

# 5. Why Do We Need a Notification Handler?

Imagine:

```text
Your app is open
       ↓
A notification arrives
       ↓
What should happen?
```

Should the notification:

```text
Show a banner?
Play a sound?
Appear in notification list?
Update badge?
```

The handler lets your application define foreground presentation behavior.

Conceptually:

```text
Notification arrives
       ↓
App is foreground?
       ↓
Notification Handler
       ↓
Presentation behavior
```

---

# 6. `handleNotification`

The important property is:

```js
handleNotification
```

Example:

```js
Notifications.setNotificationHandler({

  handleNotification:
    async () => ({

      shouldShowBanner: true,
      shouldShowList: true,
      shouldPlaySound: true,
      shouldSetBadge: true,

    }),

});
```

The handler is asynchronous:

```js
async () => ({
  ...
})
```

because the notification system expects the application to provide the handling behavior asynchronously.

---

# 7. `shouldShowBanner`

Example:

```js
shouldShowBanner: true
```

This requests banner presentation where supported.

Conceptually:

```text
Notification arrives
      ↓
Banner presentation enabled
      ↓
User can see notification banner
```

---

# 8. `shouldShowList`

Example:

```js
shouldShowList: true
```

This requests presentation in the notification list where supported.

Together:

```js
shouldShowBanner: true,
shouldShowList: true,
```

allow the notification to be presented through supported operating-system notification surfaces.

---

# 9. `shouldPlaySound`

Example:

```js
shouldPlaySound: true
```

This requests notification sound playback where supported.

If disabled:

```js
shouldPlaySound: false
```

the notification can be configured without notification sound.

---

# 10. `shouldSetBadge`

Example:

```js
shouldSetBadge: true
```

This enables badge-related behavior where supported.

Conceptually:

```text
App Icon
   ↓
Badge
   ↓
Notification indicator / count
```

Badge behavior depends on the platform and device settings.

---

# 11. Basic Notification Handler

For our classroom project:

```js
Notifications.setNotificationHandler({

  handleNotification:
    async () => ({

      shouldShowBanner: true,
      shouldShowList: true,
      shouldPlaySound: true,
      shouldSetBadge: true,

    }),

});
```

This gives us a straightforward configuration for demonstrating foreground notifications.

---

# 12. Where Should the Handler Be Configured?

Configure it early in the application's lifecycle.

For example:

```text
App Entry
   ↓
Configure notification handler
   ↓
Render application
```

A simple setup:

```js
import * as Notifications
  from 'expo-notifications';

Notifications.setNotificationHandler({

  handleNotification:
    async () => ({

      shouldShowBanner: true,
      shouldShowList: true,
      shouldPlaySound: true,
      shouldSetBadge: true,

    }),

});
```

Avoid repeatedly configuring the handler every time a screen renders.

---

# 13. Notification Permission

After package setup, the next major concept is permission.

The user controls whether the application is allowed to send notifications.

The application should therefore check the current permission state.

The API is:

```js
Notifications.getPermissionsAsync()
```

---

# 14. Checking Permission

Basic example:

```js
const permission =
  await Notifications.getPermissionsAsync();

console.log(permission);
```

A simplified mental model is:

```text
Permission
│
├── Granted
├── Denied
└── Undetermined
```

The returned object can contain additional information, so logging it during development is useful:

```js
console.log(
  await Notifications.getPermissionsAsync()
);
```

---

# 15. Why Check Before Requesting?

Imagine requesting permission every time the application starts:

```js
await Notifications.requestPermissionsAsync();
```

A better flow is:

```text
Check permission
       ↓
Granted?
   /       \
 Yes       No
  |         |
Continue   Request
```

This avoids unnecessarily requesting permission when it is already granted.

---

# 16. Requesting Permission

If permission has not already been granted:

```js
const {
  status,
} =
  await Notifications.requestPermissionsAsync();
```

Then:

```js
if (status === 'granted') {

  console.log(
    'Permission granted'
  );

} else {

  console.log(
    'Permission denied'
  );

}
```

The flow is:

```text
requestPermissionsAsync()
        ↓
Operating System
        ↓
Permission prompt
        ↓
User decision
        ↓
Your application
        ↓
Permission status
```

---

# 17. Basic Permission Function

A reusable function is better than duplicating permission logic.

```js
const requestNotificationPermission =
  async () => {

    const {
      status,
    } =
      await Notifications
        .requestPermissionsAsync();

    if (status !== 'granted') {

      console.log(
        'Notification permission denied'
      );

      return false;
    }

    console.log(
      'Notification permission granted'
    );

    return true;
  };
```

Usage:

```js
const allowed =
  await requestNotificationPermission();

if (allowed) {

  console.log(
    'We can continue with notifications'
  );

}
```

---

# 18. Better Permission Function

A better implementation checks existing permission first:

```js
const requestNotificationPermission =
  async () => {

    const {
      status: existingStatus,
    } =
      await Notifications
        .getPermissionsAsync();

    let finalStatus =
      existingStatus;

    if (
      existingStatus !==
      'granted'
    ) {

      const {
        status,
      } =
        await Notifications
          .requestPermissionsAsync();

      finalStatus = status;
    }

    return (
      finalStatus ===
      'granted'
    );
  };
```

Flow:

```text
getPermissionsAsync()
        ↓
Already granted?
   /           \
 Yes            No
  |              |
Return true    requestPermissionsAsync()
                 ↓
              Granted?
              /      \
            Yes       No
             |         |
          true       false
```

---

# 19. Why `let finalStatus`?

We start with:

```js
let finalStatus =
  existingStatus;
```

If permission is not already granted:

```js
const {
  status,
} =
  await Notifications
    .requestPermissionsAsync();

finalStatus = status;
```

Now `finalStatus` represents the latest permission state.

The logic becomes:

```text
existingStatus
      ↓
Maybe request
      ↓
finalStatus
      ↓
Final decision
```

---

# 20. Handling Permission Denial

Never assume the user will grant permission.

Handle:

```js
status !== 'granted'
```

Example:

```js
if (
  finalStatus !==
  'granted'
) {

  Alert.alert(
    'Notifications Disabled',
    'Please enable notifications in Settings.'
  );

  return false;
}
```

This is much better than silently failing.

---

# 21. Importing `Alert`

Use:

```js
import {
  Alert,
} from 'react-native';
```

Then:

```js
Alert.alert(
  'Notifications Disabled',
  'Please enable notifications in Settings.'
);
```

---

# 22. Opening Device Settings

If the user has disabled notifications, provide a way to open system settings.

React Native provides:

```js
Linking.openSettings()
```

Import:

```js
import {
  Alert,
  Linking,
} from 'react-native';
```

Example:

```js
Alert.alert(

  'Notifications Disabled',

  'Please enable notifications in Settings.',

  [
    {
      text: 'Cancel',
    },

    {
      text: 'Open Settings',

      onPress: () =>
        Linking.openSettings(),
    },
  ]

);
```

This allows the user to manually change notification settings.

---

# 23. Complete Permission Example

```jsx
import {
  Alert,
  Linking,
} from 'react-native';

import * as Notifications
  from 'expo-notifications';

const requestNotificationPermission =
  async () => {

    try {

      const {
        status: existingStatus,
      } =
        await Notifications
          .getPermissionsAsync();

      let finalStatus =
        existingStatus;

      if (
        existingStatus !==
        'granted'
      ) {

        const {
          status,
        } =
          await Notifications
            .requestPermissionsAsync();

        finalStatus = status;
      }

      if (
        finalStatus !==
        'granted'
      ) {

        Alert.alert(

          'Notifications Disabled',

          'Please enable notifications in Settings for important updates.',

          [
            {
              text: 'Cancel',
            },

            {
              text: 'Open Settings',

              onPress: () =>
                Linking.openSettings(),
            },
          ]

        );

        return false;
      }

      return true;

    } catch (error) {

      console.error(
        'Permission error:',
        error
      );

      return false;
    }
  };
```

---

# 24. Complete Permission Flow

```text
Application starts
       ↓
getPermissionsAsync()
       ↓
Is permission granted?
       │
   ┌───┴───┐
   │       │
  YES      NO
   │       │
   ↓       ↓
Continue requestPermissionsAsync()
           ↓
        User sees prompt
           ↓
      ┌────┴────┐
      │         │
   Granted    Denied
      │         │
      ↓         ↓
   Continue   Explain /
              Settings
```

---

# 25. When Should Permission Be Requested?

Do not assume that permission must always be requested immediately when the application launches.

A better user experience is often:

```text
User reaches notification feature
       ↓
Explain benefit
       ↓
Request permission
```

Example:

```text
Enable Study Reminders?

We can remind you about your scheduled
study sessions even when the app is closed.

[ Enable Notifications ]
```

When the user chooses:

```text
Enable Notifications
```

then request the operating-system permission.

---

# 26. Setup vs Permission vs Scheduling

Do not confuse these three stages.

## Stage 1 - Package Setup

```bash
npx expo install expo-notifications
```

Means:

```text
Notification API is installed
```

## Stage 2 - Permission

```js
Notifications.requestPermissionsAsync()
```

Means:

```text
Ask user for notification permission
```

## Stage 3 - Schedule

```js
Notifications.scheduleNotificationAsync()
```

Means:

```text
Create/schedule a notification request
```

Overall:

```text
Install
  ↓
Configure
  ↓
Permission
  ↓
Create / Schedule
  ↓
Notification
```

---

# 27. A Common Beginner Mistake

Students often think:

```js
await Notifications.scheduleNotificationAsync(...)
```

guarantees that a notification will always appear.

Do not make that assumption.

Notification behavior depends on:

- Permission
- Trigger
- Notification configuration
- Operating system
- Device settings
- App state
- Platform-specific behavior

A better mental model is:

```text
Permission
      +
Correct configuration
      +
Valid trigger
      +
Supported platform behavior
      ↓
Expected notification behavior
```

---

# 28. Notification Setup Checklist

Before testing local notifications:

```text
[ ] Expo project is running
[ ] expo-notifications is installed
[ ] Notification handler is configured
[ ] Permission status is checked
[ ] Permission is requested when required
[ ] Permission is granted
[ ] Notification content is valid
[ ] Trigger is valid
[ ] Target platform supports the behavior
```

---

# 29. Debugging Permission Problems

If a notification is not appearing, do not immediately assume scheduling is broken.

Check:

```text
1. Is package installed?
        ↓
2. Is permission granted?
        ↓
3. Is notification handler configured?
        ↓
4. Is notification content valid?
        ↓
5. Is trigger valid?
        ↓
6. Is the device/platform configured correctly?
```

Log the permission:

```js
const permission =
  await Notifications.getPermissionsAsync();

console.log(
  'Permission:',
  permission
);
```

Later, when scheduling notifications, also log the returned notification identifier.

---

# 30. Permission vs Device Settings

Your application may know:

```text
permission = granted
```

but the user can still modify notification behavior through operating-system settings.

For example:

```text
Sound
Badge
Alert presentation
Notification settings
```

Therefore notification behavior is influenced by:

```text
Application configuration
+
User permission
+
Operating system settings
```

---

# 31. Android and iOS Are Not Identical

The JavaScript API may be similar, but Android and iOS have different native notification systems.

For example, Android has:

```text
Notification Channels
```

while iOS has its own notification permission and presentation model.

So:

```text
Same JavaScript API
        ↓
Android native behavior
        +
iOS native behavior
```

does not mean every behavior is identical.

---

# 32. Android Notification Channels

On Android, notifications are organized through notification channels.

A channel can control things such as:

```text
Channel name
Importance
Sound
Vibration
```

An application might have:

```text
General Notifications
        ↓
Channel

Reminders
        ↓
Channel

Messages
        ↓
Channel
```

For beginner local notifications, first understand:

```text
Permission
Content
Trigger
```

Then learn Android-specific channel configuration.

---

# 33. Permission vs Android Channel

These are different concepts.

Permission asks:

```text
"Is the application allowed to send notifications?"
```

A channel controls:

```text
"How should this category of Android notifications behave?"
```

Mental model:

```text
Permission
   ↓
Notification authorization
   ↓
Channel
   ↓
Category-specific Android behavior
```

---

# 34. Reusable Notification Service

As an application grows, avoid duplicating notification code.

For example:

```js
const scheduleReminder =
  async ({
    title,
    body,
    trigger,
  }) => {

    return await Notifications
      .scheduleNotificationAsync({

        content: {
          title,
          body,
        },

        trigger,
      });

  };
```

Then:

```js
await scheduleReminder({

  title:
    'Study Reminder',

  body:
    'Time to study React Native!',

  trigger: {
    // trigger configuration
  },

});
```

This makes notification logic reusable.

---

# 35. Why Reusable Functions Help

Without a service:

```text
Screen A
  ↓
Scheduling code

Screen B
  ↓
Scheduling code

Screen C
  ↓
Scheduling code
```

With a service:

```text
Screen A ─┐
Screen B ─┼──→ Notification Service
Screen C ─┘
```

Now the core notification logic has one central location.

For a small classroom project, keeping the code in one file is perfectly fine. As the project grows, extracting notification logic into a service becomes useful.

---

# 36. Complete Handler + Permission Example

```jsx
import React, {
  useEffect,
} from 'react';

import {
  Alert,
  Linking,
} from 'react-native';

import * as Notifications
  from 'expo-notifications';

Notifications.setNotificationHandler({

  handleNotification:
    async () => ({

      shouldShowBanner: true,
      shouldShowList: true,
      shouldPlaySound: true,
      shouldSetBadge: true,

    }),

});

const requestNotificationPermission =
  async () => {

    try {

      const {
        status: existingStatus,
      } =
        await Notifications
          .getPermissionsAsync();

      let finalStatus =
        existingStatus;

      if (
        existingStatus !==
        'granted'
      ) {

        const {
          status,
        } =
          await Notifications
            .requestPermissionsAsync();

        finalStatus = status;
      }

      if (
        finalStatus !==
        'granted'
      ) {

        Alert.alert(

          'Notifications Disabled',

          'Please enable notifications in Settings.',

          [
            {
              text: 'Cancel',
            },

            {
              text: 'Open Settings',

              onPress: () =>
                Linking.openSettings(),
            },
          ]

        );

        return false;
      }

      return true;

    } catch (error) {

      console.error(
        'Notification permission error:',
        error
      );

      return false;
    }
  };

export default function App() {

  useEffect(() => {

    requestNotificationPermission();

  }, []);

  return null;
}
```

This demonstrates:

```text
Notification handler
        +
Permission check
        +
Permission request
        +
Denied state
        +
Settings redirect
```

---

# Part 3 - Local Notifications with `expo-notifications`

In this part, we will finally create our first working **local notification**.

The main API introduced here is:

```js
Notifications.scheduleNotificationAsync()
```

We will understand:

```text
Notification Content
        ↓
Trigger
        ↓
Schedule
        ↓
Notification Identifier
        ↓
Operating System
        ↓
Notification Appears
```

---

# 1. What Is a Local Notification?

A local notification is a notification that is created or scheduled locally by the application/device.

For example:

```text
User
 ↓
Presses "Remind Me"
 ↓
App schedules notification
 ↓
Device waits
 ↓
Trigger occurs
 ↓
Notification appears
```

No application backend is required for this basic reminder scenario.

Typical examples:

```text
Study Reminder
Water Reminder
Task Reminder
Alarm
Habit Reminder
Workout Reminder
```

---

# 2. The Main API

The main method we will use is:

```js
Notifications.scheduleNotificationAsync()
```

The name is important:

```text
schedule
    +
notification
    +
async
```

It creates a notification request that the operating system can use to deliver the notification according to the configured trigger.

---

# 3. Basic Structure

The general structure is:

```js
await Notifications.scheduleNotificationAsync({

  content: {
    title: 'Hello',
    body: 'This is my notification',
  },

  trigger: {
    // trigger configuration
  },

});
```

There are two important parts:

```text
content
   +
trigger
```

Think:

```text
WHAT should the user see?
        ↓
content

WHEN should it appear?
        ↓
trigger
```

---

# 4. Understanding `content`

The `content` object describes the notification itself.

For example:

```js
content: {

  title:
    'Study Reminder',

  body:
    'Time to study React Native!',

}
```

Conceptually:

```text
Notification
│
├── title
│
├── body
│
└── other notification properties
```

---

# 5. Understanding `title`

The title is the main heading.

```js
title:
  'Study Reminder'
```

The user might see:

```text
┌──────────────────────────────┐
│ Study Reminder               │
└──────────────────────────────┘
```

Good notification titles are:

- Short
- Clear
- Relevant
- Easy to understand

Examples:

```text
Study Reminder
Water Reminder
Order Update
Task Reminder
Workout Time
```

---

# 6. Understanding `body`

The body provides additional information.

```js
body:
  'Time to study React Native!'
```

Together:

```js
content: {

  title:
    'Study Reminder',

  body:
    'Time to study React Native!',

}
```

Conceptually:

```text
┌──────────────────────────────┐
│ Study Reminder               │
│ Time to study React Native!  │
└──────────────────────────────┘
```

---

# 7. Notification `data`

You can also attach custom application data.

Example:

```js
data: {

  screen:
    'Study',

  lessonId:
    'lesson-101',

}
```

Complete content:

```js
content: {

  title:
    'Study Reminder',

  body:
    'Continue your React Native lesson.',

  data: {

    screen:
      'Study',

    lessonId:
      'lesson-101',

  },

}
```

The visible notification contains:

```text
Study Reminder
Continue your React Native lesson.
```

The application can additionally receive:

```js
{
  screen: 'Study',
  lessonId: 'lesson-101',
}
```

This becomes especially useful when we later learn notification responses and navigation.

---

# 8. Understanding `trigger`

The second major part is:

```js
trigger
```

The trigger answers:

> **When should this notification be delivered?**

Think:

```text
content
   ↓
WHAT

trigger
   ↓
WHEN
```

For example:

```text
WHAT:
"Study React Native"

WHEN:
10 seconds later
```

---

# 9. Trigger Concept

A trigger can represent conditions such as:

```text
After a delay
At a particular date/time
Repeatedly
```

The exact trigger shape depends on the scheduling behavior you want.

Conceptually:

```text
Notification
      │
      ├── Content
      │     ├── title
      │     ├── body
      │     └── data
      │
      └── Trigger
            └── when
```

---

# 10. First Example - Delayed Notification

For learning purposes, a short delay is useful because we do not have to wait until tomorrow.

Example:

```js
await Notifications.scheduleNotificationAsync({

  content: {

    title:
      'Hello!',

    body:
      'Your local notification is working.',

  },

  trigger: {
    type: 'timeInterval',
    seconds: 10,
    repeats: false,
  },

});
```

The flow is:

```text
Button pressed
      ↓
Notification scheduled
      ↓
Wait 10 seconds
      ↓
Notification trigger occurs
      ↓
Notification appears
```

> Trigger syntax can vary with the installed Expo SDK/API version. Always use the SDK 54-compatible API shape in your project and verify the current Expo documentation when implementing production code.

---

# 11. Why Use a Short Delay in Class?

Suppose we schedule:

```text
Tomorrow at 8:00 AM
```

During class, we would have to wait until tomorrow.

Instead:

```text
10 seconds
```

lets students immediately observe the complete lifecycle:

```text
Schedule
 ↓
Wait
 ↓
Receive
 ↓
Display
```

This is excellent for debugging.

---

# 12. Complete First Local Notification Example

```jsx
import React from 'react';

import {
  View,
  Text,
  Pressable,
  StyleSheet,
  Alert,
} from 'react-native';

import * as Notifications
  from 'expo-notifications';

Notifications.setNotificationHandler({

  handleNotification:
    async () => ({

      shouldShowBanner: true,
      shouldShowList: true,
      shouldPlaySound: true,
      shouldSetBadge: true,

    }),

});

export default function NotificationScreen() {

  const sendNotification =
    async () => {

      try {

        const {
          status,
        } =
          await Notifications
            .getPermissionsAsync();

        let finalStatus =
          status;

        if (
          finalStatus !==
          'granted'
        ) {

          const result =
            await Notifications
              .requestPermissionsAsync();

          finalStatus =
            result.status;
        }

        if (
          finalStatus !==
          'granted'
        ) {

          Alert.alert(
            'Permission Required',
            'Please enable notifications.'
          );

          return;
        }

        const notificationId =
          await Notifications
            .scheduleNotificationAsync({

              content: {

                title:
                  'Hello!',

                body:
                  'Your first local notification is working.',

              },

              trigger: {
                type: 'timeInterval',
                seconds: 10,
                repeats: false,
              },

            });

        console.log(
          'Notification ID:',
          notificationId
        );

        Alert.alert(
          'Scheduled',
          'Notification will appear in 10 seconds.'
        );

      } catch (error) {

        console.error(
          'Notification error:',
          error
        );

        Alert.alert(
          'Error',
          'Unable to schedule notification.'
        );

      }
    };

  return (

    <View style={styles.container}>

      <Text style={styles.title}>
        Local Notification
      </Text>

      <Pressable
        style={styles.button}
        onPress={sendNotification}
      >

        <Text style={styles.buttonText}>
          Schedule Notification
        </Text>

      </Pressable>

    </View>
  );
}

const styles = StyleSheet.create({

  container: {
    flex: 1,
    justifyContent: 'center',
    padding: 20,
  },

  title: {
    fontSize: 24,
    fontWeight: 'bold',
    textAlign: 'center',
    marginBottom: 30,
  },

  button: {
    backgroundColor: '#007AFF',
    padding: 18,
    borderRadius: 12,
    alignItems: 'center',
  },

  buttonText: {
    color: '#fff',
    fontSize: 16,
    fontWeight: '600',
  },

});
```

---

# 13. Breaking Down the Example

The example has several stages.

## Stage 1

```js
Notifications.getPermissionsAsync()
```

Check existing permission.

---

## Stage 2

```js
Notifications.requestPermissionsAsync()
```

Request permission if required.

---

## Stage 3

```js
Notifications.scheduleNotificationAsync()
```

Schedule the notification.

---

## Stage 4

```js
content
```

Defines what the user sees.

---

## Stage 5

```js
trigger
```

Defines when the notification should fire.

---

# 14. Notification Identifier

This line is important:

```js
const notificationId =
  await Notifications
    .scheduleNotificationAsync({
      ...
    });
```

The scheduling method returns an identifier.

For example:

```text
Notification ID
      ↓
"some-generated-id"
```

Log it:

```js
console.log(
  'Notification ID:',
  notificationId
);
```

Why is this useful?

Because later you can use the identifier to manage the scheduled notification.

For example:

```text
Schedule
   ↓
ID
   ↓
Store ID
   ↓
Cancel / manage later
```

---

# 15. Why Does a Notification Need an ID?

Imagine your application schedules:

```text
Study Reminder
Water Reminder
Workout Reminder
```

You may need to distinguish them.

Conceptually:

```text
Study Reminder
   ↓
ID A

Water Reminder
   ↓
ID B

Workout Reminder
   ↓
ID C
```

Then your application can manage individual notification requests.

We will study cancellation and notification management in a later part.

---

# 16. Scheduling Does Not Mean "Show Immediately"

This distinction is very important.

When you call:

```js
scheduleNotificationAsync()
```

you are creating a notification request.

For a delayed trigger:

```text
scheduleNotificationAsync()
        ↓
Notification registered
        ↓
Wait
        ↓
Trigger occurs
        ↓
Notification delivered
```

So this:

```js
console.log('Scheduled');
```

does not mean:

```text
Notification already displayed.
```

It means:

```text
Notification request was scheduled.
```

---

# 17. Schedule vs Display

Think of these as two different stages:

```text
SCHEDULE
   ↓
Create notification request
```

then:

```text
TRIGGER
   ↓
Condition becomes true
```

then:

```text
DISPLAY / PRESENT
   ↓
Operating system presents notification
```

Complete lifecycle:

```text
schedule
   ↓
wait
   ↓
trigger
   ↓
present
```

---

# 18. Testing the First Notification

After pressing:

```text
Schedule Notification
```

you should see something like:

```text
Scheduled
Notification will appear in 10 seconds.
```

Then wait.

The operating system should present the notification according to the configured behavior and platform.

The test is:

```text
Press Button
      ↓
Permission
      ↓
Schedule
      ↓
Wait 10 seconds
      ↓
Notification
```

---

# 19. What If the App Is in Foreground?

If the application is open, the foreground handler from Part 2 becomes relevant:

```js
Notifications.setNotificationHandler({

  handleNotification:
    async () => ({

      shouldShowBanner: true,
      shouldShowList: true,
      shouldPlaySound: true,
      shouldSetBadge: true,

    }),

});
```

This controls how the notification should be presented while the application is in the foreground.

---

# 20. What If the App Is in Background?

If the user moves the application to the background:

```text
App
 ↓
Home button / another app
 ↓
Your app is not active
```

the operating system can manage the scheduled local notification when its trigger occurs.

This is one of the major reasons local notifications are useful.

---

# 21. What If the App Is Closed?

A scheduled local notification can still be managed by the operating system after the user closes the application, subject to platform behavior, permissions, device settings, and the notification configuration.

Conceptually:

```text
App open
   ↓
Schedule
   ↓
App closed
   ↓
Time passes
   ↓
Trigger
   ↓
Operating system
   ↓
Notification
```

This is different from a normal JavaScript `setTimeout()`.

---

# 22. Notification vs `setTimeout()`

This is an important React Native concept.

You might think:

```js
setTimeout(() => {
  console.log('Reminder');
}, 10000);
```

could replace notifications.

It cannot provide the same behavior.

`setTimeout()` is a JavaScript timer.

A local notification is handled through the operating system's notification infrastructure.

Compare:

```text
setTimeout()
    ↓
JavaScript runtime
```

versus:

```text
scheduleNotificationAsync()
    ↓
Notification system / OS
```

A timer is therefore not a replacement for a scheduled local notification.

---

# 23. Why `setTimeout()` Is Not a Notification System

Suppose:

```js
setTimeout(
  () => {
    console.log('Reminder');
  },
  10000
);
```

The callback is JavaScript code.

But a notification:

```text
User
 ↓
Closes app
 ↓
OS manages notification
 ↓
Notification appears
```

requires operating-system notification functionality.

This is why `expo-notifications` exists.

---

# 24. Creating Dynamic Notification Content

Notifications do not have to contain hard-coded text.

Example:

```js
const userName =
  'Rajesh';

await Notifications
  .scheduleNotificationAsync({

    content: {

      title:
        `Hello ${userName}`,

      body:
        'Your study reminder is ready.',

    },

    trigger: {
      type: 'timeInterval',
      seconds: 10,
      repeats: false,
    },

  });
```

Result conceptually:

```text
Hello Rajesh

Your study reminder is ready.
```

---

# 25. Dynamic To-Do Notification

Suppose:

```js
const task = {
  id: 'task-101',
  title: 'Complete React Native assignment',
};
```

Notification:

```js
await Notifications
  .scheduleNotificationAsync({

    content: {

      title:
        'Task Reminder',

      body:
        task.title,

      data: {

        screen:
          'TaskDetails',

        taskId:
          task.id,

      },

    },

    trigger: {
      type: 'timeInterval',
      seconds: 10,
      repeats: false,
    },

  });
```

Now the notification contains both:

```text
Visible content
```

and:

```text
Navigation/application data
```

---

# 26. Dynamic Order Notification

Example:

```js
const order = {
  id: 'ORD-1001',
  status: 'Shipped',
};
```

Then:

```js
await Notifications
  .scheduleNotificationAsync({

    content: {

      title:
        'Order Update',

      body:
        `Order ${order.id} is ${order.status}.`,

      data: {

        screen:
          'OrderDetails',

        orderId:
          order.id,

      },

    },

    trigger: {
      type: 'timeInterval',
      seconds: 10,
      repeats: false,
    },

  });
```

Conceptually:

```text
Order Update
Order ORD-1001 is Shipped.
```

and application data:

```js
{
  screen: 'OrderDetails',
  orderId: 'ORD-1001',
}
```

---

# 27. Multiple Notifications

You can schedule more than one notification.

Example:

```js
await Notifications
  .scheduleNotificationAsync({

    content: {

      title:
        'Reminder 1',

      body:
        'First reminder',

    },

    trigger: {
      type: 'timeInterval',
      seconds: 10,
      repeats: false,
    },

  });

await Notifications
  .scheduleNotificationAsync({

    content: {

      title:
        'Reminder 2',

      body:
        'Second reminder',

    },

    trigger: {
      type: 'timeInterval',
      seconds: 20,
      repeats: false,
    },

  });
```

Conceptually:

```text
Now
 ↓
Schedule Reminder 1
 ↓
Schedule Reminder 2
 ↓
10 sec → Reminder 1
 ↓
20 sec → Reminder 2
```

Each scheduled request can have its own identifier.

---

# 28. Example - Two Study Reminders

```js
const scheduleStudyReminders =
  async () => {

    const firstId =
      await Notifications
        .scheduleNotificationAsync({

          content: {

            title:
              'Study Reminder',

            body:
              'Start your React Native lesson.',

          },

          trigger: {
            type: 'timeInterval',
            seconds: 10,
            repeats: false,
          },

        });

    const secondId =
      await Notifications
        .scheduleNotificationAsync({

          content: {

            title:
              'Study Reminder',

            body:
              'Review what you learned today.',

          },

          trigger: {
            type: 'timeInterval',
            seconds: 20,
            repeats: false,
          },

        });

    console.log(
      'First:',
      firstId
    );

    console.log(
      'Second:',
      secondId
    );
  };
```

---

# 29. Reusable Local Notification Function

Instead of repeating:

```js
Notifications.scheduleNotificationAsync(...)
```

you can create a helper:

```js
const scheduleLocalNotification =
  async ({
    title,
    body,
    seconds,
    data = {},
  }) => {

    return await Notifications
      .scheduleNotificationAsync({

        content: {

          title,
          body,
          data,

        },

        trigger: {
          type: 'timeInterval',
          seconds,
          repeats: false,
        },

      });
  };
```

Usage:

```js
await scheduleLocalNotification({

  title:
    'Study Reminder',

  body:
    'Time to study.',

  seconds:
    10,

});
```

Another:

```js
await scheduleLocalNotification({

  title:
    'Water Reminder',

  body:
    'Drink some water.',

  seconds:
    20,

});
```

---

# 30. Why a Helper Is Useful

Without a helper:

```text
Screen A
 ↓
Large notification object

Screen B
 ↓
Large notification object

Screen C
 ↓
Large notification object
```

With a helper:

```text
Screen A ─┐
Screen B ─┼──→ scheduleLocalNotification()
Screen C ─┘
```

This makes your application easier to maintain.

---

# 31. Important: Notification Content Is Not UI

Do not write:

```jsx
<Text>
  {notification.title}
</Text>
```

and assume this creates a system notification.

That only renders text inside your React Native UI.

System notification:

```js
Notifications
  .scheduleNotificationAsync(...)
```

is handled through notification infrastructure.

---

# 32. Notification Content Can Be Dynamic

You can generate content from:

```text
API data
User input
Database
Local storage
Task list
Calendar
Application state
```

Example:

```js
const taskCount =
  5;

await Notifications
  .scheduleNotificationAsync({

    content: {

      title:
        'Pending Tasks',

      body:
        `You have ${taskCount} tasks remaining.`,

    },

    trigger: {
      type: 'timeInterval',
      seconds: 10,
      repeats: false,
    },

  });
```

---

# 33. Example - Personalized Reminder

```js
const user = {
  name:
    'Rajesh',
};

await Notifications
  .scheduleNotificationAsync({

    content: {

      title:
        `Hey ${user.name}!`,

      body:
        'Your study session starts soon.',

      data: {

        screen:
          'Study',

      },

    },

    trigger: {
      type: 'timeInterval',
      seconds: 10,
      repeats: false,
    },

  });
```

---

# 34. Error Handling

Notification scheduling is asynchronous, so use `try/catch`.

```js
try {

  const id =
    await Notifications
      .scheduleNotificationAsync({

        content: {

          title:
            'Reminder',

          body:
            'This is a reminder.',

        },

        trigger: {
          type: 'timeInterval',
          seconds: 10,
          repeats: false,
        },

      });

  console.log(
    'Scheduled:',
    id
  );

} catch (error) {

  console.error(
    'Failed:',
    error
  );

}
```

---

# 35. Why Error Handling Matters

Possible issues include:

```text
Permission denied
Invalid trigger
Invalid notification configuration
Platform limitations
Configuration problems
```

A production application should not assume every scheduling request succeeds.

---

# 36. What Does the Returned ID Mean?

When:

```js
const id =
  await Notifications
    .scheduleNotificationAsync(...);
```

succeeds, `id` identifies the scheduled notification request.

You can store it:

```js
const id =
  await Notifications
    .scheduleNotificationAsync(...);

setNotificationId(id);
```

Later:

```text
notificationId
      ↓
Cancel / manage notification
```

We will cover this in the notification-management part.

---

# 37. Notification Lifecycle With ID

Now our mental model becomes:

```text
Create content
      ↓
Create trigger
      ↓
Schedule
      ↓
Receive ID
      ↓
Operating system manages request
      ↓
Trigger occurs
      ↓
Notification appears
```

Example:

```text
Study Reminder
      ↓
10-second trigger
      ↓
scheduleNotificationAsync()
      ↓
ID: ABC123
      ↓
Wait
      ↓
Notification
```

---

# 38. Practical Assignment Example

Build a screen:

```text
--------------------------------
       Notification Demo
--------------------------------

Title
[ Study Reminder             ]

Message
[ Time to study React Native! ]

Delay
[ 10 seconds ]

[ Schedule Notification ]
```

When the button is pressed:

```text
Validate permission
      ↓
Read title
      ↓
Read message
      ↓
Read delay
      ↓
Schedule notification
      ↓
Display returned notification ID
```

This is a good first exercise because it connects:

```text
React Native UI
+
User input
+
Permission
+
expo-notifications
+
Local scheduling
```

---

# 39. Practical Example - Reminder App

A simple reminder application could contain:

```text
Reminder App

Task:
[ Complete Assignment ]

Delay:
[ 10 seconds ]

[ Set Reminder ]
```

When the user presses:

```text
Set Reminder
      ↓
Notification scheduled
      ↓
10 seconds
      ↓
"Complete Assignment"
```

This can later be expanded into:

```text
Date Picker
Time Picker
Recurring reminders
Cancel reminder
Edit reminder
Notification history
```

---

# 40. Local Notification Mental Model

Remember:

```text
              LOCAL NOTIFICATION
                       │
                       ↓
              scheduleNotificationAsync()
                       │
              ┌────────┴────────┐
              ↓                 ↓
           content            trigger
              │                 │
              ↓                 ↓
          WHAT user sees     WHEN it fires
              │                 │
              └────────┬────────┘
                       ↓
                 Notification ID
                       ↓
                Operating System
                       ↓
                     Trigger
                       ↓
                 Notification
```

---

# Part 4 - Notification Triggers & Scheduling with `expo-notifications`

In Part 3, we learned:

```text
scheduleNotificationAsync()
        ↓
content + trigger
        ↓
Notification ID
        ↓
Operating System
        ↓
Notification
```

This part focuses specifically on the **trigger**.

The key question is:

> **When should the notification appear?**

---

# 1. What Is a Notification Trigger?

A notification trigger defines the condition that determines when a scheduled notification should fire.

Remember:

```text
content = WHAT
trigger = WHEN
```

Example:

```js
content: {
  title: 'Study Reminder',
  body: 'Time to study React Native!',
}
```

answers:

```text
WHAT should the user see?
```

while:

```js
trigger: {
  // timing configuration
}
```

answers:

```text
WHEN should the notification fire?
```

---

# 2. Why Is the Trigger Important?

Suppose the user creates:

```text
Reminder:
Drink Water
```

The application still needs to know:

```text
When?
```

Possible requirements:

```text
After 10 seconds
After 30 minutes
At 8:00 AM
Tomorrow at 10:00 AM
Every day at 8:00 AM
Every Monday at 9:00 AM
```

The trigger represents this timing requirement.

---

# 3. Notification Scheduling Model

A useful mental model is:

```text
User creates reminder
        ↓
Application creates content
        ↓
Application creates trigger
        ↓
scheduleNotificationAsync()
        ↓
Notification ID returned
        ↓
Operating system manages request
        ↓
Trigger condition occurs
        ↓
Notification appears
```

The trigger is therefore one of the most important parts of local notification scheduling.

---

# 4. Main Trigger Concepts

For our learning, think about notification triggers in these categories:

```text
Trigger
│
├── Time Interval
│
├── Specific Date / Time
│
└── Repeating Schedule
```

The exact trigger object should always match the Expo SDK/API version being used.

---

# 5. Time-Interval Trigger

A time-interval trigger is useful when you want:

> "Show this notification after a certain amount of time."

Examples:

```text
10 seconds from now
60 seconds from now
10 minutes from now
30 minutes from now
```

Mental model:

```text
NOW
 ↓
Wait N seconds
 ↓
Notification
```

---

# 6. Basic Time-Interval Example

Example:

```js
await Notifications.scheduleNotificationAsync({

  content: {

    title:
      'Reminder',

    body:
      'This notification was scheduled using a time interval.',

  },

  trigger: {

    type:
      'timeInterval',

    seconds:
      10,

    repeats:
      false,

  },

});
```

Flow:

```text
Button pressed
      ↓
Schedule
      ↓
Wait 10 seconds
      ↓
Notification fires
```

> Trigger syntax should always be checked against the Expo SDK version used by your project. The examples here are intended for the SDK 54 learning flow.

---

# 7. Understanding `seconds`

The:

```js
seconds
```

value specifies the interval.

Examples:

```js
seconds: 10
```

means approximately:

```text
10 seconds
```

```js
seconds: 60
```

means:

```text
60 seconds = 1 minute
```

```js
seconds: 300
```

means:

```text
300 seconds = 5 minutes
```

---

# 8. Understanding `repeats`

A time-interval trigger can conceptually be configured as:

```js
repeats: false
```

or:

```js
repeats: true
```

### `repeats: false`

```text
Schedule
   ↓
Wait
   ↓
Notification
   ↓
Stop
```

### `repeats: true`

```text
Schedule
   ↓
Wait
   ↓
Notification
   ↓
Wait again
   ↓
Notification
   ↓
Continue
```

---

# 9. One-Time Time-Interval Notification

Example:

```js
trigger: {

  type:
    'timeInterval',

  seconds:
    30,

  repeats:
    false,

}
```

Flow:

```text
Now
 ↓
30 seconds
 ↓
Notification
 ↓
Done
```

Useful for:

- Short reminders
- Temporary timers
- Study breaks
- Workout reminders
- Classroom testing

---

# 10. Repeating Time-Interval Notification

Example:

```js
trigger: {

  type:
    'timeInterval',

  seconds:
    60,

  repeats:
    true,

}
```

Conceptually:

```text
Now
 ↓
60 sec
 ↓
Notification
 ↓
60 sec
 ↓
Notification
 ↓
60 sec
 ↓
Notification
```

This can be useful for interval-based reminders.

However, avoid unnecessarily frequent notifications in real applications because notification spam creates a poor user experience.

---

# 11. Example - Study Break Reminder

Suppose a study application wants to remind the user after 25 minutes:

```text
Start Study Session
        ↓
25 minutes
        ↓
"Take a short break"
```

Notification content:

```js
{
  title:
    'Study Break',

  body:
    'Take a short break before continuing.',
}
```

The trigger represents the 25-minute delay.

---

# 12. Example - Workout Timer

Imagine:

```text
Exercise:
Push Ups

Rest:
60 seconds
```

The application can conceptually schedule:

```text
Now
 ↓
60 seconds
 ↓
"Start Next Set"
```

This is a time-interval use case.

---

# 13. Example - Cooking Reminder

Suppose a recipe says:

```text
Bake for 20 minutes.
```

The application can schedule:

```text
Now
 ↓
20 minutes
 ↓
"Check the Oven"
```

This demonstrates how a local notification can act as a timer reminder.

---

# 14. Specific Date / Time

Sometimes the requirement is not:

> "After 10 minutes."

Instead:

> "At a specific date and time."

For example:

```text
August 25
10:00 AM
```

Mental model:

```text
Current time
      ↓
Future date/time
      ↓
Notification
```

Useful for:

- Appointments
- Meetings
- Assignments
- Reminders
- Study sessions
- Calendar-style features

---

# 15. Date/Time Trigger Mental Model

Suppose:

```text
Current:
August 23, 2026
3:00 PM
```

User chooses:

```text
August 25, 2026
10:00 AM
```

The application creates:

```text
Content
+
Future date/time trigger
```

Then:

```text
Schedule
   ↓
Wait
   ↓
August 25, 10:00 AM
   ↓
Notification
```

---

# 16. Why Date/Time Triggers Are Useful

A real reminder application normally works with dates and times.

Example:

```text
Task:
Submit Assignment

Reminder:
August 25
9:00 AM
```

The application needs to convert the selected date/time into an appropriate notification trigger.

This is different from:

```text
10 seconds from now
```

because the requirement is tied to a specific point in time.

---

# 17. JavaScript `Date`

JavaScript represents dates using:

```js
Date
```

Example:

```js
const reminderDate =
  new Date();
```

You can construct a future date/time as needed.

For example:

```js
const reminderDate =
  new Date(
    2026,
    7,
    25,
    10,
    0,
    0
  );
```

Important JavaScript detail:

```text
Month is zero-based.
```

Therefore:

```text
0 → January
1 → February
...
7 → August
```

So:

```js
new Date(2026, 7, 25, 10, 0, 0)
```

represents:

```text
August 25, 2026
10:00:00
```

---

# 18. Why Date Handling Can Be Tricky

Date/time notifications can involve:

```text
Time zones
Daylight saving changes
Device clock changes
Locale
Invalid dates
Past dates
Date parsing
```

Therefore, avoid treating date/time as simple strings.

For example:

```js
'10:00 AM'
```

is not enough information for a complete reminder.

You need:

```text
Date
+
Time
+
Time-zone/local-time context
```

---

# 19. Past Date Problem

Suppose the user selects:

```text
Yesterday at 10 AM
```

but now it is:

```text
Today at 3 PM
```

The selected trigger is already in the past.

A good application should validate this.

```js
if (
  reminderDate <= new Date()
) {

  Alert.alert(
    'Invalid Time',
    'Please select a future date and time.'
  );

  return;
}
```

---

# 20. Future Date Validation

Reusable helper:

```js
const isFutureDate =
  (date) => {

    return (
      date.getTime()
      >
      Date.now()
    );

  };
```

Usage:

```js
if (
  !isFutureDate(reminderDate)
) {

  Alert.alert(
    'Invalid Reminder',
    'Please select a future time.'
  );

  return;
}
```

This is useful for reminder applications.

---

# 21. Date/Time Scheduling Architecture

The exact trigger shape should follow the Expo SDK/API supported by the project.

The architecture is:

```text
Date Picker
      ↓
Time Picker
      ↓
Date + Time
      ↓
Validate future time
      ↓
Create trigger
      ↓
Schedule notification
```

Conceptually:

```js
const content = {

  title:
    'Assignment Reminder',

  body:
    'Submit your React Native assignment.',

};

const trigger = {

  // future date/time configuration

};

await Notifications.scheduleNotificationAsync({

  content,

  trigger,

});
```

---

# 22. Date Picker + Notification Architecture

A practical reminder screen could look like:

```text
--------------------------------
        Create Reminder
--------------------------------

Title
[ Submit Assignment ]

Date
[ 25 Aug 2026 ]

Time
[ 10:00 AM ]

[ Set Reminder ]
```

Flow:

```text
User selects date
        ↓
User selects time
        ↓
Create Date object
        ↓
Validate future
        ↓
Create notification
        ↓
Schedule
```

---

# 23. Recurring Notifications

Sometimes the requirement is:

> "Do this repeatedly."

Examples:

```text
Every day at 8 AM
Every Monday at 9 AM
Every week
Every certain interval
```

The application needs a repeating trigger.

Conceptually:

```text
Schedule
   ↓
Trigger
   ↓
Notification
   ↓
Next occurrence
   ↓
Notification
   ↓
Next occurrence
```

---

# 24. Daily Reminder

A common use case:

```text
Every day
8:00 AM
```

Example:

```text
Water Reminder

Drink water and stay hydrated.
```

Architecture:

```text
Daily trigger
      ↓
08:00
      ↓
Notification
      ↓
Next day
      ↓
08:00
      ↓
Notification
```

This is different from simply waiting 24 hours from the moment the notification was scheduled.

---

# 25. Weekly Reminder

Another example:

```text
Every Monday
9:00 AM
```

Use cases:

- Weekly meetings
- Weekly assignments
- Weekly workouts
- Weekly planning
- Weekly reports

Conceptually:

```text
Monday 9 AM
     ↓
Notification
     ↓
Next Monday 9 AM
     ↓
Notification
```

---

# 26. Daily vs Interval Reminder

These can represent different requirements.

### Interval

```text
Every 24 hours
```

means conceptually:

```text
Wait 24 hours
 ↓
Notification
 ↓
Wait 24 hours
 ↓
Notification
```

### Calendar-based daily reminder

```text
Every day at 8 AM
```

means:

```text
Today 8 AM
 ↓
Tomorrow 8 AM
 ↓
Next day 8 AM
```

The second requirement is tied to a clock time rather than simply an elapsed duration.

---

# 27. Why This Difference Matters

Imagine the user says:

```text
Daily reminder at 8 AM
```

If you interpret it as:

```text
24 hours after scheduling
```

and the user schedules it at 3 PM:

```text
Schedule at 3 PM
 ↓
24 hours
 ↓
Tomorrow at 3 PM
```

That does not satisfy:

```text
Every day at 8 AM
```

Therefore:

```text
Elapsed interval
```

and:

```text
Calendar schedule
```

are different concepts.

---

# 28. Trigger + Content Example

Requirement:

```text
Reminder:
Study React Native

Time:
Tomorrow at 8 AM
```

Think:

```js
const content = {

  title:
    'Study Reminder',

  body:
    'Time to continue your React Native course.',

};
```

Then:

```js
const trigger = {

  // tomorrow at 8 AM

};
```

Then:

```js
await Notifications.scheduleNotificationAsync({

  content,

  trigger,

});
```

The separation makes the code easier to understand.

---

# 29. Build the Notification Object First

Instead of putting everything into one large function:

```js
const content = {

  title:
    'Study Reminder',

  body:
    'Time to study React Native.',

  data: {

    screen:
      'Study',

  },

};

const trigger = {

  // timing configuration

};
```

Then:

```js
await Notifications.scheduleNotificationAsync({

  content,

  trigger,

});
```

This makes debugging easier because you can inspect:

```js
console.log(content);
console.log(trigger);
```

before scheduling.

---

# 30. Reusable Scheduler Function

Create a helper:

```js
const scheduleNotification =
  async ({
    content,
    trigger,
  }) => {

    try {

      const id =
        await Notifications
          .scheduleNotificationAsync({

            content,
            trigger,

          });

      return id;

    } catch (error) {

      console.error(
        'Scheduling failed:',
        error
      );

      throw error;
    }
  };
```

Usage:

```js
await scheduleNotification({

  content: {

    title:
      'Study Reminder',

    body:
      'Time to study.',

  },

  trigger: {

    // timing

  },

});
```

---

# 31. Reminder Application Architecture

A realistic reminder application can be organized as:

```text
Reminder Screen
      ↓
User enters reminder
      ↓
Date / Time selection
      ↓
Validation
      ↓
Notification Service
      ↓
scheduleNotificationAsync()
      ↓
Notification ID
      ↓
Store reminder + ID
```

Conceptual reminder data:

```js
{
  id:
    'task-101',

  title:
    'Submit Assignment',

  reminderDate:
    'future date',

  notificationId:
    'notification-id',
}
```

This connects:

```text
Application Reminder
        ↕
Notification Request
```

---

# 32. Why Store the Notification ID?

Suppose the user creates:

```text
Reminder:
Doctor Appointment
```

and later edits it.

The application may need to:

```text
Cancel old notification
       ↓
Create new notification
       ↓
Store new ID
```

Therefore:

```text
Reminder
   ↓
notificationId
```

is a useful relationship.

We will use this heavily when learning notification management.

---

# 33. Multiple Reminder Example

Suppose the user creates:

```text
Reminder 1
Study
8 AM

Reminder 2
Workout
6 PM

Reminder 3
Read Book
10 PM
```

The application could maintain:

```js
[
  {
    title:
      'Study',

    notificationId:
      'id-1',
  },

  {
    title:
      'Workout',

    notificationId:
      'id-2',
  },

  {
    title:
      'Read Book',

    notificationId:
      'id-3',
  },
]
```

Each application reminder can therefore be associated with its scheduled notification.

---

# 34. Notification Scheduling vs Local Storage

A reminder application often needs both:

```text
Local Storage
       +
Notifications
```

Local storage remembers:

```text
What reminder did the user create?
```

Notification scheduling handles:

```text
When should the user be notified?
```

For example:

```text
AsyncStorage / database
        ↓
Reminder information
```

and:

```text
expo-notifications
        ↓
Notification scheduling
```

They solve different problems.

---

# 35. Example Reminder Data

```js
const reminder = {

  id:
    'reminder-101',

  title:
    'Submit Assignment',

  description:
    'Complete Native Features assignment',

  reminderDate:
    new Date(),

  notificationId:
    null,

};
```

After scheduling:

```js
const notificationId =
  await Notifications
    .scheduleNotificationAsync({

      content: {

        title:
          reminder.title,

        body:
          reminder.description,

      },

      trigger: {

        // timing configuration

      },

    });
```

Then:

```js
reminder.notificationId =
  notificationId;
```

---

# 36. Common Scheduling Mistakes

## Mistake 1 - Using the Wrong Time Concept

Requirement:

```text
Every day at 8 AM
```

Implementation:

```text
Every 24 hours
```

These are not necessarily equivalent.

---

## Mistake 2 - Scheduling a Past Date

Validate that a one-time reminder is in the future:

```js
date > now
```

---

## Mistake 3 - Ignoring Time Zones

A date/time selected by the user belongs to a local context.

Do not blindly assume:

```text
Date string = universal time
```

Date/time handling should be designed carefully.

---

## Mistake 4 - Creating Too Many Repeating Notifications

For example:

```text
Every 1 minute
```

can create a terrible user experience.

Ask:

```text
Does the user actually need this frequency?
```

---

## Mistake 5 - Not Saving Notification IDs

If the application needs to later cancel or update reminders, keep track of the notification IDs.

---

# 37. Notification Frequency and UX

Technical possibility does not automatically mean good UX.

Bad:

```text
Every 30 seconds
```

for a normal reminder application.

Better:

```text
Every morning
```

or:

```text
At the time selected by the user
```

Notifications should provide value rather than becoming noise.

---

# 38. Practical Project - Reminder App

Build:

```text
--------------------------------
       Reminder App
--------------------------------

Title
[ Complete Assignment ]

Description
[ Finish Expo notifications docs ]

Date
[ 25 Aug 2026 ]

Time
[ 10:00 AM ]

[ Schedule Reminder ]
```

After scheduling:

```text
Reminder scheduled successfully.

Notification ID:
ABC123
```

Store:

```text
Reminder ID
Title
Description
Date
Time
Notification ID
```

This project prepares you for notification cancellation and management.

---

# 39. Practical Project - Study Planner

Build:

```text
--------------------------------
       Study Planner
--------------------------------

Subject
[ React Native ]

Topic
[ Notifications ]

Reminder
[ 8:00 PM ]

Repeat
[ Daily ]

[ Schedule Study Reminder ]
```

When scheduled:

```text
Every day at 8 PM
        ↓
Study notification
```

Notification:

```text
React Native Study

Continue learning Notifications.
```

This combines:

```text
User input
+
Calendar timing
+
Recurring schedule
+
Notification content
```

---

# 40. Testing Strategy

When learning notification scheduling, start with short delays:

```text
10 seconds
20 seconds
30 seconds
```

Why?

Because you can quickly verify:

```text
Permission
 ↓
Schedule
 ↓
Trigger
 ↓
Notification
```

Once the basic behavior works, move to:

```text
Minutes
 ↓
Hours
 ↓
Specific date/time
 ↓
Recurring reminders
```

---

# 41. Debugging Trigger Problems

If a notification does not appear, check:

```text
1. Permission
      ↓
2. Trigger configuration
      ↓
3. Future date/time
      ↓
4. Repetition settings
      ↓
5. Platform behavior
      ↓
6. Device notification settings
```

Log the values you are using:

```js
console.log(
  'Reminder date:',
  reminderDate
);

console.log(
  'Current date:',
  new Date()
);
```

For interval-based scheduling:

```js
console.log(
  'Delay:',
  seconds
);
```

---

# 42. Date Debugging

When dealing with dates, log:

```js
console.log(
  reminderDate.toString()
);

console.log(
  reminderDate.toISOString()
);
```

This helps identify confusing date/time values.

Remember:

```text
toString()
```

and:

```text
toISOString()
```

can display the same underlying date in different representations.

---

# 43. Time Zone Mental Model

Suppose a user chooses:

```text
8:00 AM
```

The application must understand:

```text
8:00 AM
in which local time context?
```

For a local reminder application, the user's device local time is generally important.

This becomes especially important for:

```text
Travel
International users
Remote meetings
Global calendars
Server-generated dates
```

---

# 44. Calendar Reminder vs Timer

Compare:

### Timer

```text
Start
 ↓
Wait 10 minutes
 ↓
Notification
```

### Calendar Reminder

```text
Choose:
Tomorrow
8:00 AM
 ↓
Wait
 ↓
Tomorrow 8:00 AM
 ↓
Notification
```

These represent different user requirements.

---

# 45. Real-World Examples

## Water Reminder

```text
Every morning
8 AM
```

## Study Reminder

```text
Every day
7 PM
```

## Weekly Meeting

```text
Every Monday
9 AM
```

## Assignment

```text
August 25
10 AM
```

## Cooking Timer

```text
20 minutes from now
```

## Workout Interval

```text
60 seconds from now
```

The trigger should be selected based on the actual requirement.

---

# 46. Trigger Selection Guide

| Requirement | Trigger Concept |
|---|---|
| 10 seconds later | Time interval |
| 5 minutes later | Time interval |
| 30 minutes later | Time interval |
| Tomorrow at 8 AM | Specific date/time |
| Every day at 8 AM | Recurring calendar schedule |
| Every Monday at 9 AM | Recurring calendar schedule |
| One-time appointment | Specific date/time |
| Cooking timer | Time interval |
| Workout rest timer | Time interval |

The exact API object should follow the Expo SDK version used by the project.

---

# 47. Most Important Mental Model

Remember:

```text
                  TRIGGER
                     │
       ┌─────────────┼─────────────┐
       │             │             │
       ↓             ↓             ↓
   Interval       Date/Time     Repeating
       │             │             │
       ↓             ↓             ↓
   "After N"      "At X"       "Again"
```

Examples:

```text
After 10 minutes
```

```text
Tomorrow at 10 AM
```

```text
Every day at 8 AM
```

---

# Part 5 - Managing Scheduled Notifications with `expo-notifications`

In Part 4, we learned how to decide **when** a notification should fire.

Now we move to an equally important question:

> **What do we do after a notification has already been scheduled?**

A real application should not only create notifications. It should also be able to:

```text
View scheduled notifications
        ↓
Identify notifications
        ↓
Cancel one notification
        ↓
Cancel all notifications
        ↓
Update a reminder
        ↓
Replace an old notification
        ↓
Keep application data synchronized
```

This is what notification management means.

---

# 1. Why Do We Need Notification Management?

Imagine a reminder application.

The user creates:

```text
Study React Native
Tomorrow at 8 AM
```

The application schedules a notification.

Later, the user changes it to:

```text
Study React Native
Tomorrow at 10 AM
```

Now the application has a problem.

If it simply creates another notification:

```text
Old notification
8 AM

New notification
10 AM
```

the user may receive **both**.

That is not what the user asked for.

A better flow is:

```text
Old reminder
     ↓
Find old notification ID
     ↓
Cancel old notification
     ↓
Create new notification
     ↓
Save new notification ID
```

This is why notification management is necessary.

---

# 2. Notification ID

When a notification is scheduled:

```js
const notificationId =
  await Notifications
    .scheduleNotificationAsync({
      ...
    });
```

the API returns an identifier.

Conceptually:

```text
Schedule notification
        ↓
Notification ID
```

For example:

```text
notification-id-123
```

The actual identifier is generated by the notification system.

---

# 3. Why Store the Notification ID?

Suppose:

```text
Reminder:
Submit Assignment
```

is connected to:

```text
Notification ID:
ABC123
```

Your application can maintain:

```js
{
  id: 'reminder-101',

  title:
    'Submit Assignment',

  notificationId:
    'ABC123',
}
```

Now the application knows:

```text
Reminder
   ↕
Notification
```

This relationship becomes extremely useful when:

- Editing reminders
- Deleting reminders
- Cancelling notifications
- Rescheduling notifications
- Cleaning up old notifications

---

# 4. Application ID vs Notification ID

These are different things.

Your application might have:

```js
reminder.id
```

and:

```js
reminder.notificationId
```

For example:

```js
{
  id:
    'reminder-101',

  title:
    'Study React Native',

  notificationId:
    'notification-abc123',
}
```

Think:

```text
reminder.id
     ↓
Identifies YOUR reminder

notificationId
     ↓
Identifies the scheduled notification
```

Do not assume they are the same identifier.

---

# 5. Getting Scheduled Notifications

Sometimes you need to inspect notifications that are currently scheduled.

The relevant API is:

```js
Notifications.getAllScheduledNotificationsAsync()
```

Example:

```js
const notifications =
  await Notifications
    .getAllScheduledNotificationsAsync();

console.log(
  notifications
);
```

This allows your application to inspect scheduled notification requests.

---

# 6. Why Get Scheduled Notifications?

Imagine the application contains:

```text
5 reminders
```

You may want to display:

```text
Scheduled Notifications

1. Study - 8 PM
2. Workout - 6 AM
3. Read Book - 10 PM
4. Meeting - 9 AM
5. Water Reminder
```

The application can inspect the currently scheduled notification requests.

This is useful for:

- Debugging
- Reminder lists
- Notification management screens
- Synchronization
- Troubleshooting
- Administrative logic

---

# 7. Basic Example

```js
const getScheduledNotifications =
  async () => {

    try {

      const notifications =
        await Notifications
          .getAllScheduledNotificationsAsync();

      console.log(
        'Scheduled notifications:',
        notifications
      );

    } catch (error) {

      console.error(
        'Failed to get scheduled notifications:',
        error
      );
    }
  };
```

---

# 8. Understanding the Returned Data

The returned array contains scheduled notification request information.

Conceptually:

```text
[
  Notification Request,
  Notification Request,
  Notification Request
]
```

Each request can contain information about:

```text
Identifier
Content
Trigger
```

The exact structure should be inspected in the SDK version being used.

A useful debugging pattern is:

```js
notifications.forEach(
  (notification) => {

    console.log(
      notification
    );

  }
);
```

---

# 9. Displaying Scheduled Notifications

You could build a screen:

```text
--------------------------------
     Scheduled Reminders
--------------------------------

Study React Native
8:00 PM

Workout
6:00 AM

Read Book
10:00 PM
```

The application can retrieve scheduled notification requests and display appropriate information.

However, remember:

> The notification scheduler is not automatically your application's reminder database.

This distinction is important.

---

# 10. Notification Scheduler vs Application Data

Suppose your application stores:

```js
{
  id: 'reminder-101',
  title: 'Study React Native',
  description: 'Complete notifications chapter',
  date: '...',
  notificationId: 'ABC123',
}
```

This is **application data**.

The notification system stores its own scheduled request.

Therefore:

```text
Your Application
       ↓
Reminder Model

Notification System
       ↓
Scheduled Notification Request
```

These systems should be connected using the notification ID.

---

# 11. Cancelling One Notification

If you know the notification identifier, you can cancel that scheduled notification.

The API is:

```js
Notifications.cancelScheduledNotificationAsync()
```

Example:

```js
await Notifications
  .cancelScheduledNotificationAsync(
    notificationId
  );
```

For example:

```js
const notificationId =
  'ABC123';

await Notifications
  .cancelScheduledNotificationAsync(
    notificationId
  );
```

The result is that the scheduled notification request is cancelled.

---

# 12. Why Cancel One Notification?

Imagine:

```text
Study Reminder
Water Reminder
Workout Reminder
```

The user deletes only:

```text
Workout Reminder
```

You should not cancel everything.

Instead:

```text
Workout notification ID
       ↓
cancelScheduledNotificationAsync()
       ↓
Only Workout reminder cancelled
```

The other reminders remain scheduled.

---

# 13. Complete Cancel Function

```js
const cancelNotification =
  async (notificationId) => {

    try {

      await Notifications
        .cancelScheduledNotificationAsync(
          notificationId
        );

      console.log(
        'Notification cancelled:',
        notificationId
      );

    } catch (error) {

      console.error(
        'Failed to cancel notification:',
        error
      );
    }
  };
```

Usage:

```js
await cancelNotification(
  reminder.notificationId
);
```

---

# 14. Cancelling All Scheduled Notifications

Sometimes the application needs to remove all scheduled notifications.

The API is:

```js
Notifications.cancelAllScheduledNotificationsAsync()
```

Example:

```js
await Notifications
  .cancelAllScheduledNotificationsAsync();
```

Conceptually:

```text
Scheduled Notifications
│
├── Study
├── Workout
├── Meeting
└── Water
       ↓
Cancel All
       ↓
No scheduled notifications
```

---

# 15. When Would Cancel All Be Useful?

Potential use cases:

- User logs out
- User disables all reminders
- Reset application
- Clear all test notifications
- Remove all reminders
- Notification cleanup

However, this method should be used carefully.

If the user has:

```text
20 reminders
```

then:

```js
cancelAllScheduledNotificationsAsync()
```

removes all of them.

It should not be used when you only want to remove one reminder.

---

# 16. One vs All

Remember:

### One notification

```js
cancelScheduledNotificationAsync(
  notificationId
)
```

means:

```text
Cancel this specific notification.
```

### All notifications

```js
cancelAllScheduledNotificationsAsync()
```

means:

```text
Cancel every scheduled notification.
```

Mental model:

```text
ONE
 ↓
notificationId
 ↓
Cancel one

ALL
 ↓
No ID required
 ↓
Cancel everything
```

---

# 17. Cancel Button Example

Suppose your reminder list contains:

```text
Study React Native
[Cancel]

Workout
[Cancel]

Read Book
[Cancel]
```

Each item could contain:

```js
{
  title:
    'Study React Native',

  notificationId:
    'ABC123',
}
```

When the user presses:

```text
Cancel
```

call:

```js
await Notifications
  .cancelScheduledNotificationAsync(
    item.notificationId
  );
```

---

# 18. Complete Reminder Item Example

```jsx
<Pressable
  onPress={() =>
    cancelNotification(
      item.notificationId
    )
  }
>
  <Text>
    Cancel
  </Text>
</Pressable>
```

The important relationship is:

```text
Reminder item
      ↓
notificationId
      ↓
cancelScheduledNotificationAsync()
```

---

# 19. Updating a Notification

One important concept:

> A scheduled notification is generally easier to manage by cancelling the old request and scheduling a new request than by trying to mutate the already-scheduled request in place.

Suppose:

```text
Old:
Study at 8 AM
```

User changes it to:

```text
Study at 10 AM
```

Recommended conceptual flow:

```text
Old notification ID
        ↓
Cancel old notification
        ↓
Create new trigger
        ↓
Schedule new notification
        ↓
Receive new notification ID
        ↓
Save new ID
```

---

# 20. Complete Update Flow

Suppose:

```js
const reminder = {

  id:
    'reminder-101',

  title:
    'Study React Native',

  notificationId:
    'OLD-ID',

};
```

First:

```js
await Notifications
  .cancelScheduledNotificationAsync(
    reminder.notificationId
  );
```

Then schedule the replacement:

```js
const newNotificationId =
  await Notifications
    .scheduleNotificationAsync({

      content: {

        title:
          'Study React Native',

        body:
          'Time to study.',

      },

      trigger: {

        // new trigger

      },

    });
```

Then update:

```js
reminder.notificationId =
  newNotificationId;
```

---

# 21. Why Is the Notification ID Replaced?

Because:

```text
OLD notification
      ↓
OLD ID
```

is cancelled.

Then:

```text
NEW notification
      ↓
NEW ID
```

is created.

Therefore:

```text
Old ID → no longer represents the active scheduled reminder
New ID → represents the new scheduled reminder
```

This is why the application should save the new identifier.

---

# 22. Reminder Edit Example

Imagine:

```text
--------------------------------
        Edit Reminder
--------------------------------

Title
[ Study React Native ]

Date
[ 25 Aug ]

Time
[ 8:00 PM ]

[ Save Changes ]
```

User changes:

```text
8:00 PM
```

to:

```text
10:00 PM
```

Flow:

```text
Save Changes
      ↓
Find reminder
      ↓
Get old notificationId
      ↓
Cancel old notification
      ↓
Create new trigger
      ↓
Schedule new notification
      ↓
Save new notificationId
      ↓
Update reminder
```

This is a realistic notification-management workflow.

---

# 23. Delete Reminder Flow

Suppose the user deletes:

```text
Study React Native
```

Do not only delete the application record.

You should also cancel the scheduled notification.

Flow:

```text
Delete Reminder
      ↓
Find notificationId
      ↓
Cancel scheduled notification
      ↓
Delete reminder from storage
      ↓
Update UI
```

Otherwise:

```text
Reminder deleted from app
        ↓
Notification still scheduled
        ↓
User receives unexpected notification
```

This is a common bug.

---

# 24. Correct Delete Implementation

Conceptually:

```js
const deleteReminder =
  async (reminder) => {

    try {

      if (
        reminder.notificationId
      ) {

        await Notifications
          .cancelScheduledNotificationAsync(
            reminder.notificationId
          );
      }

      // Then remove reminder
      // from your application storage.

    } catch (error) {

      console.error(
        'Delete reminder failed:',
        error
      );
    }
  };
```

---

# 25. Why Check `notificationId`?

Not every reminder necessarily has a notification.

For example:

```js
{
  id:
    'reminder-101',

  title:
    'Read Book',

  notificationId:
    null,
}
```

There is no scheduled notification to cancel.

Therefore:

```js
if (reminder.notificationId) {
  ...
}
```

prevents unnecessary cancellation calls.

---

# 26. Notification Cleanup

Imagine your application creates temporary test notifications:

```text
Test 1
Test 2
Test 3
Test 4
```

After testing, you may want:

```js
await Notifications
  .cancelAllScheduledNotificationsAsync();
```

This can clean the scheduled notification queue.

During development, this is particularly useful when you accidentally create many test notifications.

---

# 27. Getting Scheduled Notifications Before Cleanup

You can inspect the current queue first:

```js
const scheduled =
  await Notifications
    .getAllScheduledNotificationsAsync();

console.log(
  'Scheduled:',
  scheduled
);
```

Then:

```text
Inspect
 ↓
Decide
 ↓
Cancel
```

This is better for debugging than blindly cancelling everything.

---

# 28. Notification Management Screen

A useful learning project can have:

```text
--------------------------------
     Scheduled Reminders
--------------------------------

Study React Native
Tomorrow, 8 PM
[Cancel] [Edit]

Workout
Tomorrow, 6 AM
[Cancel] [Edit]

Read Book
Tomorrow, 10 PM
[Cancel] [Edit]

--------------------------------
[ Cancel All ]
--------------------------------
```

This screen demonstrates:

```text
Read
Cancel One
Edit
Cancel All
```

---

# 29. Complete Management Architecture

A reminder application can follow:

```text
                 Reminder App
                      │
             ┌────────┴────────┐
             ↓                 ↓
        Reminder Data     Notification ID
             │                 │
             └────────┬────────┘
                      ↓
              Notification APIs
                      │
       ┌──────────────┼──────────────┐
       ↓              ↓              ↓
      Get           Cancel          Schedule
   Scheduled         One             New
       │              │              │
       └──────────────┼──────────────┘
                      ↓
               Operating System
```

---

# 30. Reminder CRUD + Notification CRUD

This is an important application architecture concept.

Your reminder data has CRUD:

```text
Create
Read
Update
Delete
```

Notification scheduling also needs lifecycle management:

```text
Schedule
Read Scheduled
Cancel
Reschedule
```

Connect them:

```text
CREATE reminder
    ↓
SCHEDULE notification

READ reminder
    ↓
READ/verify notification

UPDATE reminder
    ↓
CANCEL old
    ↓
SCHEDULE new

DELETE reminder
    ↓
CANCEL notification
    ↓
DELETE reminder
```

This is a very useful pattern for real applications.

---

# 31. Create Reminder Flow

```text
User enters reminder
        ↓
Validate data
        ↓
Schedule notification
        ↓
Receive notificationId
        ↓
Save reminder + notificationId
```

Example:

```js
const notificationId =
  await scheduleNotification(...);

const reminder = {

  id:
    generateReminderId(),

  title:
    title,

  notificationId:
    notificationId,

};
```

---

# 32. Read Reminder Flow

When loading the application:

```text
Load reminders
        ↓
Read application data
        ↓
Display reminders
```

You can separately inspect:

```js
await Notifications
  .getAllScheduledNotificationsAsync();
```

This can help verify whether notification requests are still scheduled.

---

# 33. Update Reminder Flow

```text
User edits reminder
        ↓
Find old notificationId
        ↓
Cancel old notification
        ↓
Schedule new notification
        ↓
Receive new ID
        ↓
Save new ID
        ↓
Update reminder data
```

---

# 34. Delete Reminder Flow

```text
User deletes reminder
        ↓
Find notificationId
        ↓
Cancel notification
        ↓
Delete reminder data
        ↓
Refresh UI
```

This keeps the two systems synchronized.

---

# 35. Handling Missing Notifications

Suppose your application says:

```text
Reminder:
Study React Native

notificationId:
ABC123
```

but the operating system no longer has that scheduled notification.

Your application should not blindly assume:

```text
notificationId exists
=
notification is definitely scheduled
```

The scheduled notification list can be inspected:

```js
const scheduled =
  await Notifications
    .getAllScheduledNotificationsAsync();
```

This is useful when diagnosing synchronization problems.

---

# 36. Application Data and OS Data Can Become Unsynchronized

Consider:

```text
Application storage
       ↓
Reminder exists
```

but:

```text
OS scheduler
       ↓
Notification no longer exists
```

This can happen because application data and OS-managed notification requests are separate systems.

Therefore, production applications should think about synchronization and recovery.

For a classroom project, simply understanding the relationship is the important first step.

---

# 37. Cancel All - Use Carefully

This:

```js
await Notifications
  .cancelAllScheduledNotificationsAsync();
```

is powerful.

Do not accidentally call it when the user wants:

```text
Delete one reminder
```

because it means:

```text
Delete all scheduled notification requests
```

A safer pattern for normal reminder deletion is:

```js
cancelScheduledNotificationAsync(
  reminder.notificationId
);
```

---

# 38. Error Handling

Always handle notification-management operations safely.

Example:

```js
try {

  await Notifications
    .cancelScheduledNotificationAsync(
      notificationId
    );

} catch (error) {

  console.error(
    'Cancellation failed:',
    error
  );

}
```

Likewise:

```js
try {

  const notifications =
    await Notifications
      .getAllScheduledNotificationsAsync();

} catch (error) {

  console.error(
    'Failed to load notifications:',
    error
  );

}
```

---

# 39. Building a Notification Service

Instead of scattering notification operations across screens, create a service.

For example:

```text
notificationService.js
```

Conceptually:

```js
export const scheduleNotification =
  async (...) => {
    ...
  };

export const cancelNotification =
  async (...) => {
    ...
  };

export const getScheduledNotifications =
  async () => {
    ...
  };

export const cancelAllNotifications =
  async () => {
    ...
  };
```

Then screens call:

```text
Notification Screen
       ↓
Notification Service
       ↓
expo-notifications
```

---

# 40. Example Notification Service

```js
import * as Notifications
  from 'expo-notifications';

export const getScheduledNotifications =
  async () => {

    return await Notifications
      .getAllScheduledNotificationsAsync();

  };

export const cancelNotification =
  async (notificationId) => {

    return await Notifications
      .cancelScheduledNotificationAsync(
        notificationId
      );

  };

export const cancelAllNotifications =
  async () => {

    return await Notifications
      .cancelAllScheduledNotificationsAsync();

  };
```

This creates a clean boundary around notification-management APIs.

---

# 41. Why Services Are Useful

Without a service:

```text
Screen A
 ↓
Notifications API

Screen B
 ↓
Notifications API

Screen C
 ↓
Notifications API
```

With a service:

```text
Screen A ─┐
Screen B ─┼→ Notification Service
Screen C ─┘
                 ↓
        expo-notifications
```

Advantages:

- Less duplicated code
- Easier testing
- Easier maintenance
- Centralized error handling
- Cleaner screens

---

# 42. Expected Architecture

```text
User
 ↓
Create Reminder
 ↓
Validate
 ↓
Schedule notification
 ↓
notificationId
 ↓
Save reminder
 ↓
Display list
```

Edit:

```text
Edit
 ↓
Cancel old notification
 ↓
Schedule new notification
 ↓
Save new ID
```

Delete:

```text
Delete
 ↓
Cancel notification
 ↓
Delete reminder
```

---

# 46. Important Mental Model

Remember:

```text
             YOUR APP
                │
        Reminder Database
                │
        notificationId
                │
                ↓
       NOTIFICATION SYSTEM
                │
      ┌─────────┼─────────┐
      ↓         ↓         ↓
     Get      Cancel    Schedule
   Scheduled   One       New
      │         │         │
      └─────────┼─────────┘
                ↓
             OS
```

The application owns the reminder.

The notification system manages the scheduled notification.

The notification ID connects the two.

---

# Part 6 - Notification Listeners with `expo-notifications`

Until now, we learned how to:

```text
Request permission
      ↓
Create notification
      ↓
Schedule notification
      ↓
Get notification ID
      ↓
Cancel / manage notification
```

Now we need to understand another important part:

> **How does our React Native application know that a notification was received?**

This is where **notification listeners** become important.

---

# 1. What Is a Notification Listener?

A notification listener allows your application to react when a notification-related event occurs.

Think of it like:

```text
Notification event
       ↓
Listener is waiting
       ↓
Listener callback runs
       ↓
Application receives notification information
       ↓
Your code reacts
```

For example:

```text
Notification arrives
       ↓
Listener receives event
       ↓
Read title
       ↓
Read body
       ↓
Read data
       ↓
Update application state
```

---

# 2. Why Do We Need Listeners?

Suppose a notification is received while your application is running.

Your application might want to:

```text
Update UI
Show an in-app message
Refresh data
Log the notification
Read notification data
Update a badge
Trigger some application logic
```

A listener provides a way for JavaScript code to respond to the notification event.

---

# 3. The Main Listener API

For notifications received while the application is running, the important API is:

```js
Notifications.addNotificationReceivedListener()
```

Basic shape:

```js
const subscription =
  Notifications
    .addNotificationReceivedListener(
      notification => {

        console.log(
          'Notification received:',
          notification
        );

      }
    );
```

The callback runs when the listener receives a notification event.

---

# 4. Understanding the Callback

The callback:

```js
notification => {

  console.log(
    notification
  );

}
```

receives notification information.

You can inspect it:

```js
console.log(
  'Notification:',
  notification
);
```

During development, logging the complete object is useful because it lets you understand the structure of the notification.

---

# 5. Basic Listener Example

```jsx
import React, {
  useEffect,
} from 'react';

import {
  View,
  Text,
} from 'react-native';

import * as Notifications
  from 'expo-notifications';

export default function NotificationScreen() {

  useEffect(() => {

    const subscription =
      Notifications
        .addNotificationReceivedListener(
          notification => {

            console.log(
              ' Notification received'
            );

            console.log(
              notification
            );

          }
        );

    return () => {

      subscription.remove();

    };

  }, []);

  return (

    <View>

      <Text>
        Notification Listener Screen
      </Text>

    </View>
  );
}
```

This is the basic React pattern:

```text
Mount component
      ↓
Create listener
      ↓
Notification arrives
      ↓
Callback executes
      ↓
Unmount component
      ↓
Remove listener
```

---

# 6. Why Use `useEffect()`?

Listeners are subscriptions.

A subscription should generally be:

```text
Created
when needed
```

and:

```text
Removed
when no longer needed
```

React's:

```js
useEffect()
```

is a natural place to manage this lifecycle.

Example:

```js
useEffect(() => {

  const subscription =
    Notifications
      .addNotificationReceivedListener(
        ...
      );

  return () => {

    subscription.remove();

  };

}, []);
```

The empty dependency array:

```js
[]
```

means the effect is set up when the component mounts and cleaned up when it unmounts.

---

# 7. Why Is Cleanup Important?

Suppose you do this:

```js
useEffect(() => {

  Notifications
    .addNotificationReceivedListener(
      notification => {
        console.log(notification);
      }
    );

}, []);
```

but never remove the listener.

Depending on how the component is mounted and unmounted, you can end up with unwanted subscriptions.

The safer pattern is:

```js
const subscription =
  Notifications
    .addNotificationReceivedListener(
      callback
    );

return () => {

  subscription.remove();

};
```

---

# 8. Duplicate Listener Problem

Imagine the screen mounts multiple times and creates:

```text
Listener 1
Listener 2
Listener 3
```

Then one notification arrives.

Potentially:

```text
Notification
    ↓
Listener 1 → callback
Listener 2 → callback
Listener 3 → callback
```

Your code may appear to process the same event multiple times.

This is why listener cleanup matters.

---

# 9. Listener Lifecycle

The complete lifecycle should be:

```text
Component Mount
      ↓
Create Listener
      ↓
Wait
      ↓
Notification Event
      ↓
Callback
      ↓
Component Unmount
      ↓
Remove Listener
```

Mental model:

```text
subscribe
   ↓
listen
   ↓
receive
   ↓
unsubscribe
```

---

# 10. Reading Notification Content

Once the callback receives:

```js
notification
```

you can inspect its content.

Conceptually:

```js
notification.request.content
```

The notification content can contain information such as:

```text
title
body
data
```

Example:

```js
const content =
  notification.request.content;

console.log(
  'Title:',
  content.title
);

console.log(
  'Body:',
  content.body
);

console.log(
  'Data:',
  content.data
);
```

---

# 11. Complete Content Reading Example

```js
const subscription =
  Notifications
    .addNotificationReceivedListener(
      notification => {

        const content =
          notification.request.content;

        console.log(
          'Title:',
          content.title
        );

        console.log(
          'Body:',
          content.body
        );

        console.log(
          'Data:',
          content.data
        );

      }
    );
```

This is useful when your application needs to understand what notification was received.

---

# 12. Example Notification

Suppose you scheduled:

```js
await Notifications
  .scheduleNotificationAsync({

    content: {

      title:
        'Study Reminder',

      body:
        'Time to study React Native.',

      data: {

        screen:
          'Study',

        lessonId:
          'lesson-101',

      },

    },

    trigger: {
      // trigger configuration
    },

  });
```

The listener can read:

```js
notification.request.content
```

and access:

```text
title
 ↓
Study Reminder

body
 ↓
Time to study React Native.

data
 ↓
screen: Study
lessonId: lesson-101
```

---

# 13. Why Is `data` Important?

The visible notification tells the user:

```text
Study Reminder
Time to study React Native.
```

But the application may need additional information:

```js
data: {

  screen:
    'Study',

  lessonId:
    'lesson-101',

}
```

This allows the application to understand the context of the notification.

Think:

```text
title/body
    ↓
User-facing information

data
    ↓
Application-facing information
```

---

# 14. Example - Order Notification

Suppose:

```js
content: {

  title:
    'Order Update',

  body:
    'Your order has been shipped.',

  data: {

    screen:
      'OrderDetails',

    orderId:
      'ORD-1001',

  },

}
```

The listener can read:

```js
const {
  title,
  body,
  data,
} =
  notification.request.content;
```

Then:

```js
console.log(
  title
);

console.log(
  body
);

console.log(
  data.orderId
);
```

Result conceptually:

```text
Order Update
Your order has been shipped.
ORD-1001
```

---

# 15. Updating React State

Listeners can also update React state.

Example:

```jsx
const [
  latestNotification,
  setLatestNotification,
] = useState(null);
```

Then:

```js
useEffect(() => {

  const subscription =
    Notifications
      .addNotificationReceivedListener(
        notification => {

          setLatestNotification(
            notification
          );

        }
      );

  return () => {

    subscription.remove();

  };

}, []);
```

Now the application can render the latest received notification.

---

# 16. Complete State Example

```jsx
import React, {
  useEffect,
  useState,
} from 'react';

import {
  View,
  Text,
} from 'react-native';

import * as Notifications
  from 'expo-notifications';

export default function NotificationScreen() {

  const [
    latestNotification,
    setLatestNotification,
  ] =
    useState(null);

  useEffect(() => {

    const subscription =
      Notifications
        .addNotificationReceivedListener(
          notification => {

            setLatestNotification(
              notification
            );

          }
        );

    return () => {

      subscription.remove();

    };

  }, []);

  return (

    <View>

      <Text>
        Latest Notification
      </Text>

      {latestNotification && (

        <View>

          <Text>
            {
              latestNotification
                .request
                .content
                .title
            }
          </Text>

          <Text>
            {
              latestNotification
                .request
                .content
                .body
            }
          </Text>

        </View>

      )}

    </View>
  );
}
```

---

# 17. Listener + Local Notification

Now combine what we learned in earlier parts.

First schedule:

```js
await Notifications
  .scheduleNotificationAsync({

    content: {

      title:
        'Hello',

      body:
        'This is a test notification.',

    },

    trigger: {

      // trigger configuration

    },

  });
```

Then the listener:

```js
Notifications
  .addNotificationReceivedListener(
    notification => {

      console.log(
        'Received:',
        notification
      );

    }
  );
```

The complete flow becomes:

```text
Schedule
   ↓
Wait for trigger
   ↓
Notification event
   ↓
Listener
   ↓
Callback
   ↓
Your application code
```

---

# 18. Listener Does Not Schedule the Notification

This distinction is important.

This:

```js
Notifications
  .addNotificationReceivedListener(...)
```

does **not** create a notification.

It listens for notification events.

Scheduling is:

```js
Notifications
  .scheduleNotificationAsync(...)
```

Listening is:

```js
Notifications
  .addNotificationReceivedListener(...)
```

Think:

```text
schedule
    ↓
CREATE / PLAN

listener
    ↓
OBSERVE / RESPOND
```

---

# 19. Schedule vs Listen

| API | Purpose |
|---|---|
| `scheduleNotificationAsync()` | Schedule a notification |
| `addNotificationReceivedListener()` | Listen for a received notification |
| `getAllScheduledNotificationsAsync()` | Read scheduled notification requests |
| `cancelScheduledNotificationAsync()` | Cancel one scheduled notification |
| `cancelAllScheduledNotificationsAsync()` | Cancel all scheduled notifications |

This distinction is extremely important.

---

# 20. Listener and Foreground Notifications

When your application is running in the foreground, notification behavior is influenced by the notification handler configured earlier.

For example:

```js
Notifications.setNotificationHandler({

  handleNotification:
    async () => ({

      shouldShowBanner:
        true,

      shouldShowList:
        true,

      shouldPlaySound:
        true,

      shouldSetBadge:
        true,

    }),

});
```

The handler controls presentation behavior.

The listener allows your application code to react to the received notification event.

These are related but different responsibilities.

---

# 21. Handler vs Listener

This is one of the most important distinctions in this module.

### Notification Handler

```js
Notifications.setNotificationHandler(...)
```

answers:

> **How should the notification be presented while the app is in the foreground?**

### Notification Listener

```js
Notifications.addNotificationReceivedListener(...)
```

answers:

> **What should my JavaScript application do when the notification is received?**

Mental model:

```text
Notification received
        │
        ├──────────────→ Handler
        │                   ↓
        │             Presentation behavior
        │
        └──────────────→ Listener
                            ↓
                      Application logic
```

---

# 22. Example - Handler + Listener Together

```js
Notifications.setNotificationHandler({

  handleNotification:
    async () => ({

      shouldShowBanner:
        true,

      shouldShowList:
        true,

      shouldPlaySound:
        true,

      shouldSetBadge:
        true,

    }),

});
```

Then:

```js
useEffect(() => {

  const subscription =
    Notifications
      .addNotificationReceivedListener(
        notification => {

          console.log(
            'Notification received:',
            notification
          );

        }
      );

  return () => {

    subscription.remove();

  };

}, []);
```

They perform different jobs.

---

# 23. Practical Example - Notification Inbox

You can build an in-app notification list.

State:

```js
const [
  notifications,
  setNotifications,
] =
  useState([]);
```

Listener:

```js
useEffect(() => {

  const subscription =
    Notifications
      .addNotificationReceivedListener(
        notification => {

          setNotifications(
            previous => [
              notification,
              ...previous,
            ]
          );

        }
      );

  return () => {

    subscription.remove();

  };

}, []);
```

Now every received notification can be added to an in-memory list.

---

# 24. Rendering Notification History

Conceptually:

```jsx
{notifications.map(
  (notification, index) => {

    const content =
      notification
        .request
        .content;

    return (

      <View key={index}>

        <Text>
          {content.title}
        </Text>

        <Text>
          {content.body}
        </Text>

      </View>

    );

  }
)}
```

This can create a simple notification inbox.

---

# 25. Important Limitation of In-Memory State

If you use:

```js
useState([])
```

the notification list exists only while that React component/application state remains available.

It is not permanent storage.

If you need persistent notification history, you would need another storage solution.

For example:

```text
Listener
   ↓
Notification received
   ↓
Save notification
   ↓
Local storage / database
```

This is a separate concern from the notification listener itself.

---

# 26. Listener + Storage Architecture

Conceptually:

```text
Notification received
        ↓
Listener callback
        ↓
Extract content/data
        ↓
Create notification history object
        ↓
Save to storage
        ↓
Update UI
```

For example:

```js
const notificationRecord = {

  title:
    notification
      .request
      .content
      .title,

  body:
    notification
      .request
      .content
      .body,

  data:
    notification
      .request
      .content
      .data,

};
```

---

# 27. Listener + Navigation

Notification data can also be used to decide where the application should navigate.

Suppose the notification contains:

```js
data: {

  screen:
    'OrderDetails',

  orderId:
    'ORD-1001',

}
```

Your application can read:

```js
const data =
  notification
    .request
    .content
    .data;
```

and then use the data with your navigation system.

Conceptually:

```text
Notification
      ↓
Listener
      ↓
Read data
      ↓
screen = OrderDetails
      ↓
Navigate
      ↓
Order Details
```

The actual navigation implementation depends on the navigation library and app architecture.

---

# 28. Example - Study Notification

Notification:

```js
data: {

  screen:
    'Study',

  lessonId:
    'lesson-101',

}
```

Listener:

```js
const subscription =
  Notifications
    .addNotificationReceivedListener(
      notification => {

        const data =
          notification
            .request
            .content
            .data;

        console.log(
          'Screen:',
          data.screen
        );

        console.log(
          'Lesson:',
          data.lessonId
        );

      }
    );
```

The listener can extract the information needed by the application.

---

# 29. Listener Cleanup

Always remember:

```js
return () => {

  subscription.remove();

};
```

This is not optional boilerplate.

It is part of the subscription lifecycle.

Think:

```text
add listener
      ↓
subscription created
      ↓
component no longer needs it
      ↓
remove listener
```

---

# 30. Why `subscription.remove()`?

The call:

```js
Notifications
  .addNotificationReceivedListener(...)
```

returns a subscription object.

That object represents the active listener.

Calling:

```js
subscription.remove();
```

unsubscribes from the event.

Mental model:

```text
addNotificationReceivedListener()
          ↓
Subscription
          ↓
subscription.remove()
```

---

# 31. Multiple Listeners

You can technically create multiple listeners.

For example:

```js
const listener1 =
  Notifications
    .addNotificationReceivedListener(...);

const listener2 =
  Notifications
    .addNotificationReceivedListener(...);
```

But you should create listeners intentionally.

If the same component repeatedly creates listeners without cleanup:

```text
Listener 1
Listener 2
Listener 3
Listener 4
```

then one notification may cause multiple callbacks.

---

# 32. Better Listener Architecture

For many applications, one centralized listener is easier to reason about.

For example:

```text
App Root
   ↓
Notification Listener
   ↓
Notification Service
   ↓
Application State / Navigation / Storage
```

Instead of:

```text
Screen A → listener
Screen B → listener
Screen C → listener
```

unless there is a specific reason for screen-level listeners.

---

# 33. Central Listener Concept

Conceptually:

```jsx
function NotificationListener() {

  useEffect(() => {

    const subscription =
      Notifications
        .addNotificationReceivedListener(
          notification => {

            // central handling

          }
        );

    return () => {

      subscription.remove();

    };

  }, []);

  return null;
}
```

Then mount it at an appropriate high level in your application.

---

# 34. Notification Service Concept

A service can process:

```text
Notification
      ↓
Extract content
      ↓
Extract data
      ↓
Log
      ↓
Store
      ↓
Update state
      ↓
Navigation logic
```

This keeps screens cleaner.

---

# 35. Common Listener Mistakes

## Mistake 1 - Forgetting Cleanup

Bad:

```js
useEffect(() => {

  Notifications
    .addNotificationReceivedListener(
      callback
    );

}, []);
```

Better:

```js
useEffect(() => {

  const subscription =
    Notifications
      .addNotificationReceivedListener(
        callback
      );

  return () => {

    subscription.remove();

  };

}, []);
```

---

# 36. Common Mistake - Confusing Listener With Scheduling

Wrong mental model:

```text
Listener creates notification
```

Correct:

```text
scheduleNotificationAsync()
      ↓
creates/schedules notification
```

and:

```text
addNotificationReceivedListener()
      ↓
listens for received notification
```

---

# 37. Common Mistake - Assuming Listener Is Permanent Storage

This:

```js
setNotifications(...)
```

only updates React state.

It does not permanently save notification history.

For persistence:

```text
Listener
 ↓
Storage
```

is required.

---

# 38. Common Mistake - Assuming Listener Controls Presentation

The listener does not replace:

```js
Notifications.setNotificationHandler(...)
```

These have different responsibilities.

Remember:

```text
Handler
 ↓
Presentation

Listener
 ↓
Application reaction
```

---

# 39. Practical Project - Notification Inbox

Build:

```text
--------------------------------
       Notification Inbox
--------------------------------

Study Reminder
Time to study React Native.

Order Update
Your order has been shipped.

Task Reminder
Complete your assignment.
--------------------------------
```

Requirements:

```text
Listen for received notifications
       ↓
Extract title/body/data
       ↓
Add to list
       ↓
Display in UI
```

---

# 40. Practical Project - Notification Debugger

Build a screen that displays:

```text
Notification Debugger

Latest Notification
-------------------

Title:
Study Reminder

Body:
Time to study.

Data:
{
  "screen": "Study",
  "lessonId": "lesson-101"
}
```

Also add:

```text
Listener Status:
Active
```

This is an excellent learning project because it makes notification data visible.

---

# 41. Practical Example - Notification Counter

You can maintain:

```js
const [
  notificationCount,
  setNotificationCount,
] =
  useState(0);
```

Listener:

```js
useEffect(() => {

  const subscription =
    Notifications
      .addNotificationReceivedListener(
        () => {

          setNotificationCount(
            count => count + 1
          );

        }
      );

  return () => {

    subscription.remove();

  };

}, []);
```

Now every received notification increments the count.

---

# 42. Practical Example - Latest Notification

Instead of storing all notifications:

```js
const [
  latestNotification,
  setLatestNotification,
] =
  useState(null);
```

Listener:

```js
useEffect(() => {

  const subscription =
    Notifications
      .addNotificationReceivedListener(
        notification => {

          setLatestNotification(
            notification
          );

        }
      );

  return () => {

    subscription.remove();

  };

}, []);
```

This is useful for dashboards or notification previews.

---

# 43. Listener Flow With Local Notification

Complete mental model:

```text
User presses Schedule
        ↓
scheduleNotificationAsync()
        ↓
Notification request created
        ↓
Trigger condition
        ↓
Notification received
        ↓
addNotificationReceivedListener()
        ↓
Callback
        ↓
Read:
  title
  body
  data
        ↓
Application reacts
```

---

# 44. Listener Flow With Data

Example:

```js
content: {

  title:
    'Task Reminder',

  body:
    'Complete your assignment.',

  data: {

    screen:
      'TaskDetails',

    taskId:
      'task-101',

  },

}
```

Then:

```text
Notification
      ↓
Listener
      ↓
data
      ↓
screen
      ↓
TaskDetails
      ↓
taskId
      ↓
task-101
```

This pattern becomes very useful when notifications are connected to application features.

---

# 45. Listener vs Polling

Without a listener, someone might try:

```text
Every 1 second
   ↓
Check if notification arrived
```

This is polling.

A listener is event-driven:

```text
Wait
 ↓
Event occurs
 ↓
Callback
```

Event-driven architecture is generally a much cleaner way to respond to notification events.

---

# 46. Event-Driven Mental Model

Think about a listener like:

```js
button.addEventListener(
  'press',
  callback
);
```

Conceptually:

```text
Something happens
      ↓
Event emitted
      ↓
Listener receives it
      ↓
Callback executes
```

Notifications follow a similar event-driven idea.

---

# 47. Notification Listener Architecture

```text
              Notification System
                      │
                      ↓
              Notification Event
                      │
                      ↓
          addNotificationReceivedListener
                      │
                      ↓
                  Callback
                      │
          ┌───────────┼───────────┐
          ↓           ↓           ↓
        State       Storage    Navigation
          │           │           │
          └───────────┼───────────┘
                      ↓
                       UI
```

---

# 48. Testing Listener Behavior

For classroom testing:

```text
1. Start listener
2. Schedule a short-delay notification
3. Wait for trigger
4. Observe console
5. Read title/body/data
6. Update UI
7. Unmount screen
8. Verify cleanup
```

Use a short interval while learning:

```text
10 seconds
```

rather than waiting hours.

---


# 49. Complete Listener Example

```jsx
import React, {
  useEffect,
  useState,
} from 'react';

import {
  View,
  Text,
  StyleSheet,
} from 'react-native';

import * as Notifications
  from 'expo-notifications';

export default function NotificationListenerScreen() {

  const [
    latestNotification,
    setLatestNotification,
  ] =
    useState(null);

  useEffect(() => {

    console.log(
      ' Listener registered'
    );

    const subscription =
      Notifications
        .addNotificationReceivedListener(
          notification => {

            console.log(
              ' Notification received:',
              notification
            );

            setLatestNotification(
              notification
            );

          }
        );

    return () => {

      console.log(
        ' Listener removed'
      );

      subscription.remove();

    };

  }, []);

  const content =
    latestNotification
      ?.request
      ?.content;

  return (

    <View style={styles.container}>

      <Text style={styles.title}>
        Notification Listener
      </Text>

      {!latestNotification ? (

        <Text>
          Waiting for a notification...
        </Text>

      ) : (

        <View>

          <Text style={styles.label}>
            Title
          </Text>

          <Text style={styles.value}>
            {content?.title}
          </Text>

          <Text style={styles.label}>
            Body
          </Text>

          <Text style={styles.value}>
            {content?.body}
          </Text>

          <Text style={styles.label}>
            Data
          </Text>

          <Text style={styles.value}>
            {JSON.stringify(
              content?.data,
              null,
              2
            )}
          </Text>

        </View>

      )}

    </View>
  );
}

const styles = StyleSheet.create({

  container: {
    flex: 1,
    padding: 20,
    justifyContent: 'center',
  },

  title: {
    fontSize: 24,
    fontWeight: 'bold',
    marginBottom: 30,
  },

  label: {
    fontSize: 14,
    fontWeight: 'bold',
    marginTop: 15,
  },

  value: {
    fontSize: 16,
    marginTop: 5,
  },

});
```

---

# 50. Important Security/Validation Note

Notification `data` is application input.

Do not blindly trust it.

For example:

```js
const screen =
  content.data?.screen;
```

Before navigating, your application can validate:

```text
Is this an allowed screen?
Is the ID valid?
Does the referenced record exist?
```

This becomes especially important when notification data originates outside the local application.

---

# 51. Notification Data Design

Keep data focused.

Good:

```js
data: {

  screen:
    'OrderDetails',

  orderId:
    'ORD-1001',

}
```

Avoid unnecessarily putting huge objects into notification data.

Use:

```text
small identifiers
+
small routing information
```

and let the application fetch or load the actual details when needed.

---

# 52. Notification Listener Responsibilities

A listener can be responsible for:

```text
Receive event
     ↓
Read notification
     ↓
Extract data
     ↓
Update state
     ↓
Store history
     ↓
Trigger application logic
```

But avoid putting an enormous amount of business logic directly inside the listener callback.

Prefer:

```text
Listener
   ↓
Notification Handler/Service
   ↓
Business Logic
```

for larger applications.

---

# 53. What a Good Architecture Looks Like

```text
App
 │
 └── Notification Listener
          │
          ↓
   Notification Service
          │
    ┌─────┼─────┐
    ↓     ↓     ↓
 State  Storage  Navigation
```

This separates:

```text
Receiving
```

from:

```text
Processing
```

and:

```text
Presentation
```

---

# Part 7 - Notification Responses & User Interaction with `expo-notifications`

In Part 6, we learned:

```text
Notification received
        ↓
addNotificationReceivedListener()
        ↓
Callback
        ↓
Read title/body/data
```

But there is another important event:

> **What happens when the user interacts with the notification?**

For example:

```text
Notification appears
        ↓
User taps notification
        ↓
Application receives response
        ↓
Read notification data
        ↓
Decide what to do
        ↓
Navigate / open content / perform action
```

This part focuses on **notification responses and user interaction**.

---

# 1. Notification Received vs Notification Response

These are two different concepts.

### Notification received

Means:

```text
A notification was delivered/received.
```

The API we learned in Part 6 is:

```js
Notifications.addNotificationReceivedListener()
```

### Notification response

Means:

```text
The user interacted with the notification.
```

For example:

```text
User taps notification
```

The important API is:

```js
Notifications.addNotificationResponseReceivedListener()
```

Mental model:

```text
Notification
      │
      ├── Received
      │      ↓
      │   Received Listener
      │
      └── User Interaction
             ↓
       Response Listener
```

---

# 2. The Response Listener

The main API is:

```js
Notifications.addNotificationResponseReceivedListener()
```

Basic example:

```js
const subscription =
  Notifications
    .addNotificationResponseReceivedListener(
      response => {

        console.log(
          'Notification response:',
          response
        );

      }
    );
```

The callback receives a **notification response object**.

---

# 3. Why Do We Need a Response Listener?

Imagine this notification:

```text
┌───────────────────────────────┐
│ Order Update                  │
│ Your order has been shipped.  │
└───────────────────────────────┘
```

The user taps it.

The application may need to:

```text
Open Order Details
```

The response listener lets your JavaScript application react to that user interaction.

Flow:

```text
Notification
      ↓
User taps
      ↓
Response listener
      ↓
Read notification data
      ↓
Find orderId
      ↓
Navigate to Order Details
```

---

# 4. Basic Response Listener Example

```jsx
import React, {
  useEffect,
} from 'react';

import {
  View,
  Text,
} from 'react-native';

import * as Notifications
  from 'expo-notifications';

export default function NotificationResponseScreen() {

  useEffect(() => {

    const subscription =
      Notifications
        .addNotificationResponseReceivedListener(
          response => {

            console.log(
              ' Notification tapped'
            );

            console.log(
              response
            );

          }
        );

    return () => {

      subscription.remove();

    };

  }, []);

  return (

    <View>

      <Text>
        Waiting for notification interaction...
      </Text>

    </View>
  );
}
```

---

# 5. Listener Lifecycle

Just like the received-notification listener:

```js
useEffect(() => {

  const subscription =
    Notifications
      .addNotificationResponseReceivedListener(
        response => {

          console.log(response);

        }
      );

  return () => {

    subscription.remove();

  };

}, []);
```

The lifecycle is:

```text
Component Mount
      ↓
Register response listener
      ↓
Wait
      ↓
User interacts with notification
      ↓
Callback executes
      ↓
Component Unmount
      ↓
Remove listener
```

---

# 6. Why Cleanup Is Important

A response listener is also a subscription.

Therefore:

```js
subscription.remove();
```

should be called during cleanup.

Without cleanup, applications can accidentally create multiple listeners:

```text
Listener 1
Listener 2
Listener 3
```

Then:

```text
User taps notification
        ↓
Listener 1
Listener 2
Listener 3
```

may all execute.

Correct pattern:

```js
const subscription =
  Notifications
    .addNotificationResponseReceivedListener(
      callback
    );

return () => {

  subscription.remove();

};
```

---

# 7. Understanding the Response Object

The response contains information about the interaction.

One important property is:

```js
response.notification
```

This gives access to the notification associated with the response.

For example:

```js
const notification =
  response.notification;
```

Then:

```js
const content =
  notification
    .request
    .content;
```

Now you can inspect:

```text
title
body
data
```

---

# 8. Reading Notification Content From a Response

Example:

```js
Notifications
  .addNotificationResponseReceivedListener(
    response => {

      const content =
        response
          .notification
          .request
          .content;

      console.log(
        'Title:',
        content.title
      );

      console.log(
        'Body:',
        content.body
      );

      console.log(
        'Data:',
        content.data
      );

    }
  );
```

This is similar to the listener from Part 6.

The major difference is:

```text
Part 6:
Notification received

Part 7:
User interacted with notification
```

---

# 9. `data` Becomes Extremely Important

Suppose the notification contains:

```js
content: {

  title:
    'Order Update',

  body:
    'Your order has been shipped.',

  data: {

    screen:
      'OrderDetails',

    orderId:
      'ORD-1001',

  },

}
```

When the user taps it, the response listener can read:

```js
const data =
  response
    .notification
    .request
    .content
    .data;
```

Then:

```js
console.log(
  data.screen
);

console.log(
  data.orderId
);
```

Result:

```text
OrderDetails
ORD-1001
```

---

# 10. Why Put IDs in Notification Data?

Suppose you have:

```text
1000 orders
```

and the user taps:

```text
Order Update
```

The application needs to know:

```text
Which order?
```

The notification can contain:

```js
data: {

  screen:
    'OrderDetails',

  orderId:
    'ORD-1001',

}
```

Now:

```text
Notification tap
      ↓
orderId
      ↓
Fetch / load order
      ↓
Open Order Details
```

This is much more useful than only having:

```text
"Order Update"
```

---

# 11. Response + Navigation Concept

A very common real-world pattern is:

```text
Notification
      ↓
User taps
      ↓
Response listener
      ↓
Read data
      ↓
Determine target
      ↓
Navigate
```

For example:

```js
const data =
  response
    .notification
    .request
    .content
    .data;
```

Then conceptually:

```js
if (
  data.screen ===
  'OrderDetails'
) {

  // Navigate to OrderDetails

}
```

The actual navigation code depends on whether your project uses:

```text
Expo Router
```

or:

```text
React Navigation
```

The notification API only provides the response; your navigation system controls navigation.

---

# 12. Example - Order Details

Notification:

```js
content: {

  title:
    'Order Update',

  body:
    'Your order has been shipped.',

  data: {

    screen:
      'OrderDetails',

    orderId:
      'ORD-1001',

  },

}
```

Response:

```js
const data =
  response
    .notification
    .request
    .content
    .data;
```

Then:

```text
data.screen
      ↓
OrderDetails

data.orderId
      ↓
ORD-1001
```

Navigation concept:

```text
OrderDetails
      +
ORD-1001
      ↓
Open correct order
```

---

# 13. Example - Study Lesson

Suppose:

```js
data: {

  screen:
    'Study',

  lessonId:
    'lesson-101',

}
```

User taps:

```text
Study Reminder
Time to continue your lesson.
```

Flow:

```text
Tap
 ↓
Response Listener
 ↓
screen = Study
 ↓
lessonId = lesson-101
 ↓
Navigate to Study
 ↓
Open lesson-101
```

---

# 14. Example - Chat Message

Notification:

```js
content: {

  title:
    'New Message',

  body:
    'You have a new message.',

  data: {

    screen:
      'Chat',

    conversationId:
      'chat-101',

  },

}
```

User taps:

```text
New Message
```

Response listener reads:

```js
const data =
  response
    .notification
    .request
    .content
    .data;
```

Then:

```text
Chat
 ↓
chat-101
 ↓
Open conversation
```

---

# 15. Response Listener Does Not Automatically Navigate

This is important.

This:

```js
Notifications
  .addNotificationResponseReceivedListener(...)
```

does not automatically know:

```text
Which screen?
```

Your application decides that.

The flow is:

```text
Expo Notifications
      ↓
Response object
      ↓
Your application
      ↓
Navigation decision
      ↓
Navigation library
```

---

# 16. Notification Response vs Deep Link

A notification tap can behave similarly to a deep-link experience:

```text
User action
      ↓
Open specific application location
```

For example:

```text
Notification tap
      ↓
OrderDetails
```

But they are different mechanisms.

A notification response originates from:

```text
Notification interaction
```

while a deep link originates from:

```text
URL / app link
```

They can eventually lead to the same navigation destination.

---

# 17. App State Matters

One of the most important concepts is that the application can be in different states when the user interacts with a notification.

Conceptually:

```text
App State
│
├── Foreground
│
├── Background
│
└── Not currently running
```

Your notification-handling architecture needs to consider these states.

---

# 18. Foreground Interaction

Suppose:

```text
App is open
```

and a notification appears.

The user interacts with it.

Your response listener can process the response.

Conceptually:

```text
App Open
   ↓
Notification
   ↓
User taps
   ↓
Response Listener
   ↓
Navigation / state update
```

---

# 19. Background Interaction

Suppose:

```text
App is in background
```

The user sees a notification and taps it.

The operating system can bring the application into an active state.

Your application then needs to process the notification response appropriately.

Conceptually:

```text
Background
    ↓
Notification
    ↓
User taps
    ↓
App opens / resumes
    ↓
Response handling
    ↓
Navigation
```

---

# 20. Cold Start / App Not Running

This is a particularly important real-world scenario.

Suppose:

```text
Application is not currently running
```

A notification appears.

The user taps it.

The application may need to start and then determine:

```text
Why was the application opened?
```

The answer may be:

```text
Because the user interacted with this notification.
```

This is why applications often need to check the **last notification response** during startup in addition to registering a response listener.

---

# 21. Getting the Last Notification Response

Expo Notifications provides:

```js
Notifications.getLastNotificationResponseAsync()
```

This can be useful when the application needs to inspect a notification response that caused or is associated with the application being opened.

Example:

```js
const response =
  await Notifications
    .getLastNotificationResponseAsync();

console.log(
  response
);
```

If there is no relevant response, the result can be `null`.

---

# 22. Why Do We Need `getLastNotificationResponseAsync()`?

Compare:

### App already running

```text
Notification tap
      ↓
Response listener
```

### App starts because of notification interaction

```text
Application starts
      ↓
Check last notification response
      ↓
Process response
```

Therefore, a robust application can consider both:

```text
Live response listener
+
Startup response check
```

---

# 23. Startup Response Example

```js
useEffect(() => {

  const checkLastResponse =
    async () => {

      const response =
        await Notifications
          .getLastNotificationResponseAsync();

      if (!response) {

        return;

      }

      console.log(
        'Opened from notification:',
        response
      );

    };

  checkLastResponse();

}, []);
```

This is useful when your application needs to determine whether a notification interaction is responsible for startup behavior.

---

# 24. Listener + Last Response

A robust conceptual architecture is:

```text
Application starts
      │
      ├──────────────→ Check last response
      │
      ↓
Register response listener
      │
      ↓
Wait for future interactions
```

Why both?

Because:

```text
Last response
 ↓
Handles relevant startup context

Response listener
 ↓
Handles future interactions while running
```

---

# 25. Important: Avoid Duplicate Navigation

Suppose your app processes:

```text
Last notification response
```

and then also processes the same event through another path.

You could accidentally navigate twice:

```text
OrderDetails
 ↓
OrderDetails
```

Therefore, your application should design response handling carefully.

A useful approach is to centralize response processing.

---

# 26. Central Response Handler

Instead of writing navigation logic in multiple places:

```js
const handleNotificationResponse =
  (response) => {

    const data =
      response
        .notification
        .request
        .content
        .data;

    // Process data

  };
```

Then use it from:

```text
Response listener
```

and:

```text
Startup response check
```

Conceptually:

```text
                  ┌── Response Listener
                  │
                  ↓
             handleResponse()
                  ↑
                  │
                  └── Last Response
```

This reduces duplicated logic.

---

# 27. Example Central Handler

```js
const handleNotificationResponse =
  (response) => {

    if (!response) {

      return;

    }

    const data =
      response
        .notification
        .request
        .content
        .data;

    console.log(
      'Notification data:',
      data
    );

    if (
      data?.screen ===
      'Study'
    ) {

      // Navigate to Study

    }

  };
```

Then:

```js
useEffect(() => {

  const subscription =
    Notifications
      .addNotificationResponseReceivedListener(
        handleNotificationResponse
      );

  return () => {

    subscription.remove();

  };

}, []);
```

And separately:

```js
useEffect(() => {

  const checkLastResponse =
    async () => {

      const response =
        await Notifications
          .getLastNotificationResponseAsync();

      handleNotificationResponse(
        response
      );

    };

  checkLastResponse();

}, []);
```

---

# 28. Validate Notification Data

Do not blindly navigate based on arbitrary data.

Instead of:

```js
navigation.navigate(
  data.screen
);
```

consider validating:

```js
const allowedScreens = [
  'Study',
  'OrderDetails',
  'Chat',
];

if (
  !allowedScreens.includes(
    data?.screen
  )
) {

  return;

}
```

Then process the route.

The exact navigation code depends on your navigation architecture.

---

# 29. Validate IDs Too

Suppose:

```js
data: {

  screen:
    'OrderDetails',

  orderId:
    'ORD-1001',

}
```

Before loading:

```text
OrderDetails
```

validate:

```text
Does orderId exist?
Is it a valid format?
Does the order exist?
```

Then:

```text
Valid
 ↓
Open order

Invalid
 ↓
Show fallback / ignore
```

---

# 30. Response Object Mental Model

Think:

```text
Response
│
└── notification
      │
      └── request
            │
            └── content
                  │
                  ├── title
                  ├── body
                  └── data
```

So:

```js
response
  .notification
  .request
  .content
```

gives the notification content associated with the response.

And:

```js
response
  .notification
  .request
  .content
  .data
```

gives the application data.

---

# 31. Response Identifier

A response can also contain information identifying the notification interaction/request.

During debugging, log the entire response:

```js
console.log(
  'Notification response:',
  JSON.stringify(
    response,
    null,
    2
  )
);
```

Do this before writing assumptions about the exact shape of every nested property.

This is a good development practice.

---

# 32. Logging Response Data

Useful debug code:

```js
const handleResponse =
  (response) => {

    console.log(
      '========== NOTIFICATION RESPONSE =========='
    );

    console.log(
      'Response:',
      JSON.stringify(
        response,
        null,
        2
      )
    );

    console.log(
      'Title:',
      response
        ?.notification
        ?.request
        ?.content
        ?.title
    );

    console.log(
      'Body:',
      response
        ?.notification
        ?.request
        ?.content
        ?.body
    );

    console.log(
      'Data:',
      response
        ?.notification
        ?.request
        ?.content
        ?.data
    );

  };
```

This is very useful while learning.

---

# 33. Response Listener + React Navigation

If your project uses React Navigation, conceptually:

```text
Notification response
       ↓
Extract data
       ↓
Navigation object
       ↓
navigate()
```

Example concept:

```js
if (
  data?.screen ===
  'Study'
) {

  navigation.navigate(
    'Study'
  );

}
```

The exact implementation depends on where your listener lives and how the navigation reference is exposed.

---

# 34. Response Listener + Expo Router

If your project uses Expo Router, the conceptual flow is:

```text
Notification response
       ↓
Extract data
       ↓
Router
       ↓
Navigate to route
```

For example, data could identify:

```text
/study/lesson-101
```

The actual route handling depends on your project's Expo Router structure.

The notification API itself does not perform router navigation.

---

# 35. Do Not Mix Notification API and Navigation API

Keep responsibilities separate:

```text
expo-notifications
       ↓
Notification response
```

Then:

```text
Navigation library
       ↓
Screen transition
```

Architecture:

```text
Notification
     ↓
Response
     ↓
Application Handler
     ↓
Navigation
```

This makes the code easier to maintain.

---

# 36. Practical Project - Tap to Open Details

Create:

```text
Order App
```

Schedule:

```text
Title:
Order Update

Body:
Your order has been shipped.

Data:
{
  screen: "OrderDetails",
  orderId: "ORD-1001"
}
```

When user taps:

```text
Notification
     ↓
Response listener
     ↓
Read orderId
     ↓
Navigate
     ↓
OrderDetails
```

Display:

```text
Order Details

Order ID:
ORD-1001

Status:
Shipped
```

This is one of the most important real-world notification patterns.

---

# 37. Practical Project - Study Reminder

Notification:

```text
Study Reminder

Continue your React Native lesson.
```

Data:

```js
{
  screen:
    'Study',

  lessonId:
    'lesson-101',
}
```

Tap:

```text
Notification
 ↓
Response
 ↓
lessonId
 ↓
Study Screen
 ↓
Lesson 101
```

---

# 38. Practical Project - Chat Notification

Notification:

```text
New Message

Rajesh sent you a message.
```

Data:

```js
{
  screen:
    'Chat',

  conversationId:
    'chat-101',
}
```

Tap:

```text
Response
 ↓
Chat
 ↓
chat-101
 ↓
Open conversation
```

---

# 39. Notification Interaction Architecture

A robust conceptual architecture:

```text
                 Notification
                      │
                      ↓
                 User taps
                      │
                      ↓
             Notification Response
                      │
          ┌───────────┴───────────┐
          ↓                       ↓
   Response Listener       Startup Response
          │                       │
          └───────────┬───────────┘
                      ↓
             Central Handler
                      ↓
              Validate Data
                      ↓
                 Navigation
                      ↓
                  Screen
```

---

# 40. Complete Response Handling Example

```jsx
import React, {
  useEffect,
} from 'react';

import {
  View,
  Text,
} from 'react-native';

import * as Notifications
  from 'expo-notifications';

const handleNotificationResponse =
  (response) => {

    if (!response) {

      return;

    }

    const content =
      response
        ?.notification
        ?.request
        ?.content;

    const data =
      content?.data;

    console.log(
      'Notification title:',
      content?.title
    );

    console.log(
      'Notification body:',
      content?.body
    );

    console.log(
      'Notification data:',
      data
    );

    if (
      data?.screen ===
      'Study'
    ) {

      console.log(
        'Should navigate to Study'
      );

    }

  };

export default function App() {

  useEffect(() => {

    const subscription =
      Notifications
        .addNotificationResponseReceivedListener(
          handleNotificationResponse
        );

    return () => {

      subscription.remove();

    };

  }, []);

  useEffect(() => {

    const checkLastResponse =
      async () => {

        const response =
          await Notifications
            .getLastNotificationResponseAsync();

        handleNotificationResponse(
          response
        );

      };

    checkLastResponse();

  }, []);

  return (

    <View>

      <Text>
        Notification Response Handling
      </Text>

    </View>
  );
}
```

This example demonstrates the two major paths:

```text
Future interaction
      ↓
Response listener
```

and:

```text
Startup context
      ↓
getLastNotificationResponseAsync()
```

---

# 41. What Happens End-to-End?

Let's connect all the previous parts.

Suppose the application schedules:

```js
await Notifications
  .scheduleNotificationAsync({

    content: {

      title:
        'Study Reminder',

      body:
        'Continue React Native.',

      data: {

        screen:
          'Study',

        lessonId:
          'lesson-101',

      },

    },

    trigger: {

      // trigger configuration

    },

  });
```

Then:

```text
1. Schedule
      ↓
2. Wait for trigger
      ↓
3. Notification appears
      ↓
4. User taps
      ↓
5. Response generated
      ↓
6. Response listener
      ↓
7. Read data
      ↓
8. Validate
      ↓
9. Navigation
      ↓
10. Study Lesson 101
```

This is the complete interaction flow.

---

# 42. Notification System So Far

At this point we have learned:

```text
                    Notifications
                          │
        ┌─────────────────┼─────────────────┐
        ↓                 ↓                 ↓
     Permission        Scheduling       Interaction
        │                 │                 │
        ↓                 ↓                 ↓
 requestPermission   scheduleAsync    responseListener
                          │                 │
                          ↓                 ↓
                       Trigger           Data
                          │                 │
                          ↓                 ↓
                    Notification       Navigation
```

This is the foundation of a real notification system.

---

# Part 8 - Android Notification Channels with `expo-notifications`

This part is focused on an **Android-specific concept**:

> **Notification Channels**

---

# 1. What Is a Notification Channel?

Starting with **Android 8.0 (API level 26)**, notifications must belong to a notification channel. A channel defines the visual and auditory behavior for notifications assigned to it. Users can also control channel-level settings from Android's notification settings. citeturn0search0

Think of a channel as a **category of notifications**.

For example:

```text
My App
│
├── Messages
│
├── Orders
│
├── Reminders
│
└── Promotions
```

Each category can have its own notification behavior.

---

# 2. Why Were Channels Introduced?

Before notification channels, an application could largely control notification behavior directly.

Android introduced channels so that users have more control over notification categories.

For example:

```text
Messages
→ Important

Promotions
→ Less important

Reminders
→ Normal
```

The user can manage these categories from Android's notification settings.

Therefore:

```text
App Developer
      ↓
Creates notification channels
      ↓
User
      ↓
Controls channel behavior
```

---

# 3. Notification Channel Mental Model

Think:

```text
Notification
      ↓
Channel
      ↓
Android System
      ↓
User-visible notification
```

More specifically:

```text
Notification Content
        +
Channel ID
        ↓
Android Notification Channel
        ↓
Channel configuration
        ↓
Notification presentation
```

---

# 4. Android vs iOS

Notification channels are an **Android-specific** concept.

```text
Android
   ↓
Notification Channels
```

iOS does not use Android-style notification channels.

Therefore, channel code should usually be guarded:

```js
if (Platform.OS === 'android') {
  // Android channel logic
}
```

---

# 5. Creating a Notification Channel

The Expo API is:

```js
Notifications.setNotificationChannelAsync()
```

Basic example:

```js
await Notifications.setNotificationChannelAsync(
  'reminders',
  {
    name: 'Reminders',
    importance:
      Notifications.AndroidImportance.HIGH,
  }
);
```

The first argument is:

```text
channel ID
```

The second argument is:

```text
channel configuration
```

---

# 6. Understanding Channel ID

Example:

```js
'reminders'
```

is the channel ID.

Think of it as the unique identifier used by your application to refer to the channel.

For example:

```text
reminders
messages
orders
promotions
```

Good channel IDs are:

```text
short
stable
meaningful
```

---

# 7. Channel Name vs Channel ID

These are different.

Example:

```js
await Notifications.setNotificationChannelAsync(
  'order_updates',
  {
    name: 'Order Updates',
    importance:
      Notifications.AndroidImportance.HIGH,
  }
);
```

Here:

```text
ID:
order_updates

Name:
Order Updates
```

The ID is primarily for your application.

The name is user-facing.

Think:

```text
Developer
   ↓
order_updates

User
   ↓
Order Updates
```

---

# 8. Importance

A channel has an importance level.

Example:

```js
importance:
  Notifications.AndroidImportance.HIGH
```

Importance influences how Android treats notifications in that channel.

Common Expo constants include:

```js
Notifications.AndroidImportance.MIN
Notifications.AndroidImportance.LOW
Notifications.AndroidImportance.DEFAULT
Notifications.AndroidImportance.HIGH
Notifications.AndroidImportance.MAX
```

The exact behavior is ultimately controlled by Android and can also be affected by the user's settings.

---

# 9. Importance Mental Model

Think approximately:

```text
MIN
 ↓
Very low interruption

LOW
 ↓
Low interruption

DEFAULT
 ↓
Normal

HIGH
 ↓
Important

MAX
 ↓
Very high importance
```

Do not simply choose `MAX` for everything.

Use importance according to the actual importance of the notification.

---

# 10. Example - Reminder Channel

```js
await Notifications.setNotificationChannelAsync(
  'reminders',
  {
    name: 'Reminders',
    importance:
      Notifications.AndroidImportance.DEFAULT,
  }
);
```

Now the application has an Android channel called:

```text
Reminders
```

with ID:

```text
reminders
```

---

# 11. Example - Message Channel

```js
await Notifications.setNotificationChannelAsync(
  'messages',
  {
    name: 'Messages',
    importance:
      Notifications.AndroidImportance.HIGH,
  }
);
```

Now:

```text
Channel ID:
messages

Channel Name:
Messages
```

Messages can be treated differently from normal reminders.

---

# 12. Example - Promotions Channel

```js
await Notifications.setNotificationChannelAsync(
  'promotions',
  {
    name: 'Promotions',
    importance:
      Notifications.AndroidImportance.LOW,
  }
);
```

This is a useful example of channel separation:

```text
Messages
   ↓
HIGH

Reminders
   ↓
DEFAULT

Promotions
   ↓
LOW
```

---

# 13. Why Multiple Channels?

Suppose your application has:

```text
10 types of notifications
```

You could put everything into:

```text
one channel
```

but then users cannot distinguish categories as effectively.

Instead:

```text
messages
orders
reminders
promotions
```

can represent different notification categories.

This gives users more control.

---

# 14. Channel Architecture

A larger application might have:

```text
                 My Application
                       │
       ┌───────────────┼───────────────┐
       ↓               ↓               ↓
   Messages         Orders          Reminders
       ↓               ↓               ↓
    Channel          Channel          Channel
       ↓               ↓               ↓
     Android         Android         Android
```

The important idea is:

> A channel groups notifications that should share channel-level behavior.

---

# 15. Creating Channels During App Startup

A common approach is to create required Android channels when the application initializes.

Example:

```js
import {
  Platform,
} from 'react-native';

import * as Notifications
  from 'expo-notifications';

async function setupNotificationChannels() {

  if (
    Platform.OS !== 'android'
  ) {

    return;

  }

  await Notifications
    .setNotificationChannelAsync(
      'messages',
      {
        name: 'Messages',

        importance:
          Notifications
            .AndroidImportance
            .HIGH,
      }
    );

  await Notifications
    .setNotificationChannelAsync(
      'reminders',
      {
        name: 'Reminders',

        importance:
          Notifications
            .AndroidImportance
            .DEFAULT,
      }
    );

}
```

Then call it during application startup.

---

# 16. Why Use `Platform.OS`?

Because notification channels are Android-specific.

Without a platform check:

```js
Notifications
  .setNotificationChannelAsync(...)
```

may be called on iOS unnecessarily.

A clear architecture is:

```js
if (
  Platform.OS === 'android'
) {

  await setupNotificationChannels();

}
```

Expo's API is designed so unsupported platforms do not perform Android channel operations, but the explicit platform guard makes your application intent clear. citeturn0search0

---

# 17. Channel + Notification

Creating a channel alone does not create a notification.

Remember:

```text
Channel
 ↓
Defines category/configuration
```

while:

```text
scheduleNotificationAsync()
 ↓
Schedules notification
```

You need both concepts.

Flow:

```text
Create Channel
      ↓
Schedule Notification
      ↓
Assign Notification to Channel
      ↓
Android
```

---

# 18. Using a Channel When Scheduling

A notification can specify:

```js
channelId:
  'reminders'
```

Example:

```js
await Notifications.scheduleNotificationAsync({

  content: {

    title:
      'Study Reminder',

    body:
      'Time to study React Native.',

  },

  trigger: {

    type:
      Notifications
        .SchedulableTriggerInputTypes
        .TIME_INTERVAL,

    seconds:
      10,

  },

});
```

For Android channel-aware scheduling, the notification request can associate with a channel using `channelId`.

A practical pattern is:

```js
await Notifications.scheduleNotificationAsync({

  content: {

    title:
      'Study Reminder',

    body:
      'Time to study React Native.',

  },

  trigger: {

    type:
      Notifications
        .SchedulableTriggerInputTypes
        .TIME_INTERVAL,

    seconds:
      10,

    channelId:
      'reminders',

  },

});
```

This associates the scheduled notification with the `reminders` channel. citeturn0search0

---

# 19. Important: Channel Should Exist First

A clean flow is:

```text
App starts
    ↓
Create channel
    ↓
Channel exists
    ↓
Schedule notification
    ↓
Use channelId
```

For example:

```js
await Notifications.setNotificationChannelAsync(
  'reminders',
  {
    name: 'Reminders',
    importance:
      Notifications.AndroidImportance.DEFAULT,
  }
);

await Notifications.scheduleNotificationAsync({
  content: {
    title:
      'Study Reminder',
  },
  trigger: {
    // trigger
    channelId:
      'reminders',
  },
});
```

---

# 20. Android 13 Permission Connection

Android 13 introduced runtime notification permission behavior.

Expo's SDK 54 documentation notes that on Android 13, the notification permission prompt does not appear until at least one notification channel has been created. citeturn0search0

Therefore, the conceptual setup becomes:

```text
Android
   ↓
Create Channel
   ↓
Request Notification Permission
   ↓
Schedule Notification
```

This is especially important when teaching Android notification setup.

---

# 21. Recommended Android Setup Flow

For a local-notification application:

```text
Application starts
        ↓
Check Android
        ↓
Create notification channels
        ↓
Request notification permission
        ↓
User grants permission
        ↓
Schedule notification
```

The exact permission flow should still be implemented using the permissions APIs covered in earlier parts.

---

# 22. Channel Configuration

A channel can define settings such as:

```text
name
importance
sound
vibration
lights
badge behavior
description
```

The exact available fields depend on the Expo/Android API and platform.

A channel is therefore much more than simply:

```text
channel ID
```

It represents a category of notification behavior.

---

# 23. Vibration Pattern

A channel can specify vibration behavior.

Example:

```js
vibrationPattern:
  [0, 250, 250, 250]
```

Conceptually:

```text
0 ms
 ↓
250 ms vibration
 ↓
250 ms pause
 ↓
250 ms vibration
```

This is useful for notifications where vibration is meaningful.

---

# 24. Sound

A channel can also be configured with a sound.

Example:

```js
sound:
  'default'
```

Custom notification sounds require additional project configuration. Expo's SDK 54 documentation notes that custom sounds are configured through the `expo-notifications` config plugin, and Android 8+ requires the channel itself to have the appropriate sound configured. citeturn0search0

---

# 25. Custom Sound Architecture

Conceptually:

```text
Sound file
   ↓
expo-notifications config
   ↓
Build application
   ↓
Create Android channel
   ↓
Assign sound
   ↓
Schedule notification
```

This is different from simply placing a random sound filename into JavaScript.

---

# 26. Why Channel Sound Is Important

On Android 8.0+:

```text
Notification
      ↓
Channel
      ↓
Channel sound
```

The Android notification channel controls channel-level sound behavior.

Expo's documentation specifically notes that for custom sounds on Android 8+, the channel needs the appropriate sound configuration. citeturn0search0

---

# 27. Light Color

A channel can also specify a notification light color on supported Android devices.

Example:

```js
lightColor:
  '#FF231F7C'
```

This is a device-dependent feature and may not have a visible effect on every modern Android device.

The important learning point is:

```text
Channel
 ↓
Can contain Android-specific presentation configuration
```

---

# 28. Channel Description

A channel can also have a description.

Conceptually:

```js
{
  name:
    'Messages',

  description:
    'Notifications about new messages',

  importance:
    Notifications.AndroidImportance.HIGH,
}
```

This helps explain the purpose of the channel to users.

---

# 29. Complete Channel Example

```js
import {
  Platform,
} from 'react-native';

import * as Notifications
  from 'expo-notifications';

async function createReminderChannel() {

  if (
    Platform.OS !== 'android'
  ) {

    return;

  }

  await Notifications
    .setNotificationChannelAsync(
      'reminders',
      {

        name:
          'Reminders',

        description:
          'Important reminder notifications',

        importance:
          Notifications
            .AndroidImportance
            .HIGH,

        vibrationPattern:
          [0, 250, 250, 250],

        sound:
          'default',

      }
    );
}
```

---

# 30. Creating Multiple Channels

```js
async function createChannels() {

  if (
    Platform.OS !== 'android'
  ) {

    return;

  }

  await Notifications
    .setNotificationChannelAsync(
      'messages',
      {

        name:
          'Messages',

        importance:
          Notifications
            .AndroidImportance
            .HIGH,

      }
    );

  await Notifications
    .setNotificationChannelAsync(
      'reminders',
      {

        name:
          'Reminders',

        importance:
          Notifications
            .AndroidImportance
            .DEFAULT,

      }
    );

  await Notifications
    .setNotificationChannelAsync(
      'promotions',
      {

        name:
          'Promotions',

        importance:
          Notifications
            .AndroidImportance
            .LOW,

      }
    );

}
```

---

# 31. Channel Naming Strategy

Good:

```text
Messages
Order Updates
Reminders
Promotions
```

Bad:

```text
Channel 1
Channel 2
Test
ABC
```

Channel names should communicate their purpose to the user.

---

# 32. Channel ID Naming Strategy

Good IDs:

```text
messages
order_updates
reminders
promotions
```

Avoid constantly changing IDs:

```text
messages1
messages2
messages3
```

A stable ID makes the channel easier to manage.

---

# 33. Important Android Rule: Channels Are Persistent

Once an Android notification channel has been created, its settings are controlled by Android and the user.

Expo's SDK 54 documentation notes an important limitation:

> After a channel is created, only its name and description can be modified programmatically; other channel settings are controlled by Android. citeturn0search0

This has a major practical consequence.

---

# 34. Why Can't We Freely Change Importance Later?

Suppose you create:

```js
importance:
  Notifications.AndroidImportance.LOW
```

for:

```text
reminders
```

and later your code tries:

```js
importance:
  Notifications.AndroidImportance.HIGH
```

using the same channel ID.

Android may not apply the new importance because the channel has already been created.

The OS owns those channel-level settings after creation.

---

# 35. User Has Control

The user can change channel settings through Android's notification settings.

For example:

```text
Settings
 ↓
Apps
 ↓
Your App
 ↓
Notifications
 ↓
Reminders
```

The user may be able to control things such as:

```text
Allow notifications
Sound
Vibration
Importance
```

depending on Android version/device.

This is an important UX principle:

> Do not assume the settings you initially create will remain unchanged.

---

# 36. Channel vs Notification Content

Do not confuse these.

Notification content:

```js
content: {

  title:
    'Study Reminder',

  body:
    'Time to study.',

}
```

describes:

```text
WHAT the notification says.
```

Channel:

```js
{
  name:
    'Reminders',

  importance:
    HIGH,

}
```

describes:

```text
WHAT CATEGORY / BEHAVIOR the Android channel represents.
```

Mental model:

```text
Content
 ↓
Message

Channel
 ↓
Android category/settings
```

---

# 37. Channel vs Trigger

Also remember:

```text
Content
 ↓
WHAT

Trigger
 ↓
WHEN

Channel
 ↓
ANDROID CATEGORY / BEHAVIOR
```

A complete notification request can therefore be thought of as:

```text
                 Notification
                     │
        ┌────────────┼────────────┐
        ↓            ↓            ↓
     Content       Trigger      Channel
        ↓            ↓            ↓
      WHAT          WHEN       Android
                                behavior
```

---

# 38. Channel vs Listener

Similarly:

```text
Channel
 ↓
Android notification category/settings

Received Listener
 ↓
Application reacts to receipt

Response Listener
 ↓
Application reacts to user interaction
```

Each solves a different problem.

---

# 39. Getting All Channels

Expo provides:

```js
Notifications.getNotificationChannelsAsync()
```

Example:

```js
const channels =
  await Notifications
    .getNotificationChannelsAsync();

console.log(
  'Channels:',
  channels
);
```

This is useful for:

```text
Debugging
Settings screens
Verification
Administration
```

---

# 40. Getting One Channel

Use:

```js
Notifications.getNotificationChannelAsync(
  channelId
)
```

Example:

```js
const channel =
  await Notifications
    .getNotificationChannelAsync(
      'reminders'
    );

console.log(
  channel
);
```

If the channel does not exist, the result can be `null`. citeturn0search0

---

# 41. Checking Whether a Channel Exists

```js
const channel =
  await Notifications
    .getNotificationChannelAsync(
      'reminders'
    );

if (!channel) {

  console.log(
    'Channel does not exist'
  );

} else {

  console.log(
    'Channel exists:',
    channel
  );

}
```

This is useful for debugging.

---

# 42. Listing All Channels

```js
const channels =
  await Notifications
    .getNotificationChannelsAsync();

channels.forEach(
  channel => {

    console.log(
      'ID:',
      channel.id
    );

    console.log(
      'Name:',
      channel.name
    );

    console.log(
      'Importance:',
      channel.importance
    );

  }
);
```

This lets you inspect channel configuration.

---

# 43. Deleting a Channel

Expo also provides:

```js
Notifications.deleteNotificationChannelAsync(
  channelId
)
```

Example:

```js
await Notifications
  .deleteNotificationChannelAsync(
    'promotions'
  );
```

This removes the Android notification channel.

If notifications were scheduled using that channel, deleting the channel should be treated as a configuration change that requires careful application design.

---

# 44. When Would You Delete a Channel?

Possible cases:

```text
Old feature removed
       ↓
Old notification category no longer needed
```

or:

```text
Development/testing
       ↓
Clean up test channels
```

In normal production applications, avoid deleting channels casually.

---

# 45. Channel Lifecycle

The complete lifecycle is:

```text
Create
  ↓
Use
  ↓
User can modify settings
  ↓
Read
  ↓
Update name/description if needed
  ↓
Delete only when appropriate
```

---

# 46. Channel Groups

Android also supports notification channel groups.

Conceptually:

```text
Your App
│
├── Communication
│      ├── Messages
│      └── Calls
│
└── Productivity
       ├── Reminders
       └── Tasks
```

Expo provides:

```js
Notifications.setNotificationChannelGroupAsync()
```

for creating/configuring channel groups.

---

# 47. Why Use Channel Groups?

For applications with many channels, groups can organize them.

For example:

```text
Communication
   ├── Messages
   └── Calls

Orders
   ├── Order Updates
   └── Delivery

Marketing
   ├── Promotions
   └── Offers
```

This is an advanced Android notification-management concept.

---

# 48. Channel Group Example

```js
await Notifications
  .setNotificationChannelGroupAsync(
    'communication',
    {

      name:
        'Communication',

      description:
        'Communication-related notifications',

    }
  );
```

Then a channel can be associated with the group through its channel configuration.

---

# 49. Channel Group Mental Model

```text
Channel Group
      ↓
Contains channels
      ↓
Each channel
      ↓
Contains notifications
```

Example:

```text
Communication
      │
      ├── Messages
      │
      └── Calls
```

For small classroom applications, groups are optional.

For larger applications, they can improve organization.

---

# 50. Android Notification Setup Example

A practical application can initialize:

```js
async function setupNotifications() {

  if (
    Platform.OS !== 'android'
  ) {

    return;

  }

  await Notifications
    .setNotificationChannelAsync(
      'messages',
      {
        name:
          'Messages',

        importance:
          Notifications
            .AndroidImportance
            .HIGH,

      }
    );

  await Notifications
    .setNotificationChannelAsync(
      'reminders',
      {
        name:
          'Reminders',

        importance:
          Notifications
            .AndroidImportance
            .DEFAULT,

      }
    );

}
```

Then:

```text
App startup
 ↓
setupNotifications()
 ↓
Channels ready
```

---

# 51. Scheduling Through a Channel

Example:

```js
await Notifications.scheduleNotificationAsync({

  content: {

    title:
      'New Message',

    body:
      'You received a new message.',

  },

  trigger: {

    type:
      Notifications
        .SchedulableTriggerInputTypes
        .TIME_INTERVAL,

    seconds:
      10,

    channelId:
      'messages',

  },

});
```

Conceptually:

```text
Notification
    ↓
messages channel
    ↓
Android
    ↓
User sees message notification
```

---

# 52. Different Notifications, Different Channels

Example:

### Message

```text
Channel:
messages
```

### Reminder

```text
Channel:
reminders
```

### Promotion

```text
Channel:
promotions
```

This allows the application to categorize notifications.

---

# 53. Real-World Example

Imagine a food delivery application:

```text
Channels
│
├── order_updates
│
├── delivery_updates
│
├── offers
│
└── account
```

Then:

```text
Order confirmed
   ↓
order_updates

Delivery arriving
   ↓
delivery_updates

20% discount
   ↓
offers

Password changed
   ↓
account
```

This is much more organized than putting everything into one generic channel.

---

# 54. Channel Design Principles

When designing channels:

### 1. Group by meaningful category

Good:

```text
Messages
Orders
Reminders
```

### 2. Do not create a channel for every individual notification

Bad:

```text
order_1001
order_1002
order_1003
```

### 3. Keep IDs stable

Good:

```text
order_updates
```

### 4. Use importance responsibly

Do not mark everything:

```text
MAX
```

### 5. Remember user control

Users can change channel settings.

---

# 55. Complete Notification Architecture So Far

At this point:

```text
                         Notifications
                               │
        ┌──────────────────────┼──────────────────────┐
        ↓                      ↓                      ↓
   Permission              Scheduling             Channels
        │                      │                      │
        ↓                      ↓                      ↓
   User access             Content + Trigger       Android
                               │                  organization
                               ↓                      │
                          Notification                 │
                               │                      │
                    ┌──────────┴──────────┐           │
                    ↓                     ↓           │
                 Received              Response       │
                    ↓                     ↓           │
                Listener              Listener        │
                    │                     │            │
                    └──────────┬──────────┘            │
                               ↓                       │
                         Application Logic             │
                               │                       │
                               ↓                       │
                           Navigation                  │
```

---

# 56. Most Important Mental Model

Remember these four concepts:

```text
CONTENT
   ↓
WHAT should the user see?

TRIGGER
   ↓
WHEN should it fire?

CHANNEL
   ↓
WHICH Android notification category/settings?

LISTENER / RESPONSE
   ↓
WHAT should the application do?
```

This mental model will make the notification APIs much easier to understand.

---

# Part 9 - Notification Categories & Interactive Notifications with `expo-notifications`

Until now, our notifications mostly looked like:

```text
┌──────────────────────────────────┐
│ Study Reminder                   │
│ Time to study React Native.      │
└──────────────────────────────────┘
```

The user could see the notification and tap it.

But real applications often need more than that.

For example:

```text
┌─────────────────────────────────────────┐
│ New Message                             │
│ Rajesh sent you a message.              │
│                                         │
│ [ Reply ]   [ Mark as Read ]            │
└─────────────────────────────────────────┘
```

Now the notification is **interactive**.

The user can perform an action directly from the notification.

This part teaches:

```text
Notification Category
        ↓
Notification Actions
        ↓
Action Identifier
        ↓
User Interaction
        ↓
Response Listener
        ↓
Application Logic
```

---

# 1. What Is a Notification Category?

A notification category groups a notification with a set of possible user actions.

For example:

```text
Message Category
       │
       ├── Reply
       ├── Mark as Read
       └── Open
```

Another category could be:

```text
Reminder Category
       │
       ├── Complete
       └── Snooze
```

The category tells the notification system:

> **What actions are available for this type of notification?**

---

# 2. Why Do We Need Categories?

Without categories:

```text
Notification
    ↓
Title
Body
```

With categories:

```text
Notification
    ↓
Category
    ↓
Actions
    ├── Open
    ├── Reply
    └── Mark as Read
```

This allows your application to create richer notification experiences.

---

# 3. Notification Category Mental Model

Think of a category as a definition:

```text
Category
   ↓
"What actions can this notification provide?"
```

For example:

```text
message
   ↓
Reply
Mark as Read
Open
```

Then a notification can reference:

```text
message
```

as its category identifier.

---

# 4. Main API

Expo Notifications provides:

```js
Notifications.setNotificationCategoryAsync()
```

This is used to create or configure a notification category.

Conceptually:

```js
await Notifications
  .setNotificationCategoryAsync(
    'message',
    [
      // action definitions
    ]
  );
```

The first argument is the category identifier.

The second argument is the list of actions.

---

# 5. Category Identifier

Example:

```js
'message'
```

This identifies the category.

Good identifiers:

```text
message
order
reminder
task
```

Keep category identifiers:

```text
stable
meaningful
easy to understand
```

---

# 6. Category vs Channel

Do not confuse:

```text
Channel
```

with:

```text
Category
```

They solve different problems.

### Channel

Primarily an Android notification organization/configuration concept.

```text
messages
reminders
promotions
```

### Category

Defines notification interaction/actions.

```text
message
   ├── Reply
   ├── Mark as Read
   └── Open
```

Mental model:

```text
CHANNEL
 ↓
Android notification category/settings

CATEGORY
 ↓
Notification interaction/actions
```

---

# 7. Channel + Category Together

A notification can conceptually have:

```text
Content
   ↓
Trigger
   ↓
Channel
   ↓
Category
   ↓
Actions
```

For example:

```text
Message Notification

Channel:
messages

Category:
message

Actions:
Reply
Mark as Read
Open
```

These are different layers.

---

# 8. What Is a Notification Action?

An action is a button or interaction option presented with a notification.

Examples:

```text
[ Open ]
[ Reply ]
[ Mark as Read ]
[ Snooze ]
[ Delete ]
```

Each action has an identifier.

For example:

```text
OPEN
REPLY
MARK_AS_READ
```

The identifier lets your application determine:

> **Which action did the user select?**

---

# 9. Action Identifier

Suppose:

```text
[ Mark as Read ]
```

has:

```text
MARK_AS_READ
```

as its identifier.

When the user selects it, the response contains information that allows your application to identify:

```text
MARK_AS_READ
```

Then:

```text
Application
     ↓
Recognizes action
     ↓
Marks message as read
```

---

# 10. Creating a Category With Actions

Conceptually:

```js
await Notifications.setNotificationCategoryAsync(
  'message',
  [
    {
      identifier:
        'REPLY',

      buttonTitle:
        'Reply',
    },

    {
      identifier:
        'MARK_AS_READ',

      buttonTitle:
        'Mark as Read',
    },

    {
      identifier:
        'OPEN',

      buttonTitle:
        'Open',
    },
  ]
);
```

The exact available action options depend on the Expo SDK and platform.

The important concept is:

```text
identifier
+
user-facing action label
```

---

# 11. Category Setup Should Happen Before Notification Use

A good architecture is:

```text
App Startup
     ↓
Create Categories
     ↓
Create Android Channels
     ↓
Request Permissions
     ↓
Schedule Notifications
```

For example:

```js
await setupNotificationCategories();

await setupNotificationChannels();

await requestNotificationPermission();
```

The exact order can be adapted to your application's setup, but categories and channels should be configured before you rely on them for scheduled notifications.

---

# 12. Message Category Example

Let's design:

```text
Message
```

with:

```text
Reply
Mark as Read
Open
```

Category:

```text
message
```

Actions:

```text
REPLY
MARK_AS_READ
OPEN
```

Mental model:

```text
message
│
├── REPLY
├── MARK_AS_READ
└── OPEN
```

---

# 13. Complete Category Setup Example

```js
import * as Notifications
  from 'expo-notifications';

async function setupNotificationCategories() {

  await Notifications
    .setNotificationCategoryAsync(
      'message',
      [

        {
          identifier:
            'REPLY',

          buttonTitle:
            'Reply',
        },

        {
          identifier:
            'MARK_AS_READ',

          buttonTitle:
            'Mark as Read',
        },

        {
          identifier:
            'OPEN',

          buttonTitle:
            'Open',
        },

      ]
    );

}
```

This establishes the available actions for the `message` category.

---

# 14. Associating a Notification With a Category

When scheduling the notification, specify its category.

Conceptually:

```js
await Notifications
  .scheduleNotificationAsync({

    content: {

      title:
        'New Message',

      body:
        'Rajesh sent you a message.',

      data: {

        screen:
          'Chat',

        conversationId:
          'chat-101',

      },

      categoryIdentifier:
        'message',

    },

    trigger: {
      // trigger
    },

  });
```

The important property is:

```js
categoryIdentifier:
  'message'
```

This tells the notification system which category definition belongs to this notification.

---

# 15. Complete Flow

The architecture becomes:

```text
Create Category
      ↓
Define Actions
      ↓
Schedule Notification
      ↓
Set categoryIdentifier
      ↓
Notification appears
      ↓
User sees actions
      ↓
User selects action
      ↓
Response listener
      ↓
Read action identifier
      ↓
Perform application logic
```

This is the core interactive-notification flow.

---

# 16. Notification Action Response

We already learned:

```js
Notifications
  .addNotificationResponseReceivedListener()
```

Now we use it for actions.

Example:

```js
const subscription =
  Notifications
    .addNotificationResponseReceivedListener(
      response => {

        console.log(
          'Response:',
          response
        );

      }
    );
```

The response contains information about the notification interaction, including the action that was selected.

---

# 17. Reading the Action Identifier

The response includes an action identifier.

Conceptually:

```js
const actionIdentifier =
  response.actionIdentifier;
```

Then:

```js
console.log(
  'Action:',
  actionIdentifier
);
```

Possible values:

```text
REPLY
MARK_AS_READ
OPEN
```

This is the key to interactive notifications.

---

# 18. Why Is the Action Identifier Important?

Suppose your notification has:

```text
[Reply]
[Mark as Read]
[Open]
```

All three actions generate a notification response.

Your application needs to distinguish them.

Without an action identifier:

```text
Response
   ↓
???
```

With it:

```text
Response
   ↓
actionIdentifier
   ↓
MARK_AS_READ
```

Now your application knows exactly what happened.

---

# 19. Handling Different Actions

Example:

```js
const handleNotificationResponse =
  (response) => {

    const actionIdentifier =
      response.actionIdentifier;

    if (
      actionIdentifier ===
      'OPEN'
    ) {

      console.log(
        'Open message'
      );

    }

    if (
      actionIdentifier ===
      'MARK_AS_READ'
    ) {

      console.log(
        'Mark message as read'
      );

    }

    if (
      actionIdentifier ===
      'REPLY'
    ) {

      console.log(
        'Reply action selected'
      );

    }

  };
```

This is a simple action router.

---

# 20. Better: `switch`

For multiple actions, `switch` can be easier to read:

```js
const handleNotificationResponse =
  (response) => {

    const action =
      response.actionIdentifier;

    switch (action) {

      case 'OPEN':

        console.log(
          'Open notification'
        );

        break;

      case 'MARK_AS_READ':

        console.log(
          'Mark as read'
        );

        break;

      case 'REPLY':

        console.log(
          'Reply selected'
        );

        break;

      default:

        console.log(
          'Unknown action:',
          action
        );

    }

  };
```

---

# 21. Default Action

There is also an important case:

```text
User taps notification itself
```

rather than pressing one of your custom action buttons.

Therefore, your application should distinguish:

```text
Default notification interaction
```

from:

```text
Custom action interaction
```

The response's action identifier allows your application to determine which interaction occurred.

---

# 22. Action Handling Mental Model

```text
User interaction
       ↓
Notification Response
       ↓
actionIdentifier
       ↓
┌───────────────┬─────────────────┐
↓               ↓                 ↓
OPEN       MARK_AS_READ          REPLY
↓               ↓                 ↓
Open       Update message     Reply flow
screen         state
```

---

# 23. Example - Mark as Read

Suppose:

```js
data: {

  messageId:
    'msg-101',

}
```

and:

```text
actionIdentifier:
MARK_AS_READ
```

The application can perform:

```text
MARK_AS_READ
       +
messageId
       ↓
Update message
       ↓
isRead = true
```

The notification becomes an actual shortcut into application functionality.

---

# 24. Example - Open

Notification:

```text
New Message
Rajesh sent you a message.

[Open]
```

Data:

```js
{
  screen:
    'Chat',

  conversationId:
    'chat-101',
}
```

User selects:

```text
Open
```

Flow:

```text
OPEN
 ↓
Read conversationId
 ↓
Navigate to Chat
 ↓
Open chat-101
```

---

# 25. Example - Reply

A reply action is more advanced because the user may need to enter text.

Conceptually:

```text
New Message
Rajesh sent you a message.

[ Reply ]
```

The application can define the action so that it supports text input where supported by the platform/API.

Then the response can contain user-entered action data.

This is an advanced interactive-notification topic and should be tested on the actual target platform.

---

# 26. Text Input Actions

Some notification action configurations support a text-input style action.

Conceptually:

```text
[ Reply ]
```

opens an input area.

The user enters:

```text
"Sure, I will check it."
```

Then the application receives the response.

The important architecture is:

```text
User enters text
      ↓
Notification action response
      ↓
Read response data
      ↓
Send / process reply
```

The exact action options and response fields should be checked against the Expo SDK 54 API and platform behavior being tested.

---

# 27. Why Text Actions Are Platform-Sensitive

Notification UI is controlled by:

```text
iOS
Android
Expo Notifications
Operating System
```

Therefore, an action that appears one way on Android may behave differently on iOS.

Always test:

```text
Android
+
iOS
```

when your application relies on interactive notification UI.

---

# 28. Action Data vs Notification Data

There are two concepts to keep separate.

### Notification data

Defined when creating the notification:

```js
data: {

  messageId:
    'msg-101',

}
```

This identifies the application object.

### Response action

Defined by the user's interaction:

```text
MARK_AS_READ
```

So:

```text
Notification data
       ↓
Which object?

Action identifier
       ↓
What did the user do?
```

Together:

```text
MARK_AS_READ
+
msg-101
```

means:

```text
Mark message 101 as read.
```

---

# 29. Very Important Mental Model

Remember:

```text
DATA
 ↓
WHAT object/context?

ACTION
 ↓
WHAT user did?
```

Example:

```js
data: {

  messageId:
    'msg-101',

}
```

and:

```text
actionIdentifier:
MARK_AS_READ
```

becomes:

```text
User action:
Mark as read

Target:
Message 101
```

This is one of the most useful patterns in interactive notifications.

---

# 30. Interactive Order Notification

Suppose a food delivery application has:

```text
Order #1001
Driver is arriving.

[Track Order]
[Call Driver]
```

Category:

```text
order
```

Actions:

```text
TRACK_ORDER
CALL_DRIVER
```

Data:

```js
{
  orderId:
    'ORD-1001',
}
```

Then:

```text
TRACK_ORDER
+
ORD-1001
```

means:

```text
Open tracking for order 1001.
```

---

# 31. Interactive Reminder Notification

Category:

```text
reminder
```

Actions:

```text
COMPLETE
SNOOZE
OPEN
```

Data:

```js
{
  reminderId:
    'reminder-101',
}
```

Possible interactions:

```text
COMPLETE
 ↓
Mark reminder complete

SNOOZE
 ↓
Schedule again

OPEN
 ↓
Open reminder screen
```

This connects interactive notifications with the scheduling concepts from previous parts.

---

# 32. Interactive Notification Architecture

```text
Reminder
   ↓
Notification
   ↓
Category
   ↓
Actions
   ↓
User selects action
   ↓
Response
   ↓
Action Identifier
   ↓
Notification Data
   ↓
Business Logic
```

Example:

```text
SNOOZE
+
reminder-101
      ↓
Reschedule reminder 101
```

---

# 33. Action-Based Business Logic

A good architecture is:

```js
const handleAction =
  (action, data) => {

    switch (action) {

      case 'OPEN':
        // navigate
        break;

      case 'COMPLETE':
        // update reminder
        break;

      case 'SNOOZE':
        // reschedule
        break;

      default:
        break;

    }

  };
```

This keeps notification handling separate from business logic.

---

# 34. Response Listener + Action Handler

```js
useEffect(() => {

  const subscription =
    Notifications
      .addNotificationResponseReceivedListener(
        response => {

          const action =
            response.actionIdentifier;

          const data =
            response
              ?.notification
              ?.request
              ?.content
              ?.data;

          handleAction(
            action,
            data
          );

        }
      );

  return () => {

    subscription.remove();

  };

}, []);
```

This is a reusable pattern.

---

# 35. Complete Interactive Notification Example

```js
import * as Notifications
  from 'expo-notifications';

async function setupCategories() {

  await Notifications
    .setNotificationCategoryAsync(
      'reminder',
      [

        {
          identifier:
            'COMPLETE',

          buttonTitle:
            'Complete',
        },

        {
          identifier:
            'SNOOZE',

          buttonTitle:
            'Snooze',
        },

        {
          identifier:
            'OPEN',

          buttonTitle:
            'Open',
        },

      ]
    );

}
```

Then schedule:

```js
await Notifications
  .scheduleNotificationAsync({

    content: {

      title:
        'Study Reminder',

      body:
        'Continue your React Native lesson.',

      data: {

        reminderId:
          'reminder-101',

      },

      categoryIdentifier:
        'reminder',

    },

    trigger: {
      // trigger
    },

  });
```

---

# 36. Handling the Actions

```js
const handleResponse =
  (response) => {

    const action =
      response.actionIdentifier;

    const data =
      response
        ?.notification
        ?.request
        ?.content
        ?.data;

    switch (action) {

      case 'COMPLETE':

        console.log(
          'Complete:',
          data?.reminderId
        );

        break;

      case 'SNOOZE':

        console.log(
          'Snooze:',
          data?.reminderId
        );

        break;

      case 'OPEN':

        console.log(
          'Open:',
          data?.reminderId
        );

        break;

      default:

        console.log(
          'Default interaction'
        );

    }

  };
```

---

# 37. `categoryIdentifier` vs `actionIdentifier`

These names look similar but mean different things.

### `categoryIdentifier`

Answers:

```text
Which group of actions belongs to this notification?
```

Example:

```text
reminder
```

### `actionIdentifier`

Answers:

```text
Which action did the user select?
```

Example:

```text
SNOOZE
```

Mental model:

```text
categoryIdentifier
       ↓
reminder
       ↓
┌──────────┬──────────┬──────────┐
OPEN     COMPLETE    SNOOZE
```

Then user selects:

```text
SNOOZE
```

So:

```text
categoryIdentifier = reminder
actionIdentifier   = SNOOZE
```

---

# 38. Multiple Categories

A larger application can define:

```text
message
order
reminder
```

Each can have different actions.

### Message

```text
Reply
Mark as Read
Open
```

### Order

```text
Track
Open
```

### Reminder

```text
Complete
Snooze
Open
```

Architecture:

```text
Categories
│
├── message
│    ├── REPLY
│    ├── MARK_AS_READ
│    └── OPEN
│
├── order
│    ├── TRACK
│    └── OPEN
│
└── reminder
     ├── COMPLETE
     ├── SNOOZE
     └── OPEN
```

---

# 39. Why Not Create One Category for Everything?

You could define:

```text
general
```

with:

```text
Open
Delete
Reply
Snooze
Complete
Track
```

But that would be poor design.

A reminder does not need:

```text
Reply
```

An order does not need:

```text
Snooze
```

Categories should represent meaningful notification interaction types.

---

# 40. Category Design Principle

Use:

```text
One category
=
One meaningful interaction pattern
```

Examples:

```text
message
```

for:

```text
Reply
Mark as Read
Open
```

and:

```text
reminder
```

for:

```text
Complete
Snooze
Open
```

This keeps the notification system organized.

---

# 41. Removing or Updating Categories

Expo also provides category-management APIs such as:

```js
Notifications.getNotificationCategoriesAsync()
```

to inspect categories.

Example:

```js
const categories =
  await Notifications
    .getNotificationCategoriesAsync();

console.log(
  categories
);
```

This is useful for debugging and verification.

---

# 42. Getting All Categories

```js
const categories =
  await Notifications
    .getNotificationCategoriesAsync();

categories.forEach(
  category => {

    console.log(
      'Category:',
      category.identifier
    );

    console.log(
      'Actions:',
      category.actions
    );

  }
);
```

This allows you to inspect your configured interactive notification categories.

---

# 43. Deleting a Category

Expo provides:

```js
Notifications.deleteNotificationCategoryAsync(
  identifier
)
```

Example:

```js
await Notifications
  .deleteNotificationCategoryAsync(
    'promotions'
  );
```

Only delete categories that are no longer required.

For production applications, category management should be intentional.

---

# 44. Category Lifecycle

The lifecycle can be:

```text
Create Category
      ↓
Define Actions
      ↓
Schedule Notification
      ↓
Notification Appears
      ↓
User Selects Action
      ↓
Response Listener
      ↓
Business Logic
```

For maintenance:

```text
Inspect Categories
      ↓
Update configuration when appropriate
      ↓
Delete obsolete categories
```

---

# 45. Interactive Notifications + Scheduling

Now connect Part 4 and Part 9.

Suppose:

```text
Reminder:
Study React Native

Trigger:
8 PM every day

Category:
reminder

Actions:
Complete
Snooze
Open
```

The complete flow:

```text
Daily trigger
      ↓
Notification
      ↓
reminder category
      ↓
User selects Snooze
      ↓
Response
      ↓
SNOOZE
      ↓
Reminder ID
      ↓
Reschedule
```

This is a realistic application workflow.

---

# 46. Interactive Notifications + Notification IDs

Suppose the notification data contains:

```js
data: {

  reminderId:
    'reminder-101',

  notificationId:
    'notification-abc',

}
```

User selects:

```text
SNOOZE
```

The application can:

```text
SNOOZE
   +
reminderId
   ↓
Find reminder
   ↓
Cancel existing notification
   ↓
Schedule new notification
   ↓
Save new notification ID
```

This combines:

```text
Part 5
+
Part 9
```

---

# 47. Snooze Example

Imagine:

```text
Study Reminder
Time to study.

[Complete]
[Snooze]
[Open]
```

User selects:

```text
Snooze
```

Application:

```text
SNOOZE
 ↓
Find reminder-101
 ↓
Cancel old notification
 ↓
Schedule new notification
 ↓
10 minutes later
 ↓
Notification again
```

This is a very useful practical example.

---

# 48. Complete Notification Architecture

At this stage:

```text
                       NOTIFICATIONS
                              │
        ┌─────────────────────┼─────────────────────┐
        ↓                     ↓                     ↓
    Permission             Scheduling          Interaction
        │                     │                     │
        ↓                     ↓                     ↓
      Access          Content + Trigger       Category
                              │                     │
                              ↓                     ↓
                         Notification            Actions
                              │                     │
                    ┌─────────┴─────────┐          ↓
                    ↓                   ↓       User action
                Received             Response       │
                    ↓                   ↓           ↓
                Listener             Listener   Action ID
                    │                   │           │
                    └─────────┬─────────┘           │
                              ↓                     │
                         App Logic ←────────────────┘
                              │
                ┌─────────────┼─────────────┐
                ↓             ↓             ↓
             Storage       Navigation    Reschedule
```

---

# 49. Most Important Mental Model

Memorize this:

```text
CHANNEL
 ↓
Android notification organization/settings

CATEGORY
 ↓
Interaction definition

CATEGORY IDENTIFIER
 ↓
Which action group?

ACTION
 ↓
What can user do?

ACTION IDENTIFIER
 ↓
What did user select?

DATA
 ↓
Which application object/context?

RESPONSE
 ↓
User interaction event
```

This distinction is extremely important for real-world notification systems.

---

# 50. Quick Revision Table

| Concept | API / Property | Purpose |
|---|---|---|
| Create category | `setNotificationCategoryAsync()` | Define notification actions |
| Get categories | `getNotificationCategoriesAsync()` | Inspect categories |
| Delete category | `deleteNotificationCategoryAsync()` | Remove category |
| Category ID | `categoryIdentifier` | Select category for notification |
| Action ID | `identifier` | Identify an action |
| Response action | `response.actionIdentifier` | Determine what user selected |
| Notification data | `content.data` | Application-specific context |
| Response listener | `addNotificationResponseReceivedListener()` | Handle user interaction |

---

# Part 10 - Notification Badges, Sounds, Vibration & Presentation Control with `expo-notifications`

Until now, we learned how to:

```text
Request permission
      ↓
Schedule notification
      ↓
Use triggers
      ↓
Manage scheduled notifications
      ↓
Listen for notifications
      ↓
Handle notification taps
      ↓
Create Android channels
      ↓
Create interactive actions
```

Now we focus on the **presentation and user experience** of notifications.

A notification is not only:

```text
Title
+
Body
```

It can also involve:

```text
Badge
Sound
Vibration
Foreground presentation
Channel behavior
Notification appearance
```

The goal of this part is to understand:

> **How does the operating system decide how a notification should look, sound, and behave?**

---

# 1. What Is Notification Presentation?

Notification presentation means the way a notification is shown or experienced by the user.

For example:

```text
Notification
     ↓
┌───────────────────────────────┐
│ New Message                   │
│ You received a new message.   │
│ 🔊 Sound                      │
│ 📳 Vibration                  │
│ 🔴 Badge                      │
└───────────────────────────────┘
```

Different properties affect different parts of this experience.

---

# 2. The Four Main Presentation Concepts

In this part, focus on:

```text
1. Badge
2. Sound
3. Vibration
4. Foreground presentation
```

Mental model:

```text
                Notification
                     │
        ┌────────────┼────────────┐
        ↓            ↓            ↓
      Badge        Sound       Vibration
        │            │            │
        └────────────┼────────────┘
                     ↓
             User Experience
```

Foreground presentation is another layer:

```text
App is open
     ↓
Notification arrives
     ↓
How should it be presented?
```

---

# 3. Badge

A badge is an indicator associated with the application icon.

For example:

```text
┌─────────────┐
│     App     │
│      🔴3    │
└─────────────┘
```

The number can communicate something like:

```text
3 unread notifications
```

or:

```text
5 pending items
```

The exact meaning is decided by your application.

---

# 4. Badge Is Not the Notification Count Automatically

A very important concept:

```text
Notification count
```

and:

```text
App badge count
```

are not automatically the same thing.

For example:

```text
Received notifications:
5

Badge:
2
```

is possible.

Your application decides what the badge number represents.

Think:

```text
Notification system
       ↓
Application state
       ↓
Badge number
```

---

# 5. Setting a Badge

Expo Notifications provides:

```js
Notifications.setBadgeCountAsync()
```

Example:

```js
await Notifications.setBadgeCountAsync(3);
```

This asks the platform to set the application's badge count to:

```text
3
```

---

# 6. Clearing the Badge

To clear the badge:

```js
await Notifications.setBadgeCountAsync(0);
```

Mental model:

```text
3
 ↓
User reads notifications
 ↓
0
```

---

# 7. Getting the Current Badge Count

Expo provides:

```js
Notifications.getBadgeCountAsync()
```

Example:

```js
const count =
  await Notifications
    .getBadgeCountAsync();

console.log(
  'Badge count:',
  count
);
```

This is useful when your application needs to inspect the current badge state.

---

# 8. Badge Example

```js
const updateBadge =
  async (count) => {

    await Notifications
      .setBadgeCountAsync(
        count
      );

  };
```

Usage:

```js
await updateBadge(5);
```

Later:

```js
await updateBadge(2);
```

And when everything is read:

```js
await updateBadge(0);
```

---

# 9. Badge With Notification Count

Suppose your app stores:

```js
const unreadCount = 7;
```

You can synchronize the badge:

```js
await Notifications
  .setBadgeCountAsync(
    unreadCount
  );
```

Architecture:

```text
Unread messages
      ↓
unreadCount
      ↓
setBadgeCountAsync()
      ↓
App badge
```

This is a common real-world pattern.

---

# 10. Badge With a Notification Inbox

Suppose your application has:

```text
Notification Inbox

Unread:
3
```

Then:

```js
await Notifications
  .setBadgeCountAsync(3);
```

When the user opens all notifications:

```js
await Notifications
  .setBadgeCountAsync(0);
```

Now:

```text
Inbox unread count
        ↓
Badge count
```

can remain synchronized.

---

# 11. Badge Best Practice

Do not blindly increment:

```js
badge + 1
```

every time a notification arrives.

Instead, prefer a meaningful source of truth:

```text
Unread messages
Unread tasks
Unread notifications
```

Then:

```js
setBadgeCountAsync(
  unreadCount
);
```

This reduces synchronization problems.

---

# 12. Badge Is Platform-Dependent

Badge behavior differs between platforms.

In particular:

```text
iOS
```

has a strong app-icon badge concept.

Android badge behavior depends on:

```text
Android version
Launcher
Device manufacturer
Channel/settings
```

Therefore, test badge behavior on real devices.

---

# 13. Sound

A notification can play a sound when it is presented.

For example:

```text
Notification
     ↓
🔊 Sound
```

A common configuration is:

```js
sound: 'default'
```

The exact behavior depends on the platform and notification configuration.

---

# 14. Sound in Notification Content

A notification can include sound-related configuration.

For example:

```js
content: {

  title:
    'New Message',

  body:
    'You received a message.',

  sound:
    'default',

}
```

This communicates that the notification should use the default notification sound where supported.

---

# 15. Default Sound

Example:

```js
await Notifications
  .scheduleNotificationAsync({

    content: {

      title:
        'Reminder',

      body:
        'Time to study.',

      sound:
        'default',

    },

    trigger: {
      // trigger
    },

  });
```

This is the simplest sound configuration.

---

# 16. Custom Notification Sounds

Custom sounds are more advanced.

Conceptually:

```text
Sound file
     ↓
Project configuration
     ↓
Application build
     ↓
Notification configuration
     ↓
Notification
     ↓
Custom sound
```

For Expo projects, custom sounds require project configuration and a native build. They are not simply arbitrary files fetched at runtime.

---

# 17. Custom Sound Example

Conceptually, your notification might reference:

```js
sound:
  'notification.wav'
```

But the sound file must be included/configured correctly in the native application.

The important lesson is:

```text
JavaScript reference
+
Native project configuration
```

are both involved.

---

# 18. Why Custom Sounds Need Extra Configuration

A sound file must be available to the native application.

Think:

```text
JavaScript
   ↓
"notification.wav"
   ↓
Native app resources
   ↓
Operating system
   ↓
Sound
```

Simply placing:

```text
notification.wav
```

somewhere in the project does not automatically guarantee that Android/iOS can use it for notifications.

---

# 19. Android Channel Sound

On Android 8+, notification sound is closely tied to the notification channel.

This connects directly to Part 8.

Remember:

```text
Notification
      ↓
Android Channel
      ↓
Channel sound behavior
```

Expo's notification documentation notes that for Android 8+ custom sounds, the sound needs to be configured on the notification channel as well. citeturn0search0

Therefore:

```text
Android 8+
```

requires you to think about:

```text
Channel
+
Sound
```

together.

---

# 20. Channel Sound Example

```js
await Notifications
  .setNotificationChannelAsync(
    'messages',
    {

      name:
        'Messages',

      importance:
        Notifications
          .AndroidImportance
          .HIGH,

      sound:
        'default',

    }
  );
```

Now the channel has sound configuration.

---

# 21. Important Channel Reminder

From Part 8:

Once an Android channel is created, the user and Android control most channel-level settings.

Therefore, changing:

```js
sound
```

in your JavaScript later does not guarantee that an already-created channel will change its sound behavior.

This is one reason channel design should be done carefully.

---

# 22. Vibration

Notifications can also cause vibration on supported devices.

For example:

```text
Notification
     ↓
📳 Vibration
```

On Android, vibration can be configured through notification channels.

Example:

```js
vibrationPattern:
  [0, 250, 250, 250]
```

---

# 23. Understanding a Vibration Pattern

Example:

```js
[0, 250, 250, 250]
```

can conceptually be understood as:

```text
0 ms delay
 ↓
250 ms vibration
 ↓
250 ms pause
 ↓
250 ms vibration
```

The exact interpretation is platform-controlled.

The important idea is:

```text
Pattern
 ↓
Vibration sequence
```

---

# 24. Android Vibration Example

```js
await Notifications
  .setNotificationChannelAsync(
    'reminders',
    {

      name:
        'Reminders',

      importance:
        Notifications
          .AndroidImportance
          .HIGH,

      vibrationPattern:
        [0, 250, 250, 250],

    }
  );
```

This associates vibration behavior with the Android channel.

---

# 25. Vibration and Channels

On Android 8+:

```text
Notification
      ↓
Channel
      ↓
Vibration settings
```

Therefore, if vibration is important for your application, configure the channel appropriately.

Again:

```text
Channel settings
```

can be changed by the user.

---

# 26. Sound + Vibration Together

A message notification might use:

```js
await Notifications
  .setNotificationChannelAsync(
    'messages',
    {

      name:
        'Messages',

      importance:
        Notifications
          .AndroidImportance
          .HIGH,

      sound:
        'default',

      vibrationPattern:
        [0, 200, 100, 200],

    }
  );
```

Conceptually:

```text
Message
 ↓
HIGH importance
 ↓
Sound
 ↓
Vibration
```

This creates a more noticeable notification.

---

# 27. Do Not Overuse Sound and Vibration

If every notification:

```text
plays sound
+
vibrates
+
uses high importance
```

the user experience can become annoying.

Use stronger presentation for:

```text
Important messages
Urgent alerts
Time-sensitive events
```

Use quieter presentation for:

```text
Background updates
Low-priority reminders
Promotions
```

This connects directly to channel importance.

---

# 28. Notification Handler

Now we reach an important concept from earlier parts:

```js
Notifications.setNotificationHandler()
```

The notification handler determines how notifications should be presented while the app is in the foreground.

Example:

```js
Notifications.setNotificationHandler({

  handleNotification:
    async () => ({

      shouldShowBanner:
        true,

      shouldShowList:
        true,

      shouldPlaySound:
        true,

      shouldSetBadge:
        true,

    }),

});
```

---

# 29. Why Do We Need a Notification Handler?

When the application is in the foreground, the operating system may treat notification presentation differently.

The handler lets your application specify presentation behavior.

Mental model:

```text
Notification received
        ↓
App is foreground
        ↓
Notification Handler
        ↓
Presentation decision
```

---

# 30. Handler vs Listener

This distinction is extremely important.

### Handler

```js
Notifications.setNotificationHandler()
```

answers:

> How should this notification be presented while the app is running?

### Received Listener

```js
Notifications.addNotificationReceivedListener()
```

answers:

> What should my JavaScript code do when the notification is received?

### Response Listener

```js
Notifications.addNotificationResponseReceivedListener()
```

answers:

> What should my application do when the user interacts with the notification?

Mental model:

```text
Notification
     │
     ├── Handler
     │      ↓
     │   Presentation
     │
     ├── Received Listener
     │      ↓
     │   Receipt logic
     │
     └── Response Listener
            ↓
         Interaction logic
```

---

# 31. `shouldPlaySound`

Example:

```js
Notifications.setNotificationHandler({

  handleNotification:
    async () => ({

      shouldShowBanner:
        true,

      shouldShowList:
        true,

      shouldPlaySound:
        true,

      shouldSetBadge:
        true,

    }),

});
```

The property:

```js
shouldPlaySound
```

controls whether the notification should play a sound as part of foreground presentation where supported.

---

# 32. `shouldSetBadge`

Example:

```js
shouldSetBadge:
  true
```

This tells the notification presentation behavior to include badge handling where supported.

Remember:

```text
Badge count
```

and:

```text
shouldSetBadge
```

are related but not identical concepts.

The actual number can be controlled through:

```js
Notifications.setBadgeCountAsync()
```

---

# 33. `shouldShowBanner`

A notification handler can specify whether a foreground notification should be presented as a banner where supported.

Example:

```js
shouldShowBanner:
  true
```

This is part of the notification presentation configuration.

---

# 34. `shouldShowList`

Example:

```js
shouldShowList:
  true
```

This controls whether the notification should be included in the notification list where supported.

Therefore:

```text
Banner
+
Notification List
```

can be considered separately.

---

# 35. Complete Handler Example

```js
Notifications.setNotificationHandler({

  handleNotification:
    async () => ({

      shouldShowBanner:
        true,

      shouldShowList:
        true,

      shouldPlaySound:
        true,

      shouldSetBadge:
        true,

    }),

});
```

This is a common foreground presentation configuration.

---

# 36. Quiet Foreground Presentation

Suppose you want a notification to be less intrusive while the application is open.

Conceptually:

```js
Notifications.setNotificationHandler({

  handleNotification:
    async () => ({

      shouldShowBanner:
        true,

      shouldShowList:
        true,

      shouldPlaySound:
        false,

      shouldSetBadge:
        false,

    }),

});
```

This allows the notification to be shown without playing a sound or changing badge behavior through the foreground handler.

---

# 37. Important: Handler Is Global

The notification handler is not normally configured separately for each individual notification in the same way as notification content.

It acts as application-level notification presentation behavior.

Therefore, think:

```text
App
 ↓
Notification Handler
 ↓
Foreground presentation rules
```

This is why handler design should be intentional.

---

# 38. Presentation vs Content

Again, keep these separate.

### Content

```js
content: {

  title:
    'New Message',

  body:
    'Hello!',

}
```

means:

```text
What should the notification say?
```

### Presentation

```js
shouldPlaySound:
  true
```

means:

```text
How should it be presented?
```

---

# 39. Presentation vs Channel

On Android:

```text
Channel
 ↓
Android-level category behavior/settings
```

while:

```text
Notification Handler
 ↓
Foreground presentation decision
```

Therefore:

```text
Channel
+
Handler
+
Content
```

can all affect the final user experience.

---

# 40. Platform Differences

Notification behavior is platform-dependent.

You should expect differences between:

```text
Android
```

and:

```text
iOS
```

for:

```text
Badge
Sound
Vibration
Banner
Notification list
Channel behavior
```

Therefore, do not assume:

```text
same JavaScript
=
identical notification UI
```

on every device.

---

# 41. Why Real Device Testing Matters

Notification presentation depends on:

```text
Operating System
Device
OS Version
User Settings
Notification Permission
Channel Settings
App State
```

Therefore, notification features should be tested on actual devices.

For example:

```text
Android emulator
+
Android physical device
+
iOS physical device
```

can behave differently.

---

# 42. Badge + Sound + Vibration Example

Imagine:

```text
New Message
```

You want:

```text
Badge:
1

Sound:
Default

Vibration:
Enabled

Importance:
HIGH
```

Android channel:

```js
await Notifications
  .setNotificationChannelAsync(
    'messages',
    {

      name:
        'Messages',

      importance:
        Notifications
          .AndroidImportance
          .HIGH,

      sound:
        'default',

      vibrationPattern:
        [0, 200, 100, 200],

    }
  );
```

Then your application can synchronize the badge:

```js
await Notifications
  .setBadgeCountAsync(1);
```

---

# 43. Notification UX by Priority

A useful design strategy:

### Critical / time-sensitive

```text
HIGH importance
Sound
Vibration
```

### Normal

```text
DEFAULT importance
Optional sound
Optional vibration
```

### Low priority

```text
LOW importance
No unnecessary sound
No unnecessary vibration
```

Example:

```text
Messages
→ HIGH

Reminders
→ DEFAULT

Promotions
→ LOW
```

---

# 44. Badge Strategy

Possible application rule:

```text
Unread messages
 ↓
Badge count
```

For example:

```text
Unread = 4
 ↓
Badge = 4
```

When user reads one:

```text
Unread = 3
 ↓
Badge = 3
```

When all are read:

```text
Unread = 0
 ↓
Badge = 0
```

This is more reliable than blindly incrementing the badge for every notification.

---

# 45. Sound Strategy

Example:

```text
Message
 ↓
Sound

Reminder
 ↓
Default sound

Promotion
 ↓
No sound
```

The goal is:

```text
Important
 ↓
Noticeable

Unimportant
 ↓
Quiet
```

This improves user experience.

---

# 46. Vibration Strategy

Example:

```text
Important message
 ↓
Vibration

Normal reminder
 ↓
Optional vibration

Promotion
 ↓
No vibration
```

Again:

> Do not make every notification equally intrusive.

---

# 47. Foreground Notification Strategy

Suppose your application is already open.

A message arrives.

You may want:

```text
Banner:
YES

List:
YES

Sound:
YES

Badge:
YES
```

For a low-priority update:

```text
Banner:
YES

List:
YES

Sound:
NO

Badge:
NO
```

The exact implementation can vary depending on your application's needs.

---

# 48. Complete Foreground Presentation Example

```js
import * as Notifications
  from 'expo-notifications';

Notifications.setNotificationHandler({

  handleNotification:
    async () => ({

      shouldShowBanner:
        true,

      shouldShowList:
        true,

      shouldPlaySound:
        true,

      shouldSetBadge:
        true,

    }),

});
```

This should generally be configured during application initialization.

---

# 49. Badge Utility

A reusable utility:

```js
export async function
setNotificationBadge(
  count
) {

  try {

    await Notifications
      .setBadgeCountAsync(
        count
      );

  } catch (error) {

    console.error(
      'Failed to set badge:',
      error
    );

  }

}
```

Usage:

```js
await setNotificationBadge(
  unreadCount
);
```

---

# 50. Clear Badge Utility

```js
export async function
clearNotificationBadge() {

  try {

    await Notifications
      .setBadgeCountAsync(
        0
      );

  } catch (error) {

    console.error(
      'Failed to clear badge:',
      error
    );

  }

}
```

Usage:

```js
await clearNotificationBadge();
```

---

# 51. Reading Badge Utility

```js
export async function
getNotificationBadge() {

  try {

    return await Notifications
      .getBadgeCountAsync();

  } catch (error) {

    console.error(
      'Failed to get badge:',
      error
    );

    return 0;

  }

}
```

This makes badge functionality reusable across your project.

---

# 52. Complete Presentation Service

You can create a small notification utility:

```js
import * as Notifications
  from 'expo-notifications';

export const
NotificationPresentationService = {

  async setBadge(
    count
  ) {

    return Notifications
      .setBadgeCountAsync(
        count
      );

  },

  async getBadge() {

    return Notifications
      .getBadgeCountAsync();

  },

  async clearBadge() {

    return Notifications
      .setBadgeCountAsync(
        0
      );

  },

};
```

Then:

```js
await NotificationPresentationService
  .setBadge(5);
```

---

# 53. Notification Presentation Architecture

A clean architecture can look like:

```text
Notification System
       │
       ├── Content
       │
       ├── Trigger
       │
       ├── Channel
       │
       ├── Category
       │
       ├── Handler
       │
       ├── Badge
       │
       ├── Sound
       │
       └── Vibration
```

Each part has a different responsibility.

---

# 54. One Complete Example

Imagine a chat application.

### Channel

```text
messages
```

### Importance

```text
HIGH
```

### Sound

```text
default
```

### Vibration

```text
enabled
```

### Badge

```text
unread message count
```

### Category

```text
message
```

### Actions

```text
Reply
Mark as Read
Open
```

This gives:

```text
              Chat Notification
                     │
          ┌──────────┼──────────┐
          ↓          ↓          ↓
       Channel     Category    Badge
          ↓          ↓          ↓
       Messages    Actions    Unread
          │          │          │
          ↓          ↓          ↓
        Sound      Reply      Count
      Vibration    Open
                  Mark Read
```

---

# 55. End-to-End Chat Example

Suppose:

```text
Unread messages = 3
```

Application:

```js
await Notifications
  .setBadgeCountAsync(3);
```

Android channel:

```js
await Notifications
  .setNotificationChannelAsync(
    'messages',
    {

      name:
        'Messages',

      importance:
        Notifications
          .AndroidImportance
          .HIGH,

      sound:
        'default',

      vibrationPattern:
        [0, 200, 100, 200],

    }
  );
```

Category:

```text
message
 ↓
Reply
Mark as Read
Open
```

Notification:

```text
New Message
Rajesh sent you a message.
```

User:

```text
Mark as Read
```

Response:

```text
MARK_AS_READ
```

Application:

```text
Mark message as read
 ↓
Unread = 2
 ↓
Badge = 2
```

This combines:

```text
Part 5
+
Part 6
+
Part 7
+
Part 8
+
Part 9
+
Part 10
```

---

# 56. Why This Architecture Is Powerful

Instead of treating a notification as:

```text
title + body
```

you now understand it as a system:

```text
Notification
│
├── Content
│
├── Trigger
│
├── Channel
│
├── Category
│
├── Presentation
│
├── Listener
│
└── Response
```

This is the foundation of production-quality notification architecture.

---

# 57. Practical Project - Notification UX Lab

Create an application with:

```text
Notification UX Lab
```

Buttons:

```text
[ Set Badge 1 ]

[ Set Badge 5 ]

[ Clear Badge ]

[ Schedule Sound Notification ]

[ Schedule Quiet Notification ]
```

Android channels:

```text
sound_notifications
quiet_notifications
```

Compare:

```text
HIGH
vs
LOW
```

and:

```text
Sound
vs
No Sound
```

This is a good classroom experiment.

---

# 58. Practical Project - Chat Notification System

Build:

```text
Chat App
```

Requirements:

```text
Unread messages
 ↓
Badge

New message
 ↓
Messages channel

Message notification
 ↓
Sound + vibration

Notification category
 ↓
Reply
Mark as Read
Open

Action
 ↓
Update unread count
 ↓
Update badge
```

This project combines almost every notification concept learned so far.

---

# 59. Testing Matrix

Create a table while testing:

| Feature | Android | iOS | Notes |
|---|---|---|---|
| Badge | Test | Test | Device-dependent |
| Sound | Test | Test | User settings matter |
| Vibration | Test | Test | Hardware/settings matter |
| Banner | Test | Test | OS presentation |
| Notification list | Test | Test | OS behavior |
| Channel | Test | N/A | Android-specific |
| Category actions | Test | Test | UI can differ |
| Foreground handler | Test | Test | App-state dependent |

This is a good way to document platform differences.

---

# 60. Complete Mental Model

At this point, remember:

```text
CONTENT
 ↓
Title
Body
Data

TRIGGER
 ↓
When notification fires

CHANNEL
 ↓
Android notification category/settings

CATEGORY
 ↓
Interactive actions

HANDLER
 ↓
Foreground presentation

BADGE
 ↓
App icon count

SOUND
 ↓
Audio feedback

VIBRATION
 ↓
Haptic/device feedback

RECEIVED LISTENER
 ↓
Notification receipt logic

RESPONSE LISTENER
 ↓
User interaction logic
```

This is the most important summary of the notification module so far.

---

# Part 11 - Notification Permissions, Permission State & Settings with `expo-notifications`

In the previous parts, we learned how to create and present notifications.

But before an application can reliably use notifications, it needs to answer a fundamental question:

> **Does the user currently allow my application to send notifications?**

This part focuses on permission management.

The main APIs are:

```js
Notifications.getPermissionsAsync()
```

and:

```js
Notifications.requestPermissionsAsync()
```

We will also learn:

```text
Permission status
      ↓
granted
denied
undetermined
      ↓
canAskAgain
      ↓
Request permission
      ↓
Handle denial
      ↓
Guide user to Settings
      ↓
Check permission again
```

---

# 1. What Is Notification Permission?

Notification permission determines whether the operating system allows your application to present notifications to the user.

Think:

```text
Your App
   ↓
"I want to send notifications."
   ↓
Operating System
   ↓
"Is the user allowing this?"
   ↓
Yes / No
```

The application should not assume that permission is always available.

---

# 2. Why Permission Management Matters

A notification feature can fail even when your scheduling code is correct.

For example:

```text
scheduleNotificationAsync()
        ↓
Notification scheduled
        ↓
Permission denied
        ↓
User may not see the notification
```

Therefore:

```text
Scheduling
```

and:

```text
Permission
```

are separate concepts.

---

# 3. Permission Lifecycle

A typical permission lifecycle looks like:

```text
First application launch
        ↓
Permission status
        ↓
UNDETERMINED
        ↓
Request permission
        ↓
┌───────────────┬───────────────┐
↓               ↓
GRANTED        DENIED
↓               ↓
Use             Explain
notifications   / Settings
```

The exact behavior depends on the platform and user's previous choices.

---

# 4. Main Permission APIs

Expo Notifications provides:

```js
Notifications.getPermissionsAsync()
```

and:

```js
Notifications.requestPermissionsAsync()
```

### `getPermissionsAsync()`

Checks the current notification permission state.

### `requestPermissionsAsync()`

Requests notification permission from the user.

These APIs are available for Android and iOS in Expo SDK 54. citeturn0search0

---

# 5. `getPermissionsAsync()`

Basic example:

```js
const permissions =
  await Notifications
    .getPermissionsAsync();

console.log(
  permissions
);
```

This does not show a permission prompt.

It simply checks the current state.

Expo's SDK 54 documentation explicitly describes this as having no user-facing effect. citeturn0search0

---

# 6. Why Check Before Requesting?

Instead of immediately doing:

```js
await Notifications
  .requestPermissionsAsync();
```

a better approach is:

```text
Check current status
        ↓
Is permission already granted?
        ↓
Yes → Continue
No  → Decide whether to request
```

Example:

```js
const permissions =
  await Notifications
    .getPermissionsAsync();

if (
  permissions.granted
) {

  console.log(
    'Notifications are allowed'
  );

}
```

---

# 7. `requestPermissionsAsync()`

This API displays the operating system's permission request when appropriate.

Basic example:

```js
const permissions =
  await Notifications
    .requestPermissionsAsync();

console.log(
  permissions
);
```

Expo's SDK 54 documentation states that the default request asks for permission to display alerts, set badge count, and play sounds. citeturn0search0

---

# 8. Complete Basic Permission Function

```js
const requestNotificationPermission =
  async () => {

    const permissions =
      await Notifications
        .requestPermissionsAsync();

    if (
      permissions.granted
    ) {

      console.log(
        'Notification permission granted'
      );

      return true;

    }

    console.log(
      'Notification permission denied'
    );

    return false;

  };
```

This gives your application a simple boolean result.

---

# 9. `PermissionResponse`

The permission APIs return a permission response object.

Important properties include:

```text
status
granted
canAskAgain
expires
```

Conceptually:

```js
const permissions =
  await Notifications
    .getPermissionsAsync();

console.log(
  permissions.status
);

console.log(
  permissions.granted
);

console.log(
  permissions.canAskAgain
);
```

Expo documents these fields as part of `PermissionResponse`. citeturn0search0

---

# 10. Understanding `status`

The status can represent states such as:

```text
undetermined
granted
denied
```

Expo's `PermissionStatus` constants include:

```js
Notifications.PermissionStatus.UNDETERMINED
Notifications.PermissionStatus.GRANTED
Notifications.PermissionStatus.DENIED
```

These describe the general permission state. citeturn0search0

---

# 11. `UNDETERMINED`

This generally means:

```text
The user has not made a permission decision yet.
```

Mental model:

```text
App installed
     ↓
No decision yet
     ↓
UNDETERMINED
```

This is usually the most appropriate time to explain why your application needs notifications and request permission.

---

# 12. `GRANTED`

This means:

```text
The application has notification permission.
```

Example:

```js
if (
  permissions.granted
) {

  console.log(
    'Ready to use notifications'
  );

}
```

However, remember that platform-specific settings can be more granular than a single boolean.

---

# 13. `DENIED`

This means:

```text
The application does not currently have the required notification authorization.
```

The correct response is not necessarily:

```text
Keep requesting repeatedly.
```

Instead:

```text
Explain why notifications matter
        ↓
If appropriate, guide user to Settings
```

---

# 14. `canAskAgain`

Another important property is:

```js
permissions.canAskAgain
```

Example:

```js
if (
  !permissions.granted &&
  permissions.canAskAgain
) {

  // It may be appropriate to request permission.

}
```

If:

```js
canAskAgain === false
```

your application should generally guide the user toward system settings rather than repeatedly calling the request API.

Expo's SDK 54 permission type describes `canAskAgain` as indicating whether the user can be asked again; if not, the user should be directed to Settings to enable/disable the permission. citeturn0search0

---

# 15. `granted` vs `status`

These two can look similar:

```js
permissions.granted
```

and:

```js
permissions.status
```

But they are not the same property.

### `granted`

Convenience boolean:

```text
true / false
```

### `status`

Describes the permission state:

```text
granted
denied
undetermined
```

For simple checks:

```js
if (permissions.granted) {
  // allowed
}
```

is convenient.

For more detailed permission logic:

```js
switch (permissions.status) {
  // ...
}
```

can be useful.

---

# 16. A Better Permission Decision Tree

```text
getPermissionsAsync()
        ↓
      status
        │
 ┌──────┼──────────┐
 ↓      ↓          ↓
GRANTED DENIED  UNDETERMINED
 ↓      ↓          ↓
Use    Check      Explain
       canAskAgain  ↓
          │       Request
     ┌────┴────┐
     ↓         ↓
   true       false
     ↓         ↓
 Request    Settings
```

This is the mental model to remember.

---

# 17. Complete Permission Manager

```js
import * as Notifications
  from 'expo-notifications';

export async function
checkNotificationPermission() {

  const permissions =
    await Notifications
      .getPermissionsAsync();

  return permissions;

}
```

Then:

```js
const permissions =
  await checkNotificationPermission();

console.log(
  'Status:',
  permissions.status
);

console.log(
  'Granted:',
  permissions.granted
);

console.log(
  'Can ask again:',
  permissions.canAskAgain
);
```

---

# 18. Request Only When Needed

A good utility:

```js
export async function
requestNotificationPermission() {

  const current =
    await Notifications
      .getPermissionsAsync();

  if (
    current.granted
  ) {

    return true;

  }

  if (
    !current.canAskAgain
  ) {

    return false;

  }

  const result =
    await Notifications
      .requestPermissionsAsync();

  return result.granted;

}
```

Flow:

```text
Already granted?
 ↓
YES → true

NO
 ↓
Can ask again?
 ↓
NO → false
YES
 ↓
Request
 ↓
Return result
```

---

# 19. Why This Is Better Than Blind Requesting

Bad:

```js
await Notifications
  .requestPermissionsAsync();
```

every time the screen renders or every time a button is pressed.

Better:

```text
Check
 ↓
Request only when appropriate
```

This provides a better user experience.

---

# 20. Do Not Request Permission on Every Render

Never put a permission request directly into normal render logic.

Bad:

```jsx
function App() {

  Notifications
    .requestPermissionsAsync();

  return (
    <View />
  );

}
```

This is wrong because React rendering can happen many times.

Instead, explicitly trigger permission logic from:

```text
App initialization
or
user action
or
feature onboarding
```

depending on your UX.

---

# 21. Permission Request in `useEffect`

For learning purposes:

```js
useEffect(() => {

  requestNotificationPermission();

}, []);
```

This runs when the component mounts.

However, whether you should request permission immediately at app startup is a **UX decision**.

In many applications, it is better to first explain the feature and request permission when the user understands the benefit.

---

# 22. Permission UX

Instead of:

```text
App opens
 ↓
Permission popup immediately
```

you can use:

```text
App opens
 ↓
Explain notification feature
 ↓
"Get reminders and updates"
 ↓
User taps Enable
 ↓
System permission prompt
```

This is called a permission rationale/onboarding flow.

---

# 23. Example Permission Explanation Screen

```text
-----------------------------------

        Stay Updated 🔔

Enable notifications to receive:

✓ Study reminders
✓ Important task updates
✓ Order status changes

        [ Enable Notifications ]

        [ Not Now ]

-----------------------------------
```

When the user presses:

```text
Enable Notifications
```

then:

```js
await Notifications
  .requestPermissionsAsync();
```

---

# 24. Why Explain Before Asking?

A system permission dialog may only say something like:

```text
Allow App to send you notifications?
```

The operating system does not know your application's complete feature context.

Your app can explain:

```text
Why do we need this?
What will the user receive?
How can it help?
```

This can lead to better permission decisions.

---

# 25. Android 13+ Permission Behavior

Android 13 introduced runtime notification permission behavior.

Expo's SDK 54 documentation states that users must opt in to notifications through the operating-system prompt, and the prompt will not appear until at least one notification channel has been created. citeturn0search0

Therefore, Android notification initialization should account for:

```text
Android 13+
     ↓
Create at least one channel
     ↓
Permission prompt
```

---

# 26. Android Channel + Permission

This connects Part 8 with Part 11.

Conceptually:

```text
Android 13+
      ↓
Create channel
      ↓
Check permission
      ↓
Request permission if appropriate
      ↓
Schedule notification
```

Example:

```js
if (
  Platform.OS === 'android'
) {

  await Notifications
    .setNotificationChannelAsync(
      'reminders',
      {
        name:
          'Reminders',

        importance:
          Notifications
            .AndroidImportance
            .DEFAULT,
      }
    );

}
```

Then:

```js
const permissions =
  await Notifications
    .getPermissionsAsync();
```

---

# 27. iOS Permissions Are More Granular

iOS exposes additional permission information.

Expo SDK 54 documents iOS-specific authorization states:

```text
NOT_DETERMINED
DENIED
AUTHORIZED
PROVISIONAL
EPHEMERAL
```

These are available through:

```js
Notifications.IosAuthorizationStatus
```

Expo recommends using the iOS-specific `ios.status` field when interpreting iOS notification permission state rather than relying only on the root status field. citeturn0search0

---

# 28. `NOT_DETERMINED`

iOS:

```js
Notifications
  .IosAuthorizationStatus
  .NOT_DETERMINED
```

means:

```text
The user has not decided yet.
```

This is a natural point for your application to explain the notification feature and request authorization.

---

# 29. `DENIED`

iOS:

```js
Notifications
  .IosAuthorizationStatus
  .DENIED
```

means the app is not currently authorized to schedule or receive notifications.

The application should generally guide the user toward system settings when another request cannot meaningfully change the state.

---

# 30. `AUTHORIZED`

iOS:

```js
Notifications
  .IosAuthorizationStatus
  .AUTHORIZED
```

means:

```text
The app is authorized for notifications.
```

The exact presentation behavior can still depend on the user's notification settings.

---

# 31. `PROVISIONAL`

iOS also supports:

```js
Notifications
  .IosAuthorizationStatus
  .PROVISIONAL
```

This represents a provisional authorization state for noninterruptive notifications.

This is an iOS-specific concept.

For class documentation, remember:

```text
Android
 ↓
Different permission model

iOS
 ↓
More granular authorization states
```

---

# 32. `EPHEMERAL`

iOS also defines:

```js
Notifications
  .IosAuthorizationStatus
  .EPHEMERAL
```

This represents authorization for a limited amount of time.

This is an advanced iOS-specific permission state.

You should not treat all iOS authorization states as simply:

```text
true / false
```

when your application needs detailed permission handling.

---

# 33. Inspecting iOS Permission State

Example:

```js
const permissions =
  await Notifications
    .getPermissionsAsync();

console.log(
  'Root status:',
  permissions.status
);

console.log(
  'iOS status:',
  permissions.ios?.status
);
```

For iOS-specific decisions, use:

```js
permissions.ios?.status
```

as recommended by the Expo SDK 54 documentation. citeturn0search0

---

# 34. iOS Permission Settings

The iOS permission response can contain information such as:

```text
allowsAlert
allowsBadge
allowsSound
allowsDisplayOnLockScreen
allowsDisplayInNotificationCenter
allowsPreviews
```

This means:

```text
Permission
```

is not always a single simple switch.

The user can have different notification presentation permissions/settings.

Expo documents these fields as part of `NotificationPermissionsStatus`. citeturn0search0

---

# 35. Example: Badge Permission

Suppose:

```js
permissions.ios?.allowsBadge
```

is:

```text
false
```

Then:

```js
setBadgeCountAsync()
```

may not behave as expected on iOS until the relevant badge authorization is enabled.

Expo's SDK 54 documentation notes that `setBadgeCountAsync()` on iOS requires the user to have granted the `allowBadge` permission. citeturn0search0

---

# 36. Requesting Specific iOS Permissions

You can specify the scope of the iOS permission request.

Example:

```js
const permissions =
  await Notifications
    .requestPermissionsAsync({

      ios: {

        allowAlert:
          true,

        allowBadge:
          true,

        allowSound:
          true,

      },

    });
```

This explicitly requests:

```text
Alert
Badge
Sound
```

Expo documents these request options in SDK 54. citeturn0search0

---

# 37. Permission vs Presentation Settings

Important distinction:

```text
Permission
 ↓
Is the application allowed?

Presentation settings
 ↓
How is the notification allowed to appear?
```

For example, the user may allow notifications but disable sound.

So:

```text
Permission granted
```

does not necessarily mean:

```text
Sound enabled
```

---

# 38. Permission vs Channel Settings

On Android:

```text
Permission
 ↓
Can the app send notifications?

Channel
 ↓
How does a category behave?

User channel settings
 ↓
Final channel behavior
```

Therefore:

```text
Granted permission
```

does not guarantee:

```text
Sound
Vibration
High importance
```

because channel/user settings may affect those behaviors.

---

# 39. Permission Manager Architecture

A good application can centralize permission logic:

```text
NotificationPermissionService
          │
          ├── getStatus()
          │
          ├── request()
          │
          ├── canAskAgain()
          │
          └── openSettings()
```

Then screens do not need to repeat notification permission logic everywhere.

---

# 40. Permission Service Example

```js
import * as Notifications
  from 'expo-notifications';

export const
NotificationPermissionService = {

  async getStatus() {

    return Notifications
      .getPermissionsAsync();

  },

  async request() {

    return Notifications
      .requestPermissionsAsync();

  },

};
```

Usage:

```js
const permissions =
  await NotificationPermissionService
    .getStatus();

console.log(
  permissions
);
```

---

# 41. Permission-Aware Scheduling

Do not blindly schedule notifications when your feature requires permission.

Conceptual utility:

```js
async function
scheduleReminder() {

  const permissions =
    await Notifications
      .getPermissionsAsync();

  if (
    !permissions.granted
  ) {

    console.log(
      'Notification permission unavailable'
    );

    return null;

  }

  return Notifications
    .scheduleNotificationAsync({
      content: {
        title:
          'Reminder',
        body:
          'Study React Native.',
      },
      trigger: {
        // trigger
      },
    });

}
```

This makes the scheduling flow permission-aware.

---

# 42. Should You Always Check Before Scheduling?

This depends on your application.

A good design is:

```text
User enables reminder feature
       ↓
Ensure notification permission
       ↓
Schedule reminder
```

Rather than:

```text
Every schedule call
       ↓
Ask permission again
```

Keep permission handling and scheduling logically separate.

---

# 43. Permission State Can Change Outside Your App

This is extremely important.

Suppose:

```text
Monday:
Permission granted
```

Then the user goes to:

```text
Android/iOS Settings
```

and disables notifications.

Later:

```text
App opens
```

Your previous React state may still say:

```text
granted
```

if you never re-check the system.

Therefore:

> **System permission should be treated as external state that can change outside the application.**

---

# 44. Re-checking Permission When App Becomes Active

A useful architecture is:

```text
App becomes active
       ↓
getPermissionsAsync()
       ↓
Update application permission state
```

This is especially useful when users can change notification permissions in system settings.

---

# 45. Example With `AppState`

```jsx
import {
  AppState,
} from 'react-native';

import {
  useEffect,
} from 'react';

import * as Notifications
  from 'expo-notifications';

useEffect(() => {

  const subscription =
    AppState.addEventListener(
      'change',
      async nextState => {

        if (
          nextState ===
          'active'
        ) {

          const permissions =
            await Notifications
              .getPermissionsAsync();

          console.log(
            'Current permission:',
            permissions
          );

        }

      }
    );

  return () => {

    subscription.remove();

  };

}, []);
```

Conceptually:

```text
App background
      ↓
User changes notification setting
      ↓
App becomes active
      ↓
Check permission again
```

---

# 46. Why `AppState` Helps

Without re-checking:

```text
App thinks:
granted

System says:
denied
```

With re-checking:

```text
App resumes
 ↓
getPermissionsAsync()
 ↓
Application state updated
```

This keeps your UI closer to the actual system state.

---

# 47. Permission-Aware UI

Suppose your screen has:

```text
Notification Settings

Status:
Enabled

[ Disable reminders ]
```

If the user disables notifications outside the app:

```text
App resumes
 ↓
Permission check
 ↓
Status:
Disabled
```

Now your UI can update:

```text
Notification Settings

Status:
Disabled

[ Open Settings ]
```

---

# 48. Opening System Settings

When permission cannot be requested again, your application may need to guide the user to system settings.

React Native provides:

```js
Linking.openSettings()
```

Example:

```js
import {
  Linking,
} from 'react-native';

await Linking.openSettings();
```

This opens the application's settings page when supported by the platform.

---

# 49. Permission Denied Flow

A useful flow:

```text
Permission denied
       ↓
Can ask again?
       │
   ┌───┴───┐
   ↓       ↓
  YES      NO
   ↓       ↓
Request   Explain
again     Settings
           ↓
     Linking.openSettings()
```

This is much better than endlessly showing the same permission prompt.

---

# 50. Example Permission Handler

```js
async function
handleNotificationPermission() {

  const current =
    await Notifications
      .getPermissionsAsync();

  if (
    current.granted
  ) {

    return 'granted';

  }

  if (
    current.canAskAgain
  ) {

    const result =
      await Notifications
        .requestPermissionsAsync();

    if (
      result.granted
    ) {

      return 'granted';

    }

    return 'denied';

  }

  return 'settings';

}
```

Then:

```js
const result =
  await handleNotificationPermission();

if (
  result === 'settings'
) {

  await Linking.openSettings();

}
```

---

# 51. Permission Status UI

You can represent:

```text
GRANTED
 ↓
Notifications enabled

DENIED + canAskAgain
 ↓
Permission can be requested

DENIED + cannot ask again
 ↓
Open system settings

UNDETERMINED
 ↓
Explain + request
```

This makes permission UX predictable.

---

# 52. Permission State Machine

A useful mental model:

```text
             ┌──────────────┐
             │ UNDETERMINED │
             └──────┬───────┘
                    │
               Request
              ┌─────┴─────┐
              ↓           ↓
          GRANTED       DENIED
              │           │
              │      canAskAgain?
              │       ┌───┴───┐
              │       ↓       ↓
              │      YES      NO
              │       │       │
              │    Request   Settings
              │
              ↓
        Use Notifications
```

This is a very useful diagram to remember.

---

# 53. Permission Request UX Example

A professional flow:

```text
User opens Reminder feature
          ↓
App checks permission
          ↓
Not granted
          ↓
Show explanation
          ↓
"Enable notifications to receive reminders"
          ↓
User taps Enable
          ↓
System permission prompt
          ↓
User chooses
          ↓
App updates UI
```

This is usually better than requesting permission before the user understands why it is useful.

---

# 54. Permission Denial UX

Do not simply show:

```text
Permission denied.
```

Instead:

```text
Notifications are disabled.

Enable notifications to receive
study reminders.

[ Open Settings ]
[ Continue Without Notifications ]
```

This gives the user a choice.

---

# 55. Do Not Block the Entire App

If notifications are optional, do not make:

```text
Notification permission
```

a hard requirement for the entire application.

For example:

```text
User denies notifications
        ↓
Still allow:
Home
Profile
Settings
Other features
```

Only notification-dependent features should be affected.

---

# 56. Feature-Level Permission

Suppose your app has:

```text
Study
Tasks
Profile
Settings
```

Only:

```text
Study reminders
```

need notifications.

If permission is denied:

```text
Study content
     ↓
Still available

Study reminders
     ↓
Unavailable
```

This is better UX.

---

# 57. Notification Permission and Local Notifications

Even for **local notifications**, permission matters.

The notification does not need to come from a remote server to require platform notification authorization.

Mental model:

```text
Local notification
      ↓
Operating system notification system
      ↓
Permission/settings matter
```

This is especially important for your SDK 54 class because you are focusing on local notifications.

---

# 58. Local Notification Example

Suppose:

```js
await Notifications
  .scheduleNotificationAsync({

    content: {

      title:
        'Study Reminder',

      body:
        'Time to study React Native.',

    },

    trigger: {

      type:
        Notifications
          .SchedulableTriggerInputTypes
          .TIME_INTERVAL,

      seconds:
        10,

    },

  });
```

Before this feature is enabled in your UI, a permission-aware flow can be:

```text
Check permission
      ↓
Request if needed
      ↓
Schedule
```

---

# 59. Permission + Channel + Schedule

For Android:

```text
App starts
      ↓
Create channel
      ↓
Check permission
      ↓
Request if appropriate
      ↓
Schedule local notification
```

For iOS:

```text
App starts
      ↓
Check notification authorization
      ↓
Request if appropriate
      ↓
Schedule local notification
```

This is a useful platform-level mental model.

---

# 60. Permission Debugging

If a notification does not appear:

```text
1. Check permission
        ↓
2. Check channel on Android
        ↓
3. Check trigger
        ↓
4. Check notification handler
        ↓
5. Check user settings
        ↓
6. Check app/device state
```

First inspect:

```js
const permissions =
  await Notifications
    .getPermissionsAsync();

console.log(
  permissions
);
```

---

# 61. Debug Permission Object

For learning, log:

```js
console.log(
  JSON.stringify(
    permissions,
    null,
    2
  )
);
```

This can reveal:

```text
status
granted
canAskAgain
ios.status
ios.allowsAlert
ios.allowsBadge
ios.allowsSound
```

depending on platform.

---

# 62. Permission Debugging Example

```js
const permissions =
  await Notifications
    .getPermissionsAsync();

console.log(
  'Status:',
  permissions.status
);

console.log(
  'Granted:',
  permissions.granted
);

console.log(
  'Can ask again:',
  permissions.canAskAgain
);

console.log(
  'iOS status:',
  permissions.ios?.status
);
```

This is a useful development helper.

---

# 63. Android Permission Debugging

Check:

```text
Android version
        ↓
Is notification permission granted?
        ↓
Does a channel exist?
        ↓
Is channel blocked?
        ↓
Did user disable notifications?
```

You can inspect channels with:

```js
const channels =
  await Notifications
    .getNotificationChannelsAsync();

console.log(
  channels
);
```

---

# 64. iOS Permission Debugging

Check:

```js
const permissions =
  await Notifications
    .getPermissionsAsync();

console.log(
  'Authorization:',
  permissions.ios?.status
);

console.log(
  'Alert:',
  permissions.ios?.allowsAlert
);

console.log(
  'Badge:',
  permissions.ios?.allowsBadge
);

console.log(
  'Sound:',
  permissions.ios?.allowsSound
);
```

This gives a more detailed picture than simply checking:

```js
permissions.granted
```

---

# 65. Permission Service - More Complete Example

```js
import {
  Linking,
} from 'react-native';

import * as Notifications
  from 'expo-notifications';

export const
NotificationPermissionService = {

  async getPermissions() {

    return Notifications
      .getPermissionsAsync();

  },

  async requestPermissions() {

    return Notifications
      .requestPermissionsAsync();

  },

  async ensurePermission() {

    const current =
      await Notifications
        .getPermissionsAsync();

    if (
      current.granted
    ) {

      return {
        status:
          'granted',
        permissions:
          current,
      };

    }

    if (
      !current.canAskAgain
    ) {

      return {
        status:
          'settings',
        permissions:
          current,
      };

    }

    const result =
      await Notifications
        .requestPermissionsAsync();

    if (
      result.granted
    ) {

      return {
        status:
          'granted',
        permissions:
          result,
      };

    }

    return {
      status:
        'denied',
      permissions:
        result,
    };

  },

  async openSettings() {

    await Linking
      .openSettings();

  },

};
```

---

# 66. Using the Permission Service

```js
const result =
  await NotificationPermissionService
    .ensurePermission();

if (
  result.status ===
  'granted'
) {

  console.log(
    'Ready for notifications'
  );

}

if (
  result.status ===
  'settings'
) {

  console.log(
    'User should enable notifications in Settings'
  );

}
```

This keeps screens simpler.

---

# 67. Permission + Reminder Button

Imagine:

```text
Study Reminder

[ Enable Daily Reminder ]
```

When pressed:

```text
Check permission
      ↓
If granted
      ↓
Schedule reminder

If not granted
      ↓
Request permission

If cannot ask again
      ↓
Offer Settings
```

This is a strong real-world pattern.

---

# 68. Example Reminder Flow

```js
async function
enableReminder() {

  const result =
    await NotificationPermissionService
      .ensurePermission();

  if (
    result.status ===
    'settings'
  ) {

    await Linking
      .openSettings();

    return;

  }

  if (
    result.status !==
    'granted'
  ) {

    return;

  }

  await Notifications
    .scheduleNotificationAsync({
      content: {

        title:
          'Study Reminder',

        body:
          'Continue your React Native lesson.',

      },

      trigger: {
        // trigger configuration
      },

    });

}
```

---

# 69. Permission and App Lifecycle

Remember:

```text
Permission is system state.
```

It can change when your app is not running.

Therefore:

```text
App becomes active
       ↓
Re-check permission
       ↓
Update UI
```

This is especially useful for Settings-based permission changes.

---

# 70. Notification Permission Context

At this point, your complete permission architecture should look like:

```text
                    Notification Permission
                              │
                 ┌────────────┴────────────┐
                 ↓                         ↓
             Android                     iOS
                 │                         │
          Channel required           Authorization
          before prompt              states
                 │                         │
                 └────────────┬────────────┘
                              ↓
                    getPermissionsAsync()
                              ↓
                     Current permission
                              │
             ┌────────────────┼────────────────┐
             ↓                ↓                ↓
          Granted         Undetermined       Denied
             ↓                ↓                ↓
           Use             Request        canAskAgain?
        notifications          │            │
                               ↓        ┌───┴───┐
                            Result      YES     NO
                                         ↓       ↓
                                      Request  Settings
```

---

# 71. Most Important Mental Model

Memorize:

```text
getPermissionsAsync()
        ↓
CHECK

requestPermissionsAsync()
        ↓
ASK

granted
        ↓
USE

undetermined
        ↓
EXPLAIN + ASK

denied + canAskAgain
        ↓
REQUEST WHEN APPROPRIATE

denied + cannot ask again
        ↓
GUIDE TO SETTINGS
```

This is the foundation of permission-aware notification development.

---

# Part 12 - Advanced Scheduling & Trigger Types with `expo-notifications`

This part continues from:

In the earlier scheduling parts, we learned the basic idea:

```text
Notification Content
        +
Trigger
        ↓
Scheduled Notification
```

Now we go much deeper into **triggers**.

The goal is to understand how to build real-world schedules such as:

```text
In 10 seconds
Tomorrow at 9:00 AM
Every 20 minutes
Every day at 8:00 PM
Every Monday at 9:00 AM
Every month on the 1st
Every year on a specific date
At a specific calendar date/time
```

Expo SDK 54 provides several schedulable trigger types, including:

```text
TIME_INTERVAL
DATE
DAILY
WEEKLY
MONTHLY
YEARLY
CALENDAR
```

The exact trigger availability differs by platform. For example, Expo documents dedicated daily/weekly/monthly/yearly trigger types for Android, while iOS uses calendar-based triggers for equivalent calendar scheduling. citeturn0search0

---

# 1. What Is a Notification Trigger?

A trigger answers one question:

> **When should this notification be delivered?**

For example:

```text
Content:
"Time to study"

Trigger:
10 seconds from now
```

or:

```text
Content:
"Daily standup"

Trigger:
Every day at 9:00 AM
```

Mental model:

```text
Notification
      │
      ├── Content
      │     ├── title
      │     ├── body
      │     └── data
      │
      └── Trigger
            └── when?
```

---

# 2. Content vs Trigger

This distinction is extremely important.

### Content

```js
content: {
  title: 'Study Reminder',
  body: 'Time to study React Native.',
}
```

means:

```text
WHAT should the user see?
```

### Trigger

```js
trigger: {
  type:
    Notifications
      .SchedulableTriggerInputTypes
      .TIME_INTERVAL,

  seconds: 10,
}
```

means:

```text
WHEN should it happen?
```

So:

```text
CONTENT
 ↓
What?

TRIGGER
 ↓
When?
```

---

# 3. `scheduleNotificationAsync()`

The main scheduling API is:

```js
Notifications.scheduleNotificationAsync()
```

It receives a notification request:

```js
await Notifications
  .scheduleNotificationAsync({

    content: {
      // notification content
    },

    trigger: {
      // scheduling rule
    },

  });
```

Expo documents this API as scheduling a notification to be triggered in the future. It returns a notification identifier that can later be used for cancellation or identification. citeturn0search0

---

# 4. Notification Identifier

When you schedule a notification:

```js
const identifier =
  await Notifications
    .scheduleNotificationAsync({
      // ...
    });
```

the API returns:

```text
notification identifier
```

Example:

```js
console.log(
  identifier
);
```

You can save this identifier.

Why?

Because later you can cancel the exact scheduled notification:

```js
await Notifications
  .cancelScheduledNotificationAsync(
    identifier
  );
```

This connects directly to Part 5.

---

# 5. Trigger Types Overview

The main trigger types are:

| Trigger | Meaning |
|---|---|
| `TIME_INTERVAL` | After a period of time |
| `DATE` | At one specific date/time |
| `DAILY` | Every day at a time |
| `WEEKLY` | Every week at a weekday/time |
| `MONTHLY` | Every month on a day/time |
| `YEARLY` | Every year on a date/time |
| `CALENDAR` | Match specified calendar date components, primarily iOS |

Expo SDK 54 exposes these through `SchedulableTriggerInputTypes`. citeturn0search0

---

# 6. `TIME_INTERVAL`

`TIME_INTERVAL` means:

> Trigger after a specified number of seconds has elapsed.

Example:

```text
Now
 ↓
10 seconds
 ↓
Notification
```

---

# 7. One-Time Time Interval

```js
await Notifications
  .scheduleNotificationAsync({

    content: {

      title:
        'Reminder',

      body:
        '10 seconds have passed.',

    },

    trigger: {

      type:
        Notifications
          .SchedulableTriggerInputTypes
          .TIME_INTERVAL,

      seconds:
        10,

    },

  });
```

This schedules a one-time notification.

---

# 8. Repeating Time Interval

You can repeat a time interval:

```js
await Notifications
  .scheduleNotificationAsync({

    content: {

      title:
        'Drink Water',

      body:
        'Time to drink water.',

    },

    trigger: {

      type:
        Notifications
          .SchedulableTriggerInputTypes
          .TIME_INTERVAL,

      seconds:
        20 * 60,

      repeats:
        true,

    },

  });
```

Conceptually:

```text
20 minutes
 ↓
Notification
 ↓
20 minutes
 ↓
Notification
 ↓
20 minutes
 ↓
...
```

Expo documents repeating time-interval triggers through the `repeats` property. citeturn0search0

---

# 9. Important iOS Rule for Repeating Intervals

On iOS, when:

```js
repeats: true
```

the time interval must be at least:

```text
60 seconds
```

Expo SDK 54 documents this requirement. citeturn0search0

Therefore, avoid:

```js
seconds: 10,
repeats: true,
```

for iOS repeating notifications.

Use:

```js
seconds: 60,
repeats: true,
```

or greater.

---

# 10. When Should You Use `TIME_INTERVAL`?

Good use cases:

```text
Drink water every 30 minutes
Study break every 60 minutes
Timer-based reminder
Periodic local reminder
```

Not ideal for:

```text
Every Monday at 9 AM
Every month on the 1st
Every year on January 1st
```

For calendar-based requirements, use a calendar/date-based trigger.

---

# 11. `DATE`

`DATE` means:

> Trigger once at a specific date/time.

Example:

```text
August 25, 2026
10:30 AM
```

You can create a JavaScript `Date`:

```js
const date =
  new Date(
    2026,
    7,
    25,
    10,
    30
  );
```

Remember:

```text
JavaScript months are zero-based.
```

So:

```text
0 = January
1 = February
...
7 = August
```

---

# 12. Date Trigger Example

```js
const date =
  new Date(
    2026,
    7,
    25,
    10,
    30
  );

await Notifications
  .scheduleNotificationAsync({

    content: {

      title:
        'Meeting Reminder',

      body:
        'Your meeting starts now.',

    },

    trigger: {

      type:
        Notifications
          .SchedulableTriggerInputTypes
          .DATE,

      date,

    },

  });
```

Expo's SDK 54 `DateTriggerInput` supports a JavaScript `Date` or numeric timestamp and describes it as a one-time trigger. citeturn0search0

---

# 13. Date vs Time Interval

These two are commonly confused.

### Time Interval

```text
10 minutes from now
```

Example:

```js
seconds: 600
```

### Date

```text
August 25 at 10:30 AM
```

Example:

```js
date:
  new Date(...)
```

Mental model:

```text
TIME INTERVAL
 ↓
relative

DATE
 ↓
absolute date/time
```

---

# 14. Example: Reminder in 1 Hour

Use:

```text
TIME_INTERVAL
```

because the requirement is:

```text
1 hour from now
```

Example:

```js
trigger: {

  type:
    Notifications
      .SchedulableTriggerInputTypes
      .TIME_INTERVAL,

  seconds:
    60 * 60,

}
```

---

# 15. Example: Reminder Tomorrow at 9 AM

This is better represented as:

```text
DATE
```

because the requirement is a specific calendar time.

Conceptually:

```js
const tomorrow =
  new Date();

tomorrow.setDate(
  tomorrow.getDate() + 1
);

tomorrow.setHours(
  9,
  0,
  0,
  0
);
```

Then:

```js
trigger: {

  type:
    Notifications
      .SchedulableTriggerInputTypes
      .DATE,

  date:
    tomorrow,

}
```

---

# 16. `DAILY`

A daily trigger means:

```text
Every day
at a particular hour/minute
```

Example:

```text
Every day
8:00 PM
```

Mental model:

```text
Monday    8 PM
Tuesday   8 PM
Wednesday 8 PM
Thursday  8 PM
...
```

---

# 17. Daily Trigger Example

```js
await Notifications
  .scheduleNotificationAsync({

    content: {

      title:
        'Study Time',

      body:
        'Time to study React Native.',

    },

    trigger: {

      type:
        Notifications
          .SchedulableTriggerInputTypes
          .DAILY,

      hour:
        20,

      minute:
        0,

    },

  });
```

Expo SDK 54 documents `DailyTriggerInput` with `hour` and `minute`. citeturn0search0

---

# 18. Understanding `hour`

The hour uses the normal 24-hour clock.

Examples:

```text
8 AM  → 8
12 PM → 12
3 PM  → 15
8 PM  → 20
11 PM → 23
```

So:

```js
hour: 20
```

means:

```text
8:00 PM
```

---

# 19. Understanding `minute`

Example:

```js
hour:
  20,

minute:
  30,
```

means:

```text
Every day at 8:30 PM
```

---

# 20. Daily Reminder Example

```text
Goal:
Remind me to exercise every day at 6:30 AM
```

Code:

```js
await Notifications
  .scheduleNotificationAsync({

    content: {

      title:
        'Exercise Time',

      body:
        'Start your workout.',

    },

    trigger: {

      type:
        Notifications
          .SchedulableTriggerInputTypes
          .DAILY,

      hour:
        6,

      minute:
        30,

    },

  });
```

---

# 21. `WEEKLY`

A weekly trigger means:

```text
Every week
on a particular weekday
at a particular time
```

Example:

```text
Every Monday
9:00 AM
```

---

# 22. Weekly Trigger Example

```js
await Notifications
  .scheduleNotificationAsync({

    content: {

      title:
        'Weekly Meeting',

      body:
        'Team meeting starts now.',

    },

    trigger: {

      type:
        Notifications
          .SchedulableTriggerInputTypes
          .WEEKLY,

      weekday:
        2,

      hour:
        9,

      minute:
        0,

    },

  });
```

Expo SDK 54 specifies weekly weekday values from:

```text
1 through 7
```

where:

```text
1 = Sunday
```

Therefore:

```text
1 Sunday
2 Monday
3 Tuesday
4 Wednesday
5 Thursday
6 Friday
7 Saturday
```

citeturn0search0

---

# 23. Weekly Reminder Examples

### Every Monday at 9 AM

```js
weekday: 2,
hour: 9,
minute: 0,
```

### Every Friday at 6 PM

```js
weekday: 6,
hour: 18,
minute: 0,
```

### Every Sunday at 10 AM

```js
weekday: 1,
hour: 10,
minute: 0,
```

---

# 24. Common Weekly Mistake

Developers often assume:

```text
1 = Monday
```

for the Expo weekly trigger.

But SDK 54 documentation specifies:

```text
1 = Sunday
```

So always map weekdays carefully. citeturn0search0

---

# 25. `MONTHLY`

A monthly trigger means:

```text
Every month
on a specific day
at a specific time
```

Example:

```text
1st of every month
9:00 AM
```

Conceptually:

```text
January 1
February 1
March 1
April 1
...
```

---

# 26. Monthly Trigger Example

```js
await Notifications
  .scheduleNotificationAsync({

    content: {

      title:
        'Monthly Reminder',

      body:
        'Submit your monthly report.',

    },

    trigger: {

      type:
        Notifications
          .SchedulableTriggerInputTypes
          .MONTHLY,

      day:
        1,

      hour:
        9,

      minute:
        0,

    },

  });
```

Expo SDK 54 documents Android `MonthlyNotificationTrigger` with `day`, `hour`, and `minute`. citeturn0search0

---

# 27. Monthly Use Cases

Good examples:

```text
Rent reminder
Subscription reminder
Monthly report
Monthly bill
Monthly backup reminder
```

Example:

```text
Every month on the 5th at 10 AM
```

---

# 28. `YEARLY`

A yearly trigger means:

```text
Every year
on a particular date
at a particular time
```

Example:

```text
January 1
12:00 PM
```

---

# 29. Yearly Trigger Example

```js
await Notifications
  .scheduleNotificationAsync({

    content: {

      title:
        'New Year',

      body:
        'Happy New Year!',

    },

    trigger: {

      type:
        Notifications
          .SchedulableTriggerInputTypes
          .YEARLY,

      month:
        0,

      day:
        1,

      hour:
        0,

      minute:
        0,

    },

  });
```

Remember:

```text
JavaScript Date month indexing:
January = 0
```

Expo's SDK 54 yearly trigger documentation also notes that date properties use JavaScript `Date` ranges, where January is represented as `0`. citeturn0search0

---

# 30. Yearly Use Cases

Examples:

```text
Birthday reminder
Anniversary
Yearly subscription renewal
New Year reminder
Annual document renewal
```

---

# 31. Calendar Trigger

A calendar trigger is more flexible.

It allows notification delivery when specified date components match.

Expo SDK 54 documents `CalendarTriggerInput` for iOS and provides components such as:

```text
year
month
day
weekday
hour
minute
second
timezone
```

with `repeats` controlling repeated delivery. citeturn0search0

---

# 32. Calendar Trigger Example

Conceptually:

```js
await Notifications
  .scheduleNotificationAsync({

    content: {

      title:
        'Calendar Reminder',

      body:
        'Your event starts now.',

    },

    trigger: {

      type:
        Notifications
          .SchedulableTriggerInputTypes
          .CALENDAR,

      year:
        2026,

      month:
        8,

      day:
        25,

      hour:
        10,

      minute:
        30,

      repeats:
        false,

    },

  });
```

Calendar-trigger fields must be interpreted according to the platform/API's documented date-component conventions. Validate the trigger on the target platform.

---

# 33. Calendar Matching

The powerful idea behind calendar triggers is:

```text
Match specified components
```

For example:

```text
hour = 9
minute = 0
weekday = Monday
```

can represent:

```text
Every Monday at 9:00 AM
```

while:

```text
year
month
day
hour
minute
```

can identify a specific calendar date/time.

---

# 34. Calendar Trigger vs Daily Trigger

These can look similar.

### Daily

```js
type:
  DAILY,

hour:
  9,

minute:
  0,
```

means:

```text
Every day at 9 AM
```

### Calendar

```text
Specific date components
```

can be used when you need more calendar-specific matching.

For simple daily Android scheduling, use the dedicated daily trigger.

For more flexible calendar matching, calendar triggers are useful where supported.

---

# 35. Platform Availability Matters

Not every trigger type behaves identically across Android and iOS.

Expo SDK 54 documents:

```text
DailyNotificationTrigger
Android
```

and notes that the same functionality on iOS is achieved with:

```text
CalendarNotificationTrigger
```

Similarly, weekly/monthly/yearly dedicated trigger types are documented for Android, while iOS uses calendar matching. citeturn0search0

Therefore:

```text
Same requirement
      ↓
Platform-specific trigger representation
```

may sometimes be necessary.

---

# 36. `null` Trigger

A `null` trigger means:

```text
Deliver immediately
```

Example:

```js
await Notifications
  .scheduleNotificationAsync({

    content: {

      title:
        'Hello',

      body:
        'This notification should appear now.',

    },

    trigger:
      null,

  });
```

Expo SDK 54 documents a `null` trigger as scheduling a notification for immediate delivery. citeturn0search0

---

# 37. Immediate vs Scheduled

### Immediate

```js
trigger: null
```

### Future

```js
trigger: {
  ...
}
```

Mental model:

```text
trigger = null
     ↓
Now

trigger = object
     ↓
Future scheduling rule
```

---

# 38. `getNextTriggerDateAsync()`

This is one of the most useful advanced APIs.

```js
Notifications
  .getNextTriggerDateAsync()
```

It lets you calculate:

> **When would this trigger fire next?**

Expo SDK 54 documents that it returns a Unix timestamp in milliseconds or `null` when the notification will not trigger. citeturn0search0

---

# 39. Example: Next Daily Trigger

```js
const nextTrigger =
  await Notifications
    .getNextTriggerDateAsync({

      hour:
        9,

      minute:
        0,

    });

console.log(
  nextTrigger
);
```

Convert it:

```js
if (
  nextTrigger !== null
) {

  console.log(
    new Date(
      nextTrigger
    )
  );

}
```

---

# 40. Why `getNextTriggerDateAsync()` Is Useful

Suppose the user selects:

```text
Every day at 9:00 AM
```

Your UI can display:

```text
Next reminder:
Tomorrow at 9:00 AM
```

Instead of guessing, ask the notification API:

```text
Trigger
 ↓
getNextTriggerDateAsync()
 ↓
Next actual trigger
```

---

# 41. Validate a Trigger Before Scheduling

A useful pattern:

```js
const trigger = {

  type:
    Notifications
      .SchedulableTriggerInputTypes
      .DAILY,

  hour:
    9,

  minute:
    0,

};

const nextTrigger =
  await Notifications
    .getNextTriggerDateAsync(
      trigger
    );

if (
  nextTrigger === null
) {

  console.log(
    'Trigger is not valid / will not fire'
  );

  return;

}

console.log(
  'Next trigger:',
  new Date(
    nextTrigger
  )
);
```

Then schedule it.

---

# 42. Trigger Validation

A good production mindset is:

```text
Create trigger
      ↓
Validate trigger
      ↓
Calculate next trigger
      ↓
Schedule
```

This can catch invalid scheduling logic earlier.

---

# 43. Invalid Date Components

Expo SDK 54 documents validation for date components in schedulable trigger inputs.

For example:

```text
minute
```

must be in:

```text
0–59
```

and:

```text
second
```

must be in:

```text
0–59
```

Invalid values can result in an error. citeturn0search0

---

# 44. Example of Bad Trigger

```js
trigger: {

  type:
    Notifications
      .SchedulableTriggerInputTypes
      .DAILY,

  hour:
    9,

  minute:
    90,

}
```

This is invalid because:

```text
minute = 90
```

is outside the allowed range.

---

# 45. Better Input Validation

If the user enters:

```text
Hour: 9
Minute: 90
```

do not directly schedule it.

Validate:

```js
if (
  minute < 0 ||
  minute > 59
) {

  Alert.alert(
    'Invalid minute'
  );

  return;

}
```

Then create the trigger.

---

# 46. Exact Time Scheduling on Android

There is an important Android consideration.

Expo SDK 54 documents that starting with Android 12 (API 31), scheduling a notification for an exact time requires:

```text
SCHEDULE_EXACT_ALARM
```

permission/configuration in the Android manifest. citeturn0search0

Therefore:

```text
Android 12+
      ↓
Exact timing requirement
      ↓
Exact alarm permission/configuration
```

must be considered.

---

# 47. Why Exact Alarms Matter

Suppose your app says:

```text
Reminder exactly at 8:00:00 PM
```

Android may treat exact alarm scheduling differently from ordinary scheduled work.

For many normal reminders, slight timing flexibility is acceptable.

For truly time-critical alarms:

```text
Medication alarm
Alarm clock
Time-sensitive alert
```

you need to understand Android's exact-alarm rules and permissions.

---

# 48. Exact vs Normal Reminder

### Normal

```text
"Remind me around 8 PM."
```

Timing flexibility may be acceptable.

### Exact

```text
"Wake me exactly at 6:00 AM."
```

Exact alarm requirements become more important.

Always verify the current platform requirements before relying on exact timing.

---

# 49. Time Zones

Calendar scheduling becomes more complicated when time zones are involved.

Suppose the user schedules:

```text
Every day at 9 AM
```

and then travels:

```text
India
 ↓
United Kingdom
```

What should happen?

Possible meanings:

```text
9 AM local time
```

or:

```text
9 AM India time
```

These are different requirements.

---

# 50. Local-Time Reminder

Most user reminders mean:

```text
Every day at 9 AM
```

in the user's current/local time.

This is different from:

```text
Every day at 9 AM UTC
```

When designing a scheduling system, decide what the reminder's time zone should represent.

---

# 51. Timezone-Aware Calendar Trigger

Expo's calendar trigger input supports:

```js
timezone
```

where supported.

Conceptually:

```js
trigger: {

  type:
    Notifications
      .SchedulableTriggerInputTypes
      .CALENDAR,

  hour:
    9,

  minute:
    0,

  timezone:
    'Asia/Kolkata',

}
```

Use timezone explicitly when your application's business requirement depends on a specific zone.

---

# 52. Avoid Unnecessary Timezone Complexity

For a simple local reminder app:

```text
User chooses:
9:00 AM
```

you may not need to build a full timezone database system.

But for:

```text
Travel app
International calendar
Global business reminders
Cross-timezone scheduling
```

timezone handling becomes important.

---

# 53. Daily Reminder Architecture

A simple reminder app:

```text
User selects:
8:00 PM
      ↓
Create daily trigger
      ↓
Calculate next trigger
      ↓
Schedule
      ↓
Save notification ID
```

This is enough for many local reminder applications.

---

# 54. Weekly Reminder Architecture

```text
User selects:
Monday
9:00 AM

      ↓

weekday = 2
hour = 9
minute = 0

      ↓

Validate

      ↓

Schedule

      ↓

Save identifier
```

---

# 55. Monthly Reminder Architecture

```text
User selects:
1st day
10:00 AM

      ↓

day = 1
hour = 10
minute = 0

      ↓

Validate

      ↓

Schedule

      ↓

Save identifier
```

---

# 56. Yearly Reminder Architecture

```text
User selects:
January 1
12:00 PM

      ↓

month = 0
day = 1
hour = 12
minute = 0

      ↓

Validate

      ↓

Schedule
```

---

# 57. Dynamic Trigger Builder

A real application may allow:

```text
Frequency:
[ Daily ▼ ]

Time:
[ 08:00 PM ]
```

Your code can build the trigger dynamically:

```js
function buildDailyTrigger(
  hour,
  minute
) {

  return {

    type:
      Notifications
        .SchedulableTriggerInputTypes
        .DAILY,

    hour,
    minute,

  };

}
```

Usage:

```js
const trigger =
  buildDailyTrigger(
    20,
    0
  );
```

---

# 58. Weekly Trigger Builder

```js
function buildWeeklyTrigger(
  weekday,
  hour,
  minute
) {

  return {

    type:
      Notifications
        .SchedulableTriggerInputTypes
        .WEEKLY,

    weekday,
    hour,
    minute,

  };

}
```

Usage:

```js
const trigger =
  buildWeeklyTrigger(
    2,
    9,
    0
  );
```

Meaning:

```text
Monday at 9:00 AM
```

---

# 59. Monthly Trigger Builder

```js
function buildMonthlyTrigger(
  day,
  hour,
  minute
) {

  return {

    type:
      Notifications
        .SchedulableTriggerInputTypes
        .MONTHLY,

    day,
    hour,
    minute,

  };

}
```

---

# 60. Yearly Trigger Builder

```js
function buildYearlyTrigger(
  month,
  day,
  hour,
  minute
) {

  return {

    type:
      Notifications
        .SchedulableTriggerInputTypes
        .YEARLY,

    month,
    day,
    hour,
    minute,

  };

}
```

---

# 61. Generic Trigger Builder

A larger application can centralize trigger creation:

```js
function buildTrigger(
  config
) {

  switch (
    config.frequency
  ) {

    case 'daily':

      return {

        type:
          Notifications
            .SchedulableTriggerInputTypes
            .DAILY,

        hour:
          config.hour,

        minute:
          config.minute,

      };

    case 'weekly':

      return {

        type:
          Notifications
            .SchedulableTriggerInputTypes
            .WEEKLY,

        weekday:
          config.weekday,

        hour:
          config.hour,

        minute:
          config.minute,

      };

    case 'monthly':

      return {

        type:
          Notifications
            .SchedulableTriggerInputTypes
            .MONTHLY,

        day:
          config.day,

        hour:
          config.hour,

        minute:
          config.minute,

      };

    default:

      throw new Error(
        'Unsupported frequency'
      );

  }

}
```

This separates:

```text
UI
```

from:

```text
Expo notification trigger structure
```

---

# 62. Saving the Notification Identifier

When scheduling:

```js
const notificationId =
  await Notifications
    .scheduleNotificationAsync({
      content,
      trigger,
    });
```

Save it:

```js
const reminder = {

  id:
    'reminder-101',

  notificationId,

  title:
    'Study',

  frequency:
    'daily',

};
```

This allows your application to later:

```text
Cancel
Reschedule
Edit
Delete
```

the reminder.

---

# 63. Editing a Reminder

Suppose:

```text
Old:
Every day at 8 PM
```

User changes:

```text
Every day at 9 PM
```

A safe flow is:

```text
Find reminder
      ↓
Get old notificationId
      ↓
Cancel old notification
      ↓
Build new trigger
      ↓
Schedule new notification
      ↓
Save new notificationId
```

---

# 64. Editing Example

```js
await Notifications
  .cancelScheduledNotificationAsync(
    reminder.notificationId
  );

const newId =
  await Notifications
    .scheduleNotificationAsync({

      content:
        reminder.content,

      trigger:
        newTrigger,

    });

reminder.notificationId =
  newId;
```

This connects advanced scheduling with notification management.

---

# 65. Delete Reminder

When deleting a reminder:

```text
Delete application reminder
        +
Cancel scheduled notification
```

Example:

```js
await Notifications
  .cancelScheduledNotificationAsync(
    reminder.notificationId
  );
```

Then remove the reminder from your application's storage.

---

# 66. List Scheduled Notifications

Use:

```js
const scheduled =
  await Notifications
    .getAllScheduledNotificationsAsync();

console.log(
  scheduled
);
```

Expo SDK 54 documents this API as returning all scheduled notification requests. citeturn0search0

This is useful for debugging and synchronization.

---

# 67. Build a Schedule Debug Screen

For development:

```text
Scheduled Notifications

1. Study
   Daily 8:00 PM
   ID: abc123

2. Water
   Every 30 minutes
   ID: def456

3. Meeting
   Monday 9:00 AM
   ID: xyz789
```

This makes scheduling behavior much easier to inspect.

---

# 68. Trigger Debugging

For each reminder, log:

```js
console.log({
  reminderId,
  trigger,
  notificationId,
});
```

And calculate:

```js
const next =
  await Notifications
    .getNextTriggerDateAsync(
      trigger
    );

console.log(
  'Next:',
  next === null
    ? null
    : new Date(next)
);
```

---

# 69. Repeating Notification Mental Model

A repeating notification is not:

```text
One notification copied many times
```

Think:

```text
One scheduled request
        ↓
Repeating trigger
        ↓
OS calculates next occurrence
        ↓
Notification fires
        ↓
Next occurrence
```

This is important when managing and cancelling repeating reminders.

---

# 70. Time Interval vs Calendar Scheduling

Use this decision table:

| Requirement | Recommended trigger |
|---|---|
| 10 seconds from now | `TIME_INTERVAL` |
| 30 minutes from now | `TIME_INTERVAL` |
| Every 30 minutes | `TIME_INTERVAL` + `repeats` |
| Tomorrow at 9 AM | `DATE` |
| Specific future date | `DATE` |
| Every day at 8 PM | `DAILY` |
| Every Monday at 9 AM | `WEEKLY` |
| Every month on 1st | `MONTHLY` |
| Every year on Jan 1 | `YEARLY` |
| Flexible calendar component matching | `CALENDAR` |

---

# 71. Trigger Selection Rule

Ask:

> Is the requirement relative or calendar-based?

### Relative

```text
10 minutes from now
```

Use:

```text
TIME_INTERVAL
```

### Absolute

```text
August 25 at 10 AM
```

Use:

```text
DATE
```

### Repeating calendar rule

```text
Every Monday at 9 AM
```

Use:

```text
WEEKLY
```

or the appropriate calendar representation for the target platform.

---

# 72. Immediate Notification vs Local Scheduling

Remember:

```js
trigger: null
```

is still a local notification request.

It is simply delivered immediately rather than waiting for a future trigger.

So:

```text
Local notification
   ├── Immediate
   └── Scheduled
```

---

# 73. Exact Date vs Repeating Rule

Consider:

```text
Reminder:
August 25, 2026 at 9 AM
```

This is:

```text
DATE
```

But:

```text
Reminder:
Every August 25 at 9 AM
```

is:

```text
YEARLY
```

This distinction is important.

---

# 74. One-Time vs Repeating

### One-time

```text
Tomorrow at 9 AM
```

fires:

```text
once
```

### Repeating

```text
Every day at 9 AM
```

fires:

```text
repeatedly
```

Your trigger type should represent the actual business requirement.

---

# 75. Common Mistake - Using Time Interval for Everything

Bad design:

```text
Every day at 8 PM
```

implemented as:

```text
24 hours
```

This can represent a recurring interval, but it is conceptually different from:

```text
8 PM local calendar time
```

Calendar-based reminders are often better represented by calendar/day/time components when that is the actual requirement.

---

# 76. Common Mistake - Wrong Weekday

Remember SDK 54 weekly mapping:

```text
1 = Sunday
2 = Monday
3 = Tuesday
4 = Wednesday
5 = Thursday
6 = Friday
7 = Saturday
```

Always verify the weekday mapping before scheduling. citeturn0search0

---

# 77. Common Mistake - JavaScript Month Indexing

For date objects and yearly trigger month values:

```text
January = 0
February = 1
March = 2
...
December = 11
```

Therefore:

```js
month: 0
```

means:

```text
January
```

not February.

Expo SDK 54 documents JavaScript `Date` ranges for these date properties. citeturn0search0

---

# 78. Common Mistake - Invalid Minute

Bad:

```js
minute: 75
```

Correct range:

```text
0–59
```

Validate user input before scheduling.

---

# 79. Common Mistake - Assuming Scheduling Means Presentation

This is a very important point.

```js
scheduleNotificationAsync()
```

schedules the notification.

It does not by itself define all presentation behavior.

Expo's SDK 54 documentation notes that for a notification to be presented, the notification handler must return an appropriate notification behavior. citeturn0search0

So think:

```text
Schedule
 ↓
Trigger fires
 ↓
Presentation rules
 ↓
Notification appears
```

---

# 80. Common Mistake - Forgetting Android Channel

For Android:

```text
Android 8+
 ↓
Notification channel
```

And for Android 13+:

```text
Channel
+
Notification permission
```

are especially important.

---

# 81. Common Mistake - Forgetting to Save Notification IDs

If you do:

```js
await Notifications
  .scheduleNotificationAsync(...)
```

but throw away the returned identifier, later you may not know which scheduled notification belongs to which reminder.

Prefer:

```js
const notificationId =
  await Notifications
    .scheduleNotificationAsync(...);
```

Then persist it.

---

# 82. Common Mistake - Not Cancelling Old Schedules

If the user edits:

```text
8 PM → 9 PM
```

and you simply schedule a new notification, you may end up with:

```text
8 PM
+
9 PM
```

instead of only:

```text
9 PM
```

Correct flow:

```text
Cancel old
 ↓
Schedule new
 ↓
Save new ID
```

---

# 83. Production Reminder Architecture

A robust reminder model:

```js
{
  id:
    'reminder-101',

  title:
    'Study React Native',

  body:
    'Continue Notifications',

  frequency:
    'daily',

  hour:
    20,

  minute:
    0,

  notificationId:
    'expo-generated-id',

  enabled:
    true,
}
```

Now your application owns the business data while Expo owns the scheduled notification.

---

# 84. Application State vs OS State

This distinction is critical.

### Application state

```text
Reminder enabled
Reminder title
Frequency
notificationId
```

### OS notification state

```text
Scheduled notification
Channel
Permission
Presentation
```

Your application should synchronize these.

---

# 85. Synchronization Flow

```text
App Reminder
      ↓
notificationId
      ↓
OS scheduled notification
```

When user edits:

```text
App changes
      ↓
Cancel old OS notification
      ↓
Schedule new OS notification
      ↓
Update notificationId
```

This is the core architecture for a local reminder app.

---

# 86. Complete Reminder Creation Flow

```text
User creates reminder
        ↓
Validate input
        ↓
Check notification permission
        ↓
Build trigger
        ↓
Calculate next trigger
        ↓
Schedule notification
        ↓
Receive notificationId
        ↓
Save reminder + notificationId
        ↓
Display reminder in UI
```

---

# 87. Complete Reminder Edit Flow

```text
User edits reminder
        ↓
Validate new values
        ↓
Find old notificationId
        ↓
Cancel old notification
        ↓
Build new trigger
        ↓
Calculate next trigger
        ↓
Schedule new notification
        ↓
Save new notificationId
```

---

# 88. Complete Reminder Delete Flow

```text
User deletes reminder
        ↓
Find notificationId
        ↓
Cancel scheduled notification
        ↓
Delete reminder from storage
        ↓
Update UI
```

---

# 89. Practical Example - Study Reminder

Requirements:

```text
Title:
Study React Native

Schedule:
Every Monday at 8 PM

Action:
Open Study screen
```

Trigger:

```js
const trigger = {

  type:
    Notifications
      .SchedulableTriggerInputTypes
      .WEEKLY,

  weekday:
    2,

  hour:
    20,

  minute:
    0,

};
```

Then:

```js
const next =
  await Notifications
    .getNextTriggerDateAsync(
      trigger
    );
```

Then schedule.

---

# 90. Practical Example - Water Reminder

Requirements:

```text
Every 30 minutes
```

Use:

```js
const trigger = {

  type:
    Notifications
      .SchedulableTriggerInputTypes
      .TIME_INTERVAL,

  seconds:
    30 * 60,

  repeats:
    true,

};
```

This is a good example of a relative repeating schedule.

---

# 91. Practical Example - Birthday

Requirements:

```text
Every year
January 15
9 AM
```

Use:

```js
const trigger = {

  type:
    Notifications
      .SchedulableTriggerInputTypes
      .YEARLY,

  month:
    0,

  day:
    15,

  hour:
    9,

  minute:
    0,

};
```

---

# 92. Practical Example - Monthly Bill

Requirements:

```text
Every month
5th
10 AM
```

Use:

```js
const trigger = {

  type:
    Notifications
      .SchedulableTriggerInputTypes
      .MONTHLY,

  day:
    5,

  hour:
    10,

  minute:
    0,

};
```

---

# 93. Practical Example - Weekly Team Meeting

Requirements:

```text
Every Friday
4:30 PM
```

Use:

```js
const trigger = {

  type:
    Notifications
      .SchedulableTriggerInputTypes
      .WEEKLY,

  weekday:
    6,

  hour:
    16,

  minute:
    30,

};
```

Because:

```text
6 = Friday
```

according to Expo's SDK 54 weekly mapping. citeturn0search0

---

# 94. Practical Example - One-Time Event

Requirements:

```text
August 30, 2026
7:00 PM
```

Create a `Date`:

```js
const date =
  new Date(
    2026,
    7,
    30,
    19,
    0
  );
```

Then:

```js
const trigger = {

  type:
    Notifications
      .SchedulableTriggerInputTypes
      .DATE,

  date,

};
```

---

# 95. Advanced Scheduling Utility

```js
import * as Notifications
  from 'expo-notifications';

export async function
scheduleReminder({
  title,
  body,
  trigger,
}) {

  const next =
    await Notifications
      .getNextTriggerDateAsync(
        trigger
      );

  if (
    next === null
  ) {

    throw new Error(
      'Invalid or non-triggering schedule'
    );

  }

  const notificationId =
    await Notifications
      .scheduleNotificationAsync({

        content: {

          title,
          body,

        },

        trigger,

      });

  return {

    notificationId,

    nextTrigger:
      new Date(next),

  };

}
```

This gives your application:

```text
notificationId
+
nextTrigger
```

in one operation.

---

# 96. Why This Utility Is Useful

Instead of every screen doing:

```text
Validate
 ↓
Calculate next trigger
 ↓
Schedule
 ↓
Save ID
```

you centralize it:

```js
scheduleReminder(...)
```

Then:

```text
Reminder screen
Task screen
Medication screen
Study screen
```

can all use the same service.

---

# 97. Advanced Scheduling Architecture

A production-oriented architecture might be:

```text
UI
 │
 ↓
Reminder Service
 │
 ├── Permission check
 │
 ├── Trigger builder
 │
 ├── Trigger validation
 │
 ├── Next-trigger calculation
 │
 ├── Schedule
 │
 ├── Cancel
 │
 └── Reschedule
 │
 ↓
expo-notifications
 │
 ↓
Operating System
```

This is much easier to maintain than putting notification logic throughout screens.

---

# Part 13 - Complete Notification Architecture & Real-World Local Notification Project with `expo-notifications`

Until now, each part focused on one notification concept.

Now we combine those concepts into one **real-world local notification architecture**.

The goal is not simply to learn:

```js
scheduleNotificationAsync()
```

The goal is to understand how a production-style notification feature can be organized.

We will build the architecture of a:

# **Local Reminder App**

The application will support:

```text
Create reminder
        ↓
Request permission
        ↓
Create Android channel
        ↓
Build notification content
        ↓
Build trigger
        ↓
Schedule notification
        ↓
Save notification ID
        ↓
Show reminder in UI
        ↓
Notification fires
        ↓
User interacts
        ↓
Handle response
        ↓
Update reminder state
        ↓
Update badge
```

This is where the previous 12 parts come together.

---

# 1. What Are We Building?

Imagine an application called:

```text
Reminder App
```

The user can create:

```text
Study React Native
Drink Water
Team Meeting
Pay Electricity Bill
Workout
```

Each reminder has:

```text
Title
Description
Schedule
Enabled / Disabled
Notification ID
```

Example:

```text
┌─────────────────────────────────┐
│ Study React Native              │
│ Learn Notifications             │
│ Every day at 8:00 PM            │
│                                 │
│              [ Enabled ]        │
└─────────────────────────────────┘
```

---

# 2. Feature Requirements

Our application will support:

### Permission

```text
Check permission
Request permission
Handle denial
Open Settings
```

### Android

```text
Notification channel
Importance
Sound
Vibration
```

### Scheduling

```text
Immediate
Time interval
Daily
Weekly
Monthly
Yearly
```

### Management

```text
Create
Read
Update
Delete
Enable
Disable
Cancel
Reschedule
```

### Interaction

```text
Open
Mark as Complete
Snooze
```

### Presentation

```text
Badge
Sound
Vibration
Foreground behavior
```

---

# 3. Why Architecture Matters

A beginner may write:

```text
Screen
 ↓
scheduleNotificationAsync()
 ↓
Done
```

That works for a small demo.

But a real application can quickly become:

```text
Screen
 ↓
Permission
 ↓
Channel
 ↓
Trigger
 ↓
Schedule
 ↓
Storage
 ↓
Listener
 ↓
Navigation
 ↓
Badge
 ↓
Reschedule
 ↓
Cancel
```

If all of this code is placed inside one screen, it becomes difficult to maintain.

Therefore, separate responsibilities.

---

# 4. Recommended Architecture

A simple structure:

```text
src/
│
├── notifications/
│   ├── notificationConfig.js
│   ├── notificationPermissions.js
│   ├── notificationChannels.js
│   ├── notificationScheduler.js
│   ├── notificationActions.js
│   └── notificationService.js
│
├── storage/
│   └── reminderStorage.js
│
├── screens/
│   ├── ReminderListScreen.js
│   └── ReminderFormScreen.js
│
└── utils/
    └── triggerBuilder.js
```

You do not have to use this exact structure.

The important idea is:

```text
Separate notification responsibilities.
```

---

# 5. Responsibility of Each File

### `notificationConfig.js`

Contains:

```text
Handler configuration
Notification categories
Constants
```

### `notificationPermissions.js`

Handles:

```text
Check permission
Request permission
Open settings
```

### `notificationChannels.js`

Handles Android:

```text
Channels
Importance
Sound
Vibration
```

### `notificationScheduler.js`

Handles:

```text
Schedule
Cancel
Reschedule
Get scheduled notifications
```

### `notificationActions.js`

Handles:

```text
Notification actions
Response identifiers
```

### `notificationService.js`

Acts as a higher-level API:

```text
Create reminder
Update reminder
Delete reminder
Enable reminder
Disable reminder
```

---

# 6. Application Flow

The complete application flow:

```text
                 Reminder App
                      │
          ┌───────────┴───────────┐
          ↓                       ↓
      Reminder UI           Notification System
          │                       │
          ↓                       ↓
      User input             Permissions
          │                       │
          ↓                    Channels
       Service                     │
          │                    Categories
          ↓                       │
      Scheduler               Scheduler
          │                       │
          └───────────┬───────────┘
                      ↓
                  OS Scheduler
                      ↓
                 Notification
                      ↓
                   Actions
                      ↓
                   App Logic
```

---

# 7. Step 1 - Install Expo Notifications

For an Expo SDK 54 project:

```bash
npx expo install expo-notifications
```

Using:

```bash
npx expo install
```

helps install a package version compatible with your Expo SDK.

---

# 8. Step 2 - Import Notifications

```js
import * as Notifications
  from 'expo-notifications';
```

For a local notification application, most notification operations come through this namespace.

---

# 9. Step 3 - Configure the Notification Handler

Create:

```text
notificationConfig.js
```

Example:

```js
import * as Notifications
  from 'expo-notifications';

Notifications.setNotificationHandler({

  handleNotification:
    async () => ({

      shouldShowBanner:
        true,

      shouldShowList:
        true,

      shouldPlaySound:
        true,

      shouldSetBadge:
        true,

    }),

});
```

This defines foreground presentation behavior.

---

# 10. Why Configure the Handler Early?

If your application receives a notification while it is running in the foreground, the handler determines the presentation behavior.

Think:

```text
Notification arrives
        ↓
App foreground?
        ↓
Handler
        ↓
Presentation decision
```

Configure this as part of application initialization rather than randomly inside individual screens.

---

# 11. Step 4 - Create Android Channel

Android 8+ uses notification channels.

Create:

```text
notificationChannels.js
```

Example:

```js
import {
  Platform,
} from 'react-native';

import * as Notifications
  from 'expo-notifications';

export async function
setupNotificationChannels() {

  if (
    Platform.OS !== 'android'
  ) {

    return;

  }

  await Notifications
    .setNotificationChannelAsync(
      'reminders',
      {

        name:
          'Reminders',

        importance:
          Notifications
            .AndroidImportance
            .HIGH,

        sound:
          'default',

        vibrationPattern:
          [0, 200, 100, 200],

      }
    );

}
```

---

# 12. Why Use a Channel?

The channel groups Android notifications into a user-configurable category.

For example:

```text
Reminders
Messages
Updates
Promotions
```

Our application can start with:

```text
reminders
```

---

# 13. Channel Setup Should Happen Early

A common initialization flow:

```text
App starts
 ↓
setupNotificationChannels()
 ↓
Check notification permission
 ↓
Application ready
```

On Android, channel creation is also relevant to Android 13+ notification permission behavior.

---

# 14. Step 5 - Permission Service

Create:

```text
notificationPermissions.js
```

Example:

```js
import {
  Linking,
} from 'react-native';

import * as Notifications
  from 'expo-notifications';

export async function
getNotificationPermissions() {

  return Notifications
    .getPermissionsAsync();

}

export async function
requestNotificationPermissions() {

  return Notifications
    .requestPermissionsAsync();

}

export async function
ensureNotificationPermission() {

  const current =
    await Notifications
      .getPermissionsAsync();

  if (
    current.granted
  ) {

    return {
      status:
        'granted',

      permissions:
        current,
    };

  }

  if (
    !current.canAskAgain
  ) {

    return {
      status:
        'settings',

      permissions:
        current,
    };

  }

  const result =
    await Notifications
      .requestPermissionsAsync();

  if (
    result.granted
  ) {

    return {
      status:
        'granted',

      permissions:
        result,
    };

  }

  return {
    status:
      'denied',

    permissions:
      result,
  };

}

export async function
openNotificationSettings() {

  await Linking
    .openSettings();

}
```

---

# 15. Why Centralize Permissions?

Without a service, you might write permission code in:

```text
ReminderScreen
SettingsScreen
HomeScreen
NotificationScreen
```

This creates duplicated logic.

Instead:

```text
All screens
    ↓
Permission Service
    ↓
expo-notifications
```

---

# 16. Step 6 - Trigger Builder

Create:

```text
triggerBuilder.js
```

Example:

```js
import * as Notifications
  from 'expo-notifications';

export function
buildTrigger(config) {

  switch (
    config.frequency
  ) {

    case 'interval':

      return {

        type:
          Notifications
            .SchedulableTriggerInputTypes
            .TIME_INTERVAL,

        seconds:
          config.seconds,

        repeats:
          config.repeats ??
          false,

      };

    case 'daily':

      return {

        type:
          Notifications
            .SchedulableTriggerInputTypes
            .DAILY,

        hour:
          config.hour,

        minute:
          config.minute,

      };

    case 'weekly':

      return {

        type:
          Notifications
            .SchedulableTriggerInputTypes
            .WEEKLY,

        weekday:
          config.weekday,

        hour:
          config.hour,

        minute:
          config.minute,

      };

    case 'monthly':

      return {

        type:
          Notifications
            .SchedulableTriggerInputTypes
            .MONTHLY,

        day:
          config.day,

        hour:
          config.hour,

        minute:
          config.minute,

      };

    case 'yearly':

      return {

        type:
          Notifications
            .SchedulableTriggerInputTypes
            .YEARLY,

        month:
          config.month,

        day:
          config.day,

        hour:
          config.hour,

        minute:
          config.minute,

      };

    case 'date':

      return {

        type:
          Notifications
            .SchedulableTriggerInputTypes
            .DATE,

        date:
          config.date,

      };

    default:

      throw new Error(
        `Unsupported frequency: ${config.frequency}`
      );

  }

}
```

---

# 17. Why Build Triggers Separately?

Your UI might say:

```text
Frequency:
Daily
```

The notification API needs:

```js
{
  type: DAILY,
  hour: 20,
  minute: 0,
}
```

The trigger builder converts:

```text
Application data
        ↓
Expo trigger format
```

This separation is extremely useful.

---

# 18. Example Trigger Data

Application data:

```js
{
  frequency:
    'daily',

  hour:
    20,

  minute:
    0,
}
```

Trigger builder produces:

```js
{
  type:
    Notifications
      .SchedulableTriggerInputTypes
      .DAILY,

  hour:
    20,

  minute:
    0,
}
```

---

# 19. Step 7 - Notification Scheduler

Create:

```text
notificationScheduler.js
```

Example:

```js
import * as Notifications
  from 'expo-notifications';

export async function
scheduleNotification({
  content,
  trigger,
}) {

  const next =
    await Notifications
      .getNextTriggerDateAsync(
        trigger
      );

  if (
    next === null
  ) {

    throw new Error(
      'Invalid or non-triggering notification trigger'
    );

  }

  const notificationId =
    await Notifications
      .scheduleNotificationAsync({

        content,

        trigger,

      });

  return {

    notificationId,

    nextTrigger:
      new Date(next),

  };

}
```

---

# 20. What Does the Scheduler Do?

The scheduler handles:

```text
Trigger
 ↓
Validate
 ↓
Calculate next trigger
 ↓
Schedule
 ↓
Return ID
```

This keeps scheduling logic out of UI components.

---

# 21. Step 8 - Cancel Notification

Add:

```js
export async function
cancelNotification(
  notificationId
) {

  if (
    !notificationId
  ) {

    return;

  }

  await Notifications
    .cancelScheduledNotificationAsync(
      notificationId
    );

}
```

Now the service can cancel a specific notification.

---

# 22. Step 9 - Reschedule Notification

A reminder edit should follow:

```text
Old notification
      ↓
Cancel
      ↓
Create new trigger
      ↓
Schedule
      ↓
Save new ID
```

Example:

```js
export async function
rescheduleNotification({

  oldNotificationId,

  content,

  trigger,

}) {

  await cancelNotification(
    oldNotificationId
  );

  return scheduleNotification({
    content,
    trigger,
  });

}
```

---

# 23. Why Cancel Before Rescheduling?

Suppose the old reminder is:

```text
8 PM
```

User changes it to:

```text
9 PM
```

If you simply schedule the new one:

```text
8 PM
+
9 PM
```

Both may remain active.

Correct:

```text
Cancel 8 PM
      ↓
Schedule 9 PM
```

---

# 24. Step 10 - Notification Categories

From Part 9, interactive actions can be defined through categories.

Example:

```js
await Notifications
  .setNotificationCategoryAsync(
    'reminder',
    [
      {
        identifier:
          'COMPLETE',

        buttonTitle:
          'Complete',
      },

      {
        identifier:
          'SNOOZE',

        buttonTitle:
          'Snooze',
      },
    ]
  );
```

Now:

```text
Reminder notification
        ↓
Complete
Snooze
```

---

# 25. Add Category to Notification

When scheduling:

```js
const content = {

  title:
    'Study Reminder',

  body:
    'Continue your React Native lesson.',

  categoryIdentifier:
    'reminder',

};
```

Now the notification is associated with the category.

---

# 26. Step 11 - Reminder Model

Our application can store:

```js
const reminder = {

  id:
    'reminder-001',

  title:
    'Study React Native',

  body:
    'Continue Notifications.',

  frequency:
    'daily',

  hour:
    20,

  minute:
    0,

  enabled:
    true,

  notificationId:
    null,

};
```

Notice:

```text
Application ID
```

and:

```text
Notification ID
```

are different.

---

# 27. Application ID vs Notification ID

### Application ID

```text
reminder-001
```

belongs to your application.

### Notification ID

```text
generated-by-expo
```

identifies the scheduled notification request.

Think:

```text
Reminder
  │
  ├── id
  │
  └── notificationId
```

---

# 28. Why Store Both?

Your database/storage needs to know:

```text
Which reminder?
```

Expo needs:

```text
Which scheduled notification?
```

Therefore:

```text
Application
 ↓
reminder.id

Expo
 ↓
notificationId
```

---

# 29. Step 12 - Reminder Storage

For a simple local project, you can use:

```text
AsyncStorage
```

or another local persistence solution.

Example:

```js
import AsyncStorage
  from '@react-native-async-storage/async-storage';

const STORAGE_KEY =
  '@reminder_app/reminders';
```

---

# 30. Save Reminders

```js
export async function
saveReminders(
  reminders
) {

  await AsyncStorage.setItem(

    STORAGE_KEY,

    JSON.stringify(
      reminders
    )

  );

}
```

---

# 31. Load Reminders

```js
export async function
loadReminders() {

  const value =
    await AsyncStorage.getItem(
      STORAGE_KEY
    );

  if (!value) {

    return [];

  }

  return JSON.parse(
    value
  );

}
```

Now:

```text
App
 ↓
Load reminders
 ↓
Render UI
```

---

# 32. Important: Storage Is Not the Notification Scheduler

This distinction is critical.

AsyncStorage stores:

```text
Application data
```

Expo Notifications manages:

```text
Scheduled notifications
```

Therefore:

```text
AsyncStorage
     ↓
Reminder record

expo-notifications
     ↓
Scheduled OS notification
```

They are two different systems.

---

# 33. Synchronization Problem

Suppose storage contains:

```js
{
  id:
    'reminder-1',

  notificationId:
    'abc',

  enabled:
    true,
}
```

But the scheduled notification was cancelled externally.

Now:

```text
Storage:
notification exists

OS:
notification does not exist
```

This is a synchronization problem.

---

# 34. Synchronization Strategy

On app startup:

```text
Load reminders
      ↓
Get scheduled notifications
      ↓
Compare notification IDs
      ↓
Repair inconsistencies if necessary
```

This is a production-oriented concept.

---

# 35. Get Scheduled Notifications

```js
const scheduled =
  await Notifications
    .getAllScheduledNotificationsAsync();

console.log(
  scheduled
);
```

You can inspect:

```text
identifier
content
trigger
```

and compare them with your stored reminder records.

---

# 36. Step 13 - Create Reminder Service

Now combine everything.

```js
import {
  ensureNotificationPermission,
} from './notificationPermissions';

import {
  buildTrigger,
} from '../utils/triggerBuilder';

import {
  scheduleNotification,
} from './notificationScheduler';

export async function
createReminder(
  reminder
) {

  const permission =
    await ensureNotificationPermission();

  if (
    permission.status !==
    'granted'
  ) {

    return {
      success:
        false,

      reason:
        permission.status,

    };

  }

  const trigger =
    buildTrigger(
      reminder
    );

  const result =
    await scheduleNotification({

      content: {

        title:
          reminder.title,

        body:
          reminder.body,

        categoryIdentifier:
          'reminder',

      },

      trigger,

    });

  return {

    success:
      true,

    notificationId:
      result.notificationId,

    nextTrigger:
      result.nextTrigger,

  };

}
```

---

# 37. Why This Service Is Useful

The screen no longer needs to know:

```text
Permission API
Trigger API
Scheduling API
Category
Notification ID
```

It simply calls:

```js
createReminder(
  reminder
);
```

This is a major architectural improvement.

---

# 38. Reminder Creation Flow

```text
ReminderFormScreen
        ↓
createReminder()
        ↓
Permission
        ↓
Trigger Builder
        ↓
Scheduler
        ↓
Expo Notifications
        ↓
Notification ID
        ↓
Save Reminder
```

---

# 39. Step 14 - Complete Reminder Creation

Example:

```js
const reminder = {

  id:
    crypto.randomUUID(),

  title:
    'Study React Native',

  body:
    'Continue learning notifications.',

  frequency:
    'daily',

  hour:
    20,

  minute:
    0,

  enabled:
    true,

};
```

Then:

```js
const result =
  await createReminder(
    reminder
  );
```

If successful:

```js
reminder.notificationId =
  result.notificationId;
```

Then save:

```js
await saveReminders(
  reminders
);
```

---

# 40. Full Creation Flow

```text
User taps:
[Save Reminder]
        ↓
Validate form
        ↓
Check permission
        ↓
Build trigger
        ↓
Calculate next trigger
        ↓
Schedule
        ↓
Receive notificationId
        ↓
Attach ID to reminder
        ↓
Save reminder
        ↓
Show success
```

---

# 41. Step 15 - Disable Reminder

Suppose the user toggles:

```text
Enabled → OFF
```

We should cancel the scheduled notification.

```js
await cancelNotification(
  reminder.notificationId
);
```

Then:

```js
reminder.enabled =
  false;

reminder.notificationId =
  null;
```

Then save.

---

# 42. Enable Reminder Again

When the user turns it back on:

```text
Permission
 ↓
Build trigger
 ↓
Schedule
 ↓
Get new notification ID
 ↓
Save ID
```

Example:

```js
const result =
  await createReminder(
    reminder
  );

reminder.enabled =
  true;

reminder.notificationId =
  result.notificationId;
```

---

# 43. Why Notification ID Changes

When you cancel:

```text
Old scheduled request
 ↓
Cancelled
```

When you schedule again:

```text
New scheduled request
 ↓
New notification ID
```

Therefore, update the stored identifier.

---

# 44. Step 16 - Edit Reminder

Suppose:

```text
Old:
8 PM

New:
9 PM
```

Flow:

```text
Find reminder
 ↓
Cancel old notification
 ↓
Update reminder data
 ↓
Build new trigger
 ↓
Schedule
 ↓
Save new notification ID
```

---

# 45. Edit Function

```js
export async function
updateReminder(
  reminder
) {

  if (
    reminder.notificationId
  ) {

    await cancelNotification(
      reminder.notificationId
    );

  }

  const result =
    await createReminder(
      reminder
    );

  if (
    !result.success
  ) {

    return result;

  }

  reminder.notificationId =
    result.notificationId;

  reminder.enabled =
    true;

  return {

    success:
      true,

    reminder,

  };

}
```

For a production app, keep the persistence update and scheduling operation coordinated so a failure does not leave the application in an inconsistent state.

---

# 46. Step 17 - Delete Reminder

Delete flow:

```text
User taps Delete
        ↓
Cancel notification
        ↓
Remove reminder from storage
        ↓
Update UI
```

Example:

```js
async function
deleteReminder(
  reminder
) {

  if (
    reminder.notificationId
  ) {

    await cancelNotification(
      reminder.notificationId
    );

  }

  const updated =
    reminders.filter(
      item =>
        item.id !==
        reminder.id
    );

  await saveReminders(
    updated
  );

}
```

---

# 47. Step 18 - Notification Response Listener

Now the notification actually fires.

We need to handle user interaction.

Example:

```js
const subscription =
  Notifications
    .addNotificationResponseReceivedListener(
      response => {

        const action =
          response.actionIdentifier;

        console.log(
          'Action:',
          action
        );

      }
    );
```

This should be configured at application level when possible.

---

# 48. Default Notification Tap

If the user taps the notification itself, the response may use:

```js
Notifications
  .DEFAULT_ACTION_IDENTIFIER
```

Example:

```js
if (
  response.actionIdentifier ===
  Notifications
    .DEFAULT_ACTION_IDENTIFIER
) {

  // Open reminder screen

}
```

---

# 49. Complete Action

Our category includes:

```text
COMPLETE
```

So:

```js
if (
  response.actionIdentifier ===
  'COMPLETE'
) {

  // Mark reminder complete

}
```

---

# 50. Snooze Action

Our category also includes:

```text
SNOOZE
```

When tapped:

```js
if (
  response.actionIdentifier ===
  'SNOOZE'
) {

  // Schedule a snoozed notification

}
```

For example:

```text
Snooze
 ↓
10 minutes
 ↓
New notification
```

---

# 51. Notification Data

Add application-specific data:

```js
content: {

  title:
    reminder.title,

  body:
    reminder.body,

  categoryIdentifier:
    'reminder',

  data: {

    reminderId:
      reminder.id,

  },

}
```

Now when the user interacts:

```js
const data =
  response.notification
    .request
    .content
    .data;
```

Then:

```js
const reminderId =
  data.reminderId;
```

This connects the OS notification back to your application record.

---

# 52. Why Notification Data Matters

Without data:

```text
User taps notification
 ↓
App knows:
"Some notification was tapped."
```

With data:

```text
User taps notification
 ↓
App knows:
"Reminder reminder-001 was tapped."
```

This is essential for deep navigation and action handling.

---

# 53. Response Architecture

```text
Notification
      ↓
User interaction
      ↓
Response Listener
      ↓
actionIdentifier
      +
data.reminderId
      ↓
Application logic
```

This is the bridge between the notification system and your application.

---

# 54. Open Reminder Screen

Suppose you use React Navigation or Expo Router.

Conceptually:

```js
if (
  actionIdentifier ===
  Notifications
    .DEFAULT_ACTION_IDENTIFIER
) {

  const reminderId =
    response.notification
      .request
      .content
      .data
      .reminderId;

  // Navigate to reminder
}
```

The exact navigation call depends on your routing setup.

---

# 55. Step 19 - Badge Management

Suppose the app tracks:

```text
Pending reminders:
3
```

You can synchronize:

```js
await Notifications
  .setBadgeCountAsync(3);
```

When all relevant items are complete:

```js
await Notifications
  .setBadgeCountAsync(0);
```

---

# 56. Badge Should Have a Source of Truth

Do not randomly do:

```js
badge++;
```

everywhere.

Instead:

```text
Reminder state
 ↓
Calculate unread/pending count
 ↓
Set badge
```

Example:

```js
const pendingCount =
  reminders.filter(
    reminder =>
      !reminder.completed
  ).length;

await Notifications
  .setBadgeCountAsync(
    pendingCount
  );
```

---

# 57. Step 20 - App Startup

At application startup:

```text
1. Configure handler
2. Setup Android channels
3. Setup notification categories
4. Check permission
5. Load reminders
6. Synchronize scheduled notifications
7. Configure listeners
8. Update badge
```

This is a strong initialization sequence.

---

# 58. Initialization Example

```js
async function
initializeNotifications() {

  await setupNotificationChannels();

  await setupNotificationCategories();

  const permissions =
    await getNotificationPermissions();

  console.log(
    'Permissions:',
    permissions
  );

}
```

Then call initialization from the application root.

---

# 59. Listener Cleanup

Listeners return subscriptions.

Example:

```js
useEffect(() => {

  const received =
    Notifications
      .addNotificationReceivedListener(
        notification => {

          console.log(
            'Received:',
            notification
          );

        }
      );

  const response =
    Notifications
      .addNotificationResponseReceivedListener(
        response => {

          console.log(
            'Response:',
            response
          );

        }
      );

  return () => {

    received.remove();
    response.remove();

  };

}, []);
```

This prevents listener leaks.

---

# 60. Why Cleanup Matters

Without cleanup:

```text
Component mounts
 ↓
Listener added

Component mounts again
 ↓
Another listener added
```

Eventually:

```text
One notification
 ↓
Multiple callbacks
```

Always remove subscriptions when the component owns their lifecycle.

---

# 61. App-Level Listeners

Notification listeners are often best placed at a high application level.

For example:

```text
App root
 ↓
Notification listeners
 ↓
Navigation / state update
```

instead of putting global listeners inside individual reminder cards.

---

# 62. Notification Architecture

At this point:

```text
                  App
                   │
        ┌──────────┴──────────┐
        ↓                     ↓
   Reminder UI          Notification System
        │                     │
        ↓                     ├── Permissions
 Reminder Service             ├── Channels
        │                     ├── Categories
        ↓                     ├── Handler
    Scheduler                 ├── Scheduler
        │                     ├── Listeners
        ↓                     ├── Badge
   Storage                    └── Responses
```

This is a scalable mental model.

---

# 63. Complete Reminder Service API

A useful service could expose:

```js
createReminder()
updateReminder()
deleteReminder()
enableReminder()
disableReminder()
getScheduledNotifications()
syncReminders()
```

Then UI components only call these functions.

---

# 64. Example Service API

```js
const ReminderService = {

  create:
    createReminder,

  update:
    updateReminder,

  delete:
    deleteReminder,

  enable:
    enableReminder,

  disable:
    disableReminder,

  getScheduled:
    getScheduledNotifications,

};
```

Usage:

```js
await ReminderService
  .create(reminder);
```

This makes the application easier to reason about.

---

# 65. Error Handling

Notification APIs can fail.

Do not write:

```js
await Notifications
  .scheduleNotificationAsync(...);
```

everywhere without error handling.

Prefer:

```js
try {

  const result =
    await scheduleNotification({
      content,
      trigger,
    });

  console.log(
    result
  );

} catch (error) {

  console.error(
    'Failed to schedule:',
    error
  );

}
```

---

# 66. User-Friendly Errors

Do not show:

```text
Error: ERR_INVALID_TRIGGER
```

directly to the user.

Instead:

```text
Could not create reminder.

Please check the selected
date and time and try again.
```

Log technical details for development.

---

# 67. Scheduling Failure Strategy

Suppose:

```text
Storage save succeeds
```

but:

```text
Notification scheduling fails
```

You do not want:

```text
UI:
Reminder enabled

OS:
No notification
```

Therefore, schedule first:

```text
Validate
 ↓
Schedule
 ↓
Get notification ID
 ↓
Persist reminder
```

This reduces inconsistency.

---

# 68. Editing Failure Strategy

Suppose:

```text
Old notification:
8 PM
```

User changes to:

```text
9 PM
```

If you cancel old first and new scheduling fails:

```text
8 PM → cancelled
9 PM → failed
```

Now there is no schedule.

Depending on your application's requirements, you may:

```text
Attempt new schedule first
```

or:

```text
Cancel old
Try new
If new fails, restore old
```

This is a transaction-like design problem.

For a simple class project, keep the flow explicit and handle failures visibly.

---

# 69. Reminder State Machine

A reminder can have states:

```text
NEW
 ↓
SCHEDULED
 ↓
ACTIVE
 ↓
COMPLETED
```

Or:

```text
ACTIVE
 ↓
DISABLED
 ↓
ACTIVE
```

Or:

```text
ACTIVE
 ↓
DELETED
```

The notification schedule should remain synchronized with the application state.

---

# 70. Reminder State vs Notification State

Example:

```text
Reminder:
enabled = true

Notification:
scheduled = true
```

Good.

But:

```text
Reminder:
enabled = true

Notification:
scheduled = false
```

Bad synchronization.

Your service should try to keep them consistent.

---

# 71. Real-World Example

Imagine:

```text
Reminder:
"Take a break"

Frequency:
Every 60 minutes

Enabled:
true

Notification ID:
abc123
```

The user taps:

```text
Snooze
```

Application:

```text
Receive SNOOZE
 ↓
Find reminder
 ↓
Schedule one-time snooze
 ↓
Keep original repeating reminder
```

This demonstrates that:

```text
Original schedule
```

and:

```text
Temporary snooze
```

can be separate notification requests.

---

# 72. Snooze Architecture

```text
Repeating reminder
       │
       ├── Original notification
       │
       └── Snooze action
               ↓
          Temporary trigger
               ↓
           10 minutes
               ↓
        Snoozed notification
```

This is a useful production pattern.

---

# 73. Complete Notification Data Example

```js
const content = {

  title:
    reminder.title,

  body:
    reminder.body,

  categoryIdentifier:
    'reminder',

  sound:
    'default',

  data: {

    reminderId:
      reminder.id,

    type:
      'REMINDER',

  },

};
```

Now your response handler can distinguish:

```text
type:
REMINDER
```

and:

```text
reminderId:
reminder-001
```

---

# 74. Why Use a `type` Field?

Suppose your application later has:

```text
REMINDER
MESSAGE
TASK
ORDER
```

Then notification data can include:

```js
data: {

  type:
    'REMINDER',

  reminderId:
    'reminder-001',

}
```

The response handler can route the event appropriately.

---

# 75. Generic Response Handler

```js
function
handleNotificationResponse(
  response
) {

  const {
    actionIdentifier,
    notification,
  } = response;

  const data =
    notification
      .request
      .content
      .data;

  if (
    data?.type !==
    'REMINDER'
  ) {

    return;

  }

  const reminderId =
    data.reminderId;

  switch (
    actionIdentifier
  ) {

    case Notifications
      .DEFAULT_ACTION_IDENTIFIER:

      // Open reminder

      break;

    case 'COMPLETE':

      // Complete reminder

      break;

    case 'SNOOZE':

      // Snooze reminder

      break;

  }

}
```

This is a clean pattern for action routing.

---

# 76. Notification Lifecycle

Now we can describe the complete lifecycle:

```text
1. User creates reminder
        ↓
2. Permission checked
        ↓
3. Android channel prepared
        ↓
4. Category prepared
        ↓
5. Trigger built
        ↓
6. Trigger validated
        ↓
7. Notification scheduled
        ↓
8. Notification ID stored
        ↓
9. Notification waits
        ↓
10. Trigger fires
        ↓
11. OS presents notification
        ↓
12. User sees notification
        ↓
13. User taps/action
        ↓
14. Response listener receives event
        ↓
15. Application identifies reminder
        ↓
16. Application updates state
        ↓
17. Badge updated
```

This is the complete local notification lifecycle.

---

# 77. Suggested Project Folder

A more complete project can look like:

```text
src/
│
├── notifications/
│   │
│   ├── notificationConfig.js
│   ├── notificationPermissions.js
│   ├── notificationChannels.js
│   ├── notificationCategories.js
│   ├── notificationScheduler.js
│   ├── notificationListeners.js
│   └── notificationService.js
│
├── reminders/
│   │
│   ├── reminderService.js
│   ├── reminderStorage.js
│   ├── reminderTypes.js
│   └── reminderUtils.js
│
├── screens/
│   │
│   ├── ReminderListScreen.js
│   ├── AddReminderScreen.js
│   └── EditReminderScreen.js
│
└── utils/
    │
    └── triggerBuilder.js
```

---

# 78. Why Separate `reminders` From `notifications`?

This is a very important architecture idea.

### Reminder domain

```text
What does the user want?
```

### Notification domain

```text
How does the operating system schedule it?
```

So:

```text
Reminder
 ↓
Business object

Notification
 ↓
OS implementation
```

This means your reminder model does not need to know every Expo API detail.

---

# 79. Example Domain Model

```js
const reminder = {

  id:
    'r1',

  title:
    'Study',

  body:
    'Study React Native',

  schedule: {

    frequency:
      'daily',

    hour:
      20,

    minute:
      0,

  },

  enabled:
    true,

  notificationId:
    'expo-id',

};
```

The UI deals with:

```text
schedule
```

The notification service converts it to:

```text
Expo trigger
```

---

# 80. Benefits of This Architecture

### Easier debugging

You know where to look.

### Easier testing

You can test trigger builders separately.

### Easier maintenance

Notification APIs are centralized.

### Easier migration

If your notification implementation changes, fewer screens need modification.

### Better scalability

You can add:

```text
Tasks
Messages
Orders
Reminders
```

without duplicating all notification code.

---

# 81. Testing Strategy

Test each layer separately.

### Permission

```text
Granted
Denied
Can ask again
Cannot ask again
```

### Scheduling

```text
Immediate
Interval
Daily
Weekly
Monthly
Yearly
```

### Interaction

```text
Open
Complete
Snooze
```

### Management

```text
Create
Edit
Delete
Enable
Disable
```

### Presentation

```text
Sound
Vibration
Badge
Foreground
```

---

# 82. Test Matrix

| Feature | Test |
|---|---|
| Permission | Allow |
| Permission | Deny |
| Permission | Settings |
| Channel | Exists |
| Channel | Sound |
| Channel | Vibration |
| Scheduling | Immediate |
| Scheduling | Interval |
| Scheduling | Daily |
| Scheduling | Weekly |
| Scheduling | Monthly |
| Scheduling | Yearly |
| Management | Cancel |
| Management | Reschedule |
| Management | Delete |
| Interaction | Tap |
| Interaction | Complete |
| Interaction | Snooze |
| Badge | Increment/update |
| Badge | Clear |
| Foreground | Banner |
| Foreground | Sound |

---

# 83. Debugging Checklist

When a notification does not appear:

```text
1. Is notification permission granted?
        ↓
2. Is Android channel configured?
        ↓
3. Is the trigger valid?
        ↓
4. Does getNextTriggerDateAsync() return a date?
        ↓
5. Is the notification actually scheduled?
        ↓
6. Is the notification handler configured?
        ↓
7. Did the user disable notifications?
        ↓
8. Is the device in Do Not Disturb?
        ↓
9. Is the app using the correct build/environment?
        ↓
10. Is the notification ID saved correctly?
```

---

# 84. Debug Scheduled Notifications

```js
const scheduled =
  await Notifications
    .getAllScheduledNotificationsAsync();

console.log(
  JSON.stringify(
    scheduled,
    null,
    2
  )
);
```

Check:

```text
identifier
content
trigger
```

If your reminder exists in storage but not here:

```text
Storage and OS are out of sync.
```

---

# 85. Debug Permissions

```js
const permissions =
  await Notifications
    .getPermissionsAsync();

console.log(
  JSON.stringify(
    permissions,
    null,
    2
  )
);
```

Check:

```text
status
granted
canAskAgain
ios.status
```

---

# 86. Debug Next Trigger

```js
const next =
  await Notifications
    .getNextTriggerDateAsync(
      trigger
    );

console.log(
  'Next trigger:',
  next
    ? new Date(next)
    : null
);
```

If:

```text
null
```

investigate the trigger.

---

# 87. Debug Android Channels

```js
const channels =
  await Notifications
    .getNotificationChannelsAsync();

console.log(
  JSON.stringify(
    channels,
    null,
    2
  )
);
```

Look for:

```text
id
name
importance
sound
vibrationPattern
```

---

# 88. Production Rule: Do Not Trust UI Alone

Your UI may show:

```text
Reminder enabled
```

but the actual OS schedule may be missing.

Therefore:

```text
UI state
+
stored state
+
OS scheduled state
```

should be considered separately.

---

# 89. Synchronization Example

```text
Stored reminder:
enabled = true

OS schedule:
missing

        ↓

Repair

        ↓

Schedule notification

        ↓

Save notificationId
```

This is especially useful after:

```text
App reinstall
Data migration
User changes
Permission changes
Unexpected cancellation
```

The exact repair strategy depends on the application.

---

# 90. What Happens After App Restart?

A scheduled local notification is handled by the operating system.

Your JavaScript application does not need to remain continuously running for a normal scheduled notification to fire.

Mental model:

```text
App schedules notification
        ↓
OS stores scheduling information
        ↓
App can close
        ↓
Trigger time arrives
        ↓
OS presents notification
```

The exact behavior depends on platform restrictions and notification configuration.

---

# 91. Local Notification vs JavaScript Timer

Do not confuse:

```js
setTimeout()
```

with:

```js
scheduleNotificationAsync()
```

### JavaScript timer

```text
Requires JS runtime/process
```

### Local notification

```text
Registered with OS notification system
```

For user-facing reminders, local notifications are the appropriate mechanism.

---

# 92. Why Not Use `setInterval()` for Reminders?

Bad:

```js
setInterval(
  () => {

    // show reminder

  },
  60 * 60 * 1000
);
```

This is not a reliable replacement for operating-system notification scheduling.

The application may be:

```text
backgrounded
terminated
restricted
```

Use the notification scheduling system for actual notification delivery.

---

# 93. Complete Example: Daily Study Reminder

Application data:

```js
const reminder = {

  id:
    'study-1',

  title:
    'Study React Native',

  body:
    'Continue learning Notifications.',

  frequency:
    'daily',

  hour:
    20,

  minute:
    0,

  enabled:
    true,

};
```

Create:

```js
const result =
  await createReminder(
    reminder
  );

if (
  result.success
) {

  reminder.notificationId =
    result.notificationId;

}
```

Save:

```js
await saveReminders(
  [reminder]
);
```

Result:

```text
Every day at 8 PM
 ↓
Study React Native
```

---

# 94. Complete Example: Weekly Meeting

```js
const reminder = {

  id:
    'meeting-1',

  title:
    'Team Meeting',

  body:
    'Weekly team meeting.',

  frequency:
    'weekly',

  weekday:
    2,

  hour:
    9,

  minute:
    0,

  enabled:
    true,

};
```

According to the Expo SDK 54 weekly mapping:

```text
2 = Monday
```

So this represents:

```text
Every Monday at 9 AM
```

---

# 95. Complete Example: Monthly Bill

```js
const reminder = {

  id:
    'bill-1',

  title:
    'Electricity Bill',

  body:
    'Pay your electricity bill.',

  frequency:
    'monthly',

  day:
    5,

  hour:
    10,

  minute:
    0,

  enabled:
    true,

};
```

Result:

```text
Every month
5th
10 AM
```

---

# 96. Complete Example: Yearly Birthday

```js
const reminder = {

  id:
    'birthday-1',

  title:
    'Birthday',

  body:
    'Wish your friend a happy birthday.',

  frequency:
    'yearly',

  month:
    0,

  day:
    15,

  hour:
    9,

  minute:
    0,

  enabled:
    true,

};
```

Result:

```text
Every January 15
at 9 AM
```

---

# 97. Notification System Summary

At this stage, we have:

```text
Permission
       ↓
Channel
       ↓
Category
       ↓
Content
       ↓
Trigger
       ↓
Scheduler
       ↓
Storage
       ↓
Listener
       ↓
Response
       ↓
Badge
       ↓
Navigation
```

This is a complete local notification architecture.

---

# 98. Final Mental Model

Remember this:

```text
                     USER
                      │
                      ↓
                Reminder UI
                      │
                      ↓
              Reminder Service
                      │
        ┌─────────────┼─────────────┐
        ↓             ↓             ↓
   Permission     Trigger        Storage
        │          Builder          │
        ↓             ↓             ↓
      Expo       Notification     Reminder
  Notifications    Scheduler       Data
        │             │
        └──────┬──────┘
               ↓
        Operating System
               │
               ↓
         Scheduled Trigger
               │
               ↓
          Notification
               │
       ┌───────┴────────┐
       ↓                ↓
    Display          Actions
       │                │
       ↓                ↓
    Badge           Response
    Sound               │
    Vibration            ↓
                  Application State
                         │
                         ↓
                     Navigation
```

This is the complete architecture you should remember.

---

# Part 14 - Debugging, Best Practices, Limitations, Production Checklist & Final Revision

This part focuses on:

```text
How to debug them
How to design them correctly
How to avoid common mistakes
How to test them
How to prepare them for production
```

---

# 1. The Complete Notification Mental Model

Before debugging anything, remember the complete pipeline:

```text
Application
    ↓
Permission
    ↓
Android Channel / iOS Authorization
    ↓
Notification Content
    ↓
Trigger
    ↓
Schedule
    ↓
Operating System
    ↓
Trigger Fires
    ↓
Notification Presentation
    ↓
User Interaction
    ↓
Response Listener
    ↓
Application Logic
```

A failure at any stage can make it look like:

```text
"Notification is not working."
```

But the actual problem may be:

```text
Permission
Channel
Trigger
Scheduling
Presentation
Listener
Navigation
```

Therefore, debugging should happen layer by layer.

---

# 2. First Debugging Rule

Do not immediately change the code.

First ask:

```text
Which stage failed?
```

For example:

```text
Was permission granted?
```

If no:

```text
Do not debug scheduling yet.
```

Or:

```text
Was the notification successfully scheduled?
```

If no:

```text
Do not debug the response listener yet.
```

This simple approach makes debugging much faster.

---

# 3. Debugging Checklist

When a notification does not appear, check:

```text
1. Is the package installed?
2. Is notification permission granted?
3. Is the user blocking notifications?
4. Is the Android channel configured?
5. Is the trigger valid?
6. Is the notification actually scheduled?
7. Does the trigger have a next occurrence?
8. Is the notification handler configured?
9. Is the app in foreground/background?
10. Is the device/platform behaving as expected?
11. Is the notification response listener registered?
12. Is navigation configured correctly?
```

---

# 4. Check Installation

For Expo SDK 54:

```bash
npx expo install expo-notifications
```

Then verify the package exists in:

```text
package.json
```

You should have:

```json
{
  "dependencies": {
    "expo-notifications": "..."
  }
}
```

Use the version selected by the Expo SDK 54-compatible install process rather than manually choosing an unrelated version.

---

# 5. Check Permission First

Run:

```js
const permissions =
  await Notifications
    .getPermissionsAsync();

console.log(
  JSON.stringify(
    permissions,
    null,
    2
  )
);
```

Look at:

```text
granted
status
canAskAgain
```

If:

```js
permissions.granted
```

is:

```text
false
```

then permission is not currently granted.

Do not continue debugging notification scheduling until permission behavior is understood.

---

# 6. Permission States

A useful mental model:

```text
UNDETERMINED
      ↓
Request
      ↓
GRANTED
```

or:

```text
UNDETERMINED
      ↓
Request
      ↓
DENIED
```

After denial:

```text
canAskAgain
```

helps determine whether another permission request can be made.

If the operating system no longer allows the app to request permission directly:

```text
Open Settings
```

may be necessary.

---

# 7. Debug Permission Request

```js
const result =
  await Notifications
    .requestPermissionsAsync();

console.log(
  JSON.stringify(
    result,
    null,
    2
  )
);
```

Do not simply write:

```js
await Notifications
  .requestPermissionsAsync();
```

and ignore the result while debugging.

Always inspect the returned permission state.

---

# 8. Android 13+ Permission

On Android 13 and newer, notifications are permission-controlled.

Therefore:

```text
Android version
        ↓
Notification permission
```

matters.

A channel existing does not automatically mean the user has granted notification permission.

Think:

```text
Channel
+
Permission
=
Notification can be presented
```

Both should be considered during debugging.

---

# 9. Android Notification Channel

On Android 8+:

```text
Notification Channel
```

is important.

Create one:

```js
await Notifications
  .setNotificationChannelAsync(
    'reminders',
    {
      name:
        'Reminders',

      importance:
        Notifications
          .AndroidImportance
          .HIGH,

      sound:
        'default',

      vibrationPattern:
        [0, 200, 100, 200],
    }
  );
```

---

# 10. Check Existing Channels

You can inspect channels:

```js
const channels =
  await Notifications
    .getNotificationChannelsAsync();

console.log(
  JSON.stringify(
    channels,
    null,
    2
  )
);
```

Look for:

```text
id
name
importance
sound
vibrationPattern
```

If your notification uses:

```js
channelId:
  'reminders'
```

make sure the channel actually exists.

---

# 11. Important Android Channel Behavior

Once a channel has been created, users can control channel behavior from Android system settings.

For example:

```text
Sound
Vibration
Importance
```

can become user-controlled.

Therefore, changing your JavaScript configuration does not necessarily mean the user's existing channel settings will magically change.

This is one reason channel design should be intentional.

---

# 12. Channel ID Is Important

Suppose you create:

```js
'reminders'
```

but schedule with:

```js
channelId:
  'reminder'
```

These are different IDs:

```text
reminders
≠
reminder
```

That can cause unexpected behavior.

Keep channel IDs centralized:

```js
export const CHANNELS = {

  REMINDERS:
    'reminders',

};
```

Then:

```js
channelId:
  CHANNELS.REMINDERS
```

This reduces spelling mistakes.

---

# 13. Debug the Trigger

If permission and channels look correct, inspect the trigger.

Example:

```js
const trigger = {

  type:
    Notifications
      .SchedulableTriggerInputTypes
      .DAILY,

  hour:
    20,

  minute:
    0,

};
```

Ask:

```text
Is the trigger type correct?
Is hour valid?
Is minute valid?
Does the trigger have a next occurrence?
```

---

# 14. Use `getNextTriggerDateAsync()`

This is one of the best debugging tools.

```js
const next =
  await Notifications
    .getNextTriggerDateAsync(
      trigger
    );

console.log(
  'Next trigger:',
  next
    ? new Date(next)
    : null
);
```

If you receive a valid date:

```text
Trigger can calculate a next occurrence.
```

If you receive:

```text
null
```

investigate the trigger.

---

# 15. Debug a Daily Trigger

```js
const trigger = {

  type:
    Notifications
      .SchedulableTriggerInputTypes
      .DAILY,

  hour:
    20,

  minute:
    0,

};

const next =
  await Notifications
    .getNextTriggerDateAsync(
      trigger
    );

console.log(
  new Date(next)
);
```

You should be able to reason about:

```text
Today at 8 PM
```

or:

```text
Tomorrow at 8 PM
```

depending on the current time.

---

# 16. Debug a Weekly Trigger

```js
const trigger = {

  type:
    Notifications
      .SchedulableTriggerInputTypes
      .WEEKLY,

  weekday:
    2,

  hour:
    9,

  minute:
    0,

};
```

Remember the Expo SDK 54 weekday mapping used in the previous documentation:

```text
1 = Sunday
2 = Monday
3 = Tuesday
4 = Wednesday
5 = Thursday
6 = Friday
7 = Saturday
```

---

# 17. Debug a Monthly Trigger

```js
const trigger = {

  type:
    Notifications
      .SchedulableTriggerInputTypes
      .MONTHLY,

  day:
    5,

  hour:
    10,

  minute:
    0,

};
```

Check:

```text
day
hour
minute
```

Make sure the selected day is supported by the target platform's monthly scheduling behavior.

---

# 18. Debug a Yearly Trigger

```js
const trigger = {

  type:
    Notifications
      .SchedulableTriggerInputTypes
      .YEARLY,

  month:
    0,

  day:
    15,

  hour:
    9,

  minute:
    0,

};
```

Remember:

```text
January = 0
```

for JavaScript date-based month values.

---

# 19. Debug a Time Interval

```js
const trigger = {

  type:
    Notifications
      .SchedulableTriggerInputTypes
      .TIME_INTERVAL,

  seconds:
    10,

};
```

This means:

```text
10 seconds from the current scheduling time
```

For a repeating interval:

```js
const trigger = {

  type:
    Notifications
      .SchedulableTriggerInputTypes
      .TIME_INTERVAL,

  seconds:
    60,

  repeats:
    true,

};
```

Remember the platform-specific repeating interval rules discussed earlier.

---

# 20. Verify the Notification Was Actually Scheduled

After scheduling:

```js
const id =
  await Notifications
    .scheduleNotificationAsync({

      content: {

        title:
          'Test',

        body:
          'Testing notification',

      },

      trigger,

    });

console.log(
  'Scheduled ID:',
  id
);
```

If an identifier is returned, the scheduling call succeeded.

---

# 21. Inspect All Scheduled Notifications

```js
const scheduled =
  await Notifications
    .getAllScheduledNotificationsAsync();

console.log(
  JSON.stringify(
    scheduled,
    null,
    2
  )
);
```

This answers:

> Is the notification actually registered with the notification scheduler?

If it is missing:

```text
The problem is before OS delivery.
```

If it exists:

```text
Investigate permission, presentation,
platform behavior, timing, or device settings.
```

---

# 22. Inspect One Notification

You can search the returned list:

```js
const notification =
  scheduled.find(
    item =>
      item.identifier ===
      notificationId
  );

console.log(
  notification
);
```

Now you can inspect:

```text
content
trigger
identifier
```

---

# 23. Debug Notification Content

Check:

```js
content: {

  title:
    'Study Reminder',

  body:
    'Continue learning.',

  data: {

    reminderId:
      'r1',

  },

}
```

Common mistakes:

```text
Empty title
Wrong body
Wrong category
Wrong data
Wrong channel
```

During debugging:

```js
console.log(
  JSON.stringify(
    content,
    null,
    2
  )
);
```

---

# 24. Debug Category Actions

Suppose your category is:

```text
reminder
```

and the notification contains:

```js
categoryIdentifier:
  'reminder'
```

Make sure the category was registered using the exact same identifier.

These must match:

```text
setNotificationCategoryAsync(
  'reminder',
  ...
)
```

and:

```js
categoryIdentifier:
  'reminder'
```

---

# 25. Debug Action Identifiers

If the button is:

```text
COMPLETE
```

your response handler must check:

```js
if (
  response.actionIdentifier ===
  'COMPLETE'
) {
  // ...
}
```

These are case-sensitive concepts in your application logic.

Avoid:

```text
complete
```

in one place and:

```text
COMPLETE
```

in another unless that difference is intentional.

Centralize identifiers:

```js
export const ACTIONS = {

  COMPLETE:
    'COMPLETE',

  SNOOZE:
    'SNOOZE',

};
```

---

# 26. Debug Notification Data

Log:

```js
const data =
  response
    .notification
    .request
    .content
    .data;

console.log(
  'Notification data:',
  data
);
```

You might expect:

```js
{
  type:
    'REMINDER',

  reminderId:
    'r1',
}
```

If:

```text
reminderId
```

is missing, navigation or action logic may fail.

---

# 27. Debug Response Listener

Use:

```js
const subscription =
  Notifications
    .addNotificationResponseReceivedListener(
      response => {

        console.log(
          'Notification response:',
          JSON.stringify(
            response,
            null,
            2
          )
        );

      }
    );
```

Test:

```text
Tap notification
```

Then inspect the response.

---

# 28. Debug Default Tap

For a normal notification tap:

```js
if (
  response.actionIdentifier ===
  Notifications
    .DEFAULT_ACTION_IDENTIFIER
) {

  console.log(
    'User opened notification'
  );

}
```

If this never runs, investigate:

```text
Listener registration
Application lifecycle
Notification interaction
```

---

# 29. Listener Cleanup

If a component owns the listener:

```js
useEffect(() => {

  const subscription =
    Notifications
      .addNotificationResponseReceivedListener(
        handleResponse
      );

  return () => {

    subscription.remove();

  };

}, []);
```

Without cleanup, repeated mounting can create duplicate handlers.

---

# 30. Common Bug: Listener Registered Too Late

Bad architecture:

```text
Notification interaction
        ↓
App opens
        ↓
Screen mounts
        ↓
Listener is finally added
```

Depending on your application flow, this can make response handling unreliable.

Prefer a notification listener at a stable application/root level and process the response centrally.

---

# 31. Common Bug: Navigation Before Navigation Is Ready

Suppose:

```text
Notification tapped
        ↓
Response received
        ↓
Navigate
```

but your navigation system has not finished initializing.

Then navigation may fail.

A robust architecture may:

```text
Receive response
        ↓
Store pending navigation target
        ↓
Wait until navigation is ready
        ↓
Navigate
```

This is especially relevant when the app is launched from a notification.

---

# 32. Pending Notification Response

Conceptually:

```js
let pendingResponse =
  null;

function handleResponse(
  response
) {

  if (
    !navigationReady
  ) {

    pendingResponse =
      response;

    return;

  }

  navigateFromResponse(
    response
  );

}
```

Then when navigation becomes ready:

```js
if (
  pendingResponse
) {

  navigateFromResponse(
    pendingResponse
  );

  pendingResponse =
    null;

}
```

---

# 33. Foreground vs Background

Notification behavior can differ depending on whether the application is:

```text
Foreground
Background
Terminated
```

Think:

```text
                 App State
                    │
       ┌────────────┼────────────┐
       ↓            ↓            ↓
   Foreground    Background   Terminated
       │            │            │
       ↓            ↓            ↓
   Handler      OS behavior   OS launches/
                               routes as needed
```

Always test all relevant states.

---

# 34. Foreground Testing

Keep the application open.

Schedule:

```text
10 seconds
```

Then observe:

```text
Does the notification appear?
Does sound play?
Does banner appear?
Does the handler behave as expected?
```

If it behaves differently from a background test, inspect the notification handler.

---

# 35. Background Testing

Send the app to the background:

```text
Home button
```

or equivalent.

Then wait for the notification.

Test:

```text
Banner
Sound
Badge
Interaction
```

---

# 36. Terminated-App Testing

Completely close the application.

Then wait for the scheduled notification.

Test:

```text
Does notification appear?
What happens when it is tapped?
Does the app open?
Does navigation reach the expected screen?
```

This is especially important for a reminder application.

---

# 37. Physical Device Testing

Notification behavior should be tested on real devices.

A simulator/emulator can be useful for development, but device-specific behavior can differ.

Especially test:

```text
Android physical device
iOS physical device
```

when the project supports both.

---

# 38. Development Environment Matters

Do not assume:

```text
Works in one development environment
=
Works everywhere
```

Notification functionality can depend on:

```text
Expo SDK version
Platform
OS version
Build configuration
Permissions
Device settings
```

For SDK-specific or native notification behavior, always validate against the environment you actually intend to ship.

---

# 39. Expo Go vs Development Builds

When working with Expo SDK 54, understand the difference between:

```text
Expo Go
```

and:

```text
Development Build
```

Expo Go is a prebuilt host application with a predefined set of native capabilities.

A development build gives your application a custom native runtime.

Therefore, when a feature depends on native configuration, verify whether your chosen workflow supports the required capability.

For production-style notification work, testing with an appropriate development build is often more representative of the final application.

---

# 40. Do Not Assume a JavaScript-Only Environment Represents Production

Your local JavaScript code may be correct while native configuration is wrong.

Think:

```text
JavaScript
   +
Native configuration
   +
OS permissions
   +
Device settings
```

All four can affect notification behavior.

---

# 41. Common Bug: Permission Granted but No Notification

Possible causes:

```text
Channel disabled
Wrong channel ID
Trigger invalid
Notification not scheduled
Handler/presentation behavior
Device settings
OS restrictions
```

Do not immediately blame permissions.

---

# 42. Common Bug: Notification Scheduled but No Sound

Check:

```text
Notification content
Android channel
Channel sound
User channel settings
iOS authorization/presentation behavior
Device silent/DND state
```

On Android especially, channel-level settings matter.

---

# 43. Common Bug: Vibration Does Not Work

Check:

```text
Android channel vibration configuration
Device vibration settings
Do Not Disturb
Platform behavior
```

Again:

```text
JavaScript setting
≠
guaranteed physical device behavior
```

The operating system and user settings remain involved.

---

# 44. Common Bug: Badge Does Not Change

Check:

```js
const count =
  await Notifications
    .getBadgeCountAsync();

console.log(
  'Badge:',
  count
);
```

Set explicitly:

```js
await Notifications
  .setBadgeCountAsync(
    3
  );
```

Then verify platform behavior.

---

# 45. Common Bug: Badge Becomes Incorrect

Avoid scattered code:

```js
badge++;
badge--;
badge++;
```

Instead derive it from application state:

```js
const count =
  reminders.filter(
    reminder =>
      reminder.enabled &&
      !reminder.completed
  ).length;

await Notifications
  .setBadgeCountAsync(
    count
  );
```

This gives you a source of truth.

---

# 46. Common Bug: Duplicate Notifications

Typical cause:

```text
Old schedule was not cancelled
```

Example:

```text
8 PM reminder
 ↓
User edits to 9 PM
 ↓
9 PM schedule added
 ↓
8 PM schedule still exists
```

Result:

```text
Two notifications
```

Fix:

```text
Cancel old
 ↓
Schedule new
 ↓
Save new ID
```

---

# 47. Common Bug: Duplicate Listeners

Typical cause:

```text
Listener added
Listener added again
Listener added again
```

Then:

```text
One tap
 ↓
Three callbacks
```

Fix:

```js
return () => {

  subscription.remove();

};
```

---

# 48. Common Bug: Wrong Trigger Type

Requirement:

```text
Every Monday at 9 AM
```

but code uses:

```text
TIME_INTERVAL
```

This changes the meaning.

Choose trigger based on the actual requirement:

```text
Relative
→ TIME_INTERVAL

Specific date
→ DATE

Calendar repetition
→ appropriate calendar/dedicated trigger
```

---

# 49. Common Bug: Wrong Weekday

Remember:

```text
1 Sunday
2 Monday
3 Tuesday
4 Wednesday
5 Thursday
6 Friday
7 Saturday
```

Do not assume:

```text
1 Monday
```

---

# 50. Common Bug: Wrong Month

For JavaScript date-based month values:

```text
January = 0
```

not:

```text
January = 1
```

So:

```js
month: 0
```

represents January in the documented JavaScript-style month indexing.

---

# 51. Common Bug: Invalid Time

Bad:

```js
hour:
  30
```

or:

```js
minute:
  80
```

Validate user input before creating the trigger.

A reusable validator:

```js
function validateTime(
  hour,
  minute
) {

  if (
    hour < 0 ||
    hour > 23
  ) {

    return false;

  }

  if (
    minute < 0 ||
    minute > 59
  ) {

    return false;

  }

  return true;

}
```

---

# 52. Common Bug: Scheduling in the Past

Suppose the user chooses:

```text
Today
8:00 AM
```

but the current time is:

```text
10:00 AM
```

A one-time date trigger may represent a time that has already passed.

Your UI should decide what this means:

```text
Reject
```

or:

```text
Schedule tomorrow
```

Do not silently assume the desired behavior.

---

# 53. Better Date-Time Validation

Before scheduling:

```text
Selected date/time
        ↓
Compare with current date/time
        ↓
Past?
 ├── Yes → ask/adjust
 └── No  → schedule
```

This is application-level business logic.

---

# 54. Recurring Reminder Edge Cases

Calendar schedules can have edge cases.

Examples:

```text
31st of every month
```

But some months do not have:

```text
31 days
```

Similarly:

```text
February 29
```

has special calendar behavior.

Your application should define what it means for such schedules.

For example:

```text
Skip months without the selected day
```

or:

```text
Use the last day of the month
```

Do not leave this ambiguous in a production reminder application.

---

# 55. Exact Timing Considerations

On Android 12+, exact alarm behavior has additional requirements.

If your app truly requires exact timing:

```text
Alarm clock
Critical reminder
Time-sensitive event
```

you need to understand Android's exact alarm permission/configuration.

Do not claim:

```text
"Every notification will fire at the exact millisecond."
```

Operating systems may apply scheduling policies.

---

# 56. Notification Timing Is Not a Stopwatch

A local notification should be treated as:

```text
OS-managed scheduling
```

not:

```text
A guaranteed JavaScript timer
```

Therefore, avoid business requirements that assume unrealistic precision.

---

# 57. Time Zone Design

Ask:

```text
What timezone does the reminder belong to?
```

Possible answers:

```text
User's current local timezone
```

or:

```text
A fixed business timezone
```

or:

```text
The timezone selected by the user
```

This should be a deliberate product decision.

---

# 58. Travel Example

User schedules:

```text
Every day at 9 AM
```

Then travels from:

```text
India
```

to:

```text
United Kingdom
```

Your product must define whether the reminder remains:

```text
9 AM local time
```

or:

```text
9 AM India time
```

This is not merely an API question.

It is a product requirement.

---

# 59. Notification UX

Do not use notifications for everything.

Bad:

```text
Notification every 2 minutes
```

This can annoy users.

Good notification design:

```text
Useful
Relevant
Expected
Actionable
Clear
```

---

# 60. Notification Title

Prefer:

```text
Study Reminder
```

instead of:

```text
Notification
```

The title should communicate purpose immediately.

---

# 61. Notification Body

Good:

```text
Your React Native study session starts now.
```

Bad:

```text
Reminder.
```

The body should add useful context.

---

# 62. Action Buttons

Only add actions that users are likely to use.

Good:

```text
Complete
Snooze
```

Bad:

```text
Action 1
Action 2
Action 3
Action 4
Action 5
```

Keep interaction simple.

---

# 63. Do Not Overuse Sound

Sound can be disruptive.

Use sound for:

```text
Important reminders
Time-sensitive events
```

For lower-priority events, consider quieter presentation.

---

# 64. Do Not Overuse Vibration

Vibration should support the notification, not become noise.

Consider:

```text
High importance
→ stronger notification behavior

Low importance
→ quieter behavior
```

---

# 65. Notification Categories Should Reflect Product Intent

Examples:

```text
Reminders
Messages
Orders
Security
Marketing
```

Do not create dozens of categories without a reason.

---

# 66. Android Channel Design

A useful channel strategy:

```text
reminders
messages
orders
```

Each channel can have different intended importance and behavior.

The user can then control categories independently in Android settings.

---

# 67. Avoid Hardcoding Everywhere

Bad:

```js
channelId:
  'reminders'
```

in 20 files.

Better:

```js
export const CHANNEL_IDS = {

  REMINDERS:
    'reminders',

};
```

Then:

```js
channelId:
  CHANNEL_IDS.REMINDERS
```

---

# 68. Centralize Actions

```js
export const NOTIFICATION_ACTIONS = {

  COMPLETE:
    'COMPLETE',

  SNOOZE:
    'SNOOZE',

};
```

Then:

```js
identifier:
  NOTIFICATION_ACTIONS.COMPLETE
```

This prevents typo-based bugs.

---

# 69. Centralize Category IDs

```js
export const CATEGORY_IDS = {

  REMINDER:
    'reminder',

};
```

Use:

```js
categoryIdentifier:
  CATEGORY_IDS.REMINDER
```

---

# 70. Centralize Notification Data Types

```js
export const NOTIFICATION_TYPES = {

  REMINDER:
    'REMINDER',

  TASK:
    'TASK',

  ORDER:
    'ORDER',

};
```

Now the response handler can safely distinguish event types.

---

# 71. Production Notification Constants

A useful file:

```text
notificationConstants.js
```

Example:

```js
export const CHANNEL_IDS = {

  REMINDERS:
    'reminders',

};

export const CATEGORY_IDS = {

  REMINDER:
    'reminder',

};

export const ACTION_IDS = {

  COMPLETE:
    'COMPLETE',

  SNOOZE:
    'SNOOZE',

};

export const NOTIFICATION_TYPES = {

  REMINDER:
    'REMINDER',

};
```

This is much easier to maintain.

---

# 72. Keep UI Separate

Avoid:

```js
<Button
  onPress={async () => {

    await Notifications
      .requestPermissionsAsync();

    await Notifications
      .scheduleNotificationAsync(...);

    await AsyncStorage
      .setItem(...);

  }}
/>
```

This mixes:

```text
UI
Permission
Notification
Storage
```

in one handler.

Prefer:

```js
const handleSave =
  async () => {

    await ReminderService
      .create(reminder);

  };
```

---

# 73. Why Service-Based Architecture Is Better

The UI asks:

```text
Create reminder
```

The service decides:

```text
Permission?
Trigger?
Schedule?
Storage?
```

This creates:

```text
Thin UI
+
Reusable business logic
```

---

# 74. Error Boundaries

Notification failures should not crash the entire app.

Use:

```js
try {

  await ReminderService
    .create(reminder);

} catch (error) {

  console.error(
    error
  );

  Alert.alert(
    'Reminder Error',
    'Could not create the reminder.'
  );

}
```

---

# 75. Logging Strategy

During development:

```js
console.log(
  '[Notifications] Permission:',
  permissions
);

console.log(
  '[Notifications] Trigger:',
  trigger
);

console.log(
  '[Notifications] Scheduled ID:',
  notificationId
);
```

Useful prefixes make logs easier to search.

---

# 76. Avoid Sensitive Data in Notifications

Do not put sensitive information into notification content unless necessary.

Bad:

```text
Your bank account password is...
```

Remember:

```text
Notification content can be visible
on a locked device depending on OS/user settings.
```

Use privacy-conscious content.

---

# 77. Security Principle

Notification data should contain only what the application needs.

Instead of:

```js
data: {

  entireUserObject:
    user,

}
```

prefer:

```js
data: {

  type:
    'REMINDER',

  reminderId:
    reminder.id,

}
```

Keep notification payloads small and purposeful.

---

# 78. Performance Considerations

Do not schedule hundreds or thousands of notifications unnecessarily.

Instead of:

```text
One notification for every minute
for a year
```

consider:

```text
A recurring trigger
```

when the requirement allows it.

Keep scheduling logic efficient.

---

# 79. Avoid Duplicate Scheduling

Before creating a new schedule, know whether one already exists.

For an edit operation:

```text
Existing ID
 ↓
Cancel
 ↓
Schedule replacement
```

For an enable operation:

```text
Already enabled?
 ↓
Do not blindly schedule another copy
```

---

# 80. Idempotent Reminder Operations

An operation is easier to reason about if calling it repeatedly does not create unexpected duplicates.

For example:

```text
enableReminder()
```

should ideally result in:

```text
Exactly one active schedule
```

rather than:

```text
One schedule per call
```

This is an important production design principle.

---

# 81. App Startup Synchronization

A robust startup process:

```text
App starts
 ↓
Configure notification handler
 ↓
Create/check channels
 ↓
Create/check categories
 ↓
Read permission state
 ↓
Load reminders
 ↓
Inspect scheduled notifications
 ↓
Synchronize IDs/state
 ↓
Update badge
 ↓
Register listeners
```

---

# 82. Notification Service Initialization

Example:

```js
export async function
initializeNotifications() {

  await setupNotificationChannels();

  await setupNotificationCategories();

  const permissions =
    await Notifications
      .getPermissionsAsync();

  return {
    permissions,
  };

}
```

Keep initialization in one place.

---

# 83. Testing: Permission Cases

Test:

```text
Case 1:
Permission already granted

Case 2:
Permission not determined

Case 3:
User denies

Case 4:
User denies permanently / cannot ask again

Case 5:
User enables permission from Settings
```

Your application should behave correctly in each case.

---

# 84. Testing: Scheduling Cases

Test:

```text
Immediate
10-second interval
Repeating interval
Daily
Weekly
Monthly
Yearly
One-time date
```

For every test record:

```text
Trigger
Next trigger
Notification ID
Actual result
```

---

# 85. Testing: Reminder CRUD

Test:

```text
Create
Read
Update
Delete
```

Then:

```text
Enable
Disable
Reschedule
```

Verify both:

```text
Application storage
```

and:

```text
Scheduled notifications
```

---

# 86. Testing: Interaction

Test:

```text
Tap notification
Complete
Snooze
```

Verify:

```text
Correct reminder ID
Correct action ID
Correct navigation
Correct state update
```

---

# 87. Testing: App Lifecycle

Test:

```text
Foreground
Background
Terminated
```

For each:

```text
Notification appears
Notification interaction works
Application state updates
```

---

# 88. Testing: Device Settings

Test with:

```text
Notifications ON
Notifications OFF
Sound ON
Sound OFF
Vibration ON
Vibration OFF
Do Not Disturb
```

This reveals real-world behavior that code-only testing cannot.

---

# 89. Production Checklist - Configuration

Before release:

```text
□ expo-notifications installed
□ SDK version verified
□ Native configuration verified
□ Android channel strategy defined
□ iOS notification behavior verified
□ Permission flow implemented
□ Permission denial handled
□ Settings fallback handled
```

---

# 90. Production Checklist - Scheduling

```text
□ Trigger validation
□ Correct trigger type
□ Correct weekday mapping
□ Correct month mapping
□ Date validation
□ Past-date handling
□ Timezone behavior defined
□ Recurring edge cases defined
□ Notification ID stored
```

---

# 91. Production Checklist - Management

```text
□ Create reminder
□ Edit reminder
□ Delete reminder
□ Enable reminder
□ Disable reminder
□ Cancel old schedule
□ Reschedule correctly
□ Prevent duplicate schedules
□ Synchronize storage with OS
```

---

# 92. Production Checklist - Interaction

```text
□ Response listener
□ Listener cleanup
□ Default tap handling
□ Action handling
□ Notification data
□ Reminder ID mapping
□ Navigation handling
□ Pending navigation handling
```

---

# 93. Production Checklist - UX

```text
□ Useful titles
□ Useful bodies
□ Appropriate sound
□ Appropriate vibration
□ Appropriate badge behavior
□ Minimal actions
□ Clear permission explanation
□ No notification spam
□ Privacy-conscious content
```

---

# 94. Production Checklist - Debugging

```text
□ Permission logging
□ Trigger logging
□ Scheduled notification logging
□ Channel inspection
□ Response logging
□ Device testing
□ Foreground testing
□ Background testing
□ Terminated-state testing
```

---

# 95. Final Architecture

A strong final architecture:

```text
                    APPLICATION
                         │
             ┌───────────┴───────────┐
             │                       │
             ↓                       ↓
        Reminder UI            Notification UI
             │                       │
             └───────────┬───────────┘
                         ↓
                 Reminder Service
                         │
          ┌──────────────┼──────────────┐
          ↓              ↓              ↓
      Permission      Trigger         Storage
       Service        Builder         Service
          │              │              │
          └───────┬──────┘              │
                  ↓                     │
             Scheduler                  │
                  │                     │
                  ↓                     │
        expo-notifications             │
                  │                     │
                  ↓                     │
            Operating System            │
                  │                     │
                  ↓                     │
             Notification               │
                  │                     │
          ┌───────┴────────┐            │
          ↓                ↓            │
       Display           Action         │
          │                │            │
          └───────┬────────┘            │
                  ↓                     │
          Response Listener             │
                  │                     │
                  ↓                     │
          Reminder Service ─────────────┘
```

---

# 96. The Most Important Concepts

If you forget everything else, remember these:

### 1. Permission

```text
Can the app send notifications?
```

### 2. Channel

```text
How does Android classify/control the notification?
```

### 3. Content

```text
What does the user see?
```

### 4. Trigger

```text
When should it fire?
```

### 5. Scheduler

```text
Register it with the OS.
```

### 6. Identifier

```text
How do we later manage this scheduled request?
```

### 7. Listener

```text
How do we know the user interacted?
```

### 8. Data

```text
Which application object does this notification belong to?
```

### 9. Storage

```text
What does our application believe the reminder state is?
```

### 10. Synchronization

```text
Does application state match OS scheduling state?
```

---

# 97. One-Line Mental Model

You can summarize the whole module as:

```text
Permission
→ Configure
→ Build
→ Schedule
→ Store
→ Deliver
→ Interact
→ Synchronize
```

---

# 98. Local Notifications vs Push Notifications

This documentation has focused on:

```text
Local Notifications
```

A local notification is scheduled by the application/device.

Conceptually:

```text
App
 ↓
OS
 ↓
Notification
```

Push notifications involve a remote sender/service:

```text
Server
 ↓
Push service
 ↓
Device
 ↓
Notification
```

These are different architectures.

For the SDK 54 class material covered here, the focus is local notification functionality.

---

# 99. Local Notification Use Cases

Good examples:

```text
Reminders
Study schedules
Workout reminders
Medication reminders
Calendar events
Timers
Habits
Offline tasks
Local alarms
```

The exact product requirements determine whether local notifications are appropriate.

---

# 100. When Local Notifications Are Not Enough

If the requirement is:

```text
A server tells the device about a new event
```

then a local schedule alone is not enough.

For example:

```text
New chat message
 ↓
Server creates event
 ↓
Device receives push
```

This belongs to push notification architecture.

---

# 101. Final Comparison

| Feature | Local Notification | Push Notification |
|---|---|---|
| Scheduled on device | Yes | Usually server-driven |
| Requires backend | No | Usually yes |
| Works for local reminders | Excellent | Usually unnecessary |
| Server-triggered event | No | Yes |
| Offline scheduling | Yes | Depends on received push |
| Typical use | Reminders | Messages/events |

