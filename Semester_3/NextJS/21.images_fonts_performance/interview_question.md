# Images, Fonts & Performance — Interview Questions

1. Why does performance matter for user experience and SEO in Next.js apps?

2. What does `next/image` optimize compared to a plain `<img>` tag?

3. Why define `sizes` on responsive images using `next/image`?

4. When should you set `priority` on an image?

5. What must you configure to use remote images with `next/image`?

6. How does `next/font` improve performance and layout stability?

7. What problem do web fonts cause with layout shift, and how does Next.js reduce it?

8. What does `next/dynamic` help you defer from the initial bundle?

9. When should a dynamically imported component use `ssr: false`?

10. Why does marking the root layout `"use client"` hurt performance across the whole app?

11. A page imports a heavy chart library but only shows it after a tab click. What pattern reduces bundle size?

12. What is a common cause of large JavaScript bundles in App Router projects?

13. How does Next.js code splitting differ between routes and dynamic imports?

14. Why avoid importing large libraries into shared Client Components used everywhere?

15. A homepage hero image loads late and hurts LCP. What `next/image` settings should you review?
